// Test CPS transformation integration
// This program uses effects and should be transformable to CPS

fn test_io() with IO {
    perform IO.println("Testing CPS transformation")
    let x = 42
    x
}

fn main() with IO {
    let result = test_io()
    perform IO.println("CPS test complete")
}
