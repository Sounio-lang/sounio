name: GLM-4.7 Performance Benchmarks

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]
  schedule:
    # Run benchmarks daily at 2 AM UTC
    - cron: '0 2 * * *'

env:
  GLM_API_KEY: ${{ secrets.GLM_API_KEY }}

jobs:
  benchmark:
    runs-on: ubuntu-latest
    outputs:
      glm_enabled: ${{ steps.glm-key.outputs.glm_enabled }}

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Check GLM API key
      id: glm-key
      env:
        GLM_API_KEY: ${{ secrets.GLM_API_KEY }}
      run: |
        if [ -n "$GLM_API_KEY" ]; then
          echo "glm_enabled=true" >> "$GITHUB_OUTPUT"
          echo "GLM API key detected. Running benchmark workflow."
        else
          echo "glm_enabled=false" >> "$GITHUB_OUTPUT"
          echo "::notice::GLM_API_KEY is not configured; GLM benchmark workflow will skip benchmark steps."
        fi

    - name: Skip benchmark run (missing GLM key)
      if: steps.glm-key.outputs.glm_enabled != 'true'
      run: |
        echo "::notice::Skipping GLM benchmark execution because GLM_API_KEY is unavailable."

    - name: Setup Rust
      if: steps.glm-key.outputs.glm_enabled == 'true'
      uses: dtolnay/rust-toolchain@stable
      with:
        components: llvm-tools-preview

    - name: Cache cargo registry
      if: steps.glm-key.outputs.glm_enabled == 'true'
      uses: actions/cache@v3
      with:
        path: ~/.cargo/registry
        key: ${{ runner.os }}-cargo-registry-${{ hashFiles('**/Cargo.lock') }}

    - name: Cache cargo index
      if: steps.glm-key.outputs.glm_enabled == 'true'
      uses: actions/cache@v3
      with:
        path: ~/.cargo/git
        key: ${{ runner.os }}-cargo-index-${{ hashFiles('**/Cargo.lock') }}

    - name: Cache cargo build
      if: steps.glm-key.outputs.glm_enabled == 'true'
      uses: actions/cache@v3
      with:
        path: target
        key: ${{ runner.os }}-cargo-build-target-${{ hashFiles('**/Cargo.lock') }}

    - name: Install system dependencies
      if: steps.glm-key.outputs.glm_enabled == 'true'
      run: |
        sudo apt-get update
        WEBKIT_DEV_PACKAGE="libwebkit2gtk-4.1-dev"
        if ! apt-cache show "$WEBKIT_DEV_PACKAGE" >/dev/null 2>&1; then
          WEBKIT_DEV_PACKAGE="libwebkit2gtk-4.0-dev"
        fi
        echo "Installing WebKit development package: $WEBKIT_DEV_PACKAGE"
        sudo apt-get install -y \
          libgtk-3-dev \
          "$WEBKIT_DEV_PACKAGE" \
          build-essential \
          curl \
          wget \
          file \
          libssl-dev \
          libayatana-appindicator3-dev \
          librsvg2-dev

    - name: Install Python dependencies
      if: steps.glm-key.outputs.glm_enabled == 'true'
      run: |
        python3 -m pip install --upgrade pip
        pip3 install matplotlib pandas numpy

    - name: Build compiler (Traditional)
      if: steps.glm-key.outputs.glm_enabled == 'true'
      run: |
        cargo build -p souc --release

    - name: Build compiler (GLM)
      if: steps.glm-key.outputs.glm_enabled == 'true'
      run: |
        cargo build -p souc --release --features glm

    - name: Run GLM Benchmarks
      if: steps.glm-key.outputs.glm_enabled == 'true'
      run: |
        chmod +x scripts/run_glm_benchmarks.sh
        ./scripts/run_glm_benchmarks.sh comprehensive

    - name: Generate Dashboard
      if: steps.glm-key.outputs.glm_enabled == 'true'
      run: |
        python3 scripts/glm_dashboard.py --charts

    - name: Upload benchmark results
      if: steps.glm-key.outputs.glm_enabled == 'true'
      uses: actions/upload-artifact@v4
      with:
        name: benchmark-results
        path: |
          benchmark_results/
          glm_performance_dashboard.html
          *.png

    - name: Upload benchmark report
      if: steps.glm-key.outputs.glm_enabled == 'true'
      uses: actions/upload-artifact@v4
      with:
        name: benchmark-report
        path: benchmark_report.md

    - name: Check for regressions
      if: steps.glm-key.outputs.glm_enabled == 'true'
      run: |
        ./scripts/check_regressions.sh

  regression-analysis:
    runs-on: ubuntu-latest
    needs: benchmark
    if: needs.benchmark.outputs.glm_enabled == 'true'

    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        # Fetch full history for regression analysis
        fetch-depth: 0
    
    - name: Download previous benchmark results
      uses: actions/download-artifact@v4
      with:
        name: benchmark-results
        path: previous_results/
    
    - name: Compare with baseline
      run: |
        echo "Comparing current results with baseline..."
        ./scripts/compare_benchmarks.sh current_results/ previous_results/
    
    - name: Generate regression report
      run: |
        ./scripts/generate_regression_report.sh > regression_report.md
    
    - name: Comment on PR (if applicable)
      if: github.event_name == 'pull_request'
      uses: actions/github-script@v6
      with:
        script: |
          const fs = require('fs');
          const regressionReport = fs.readFileSync('regression_report.md', 'utf8');
          
          github.rest.issues.createComment({
            issue_number: context.issue.number,
            owner: context.repo.owner,
            repo: context.repo.repo,
            body: `## 📊 Performance Regression Analysis\n\n${regressionReport}`
          });

  performance-alerts:
    runs-on: ubuntu-latest
    needs: benchmark
    if: needs.benchmark.outputs.glm_enabled == 'true' && (github.event_name == 'schedule' || github.event_name == 'push')

    steps:
    - name: Setup monitoring
      run: |
        echo "Setting up performance monitoring alerts..."
        # This would integrate with monitoring systems like Prometheus, Grafana, or custom alerting
    
    - name: Check performance thresholds
      run: |
        ./scripts/check_performance_thresholds.sh
    
    - name: Send alerts (if thresholds exceeded)
      if: failure()
      run: |
        echo "Performance thresholds exceeded. Sending alerts..."
        # This would send notifications to Slack, email, or other notification systems
        # Example: curl -X POST -H 'Content-type: application/json' --data '{"text":"Performance regression detected!"}' $SLACK_WEBHOOK_URL

  benchmark-comparison:
    runs-on: ubuntu-latest
    needs: benchmark
    if: needs.benchmark.outputs.glm_enabled == 'true'

    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Download benchmark results
      uses: actions/download-artifact@v4
      with:
        name: benchmark-results
        path: current_results/
    
    - name: Download historical benchmarks
      uses: actions/download-artifact@v4
      with:
        name: historical-benchmarks
        path: historical_data/
    
    - name: Run trend analysis
      run: |
        python3 scripts/trend_analysis.py \
          --current current_results/ \
          --historical historical_data/ \
          --output trend_analysis.json
    
    - name: Generate trend report
      run: |
        python3 scripts/generate_trend_report.py trend_analysis.json > trend_report.md
    
    - name: Upload trend analysis
      uses: actions/upload-artifact@v4
      with:
        name: trend-analysis
        path: |
          trend_analysis.json
          trend_report.md
