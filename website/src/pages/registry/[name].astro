---
import { registryPackages } from '../../data/registryPackages';

export async function getStaticPaths() {
  return registryPackages.map((pkg) => ({ params: { name: pkg.name } }));
}

return Astro.redirect(`/packages/${Astro.params.name}`, 308);
---
