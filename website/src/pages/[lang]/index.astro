---
import HomePage from '../index.astro';

export async function getStaticPaths() {
  return ['pt', 'el', 'zh', 'zh-hk', 'ja', 'es', 'zh-hk'].map((lang) => ({ params: { lang } }));
}
---

<HomePage />
