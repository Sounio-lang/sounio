---
import { registryPackages } from '../../../data/registryPackages';
import BaseLayout from '../../../layouts/BaseLayout.astro';

export async function getStaticPaths() {
  const languages = ['pt', 'el', 'zh', 'ja', 'es'];
  return languages.flatMap((lang) =>
    registryPackages.map((pkg) => ({
      params: { lang, name: pkg.name },
      props: { pkg },
    }))
  );
}

const { pkg } = Astro.props;
---

<BaseLayout title={`Registry: ${pkg.name}`} description={pkg.description}>
  <section class="py-16 bg-[var(--color-surface-primary)]">
    <div class="container max-w-4xl">
      <a href={`/${Astro.params.lang}/registry`} class="inline-flex items-center gap-2 text-sm text-[var(--color-accent-gold)] hover:gap-3 transition-all mb-8">
        <svg class="w-4 h-4 rotate-180" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
        </svg>
        Back to registry
      </a>

      <div class="p-8 glass glass-specular rounded-2xl">
        <div class="flex flex-col md:flex-row md:items-start md:justify-between gap-4 mb-6">
          <div>
            <h1 class="text-3xl font-semibold text-[var(--color-text-primary)] font-mono">{pkg.name}</h1>
            <p class="text-[var(--color-text-secondary)] mt-2">{pkg.description}</p>
          </div>
          <span class="inline-flex items-center px-3 py-1 rounded-full glass text-sm font-mono text-[var(--color-text-secondary)]">
            v{pkg.version}
          </span>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-3 gap-4 mb-8">
          <div class="rounded-lg border border-[var(--glass-border)] p-4">
            <p class="text-xs uppercase tracking-wider text-[var(--color-text-tertiary)]">Downloads</p>
            <p class="text-xl font-semibold text-[var(--color-text-primary)] mt-1">{pkg.downloads.toLocaleString()}</p>
          </div>
          <div class="rounded-lg border border-[var(--glass-border)] p-4">
            <p class="text-xs uppercase tracking-wider text-[var(--color-text-tertiary)]">Status</p>
            <p class="text-xl font-semibold text-[var(--color-text-primary)] mt-1">{pkg.featured ? 'Featured' : 'Community'}</p>
          </div>
          <div class="rounded-lg border border-[var(--glass-border)] p-4">
            <p class="text-xs uppercase tracking-wider text-[var(--color-text-tertiary)]">Install</p>
            <p class="text-sm font-mono text-[var(--color-accent-gold)] mt-1">souc add {pkg.name}</p>
          </div>
        </div>

        <h2 class="text-lg font-semibold text-[var(--color-text-primary)] mb-3">Keywords</h2>
        <div class="flex flex-wrap gap-2">
          {pkg.keywords.map((k) => (
            <span class="px-3 py-1 rounded-full text-sm glass text-[var(--color-text-secondary)]">
              #{k}
            </span>
          ))}
        </div>
      </div>
    </div>
  </section>
</BaseLayout>
