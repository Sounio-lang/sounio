---
import DocsIndexPage from '../../docs/index.astro';

export async function getStaticPaths() {
  return ['pt', 'el', 'zh', 'ja', 'es'].map((lang) => ({ params: { lang } }));
}
---

<DocsIndexPage />
