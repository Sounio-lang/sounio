---
import PlaygroundPage from '../playground.astro';

export async function getStaticPaths() {
  return ['pt', 'el', 'zh', 'ja', 'es'].map((lang) => ({ params: { lang } }));
}
---

<PlaygroundPage />
