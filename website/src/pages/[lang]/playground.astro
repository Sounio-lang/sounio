---
import PlaygroundPage from '../playground.astro';

export async function getStaticPaths() {
  return ['pt', 'el', 'zh', 'zh-hk', 'ja', 'es'].map((lang) => ({ params: { lang } }));
}
---

<PlaygroundPage />
