---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { getLocaleFromUrl, getLocalizedPath } from '../../lib/i18n';

const locale = getLocaleFromUrl(Astro.url);

const domains = [
  {
    id: 'pharmacokinetics',
    title: 'Pharmacokinetics',
    description: 'ISO GUM uncertainty propagation for AUC, Cmax, and clearance models.',
    icon: '💊',
    color: 'var(--color-accent-teal)',
    slug: 'science/pharma'
  },
  {
    id: 'epistemic',
    title: 'Epistemic Logic',
    description: 'First-class Knowledge<T> types with confidence thresholds.',
    icon: '🧠',
    color: 'var(--color-accent-gold)',
    slug: 'learn/epistemic'
  },
  {
    id: 'octonions',
    title: 'Octonion Algebra',
    description: 'Computational proof of the 168 Fano plane automorphisms.',
    icon: '∈',
    color: 'var(--color-accent-purple)',
    slug: 'proof#theorem-168'
  },
  {
    id: 'compiler',
    title: 'Self-Hosting',
    description: 'A compiler that compiles itself, proving semantic consistency.',
    icon: '⚙️',
    color: 'var(--color-accent-blue)',
    slug: 'learn/compiler'
  },
  {
    id: 'climate',
    title: 'Climate Models',
    description: 'Fractional calculus for long-memory climate simulations.',
    icon: '🌍',
    color: 'var(--color-accent-green)',
    slug: 'science/climate'
  },
  {
    id: 'gpu',
    title: 'GPU Acceleration',
    description: 'Native PTX emission for massively parallel tensor operations.',
    icon: '⚡',
    color: 'var(--color-accent-orange)',
    slug: 'learn/gpu'
  }
];
---

<BaseLayout title="Explore Sounio" description="Discover the domains and capabilities of the Sounio language.">
  <div class="pt-32 pb-24 min-h-screen bg-[#020617]">
    <div class="container">
      <header class="mb-16 md:mb-24">
        <h1 class="font-display text-5xl md:text-7xl font-medium tracking-tight mb-6" style="view-transition-name: explore-title">
          Explore the <br/><span class="text-[var(--color-accent-gold)]">Frontier.</span>
        </h1>
        <p class="text-xl text-[var(--color-text-secondary)] max-w-2xl font-sans font-light">
          Sounio is not just a language; it's a new way to compute with uncertainty, prove theorems, and build safety-critical systems.
        </p>
      </header>

      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
        {domains.map((domain, index) => (
          <a 
            href={getLocalizedPath(`/${domain.slug}`, locale)}
            class="group relative block h-80 rounded-2xl overflow-hidden bg-[rgba(255,255,255,0.03)] border border-[rgba(255,255,255,0.05)] hover:border-[rgba(255,255,255,0.2)] transition-all duration-500"
            style={`view-transition-name: card-${domain.id}`}
          >
            <div class="absolute inset-0 opacity-0 group-hover:opacity-10 transition-opacity duration-500" style={`background-color: ${domain.color}`}></div>
            
            <div class="relative h-full p-8 flex flex-col">
              <div class="text-4xl mb-auto opacity-80 group-hover:opacity-100 group-hover:scale-110 transition-all duration-500 origin-left">
                {domain.icon}
              </div>
              
              <div>
                <h2 class="font-display text-2xl font-medium mb-3 group-hover:text-white transition-colors" style={`color: ${domain.color}`}>
                  {domain.title}
                </h2>
                <p class="text-[var(--color-text-secondary)] font-sans text-sm leading-relaxed">
                  {domain.description}
                </p>
              </div>
            </div>
            
            {/* "Punch card" dots decoration */}
            <div class="absolute top-4 right-4 flex gap-1 opacity-20">
              <div class="w-1.5 h-1.5 rounded-full bg-white"></div>
              <div class="w-1.5 h-1.5 rounded-full bg-white"></div>
              <div class="w-1.5 h-1.5 rounded-full bg-white"></div>
            </div>
          </a>
        ))}
      </div>
    </div>
  </div>
</BaseLayout>