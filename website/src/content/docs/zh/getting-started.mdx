---
title: "快速开始"
description: "安装编译器，运行你的第一个 .sio 程序，并了解基础用法。"
weight: 1
category: "getting-started"
topic_id: "website.docs.getting-started"
authority: "dual"
audience: "users"
last_validated: "2026-03-07"
validated_by: "A2"
source_of_truth: "docs/governance/topic-registry.v1.json#website.docs.getting-started"
---

# 快速开始

Sounio 程序是用 `souc` 检查、运行或构建的 `.sio` 文件。当前公开可依赖的入口是仓库中检查过（checked-in）的编译器构件，用 `souc check` 校验，并把仓库里的 gate 与 fixture 当作"现在到底支持什么"的事实来源。

## 今日已验证
- 文档中最安全的入口是位于 `$(pwd)/bin/souc` 的规范启动器（canonical launcher）。
- 首先应该信任的是 `check`，而不是 `run`，因为执行路径会因宿主构件和 backend 支持而不同。
- 已检查的启动器目前报告 `souc 1.0.0-beta.5`（在发布对齐期间，README 徽章可能显示 `1.0.0-beta.6`）。默认公开工作流是 **self-hosted native ELF/Mach-O**——不是 Rust/Cargo 构建，也不是以 Cranelift JIT 作为 onboarding 故事。
- Gate-backed 状态目前在 stdlib 可靠性通道（已提交构件中为 `251/251`）、`fmri` 与 `darwin_pbpk` 科学通道，以及七条必需的 hyper-execution 通道上表现良好。
- 可选的 omega profile（Cranelift JIT、GPU）独立于默认的 `bin/souc` contract 存在。在把某项能力写入文档之前，先用 `souc info` 确认。
- 把 `SOUNIO_STDLIB_PATH` 指向仓库中的 `stdlib/`。
- 先运行 `souc check`，再考虑 `run` 或 `build`。

## 当前限制
- 真实行为取决于具体的 `souc` 二进制，而不只是仓库里有哪些文件。
- 顶层 Cargo 构建已经不再是公共文档里最合适的首次体验。

## 在这个仓库中验证

```bash
export SOUC_BIN="$(pwd)/bin/souc"
export SOUNIO_STDLIB_PATH="$(pwd)/stdlib"

"$SOUC_BIN" --version
"$SOUC_BIN" check examples/hello.sio
"$SOUC_BIN" check tests/run-pass/covid_2020_kernel.sio
"$SOUC_BIN" check tests/run-pass/vancomycin_propagation.sio
"$SOUC_BIN" check tests/compile-fail/vancomycin_low_conf.sio
```
