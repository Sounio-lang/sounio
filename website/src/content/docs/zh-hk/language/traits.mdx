---
title: "特性"
description: "通过特性声明和实现定义跨类型共享行为。"
weight: 17
category: "语言"
topic_id: "website.docs.language.traits"
authority: "website_only"
audience: "用户"
last_validated: "2026-03-07"
validated_by: "A2"
source_of_truth: "docs/governance/topic-registry.v1.json#website.docs.language.traits"
---

# 特性

特性定义了多个类型可以实施的共享接口。它们是Sounio的受限多态机制——一个函数可以要求其类型参数实现特定的特性。

## 声明特性

```sio
trait Printable {
    fn to_string(self) -> str
}

trait Measurable {
    fn magnitude(self) -> f64
}
```

## 实现特性

```sio
struct Point { x: f64, y: f64 }

impl Printable for Point {
    fn to_string(self) -> str {
        format("({}, {})", self.x, self.y)
    }
}

impl Measurable for Point {
    fn magnitude(self) -> f64 {
        sqrt(self.x * self.x + self.y * self.y)
    }
}
```

## 特性界限

使用特性界限约束泛型类型参数：

```sio
fn display<T: Printable>(value: T) with IO {
    println(value.to_string())
}

fn compare_magnitudes<T: Measurable>(a: T, b: T) -> bool {
    a.magnitude() > b.magnitude()
}
```

## 特性检查的位置

特性的解析在`self-hosted/check/traits.sio`中实现。检查器验证：
- 每个`impl`块都提供了所有必需的方法。
- `impl`中的方法签名与特性声明相匹配。
- 调用站点上的特性界限由具体类型满足。

## 当前状态

特性声明、实现和简单的界限由当前解析器和类型检查器支持。更高级的功能（关联类型、默认方法、特性继承）在特性状态页面上跟踪。