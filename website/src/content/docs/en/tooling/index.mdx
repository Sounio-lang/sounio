---
title: "Tooling"
description: "CLI, formatter, linter, tests, REPL, and the Language Server."
weight: 50
category: "tooling"
topic_id: "website.docs.tooling.index"
authority: "website_only"
audience: "users"
last_validated: "2026-03-07"
validated_by: "A5"
source_of_truth: "docs/governance/topic-registry.v1.json#website.docs.tooling.index"
---

# Tooling

Sounio ships with a compiler CLI (`souc`) and companion tools (LSP, docs generator).

## `souc` (Compiler CLI)

Common commands:

```bash
# Type-check a file
souc check file.sio

# Run with the interpreter
souc run file.sio

# Build a native binary (backend/feature dependent)
souc build file.sio -o output

# Format
souc fmt file.sio

# Lint
souc lint file.sio

# REPL
souc repl

# Test suite
souc test
```

Debugging helpers:

```bash
souc check file.sio --show-ast
souc check file.sio --show-types
souc check file.sio --show-effects
```

## LSP (`sounio-lsp`)

The language server is a separate binary, built with the `lsp` feature:

```bash
cargo build -p souc --release --features lsp
./target/release/sounio-lsp --help
```

See [LSP Setup](./lsp) for editor configuration.

## Docs Generators

- `souniodoc`: stdlib API documentation generator
- `souc doc`: package-level docs command (where supported)

## Next

- [LSP Setup](./lsp)

