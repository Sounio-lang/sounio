---
title: "Έλεγχος Ροής"
description: "Συνθήκες, βρόχοι και εκφράσεις match."
weight: 12
category: "language"
topic_id: "website.docs.language.control-flow"
authority: "website_only"
audience: "users"
last_validated: "2026-03-07"
validated_by: "A2"
source_of_truth: "docs/governance/topic-registry.v1.json#website.docs.language.control-flow"
---

# Έλεγχος Ροής

Το Sounio παρέχει γνώριμες δομές ελέγχου ροής: `if`, `while`, `for` και `match`.

## if / else

Το `if` είναι έκφραση, οπότε μπορείτε να δεσμεύσετε το αποτέλεσμα:

```sio
let max = if a > b { a } else { b }
```

## while

```sio
fn main() with IO {
    var count = 0
    while count < 3 {
        println(count)
        count = count + 1
    }
}
```

## for-in

Διαστήματα:

```sio
fn main() with IO {
    // Exclusive range: 0..5
    var sum = 0
    for i in 0..5 {
        sum = sum + i
    }

    // Inclusive range: 0..=5
    var sum_inclusive = 0
    for i in 0..=5 {
        sum_inclusive = sum_inclusive + i
    }
}
```

Πίνακες και `Vec<T>`:

```sio
fn main() with IO {
    let arr = [10, 20, 30]
    for x in arr {
        println(x)
    }

    let vec: Vec<i32> = [1, 2, 3, 4]
    for x in vec {
        println(x)
    }
}
```

`break` και `continue`:

```sio
fn main() with IO {
    for i in 0..10 {
        if i % 2 == 0 {
            continue
        }
        if i >= 7 {
            break
        }
        println(i)
    }
}
```

## match

```sio
fn main() with IO {
    let x = 1
    let y = match x {
        0 => 10
        1 => 20
        _ => 30
    }

    println(y)
}
```

> Σημείωση spec: match guards, range patterns και πιο πλούσια enum patterns είναι μέρος της κατεύθυνσης της γλώσσας. Προτιμήστε το πιο απλό σχήμα `match` όταν γράφετε portable παραδείγματα.

## Τι Ακολουθεί;

- [Επιδράσεις](../../effects)
- [Επιστημικοί Τύποι](../../epistemic)
