---
title: "Hello World"
description: "Γράψτε το πρώτο σας πρόγραμμα Sounio"
weight: 2
topic_id: "website.docs.getting-started.hello-world"
authority: "website_only"
audience: "users"
last_validated: "2026-03-07"
validated_by: "A2"
source_of_truth: "docs/governance/topic-registry.v1.json#website.docs.getting-started.hello-world"
---

# Hello World

Η μικρότερη τρέχουσα διαδρομή hello world παραμένει ένα απλό `main` με `with IO`, επαληθευμένο πρώτα με `check` πριν δοκιμάσετε `run`.

## Επαληθευμένο σήμερα
- Το `examples/hello.sio` παραμένει το μικρότερο επαληθευμένο σημείο εκκίνησης.
- Τα ρητά effects με `with ...` παραμένουν πραγματικό μέρος του συμβολαίου της γλώσσας.
- Προτιμήστε `souc check` πριν από `run` ή `build`.

## Τρέχουσες επιφυλάξεις
- Το `run` εξαρτάται από το artifact και το runtime path που επιλέξατε.
- Η συμπεριφορά εξαρτάται από το ακριβές binary `souc`, όχι μόνο από τα αρχεία που υπάρχουν στο αποθετήριο.

## Δοκιμάστε το σε αυτό το αποθετήριο

```bash
fn main() with IO {
    println("Hello, Sounio!")
}

# then
"$SOUC_BIN" check hello.sio
"$SOUC_BIN" run hello.sio
```
