---
title: "IO"
description: "Console και filesystem I/O, με παρακολούθηση μέσω του effect `IO`."
weight: 31
category: "effects"
topic_id: "website.docs.effects.io"
authority: "website_only"
audience: "users"
last_validated: "2026-03-07"
validated_by: "A2"
source_of_truth: "docs/governance/topic-registry.v1.json#website.docs.effects.io"
---

# IO

Το effect `IO` αντιπροσωπεύει αλληλεπιδράσεις με τον έξω κόσμο: output στην κονσόλα, ανάγνωση input, πρόσβαση σε αρχεία κ.λπ.

## Console Output

```sio
fn main() with IO {
    print("Hello, ")
    println("Sounio!")
}
```

## Reading Input

Το `read_line()` επιστρέφει `string`:

```sio
fn main() with IO {
    println("Enter your name:")
    let name = read_line()
    print("Hi, ")
    println(name)
}
```

## Effect Enforcement

Αν μια συνάρτηση απαιτεί `IO`, οι callers πρέπει επίσης να δηλώσουν `with IO` (ή να το propagate/handle).

Αυτό αποτυγχάνει:

```sio
fn read_file(path: string) -> string with IO {
    ""
}

fn main() {
    let _ = read_file("test.txt")  // ERROR: IO not declared
}
```

Και αυτή είναι η διόρθωση:

```sio
fn read_file(path: string) -> string with IO {
    ""
}

fn main() with IO {
    let _ = read_file("test.txt")
}
```

## Επόμενο

- [Custom Effects](../custom)
- [Effects Overview](..)

