---
title: "Ξεκινώντας"
description: "Εγκαταστήστε τον compiler, τρέξτε το πρώτο σας πρόγραμμα .sio και μάθετε τα βασικά."
weight: 1
category: "getting-started"
topic_id: "website.docs.getting-started"
authority: "dual"
audience: "users"
last_validated: "2026-03-07"
validated_by: "A2"
source_of_truth: "docs/governance/topic-registry.v1.json#website.docs.getting-started"
---

# Ξεκινώντας

Τα προγράμματα Sounio είναι αρχεία `.sio` που ελέγχονται, εκτελούνται ή γίνονται build με το `souc`. Η τρέχουσα δημόσια διαδρομή ξεκινά από το checked-in JIT artifact και ένα check-first workflow.

## Επαληθευμένο σήμερα
- Το ενσωματωμένο JIT artifact είναι σήμερα το ασφαλέστερο δημόσιο σημείο εκκίνησης.
- Ορίστε το `SOUNIO_STDLIB_PATH` προς το versioned δέντρο `stdlib/`.
- Προτιμήστε `souc check` πριν από `run` ή `build`.

## Τρέχουσες επιφυλάξεις
- Η συμπεριφορά εξαρτάται από το ακριβές binary `souc`, όχι μόνο από τα αρχεία που υπάρχουν στο αποθετήριο.
- Ένα top-level Cargo build δεν είναι πια η καλύτερη πρώτη εμπειρία για τα δημόσια docs.

## Δοκιμάστε το σε αυτό το αποθετήριο

```bash
export SOUC_BIN="$(pwd)/bin/souc"
export SOUNIO_STDLIB_PATH="$(pwd)/stdlib"

"$SOUC_BIN" --version
"$SOUC_BIN" check examples/hello.sio
"$SOUC_BIN" check tests/run-pass/covid_2020_kernel.sio
"$SOUC_BIN" check tests/run-pass/vancomycin_propagation.sio
"$SOUC_BIN" check tests/compile-fail/vancomycin_low_conf.sio
```
