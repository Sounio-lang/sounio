---
title: "Ξεκινώντας"
description: "Εγκαταστήστε τον compiler, τρέξτε το πρώτο σας πρόγραμμα .sio και μάθετε τα βασικά."
weight: 1
category: "getting-started"
---

# Ξεκινώντας

Τα προγράμματα Sounio είναι αρχεία `.sio` που γίνονται compile και run με τον compiler `souc`.

## 1) Εγκατάσταση `souc`

### Build από πηγαίο κώδικα (προτείνεται)

```bash
git clone https://github.com/sounio-lang/sounio.git
cd sounio

# Build the compiler
cargo build -p souc --release

# Run it from the repo
./target/release/souc --version
```

### Προαιρετικό: βάλτε το `souc` στο PATH

```bash
sudo cp ./target/release/souc /usr/local/bin/souc
```

## 2) Hello World

Δημιουργήστε `hello.sio`:

```sio
fn main() with IO {
    println("Hello, Sounio!")
}
```

Κάντε type-check και τρέξτε το:

```bash
souc check hello.sio
souc run hello.sio
```

## 3) Επόμενα Βήματα

- [Hello World](./hello-world)
- [Το Πρώτο Σας Πρόγραμμα](./first-program)
- [Μεταβλητές και Τύποι](../language/variables)
- [Επιδράσεις](../effects)
- [Επιστημικοί Τύποι](../epistemic)

