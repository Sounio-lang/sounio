---
title: "Ferramentas"
description: "CLI, formatador, linter, testes, REPL e o Language Server."
weight: 50
category: "tooling"
topic_id: "website.docs.tooling.index"
authority: "website_only"
audience: "users"
last_validated: "2026-03-07"
validated_by: "A5"
source_of_truth: "docs/governance/topic-registry.v1.json#website.docs.tooling.index"
---

# Ferramentas

Sounio vem com uma CLI do compilador (`souc`) e ferramentas auxiliares (LSP, gerador de docs).

## `souc` (CLI do Compilador)

Comandos comuns:

```bash
# Type-check a file
souc check file.sio

# Run with the interpreter
souc run file.sio

# Build a native binary (backend/feature dependent)
souc build file.sio -o output

# Format
souc fmt file.sio

# Lint
souc lint file.sio

# REPL
souc repl

# Test suite
souc test
```

Ajuda para debugging:

```bash
souc check file.sio --show-ast
souc check file.sio --show-types
souc check file.sio --show-effects
```

## LSP (`sounio-lsp`)

O language server é um binário separado, compilado com a feature `lsp`:

```bash
cargo build -p souc --release --features lsp
./target/release/sounio-lsp --help
```

Veja [Configurar LSP](./lsp) para configuração do editor.

## Geradores de Documentação

- `souniodoc`: gerador de docs de API da stdlib
- `souc doc`: comando de docs em nível de pacote (quando suportado)

## Próximo

- [Configurar LSP](./lsp)

