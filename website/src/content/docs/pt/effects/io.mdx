---
title: "IO"
description: "IO de console e filesystem, rastreado pelo efeito `IO`."
weight: 31
category: "effects"
topic_id: "website.docs.effects.io"
authority: "website_only"
audience: "users"
last_validated: "2026-03-07"
validated_by: "A2"
source_of_truth: "docs/governance/topic-registry.v1.json#website.docs.effects.io"
---

# IO

O efeito `IO` representa interações com o mundo externo: saída no console, leitura de entrada, acesso a arquivos, etc.

## Saída no Console

```sio
fn main() with IO {
    print("Hello, ")
    println("Sounio!")
}
```

## Lendo Entrada

`read_line()` retorna uma `string`:

```sio
fn main() with IO {
    println("Enter your name:")
    let name = read_line()
    print("Hi, ")
    println(name)
}
```

## Aplicação de Efeitos

Se uma função exige `IO`, quem chama também precisa declarar `with IO` (ou propagar/tratar o efeito).

Este padrão falha:

```sio
fn read_file(path: string) -> string with IO {
    ""
}

fn main() {
    let _ = read_file("test.txt")  // ERROR: IO not declared
}
```

E esta é a correção:

```sio
fn read_file(path: string) -> string with IO {
    ""
}

fn main() with IO {
    let _ = read_file("test.txt")
}
```

## Próximo

- [Efeitos Customizados](../custom)
- [Visão Geral de Efeitos](..)

