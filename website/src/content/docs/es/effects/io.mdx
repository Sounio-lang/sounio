---
title: "IO"
description: "IO de consola y filesystem, rastreado con el efecto `IO`."
weight: 31
category: "effects"
topic_id: "website.docs.effects.io"
authority: "website_only"
audience: "users"
last_validated: "2026-03-07"
validated_by: "A2"
source_of_truth: "docs/governance/topic-registry.v1.json#website.docs.effects.io"
---

# IO

El efecto `IO` representa interacciones con el mundo exterior: salida por consola, lectura de entrada, acceso a archivos, etc.

## Salida por Consola

```sio
fn main() with IO {
    print("Hello, ")
    println("Sounio!")
}
```

## Leer Entrada

`read_line()` devuelve un `string`:

```sio
fn main() with IO {
    println("Enter your name:")
    let name = read_line()
    print("Hi, ")
    println(name)
}
```

## Aplicación de Efectos

Si una función requiere `IO`, quien la llame también debe declarar `with IO` (o propagar/manejarlo).

Este patrón falla:

```sio
fn read_file(path: string) -> string with IO {
    ""
}

fn main() {
    let _ = read_file("test.txt")  // ERROR: IO not declared
}
```

Y esta es la corrección:

```sio
fn read_file(path: string) -> string with IO {
    ""
}

fn main() with IO {
    let _ = read_file("test.txt")
}
```

## Siguiente

- [Efectos Personalizados](../custom)
- [Resumen de Efectos](..)

