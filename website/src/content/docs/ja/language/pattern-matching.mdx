---
title: "Pattern Matching"
description: "Exhaustive pattern matching with match expressions, guards, and nested patterns."
weight: 18
category: "language"
topic_id: "website.docs.language.pattern-matching"
authority: "website_only"
audience: "users"
last_validated: "2026-03-07"
validated_by: "A2"
source_of_truth: "docs/governance/topic-registry.v1.json#website.docs.language.pattern-matching"
---

# パターンマッチング

`match` 式は、値に対する網羅的なパターンマッチングを提供します。コンパイラはすべてのケースが網羅されているかを検査し、到達不能なアームについて警告を出します。

## 基本的なマッチング

```sio
fn describe(x: i32) -> str {
    match x {
        0 => "zero",
        1 => "one",
        _ => "other",
    }
}
```

ワイルドカード `_` は任意の値にマッチし、キャッチオールとして使用されます。

## enumに対するマッチング

```sio
enum Shape {
    Circle(f64),
    Rectangle(f64, f64),
    Triangle(f64, f64, f64),
}

fn area(s: Shape) -> f64 {
    match s {
        Shape::Circle(r) => 3.14159 * r * r,
        Shape::Rectangle(w, h) => w * h,
        Shape::Triangle(a, b, c) => {
            let s = (a + b + c) / 2.0
            sqrt(s * (s - a) * (s - b) * (s - c))
        },
    }
}
```

バリアントを省略すると、コンパイラは非網羅的なマッチングについてエラーを報告します。

## ガード

パターンアームに `if` ガードを含めて、追加の条件を指定できます:

```sio
fn classify_temp(celsius: f64) -> str {
    match celsius {
        t if t < 0.0 => "freezing",
        t if t < 20.0 => "cold",
        t if t < 35.0 => "comfortable",
        _ => "hot",
    }
}
```

## 構造体に対するマッチング

```sio
struct Point { x: f64, y: f64 }

fn quadrant(p: Point) -> str {
    match p {
        Point { x, y } if x > 0.0 && y > 0.0 => "I",
        Point { x, y } if x < 0.0 && y > 0.0 => "II",
        Point { x, y } if x < 0.0 && y < 0.0 => "III",
        Point { x, y } if x > 0.0 && y < 0.0 => "IV",
        _ => "on axis",
    }
}
```

## 現在の制限事項

- `let` バインディングでのタプル分解（`let (a, b) = pair`）はまだサポートされていません。代わりに `match` を使用してください。
- 深くネストされた構造体に対するネストされたパターンマッチングは動作しますが、すべてのケースで網羅性検査が完全ではありません。
- Orパターン（`A | B => ...`）はパースされますが、カバレッジ検査はまだ成熟途上です。
