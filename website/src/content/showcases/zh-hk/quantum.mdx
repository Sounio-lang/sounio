---
title: "量子计算"
description: "透过线性类型建模量子态"
domain: "quantum"
icon: "⚛️"
topic_id: "website.showcases.zh-hk.quantum"
authority: "website_only"
audience: "researchers"
last_validated: "2026-03-07"
validated_by: "A6"
source_of_truth: "docs/governance/topic-registry.v1.json#website.showcases.zh-hk.quantum"
---
# 用Sounio进行量子计算

Sounio透过线性类型在**编译时**强制执行量子力学的无克隆定理：一个`Qubit`是一个线性资源，必须恰好消费一次。尝试复制它会产生编译错误。

## 线性类型与量子态

```sio
// 一个Qubit不能被克隆。
// 标准量子门（H, X, Y, Z, CNOT）会消耗和返回Qubits。

fn apply_hadamard(q: Qubit) -> Qubit {
    // 应用H门 ...
}

struct QubitPair { fst: Qubit, snd: Qubit }

fn apply_cnot_pair(control: Qubit, target: Qubit) -> QubitPair {
    // 应用CNOT门 ...
}

fn bell_state() -> QubitPair {
    let q1 = allocate_qubit()
    let q2 = allocate_qubit()

    let h1 = apply_hadamard(q1)
    let pair = apply_cnot_pair(h1, q2)

    pair
}
```
