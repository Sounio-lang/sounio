---
title: "量子计算"
description: "通过线性类型建模量子态"
domain: "quantum"
icon: "⚛️"
topic_id: "website.showcases.zh.quantum"
authority: "website_only"
audience: "researchers"
last_validated: "2026-03-07"
validated_by: "A6"
source_of_truth: "docs/governance/topic-registry.v1.json#website.showcases.zh.quantum"
---
# 使用Sounio进行量子计算

Sounio在**编译时**强制执行量子力学的无克隆定理，通过线性类型：`Qubit`是一种线性资源，必须恰好使用一次。尝试复制它会产生编译错误。

## 量子态的线性类型

```sio
// Qubit不能被克隆。
// 标准量子门（H, X, Y, Z, CNOT）消耗并返回Qubit。

fn apply_hadamard(q: Qubit) -> Qubit {
    // 应用H门 ...
}

struct QubitPair { fst: Qubit, snd: Qubit }

fn apply_cnot_pair(control: Qubit, target: Qubit) -> QubitPair {
    // 应用CNOT门 ...
}

fn bell_state() -> QubitPair {
    let q1 = allocate_qubit()
    let q2 = allocate_qubit()

    let h1 = apply_hadamard(q1)
    let pair = apply_cnot_pair(h1, q2)

    pair
}
```
