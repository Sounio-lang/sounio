CLEAR

Backend drops only the Slurm FreeMem conjunct; both host `memory` bits and heartbeat/watchdog stay required. Freeze delta is `material_backend_sha256` only. Sounio admits observer rebind with no GPU/resume/claim, CAS journal-then-lease, partial replay bound to same lease UID/old RV and exact freeze pair, second non-`--apply` observation before commit, post-hosts must stay FENCED, scheduler spec/owner/epoch/state untouched.
