CLEAR

`--close` is correct for this util-linux 2.39.3 parent-holds-FD model; the executed parent-FD / competing-lock proof matches. Lock wait 10, emergency wait 60, op 45+kill 2, GPU stop `-t 5`, systemd Stop 120 / `KillMode=control-group`, no heartbeat on non-zero, and the mock stage→admission→deny→host-manifest order are consistent with the passing logs. Freeze delta is the four allowed keys; observer CAS stays sequential; no live install/Slurm restore.
