# Instrumented source qualification: explicit timeout

Source 0655747507, CI run 34463834489, Current-Source job 102828072324 was cancelled after the explicit 75-minute job deadline. CI Decision failed. This establishes the termination mechanism for this attempt, not for earlier shutdowns.

113 gen2 samples record available host memory from 15,270,192 KiB down to a minimum 183,752 KiB; last sample 200,544 KiB. Swap free began at 3,145,724 KiB, reached a minimum 4 KiB, and ended at 3,356 KiB. Host oom_kill remained zero across all samples. Visible cgroup-root memory.events was unavailable. Samples do not attribute allocations to Madaros or prove that memory pressure caused the slowdown. No gen2 completion receipt exists.

Artifact 10149325534 was downloaded before expiry, verified against the published ZIP digest, and retained in the remote private archive named by summary.json. The committed inventory hashes all 28 members; gen2.log is preserved separately. No gen2 executable is present in that artifact. The compiler executable remains available in the private ZIP for a separately controlled resource experiment.

Contracts independently failed because the observer shell file referenced by the new workflow path filter lacked executable mode. The follow-up mode correction preserves its bytes and is validated with the actual workflow-reference checker.

No model job was submitted. The old source-342 frozen packet remains unexecuted and unqualified. Next: compare preserved compiler bytes and evaluate the compilation resource envelope before another source qualification. Do not alter Spark thresholds or rerun the frozen pilot.
