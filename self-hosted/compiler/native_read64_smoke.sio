fn main() -> i64 with IO {
    let path = get_arg(0)
    let raw = read_file(path)
    let a: f64 = read_f64(raw, 0)
    let b: f64 = read_f64(raw, 1)
    let c = read_i64(raw, 2)
    print_f64(a)
    print("\n")
    print_f64(b)
    print("\n")
    print_int(c)
    print("\n")
    return 0
}
