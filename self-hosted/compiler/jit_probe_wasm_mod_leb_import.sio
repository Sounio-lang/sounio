use wasm::buf::{wasm_buf_new, wasm_push_u32_leb128}

fn main() with IO, Mut, Panic, Div {
    let buf = wasm_buf_new()
    let encoded = wasm_push_u32_leb128(buf, 624485)
    if encoded.len > 0 {
        println("jit_wasm_mod_leb_ok")
    } else {
        println("jit_wasm_mod_leb_bad")
    }
}
