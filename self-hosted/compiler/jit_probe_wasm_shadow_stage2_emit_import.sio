use jit_probe::wasm_stage2_emit::{wasm_buf_new, wasm_push_u32_leb128}

fn main() with IO, Mut, Panic, Div {
    let buf = wasm_buf_new()
    let out = wasm_push_u32_leb128(buf, 624485)
    if out.len < 0 {
        println("")
    }
}
