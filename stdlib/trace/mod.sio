// stdlib/trace/mod.sio — Public exports for the trace module
//
// Per stdlib/CONVENTIONS.md §2, mod.sio declares only the public surface
// (pub use re-exports). Implementations live in the sibling files.

pub use trace::trace::{
    Tracer,
    tracer_begin,
    tracer_end,
    tracer_new,
    tracer_span_duration,
}

