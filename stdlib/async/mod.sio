// stdlib/async — Cooperative async runtime
//
// Modules:
//   future   — PollResult (Pending/Ready)
//   task     — Task, TaskQueue
//   executor — Executor (round-robin scheduler)

mod future;
mod task;
mod executor;
