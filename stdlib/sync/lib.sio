// stdlib/sync — Synchronization primitives
//
// Modules:
//   mutex   — Mutex
//   atomic  — AtomicI64 operations
//   channel — IntChannel (bounded SPSC)
//   rwlock  — RwLock (reader-writer)


// Submodules are resolved by path (`use sync::<name>`);
// Sounio has no `mod` item, so the declarations that stood here were
// inert Rust syntax that also stopped this file parsing. Covered:
//   mutex, atomic, channel, rwlock
