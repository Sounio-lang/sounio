// stdlib/sync — Synchronization primitives
//
// Modules:
//   mutex   — Mutex
//   atomic  — AtomicI64 operations
//   channel — IntChannel (bounded SPSC)
//   rwlock  — RwLock (reader-writer)

mod mutex;
mod atomic;
mod channel;
mod rwlock;
