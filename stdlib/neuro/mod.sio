// stdlib/neuro/mod.sio — Public exports for the neuro module
//
// Per stdlib/CONVENTIONS.md §2, mod.sio declares only the public surface
// (pub use re-exports). Implementations live in the sibling files.

pub use neuro::connectome::{
    ROIFeatures,
    connectome_embed_interaction,
    connectome_embed_mean,
    connectome_to_features,
    connectome_to_features_interaction,
    roi_features_zero,
    roi_to_sedenion,
}

pub use neuro::ct::{
    MorphometricROI,
    StructuralCovariance,
    morphometry_to_sedenion,
    struct_cov_new,
    struct_eta,
    struct_set_cov,
    struct_set_roi,
    struct_threshold,
}

pub use neuro::eeg::{
    EegBandConnectivity,
    EegMultiBand,
    band_alpha,
    band_beta,
    band_delta,
    band_gamma,
    band_theta,
    eeg_band_eta,
    eeg_band_new,
    eeg_set_coherence,
    eeg_threshold_band,
    eeg_to_sedenion,
}

pub use neuro::fmri::{
    FmriConnectivity,
    fmri_clustering,
    fmri_eta,
    fmri_eta_c,
    fmri_new,
    fmri_phase,
    fmri_phase_distance,
    fmri_set_corr,
    fmri_threshold,
}

pub use neuro::meg::{
    MegConnectivity,
    meg_eta,
    meg_new,
    meg_set_conn,
    meg_threshold,
    meg_to_sedenion,
}

pub use neuro::sleep::{
    SleepCoherence,
    generate_sleep_cycle,
    generate_stage_coherence,
    sleep_coherence_new,
    sleep_eta,
    sleep_eta_c,
    sleep_phase,
    sleep_to_sedenion,
    stage_n1,
    stage_n2,
    stage_n3,
    stage_rem,
    stage_wake,
}

