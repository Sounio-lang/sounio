// stdlib/units/mod.sio — Compatibility shim
// Canonical: packages/sounio-units

pub use sounio_units::lib::{
    UnitDim,
    Quantity,

    dim_dimensionless,
    dim_mass,
    dim_length,
    dim_time,
    dim_temperature,
    dim_amount,
    dim_current,
    dim_velocity,
    dim_acceleration,
    dim_force,
    dim_energy,
    dim_power,
    dim_pressure,

    dim_eq,

    quantity_new,
    quantity_is_compatible,
    quantity_add,
    quantity_sub,
    quantity_mul,
    quantity_div,
    quantity_scale,

    convert_celsius_to_kelvin,
    convert_kelvin_to_celsius,
    convert_kg_to_g,
    convert_g_to_kg,
    convert_m_to_cm,
    convert_cm_to_m,
    convert_joule_to_ev,
    convert_ev_to_joule,
}