// stdlib/toml/lib.sio — TOML parser
//
// Flat key-value TOML documents in fixed-size buffers.
// Import: use toml::parser::{toml_doc_new, toml_parse, ...}

// Submodules are resolved by path (`use toml::<name>`);
// Sounio has no `mod` item, so the declarations that stood here were
// inert Rust syntax that also stopped this file parsing. Covered:
//   parser
