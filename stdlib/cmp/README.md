# stdlib/cmp

Comparison utilities.

## Functions
- `cmp_i64`, `cmp_f64`: Compare two values
- `min`, `max`, `clamp`: Clamp values to range
- `approx_eq`: Approximate equality for floating point
- `sign_f64`: Get sign of f64