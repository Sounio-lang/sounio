// stdlib/verify/mod.sio — Public exports for the verify module
//
// Per stdlib/CONVENTIONS.md §2, mod.sio declares only the public surface
// (pub use re-exports). Implementations live in the sibling files.

pub use verify::interval::{
    Interval,
    VerifiedResult,
    interval,
    interval_empty,
    interval_exact,
    interval_from_measurement,
    iv_add,
    iv_contains,
    iv_div,
    iv_from_epistemic,
    iv_hull,
    iv_intersect,
    iv_is_common,
    iv_is_empty,
    iv_is_singleton,
    iv_midpoint,
    iv_mul,
    iv_overlaps,
    iv_relative_width,
    iv_sub,
    iv_subset,
    iv_to_epistemic_u,
    iv_width,
    vr_chain,
    vr_new,
}

