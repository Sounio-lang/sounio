// stdlib/yaml/lib.sio — YAML parser
//
// Flat key-value YAML documents in fixed-size buffers.
// Import: use yaml::parser::{yaml_doc_new, yaml_parse, ...}

// Submodules are resolved by path (`use yaml::<name>`);
// Sounio has no `mod` item, so the declarations that stood here were
// inert Rust syntax that also stopped this file parsing. Covered:
//   parser
