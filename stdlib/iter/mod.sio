// stdlib/iter/mod.sio — Public exports for the iter module
//
// Per stdlib/CONVENTIONS.md §2, mod.sio declares only the public surface
// (pub use re-exports). Implementations live in the sibling files.

pub use iter::lib::{
    ArrayView,
    array_view_new,
    iter_all_positive,
    iter_any_negative,
    iter_count_gt,
    iter_enumerate_sum_indexed,
    iter_filter_gt,
    iter_filter_positive,
    iter_map_id,
    iter_reduce_max,
    iter_reduce_min,
    iter_reduce_product,
    iter_reduce_sum,
    iter_skip,
    iter_take,
    iter_zip_add,
    iter_zip_mul,
}

pub use iter::range::{
    Range,
    RangeArr64,
    range_collect,
    range_has_next,
    range_len,
    range_new,
    range_next,
    range_reset,
    range_step,
}

pub use iter::transform::{
    arr64_contains,
    arr64_filter_eq,
    arr64_filter_gt,
    arr64_filter_lt,
    arr64_max,
    arr64_min,
    arr64_sum,
}

