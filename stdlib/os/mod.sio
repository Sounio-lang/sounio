// stdlib/os/mod.sio — Public exports for the os module
//
// Per stdlib/CONVENTIONS.md §2, mod.sio declares only the public surface
// (pub use re-exports). Implementations live in the sibling files.

pub use os::process::{
    process_abort,
    process_args,
    process_exit,
    process_id,
    process_parent_id,
    test_all_process,
    test_exit_signatures,
    test_process_args,
    test_process_id,
    test_process_parent_id,
};

pub use os::env::{env_var_present, env_get, envvar_new};

