// stdlib/profile/mod.sio — Public exports for the profile module
//
// Per stdlib/CONVENTIONS.md §2, mod.sio declares only the public surface
// (pub use re-exports). Implementations live in the sibling files.

pub use profile::profiler::{
    profiler_begin,
    profiler_end,
    profiler_new,
    profiler_report,
    profiler_reset,
    profiler_slot_avg_us,
    profiler_slot_calls,
    profiler_slot_count,
    profiler_slot_total_ms,
}

