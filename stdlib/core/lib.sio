// stdlib/core — Core types and prelude
//
// Modules:
//   option  — IntOption, FloatOption
//   result  — IntResult, FloatResult
//   prelude — Minimal imports for bootstrap


// Submodules are resolved by path (`use core::<name>`);
// Sounio has no `mod` item, so the declarations that stood here were
// inert Rust syntax that also stopped this file parsing. Covered:
//   option, result, prelude
