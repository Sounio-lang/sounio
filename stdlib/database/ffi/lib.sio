//! stdlib/database/ffi — FFI Database Backends
//!
//! Provides FFI bindings to native database libraries.


// Submodules are resolved by path (`use database::<name>`);
// Sounio has no `mod` item, so the declarations that stood here were
// inert Rust syntax that also stopped this file parsing. Covered:
//   bindings, wrapper, fallback

pub use bindings::*;
pub use wrapper::*;
pub use fallback::*;