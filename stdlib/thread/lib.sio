// stdlib/thread — Thread management
//
// Modules:
//   spawn — ThreadHandle
//   pool  — ThreadPool


// Submodules are resolved by path (`use thread::<name>`);
// Sounio has no `mod` item, so the declarations that stood here were
// inert Rust syntax that also stopped this file parsing. Covered:
//   spawn, pool
