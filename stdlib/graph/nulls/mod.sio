// Null models for statistical comparison in graph analysis
// These modules generate random graphs with controlled properties
// to perform statistical significance testing


// Submodules are resolved by path (`use graph::<name>`);
// Sounio has no `mod` item, so the declarations that stood here were
// inert Rust syntax that also stopped this file parsing. Covered:
//   configuration
