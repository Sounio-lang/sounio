// stdlib/theorem/mod.sio — Public exports for the theorem module
//
// Per stdlib/CONVENTIONS.md §2, mod.sio declares only the public surface
// (pub use re-exports). Implementations live in the sibling files.

pub use theorem::search::{
    Proof,
    ProofContext,
    Prop,
    VerifyResult,
    and_intro,
    assume,
    auto_prove,
    ctx_get_proof,
    ctx_get_prop,
    ctx_prop,
    proof_context_new,
    prop_and,
    prop_custom,
    prop_eq_check,
    prop_false,
    prop_implies,
    prop_not,
    prop_or,
    prop_true,
    resolve,
    saturate,
    search_reset,
    tactic_or,
    tactic_then,
    to_cnf,
    verify,
}

