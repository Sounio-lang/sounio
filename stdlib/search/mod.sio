// stdlib/search/mod.sio — Public exports for the search module
//
// Per stdlib/CONVENTIONS.md §2, mod.sio declares only the public surface
// (pub use re-exports). Implementations live in the sibling files.

pub use search::algorithms::{
    SearchResult,
    SortResult,
    SortStats,
    binary_search,
    heapsort,
    insertion_sort,
    interpolation_search,
    is_sorted,
    linear_search,
    median,
    merge_sort,
    nth_smallest,
    quicksort,
    selection_sort,
    unique_count,
}

pub use search::basic::{
    EqualRange,
    Ordering,
    binary_search_by,
    binary_search_f64,
    binary_search_i64,
    contains_i64,
    contains_sorted_i64,
    count_equal_i64,
    equal_range_i64,
    exponential_search_i64,
    index_of_max_i64,
    index_of_min_i64,
    index_of_minmax_i64,
    interpolation_search_i64,
    linear_search_by,
    linear_search_f64,
    linear_search_i64,
    lower_bound_f64,
    lower_bound_i64,
    run_tests,
    upper_bound_f64,
    upper_bound_i64,
}

pub use search::pattern::{
    FuzzyMatch,
    boyer_moore_search,
    boyer_moore_search_all,
    edit_distance,
    fuzzy_match_single,
    hamming_distance,
    kmp_failure_function,
    kmp_search,
    kmp_search_all,
    lcs_length,
    normalized_edit_distance,
    rabin_karp_search,
    similarity,
}

