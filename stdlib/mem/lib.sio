// stdlib/mem — Memory management utilities
//
// Modules:
//   arena — Bump allocator (arena_new, arena_alloc_bytes, arena_reset)
//   box   — Heap allocation via C FFI (heap_alloc, heap_free)


// Submodules are resolved by path (`use mem::<name>`);
// Sounio has no `mod` item, so the declarations that stood here were
// inert Rust syntax that also stopped this file parsing. Covered:
//   arena, box
