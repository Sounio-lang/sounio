// stdlib/stochastic/mod.sio — Public exports for the stochastic module
//
// Per stdlib/CONVENTIONS.md §2, mod.sio declares only the public surface
// (pub use re-exports). Implementations live in the sibling files.

pub use stochastic::sde::{
    SDEState,
    euler_maruyama,
    gbm_simulate,
    milstein_step,
    ou_simulate,
    sde_epistemic_gbm,
    sde_state_new,
}

