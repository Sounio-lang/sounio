//! stdlib/testmod/tc_b.sio — mid module; imports tc_c. NON-first fn calls transitive tc_c_add.
use testmod::tc_c::{tc_c_add}
pub fn tc_b_first(x: i64) -> i64 with Mut, Panic { x * 2 }
pub fn tc_b_second(x: i64) -> i64 with Mut, Panic, Div { tc_c_add(x, 5) }
