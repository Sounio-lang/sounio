// Test file for clean import resolution
import linalg;
import epistemic;
import std::math;

fn main() {
    // Test direct imports
    let v = linalg::DenseVector::zeros(3);
    
    // Test qualified import  
    let result = math::sin(1.0);
    
    // Test epistemic import
    let prior = epistemic::beta_uniform();
}