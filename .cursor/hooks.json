{
  "version": 1,
  "hooks": {
    "sessionStart": [
      {
        "command": "project_dir=\"${CURSOR_PROJECT_DIR:-$PWD}\" && runtime_dir=\"$(readlink -f \"${SOUNIO_COORD_RUNTIME_DIR:-$(git -C \"$project_dir\" rev-parse --path-format=absolute --git-common-dir)/sounio-coord-runtime}/native-next\")\" && test -n \"$runtime_dir\" && exec env SOUNIO_LOOM_LANGUAGE_AUTHORITY_ROOT=\"$runtime_dir/policy/language-authority\" \"$runtime_dir/bin/sounio-loom-runtime\" agent-hook --agent cursor",
        "failClosed": true,
        "timeout": 30
      }
    ],
    "beforeSubmitPrompt": [
      {
        "command": "project_dir=\"${CURSOR_PROJECT_DIR:-$PWD}\" && runtime_dir=\"$(readlink -f \"${SOUNIO_COORD_RUNTIME_DIR:-$(git -C \"$project_dir\" rev-parse --path-format=absolute --git-common-dir)/sounio-coord-runtime}/native-next\")\" && test -n \"$runtime_dir\" && exec env SOUNIO_LOOM_LANGUAGE_AUTHORITY_ROOT=\"$runtime_dir/policy/language-authority\" \"$runtime_dir/bin/sounio-loom-runtime\" agent-hook --agent cursor",
        "failClosed": true,
        "timeout": 30
      }
    ],
    "preToolUse": [
      {
        "command": "project_dir=\"${CURSOR_PROJECT_DIR:-$PWD}\" && runtime_dir=\"$(readlink -f \"${SOUNIO_COORD_RUNTIME_DIR:-$(git -C \"$project_dir\" rev-parse --path-format=absolute --git-common-dir)/sounio-coord-runtime}/native-next\")\" && test -n \"$runtime_dir\" && exec env SOUNIO_LOOM_LANGUAGE_AUTHORITY_ROOT=\"$runtime_dir/policy/language-authority\" \"$runtime_dir/bin/sounio-loom-runtime\" agent-hook --agent cursor",
        "failClosed": true,
        "timeout": 30
      }
    ],
    "postToolUse": [
      {
        "command": "project_dir=\"${CURSOR_PROJECT_DIR:-$PWD}\" && runtime_dir=\"$(readlink -f \"${SOUNIO_COORD_RUNTIME_DIR:-$(git -C \"$project_dir\" rev-parse --path-format=absolute --git-common-dir)/sounio-coord-runtime}/native-next\")\" && test -n \"$runtime_dir\" && exec env SOUNIO_LOOM_LANGUAGE_AUTHORITY_ROOT=\"$runtime_dir/policy/language-authority\" \"$runtime_dir/bin/sounio-loom-runtime\" agent-hook --agent cursor",
        "failClosed": true,
        "timeout": 30
      }
    ],
    "stop": [
      {
        "command": "project_dir=\"${CURSOR_PROJECT_DIR:-$PWD}\" && runtime_dir=\"$(readlink -f \"${SOUNIO_COORD_RUNTIME_DIR:-$(git -C \"$project_dir\" rev-parse --path-format=absolute --git-common-dir)/sounio-coord-runtime}/native-next\")\" && test -n \"$runtime_dir\" && exec env SOUNIO_LOOM_LANGUAGE_AUTHORITY_ROOT=\"$runtime_dir/policy/language-authority\" \"$runtime_dir/bin/sounio-loom-runtime\" agent-hook --agent cursor",
        "failClosed": true,
        "timeout": 30
      }
    ],
    "sessionEnd": [
      {
        "command": "project_dir=\"${CURSOR_PROJECT_DIR:-$PWD}\" && runtime_dir=\"$(readlink -f \"${SOUNIO_COORD_RUNTIME_DIR:-$(git -C \"$project_dir\" rev-parse --path-format=absolute --git-common-dir)/sounio-coord-runtime}/native-next\")\" && test -n \"$runtime_dir\" && exec env SOUNIO_LOOM_LANGUAGE_AUTHORITY_ROOT=\"$runtime_dir/policy/language-authority\" \"$runtime_dir/bin/sounio-loom-runtime\" agent-hook --agent cursor",
        "failClosed": true,
        "timeout": 30
      }
    ]
  }
}
