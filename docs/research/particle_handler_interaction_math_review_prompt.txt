Math-review target: formal/lean4/SounioEffects.lean §19 + formal/lean4/SounioNonUnitaryNWA.lean §4

Claims to audit:
1. Effectful α + handle preserves value (identity handlers) — handle_preserves_value.
2. handle_comm: whole Effectful (value+row) order-independent via mask_comm.
3. discharge_pair_interaction: sequential handle clears both targets and keeps value.
4. NonUnitaryNWA discharge_eq_mask_fold / discharge_interaction: TagRow masks match structural tag clears; continuum/peak/perturbative preserved appropriately.

Non-claims: not optical theorem; not Float BW; not Madaros Var; not full Plotkin–Pretnar deep handlers (identity clears only).

Return numbered [OK]/[BUG]/[WARN] for each claim.
