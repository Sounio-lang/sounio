<!-- docs:meta
topic_id: repo.docs.research.functor-f-e8-capstone-spec-2026-07-26
authority: historical
audience: researchers
last_validated: 2026-03-07
validated_by: A6
source_of_truth: docs/governance/topic-registry.v1.json#repo.docs.research.functor-f-e8-capstone-spec-2026-07-26
-->


<!-- docs:status-note:start -->
> Docs status: `historical`
> This page is preserved for lineage. Start at [Docs Authority Matrix](../governance/DOCS_AUTHORITY_MATRIX.md) and [docs index](../README.md) for the current canonical surface for this topic.
<!-- docs:status-note:end -->

# Functor F — the E₈ capstone: φ is the octonion form-datum of the whole exceptional tower

**Date:** 2026-07-26
**Orthography:** EN-UK
**Status:** `EXECUTABLE` — `E8_NO_SMALL_REP_PHI_IS_TOWER_FORM_DATUM` (structural capstone)
**Parents:** `functor_f_e7_quartic_spec_2026-07-26.md` (`ψ=*φ`, the tower sees `{g,φ}`), `functor_f_e6_albert_shadow_spec_2026-07-25.md` (`φ = E₆ cubic cross-term`)
**Harness:** `scripts/research/functor_f_e8_capstone_contract.py`

---

## 0. The result — honest about E₈

`E₈` is **not** like `E₆`/`E₇`. It has no minuscule representation — its smallest nontrivial
irrep is the 248-dim adjoint — so there is **no small-rep polynomial invariant** to build `φ`
against, the way the `E₆` cubic (on the **27**) and the `E₇` quartic (on the **56**) were. This
rung therefore does **not** construct `E₈` or a new invariant. It establishes the structural
capstone, with a concrete verified anchor:

> `dim der(𝕆) = 14 = 𝔤₂` (verified). By the Tits / Freudenthal magic square,
> `e₈ = der(𝕆) ⊕ (Im 𝕆 ⊗ J₃(𝕆)₀) ⊕ der(J₃(𝕆)) = 14 + 7·26 + 52 = 248`. The whole octonionic
> tower `G₂(14) ⊂ F₄(52) ⊂ E₆(78) ⊂ E₇(133) ⊂ E₈(248)` is built from 𝕆 and Jordan algebras
> over 𝕆, whose entire `G₂`-invariant form-content is `{g, φ}` (with `ψ = *φ`). So **`φ` is the
> octonion form-datum of the whole tower**; its **clean invariant-cross-term home is `E₆`**, and
> the clean `φ = cross-term` phenomenon **tops out below `E₈`** (which has no small-rep invariant).

---

## 1. Results

| Clause | Result | Reading |
|---|---|---|
| `G1_DER_O_IS_G2` | `dim der(𝕆) = 14` (verified by solving `D(e_ie_j)=D(e_i)e_j+e_iD(e_j)`) | the base summand of `e₈`'s Tits construction is exactly `𝔤₂ = Aut(𝕆)` — functor-F's own symmetry algebra. |
| `G2_TITS_DIMS` | `e₈ = 14 + 7·26 + 52 = 248`; tower `14/52/78/133/248` | `E₈` is octonion-built (Tits / magic square). |
| `G3_FORM_CONTENT` | `ψ = *φ` (dev `0.0`) | the octonion `G₂` form-content is generated by `{g, φ}`. |
| `G4_CAPSTONE` | `E₈` min nontrivial irrep = 248 (adjoint), no small rep → no small-rep invariant | the clean `φ=cross-term` phenomenon tops out below `E₈`; but `φ` is the tower's octonion form-datum (via Tits + `{g,φ}`). |

Verdict: `FUNCTOR_F_E8_VERDICT E8_NO_SMALL_REP_PHI_IS_TOWER_FORM_DATUM`.

---

## 2. The exceptional-tower story, complete (G₂ → E₈)

```
octonion G2 form-content = {metric g, phi}     (psi = *phi; F5/G3)
      |
G2 = Aut(O) = Der(O), dim 14   [functor-F's symmetry; e8's Tits base summand, verified]
      |
F4 = Aut(J3(O)), dim 52        [preserves the cubic N]
      |
E6, dim 78                     phi = imaginary restriction of the cubic cross-term Re(xyz)
      |                        [CLEAN invariant-cross-term home of functor-F's phi]
E7, dim 133                    Freudenthal quartic (built + verified); phi via the cubic +
      |                        adjoint-metric content; no new octonion form-object
E8, dim 248                    no minuscule rep => no small-rep invariant; the clean
                               cross-term phenomenon tops out here, but e8 is octonion-built
                               (Tits), so phi is still its octonion form-datum
```

**One line:** functor-F's `φ` — the `G₂` 3-form the whole programme is built on — is the
octonion form-datum threading the entire exceptional tower, manifesting *cleanly* as the `E₆`
cubic cross-term and topping out (as an explicit small-rep invariant) below `E₈`.

---

## 3. What this is NOT

- **Not** a construction of `E₈`, nor a new `E₈` invariant (E₈ has no small rep for one).
- **Not** the Petitot semantic conjecture (`petitot §4`, quarantined `D3`-class).
- **Not** an identity; **not** clinical.

---

## 4. Reproduce

```bash
python3 scripts/research/functor_f_e8_capstone_contract.py
# expect: G0..G4 PASS, FUNCTOR_F_E8_VERDICT E8_NO_SMALL_REP_PHI_IS_TOWER_FORM_DATUM
```

Pure Python (numpy); verifies `dim der(𝕆)=14`, the magic-square dims, and `ψ=*φ`.

---

## 5. AI disclosure

Probe, contract, and note produced under human direction (2026-07-26). `E₈` is structurally
different from `E₆`/`E₇` (no small rep), so this rung is an honest structural capstone, not a
new-invariant computation: `dim der(𝕆)=14=𝔤₂` is verified computationally; the Tits/magic-square
dimensions and "E₈ has no minuscule rep" are standard facts; `ψ=*φ` is verified exact. The
capstone (φ = the tower's octonion form-datum, clean home at `E₆`) was reviewed for overclaim
(§10, Grok `[OK]`, "bounded correctly"). No `E₈` construction, no new invariant, no semantic
claim. No clinical content. GAIDeT-ICMJE 2025.
