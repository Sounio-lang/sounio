//@ run-pass
// D1 repro: TWO imported modules — known to silently false-green to a stub binary.

use d1_modA::{a_value}
use d1_modB::{b_value}

fn main() -> i32 with IO {
    let ra = a_value(3, 4)
    let rb = b_value(3, 4)
    println("two-import result A:")
    print_int(ra)
    println("two-import result B:")
    print_int(rb)
    0
}
