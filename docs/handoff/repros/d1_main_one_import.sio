//@ run-pass
// D1 control case: only ONE imported module. Expected: real printed output.

use d1_modA::{a_value}

fn main() -> i32 with IO {
    let r = a_value(3, 4)
    println("one-import result:")
    print_int(r)
    0
}
