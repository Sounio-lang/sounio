// validated_lane: lean_single
// WP-B1 witness (W1) middle module: forwards to depclosure_str_b.
// It does NOT itself import str::lib; the str::lib body must reach the merged
// module transitively through depclosure_str_b's own use-deps.
use depclosure_str_b::{depb_probe}

fn depa_probe() -> i64 with Mut, Div, Panic {
    depb_probe()
}

fn main() -> i64 with Mut, Div, Panic {
    if depa_probe() == 5 { 0 } else { 1 }
}
