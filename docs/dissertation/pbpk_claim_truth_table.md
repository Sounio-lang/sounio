<!-- docs:meta
topic_id: repo.docs.dissertation.pbpk-claim-truth-table
authority: repo_only
audience: users
last_validated: 2026-03-07
validated_by: A2
source_of_truth: docs/governance/topic-registry.v1.json#repo.docs.dissertation.pbpk-claim-truth-table
-->

# PBPK Dissertation Claim Truth Table

Purpose: map PBPK, GUM, and GPU dissertation-language candidates to the current repository evidence. This is an evidence table, not promotional copy. Use the narrowest claim that the cited files and gates can actually support.

Status vocabulary:

- `repo-backed`: directly supported by committed source and an executable gate.
- `experimental`: supported by a prototype or artifact but not yet a full dissertation claim.
- `future-work`: plausible direction, but not current evidence.
- `unsupported/overclaim`: no current repo-backed implementation, gate, or benchmark supports the wording.
- `unsupported/benchmark-needed`: no current benchmark gate or committed result supports the performance wording.

## Current Validation Snapshot

This table was re-audited against `origin/main` at `8c6631a2a` on 2026-05-21, after the 2026-05-17 PBPK28 epistemic stack landed (`652133d7d` … `cb51778fa`). **One-dissertation framing (operator directive 2026-05-21):** the PBPK28 biomaterials track and the broader Sounio epistemic track are a single dissertation; `RECONCILIATION_MEMO_2026-05-12.md`'s two-lane split is superseded. The PBPK14 / PBPK28 / Hessian / K-AXI *gates* below remain distinct as evidence-scoping discipline, **not** as a thesis boundary — cite the right gate for the right claim, but they back one document. The claim surface is intentionally conservative.

> **Addendum 2026-05-24 (`b4ead791a`):** a native multi-measurement epistemic-fusion
> capability landed (`uncertainty_of()` + stdlib `kaxi_fuse()`, inverse-variance weighting).
> It is recorded below as a **language capability** (`repo-backed`).
> **Update 2026-05-25 (PR #199, `0db01233f`):** the "wire into a PBPK witness behind a gate"
> next-gate is now **SATISFIED** — `kaxi_fuse` drives the `rapamycin_kaxi_fuse_prior` witness
> (clearance prior-assimilation) gated by `dissertation_pbpk_suite_gate.sh`. This is a **method**
> witness with illustrative/synthetic individual data; there is still **no clinical application** —
> real repeated-measurement data + a calibration check remain future-work.

> Gate-status note: the six dissertation gates were **re-executed 2026-05-21 at `8c6631a2a`** (worktree, `SKIP_BUILD=1`) and **all PASS**:
> `dissertation_pbpk28_parity_gate.sh` (9/9 cases — PBPK28 parity, mass conservation, TMDD, PD, semaglutide arms, all within 1.0% RMSE),
> `dissertation_pbpk_suite_gate.sh` (50/50 rapamycin PBPK tests + smoke demos),
> `dissertation_pbpk_hessian_gate.sh` (5/0),
> `dissertation_frontend_parity_gate.sh` (14/14 compartments within 1.0% RMSE),
> `dissertation_dossier_gate.sh` (5/0),
> `kretikos_kaxi_phase_y_gate.sh` (3/3 truth claims; ran on real CUDA hardware — `ptxas` + A5000 present — with bit-exact GPU↔CPU f32 digests at a 10M cohort, ISO budget written to `benchmarks/pbpk/gum_budget.csv`).
> The `repo-backed` rows below are therefore confirmed green at this HEAD, not merely carried forward.
>
> **Resync 2026-06-29 (HEAD `d12196618`):** the inventory audit (`PBPK_INVENTORY_AUDIT_2026-06-29`) and the ω²/u_c separation PR re-grounded several rows against the current HEAD (not the 2026-05-21 `8c6631a2a` worktree): the three formerly-"unauthored" modules now exist and compile; Rodgers-Rowland, VoI and the PopSim ω²/u_c separation are added below. Parity gate now 19 cases (5 canonical drugs incl. midazolam DDI), suite gate 51/53 (2 PENDING `awaiting_observed_data`).

- `scripts/ci/dissertation_pbpk_suite_gate.sh` is the broad CPU PBPK evidence gate. It currently includes rapamycin, haloperidol, haloperidol PGx, olanzapine D2/mTOR, tirzepatide, vancomycin, tacrolimus, tacrolimus-sirolimus DDI, cross-drug ISO budget, PBPK28 epistemic/Sobol-PCE witnesses, smoke demos, and supporting ML/scenario examples.
- `scripts/ci/dissertation_pbpk28_parity_gate.sh` is the PBPK28 parity gate. It checks Node/Sounio PBPK28 agreement, PBPK28 mass conservation, TMDD/PD parity, semaglutide PBPK28/TMDD/PD parity, and publishes model-reduction/model-form CSV artifacts. The newer PBPK28 epistemic and Sobol/PCE witnesses are separately covered by the broad dissertation PBPK suite.
- **New as of 2026-05-17:** the PBPK28 TMDD/PD/epistemic logic is now first-class **Sounio stdlib**, not only JS-engine + parity-ref. `stdlib/darwin_pbpk/epistemic_pbpk28.sio` (first-order GUM + Sobol/PCE), `epistemic_pbpk28_hessian.sio` (second-order budget), `tmdd/fkbp12_mtorc1.sio`, `tmdd/glp1r.sio`, `pd/coronary_smc_prolif.sio`, and `pd/bergman_glucose_insulin.sio` exist and are mutually consistent with the JS engine and parity refs. Per `audit/gap_report.json` (re-audited 2026-05-21), the remaining PBPK28 gaps are numeric (draft-vs-implementation parameter reconciliation). **UPDATE 2026-06-29 (HEAD `d12196618`):** the three modules previously listed unauthored (`core/tissue_composition.sio`, `core/peptide_partitioning.sio`, `tmdd/qe_approximation.sio`) are now confirmed **authored and compile-clean** — they exist as standalone/reference modules (see the corrected table row); the "unauthored" status was a stale-HEAD (`8c6631a2a`) artifact.
- `scripts/ci/dissertation_pbpk_hessian_gate.sh` is the second-order PBPK14 Hessian GUM gate. It checks the Hessian module, a quadratic-recovery unit witness, an end-to-end residual-reduction witness, and bytewise drift against `benchmarks/pbpk/hessian_budget.csv`.
- GPU dissertation claims remain scoped to bounded K-AXI witnesses and Kretikos gates. The current table still does not support "PBPK14 Tsit5 single-kernel GPU" wording.
- Compiler/bootstrap gates may be cited as enabling infrastructure only. They are not PBPK model-evidence gates unless a row names a dedicated PBPK source/gate pair.

> **Engine dependency (verified 2026-08-17) — read before citing any `repo-backed` row below.**
> Sounio ships two compiler engines: default Madaros (`bin/souc`, per `CLAUDE.md` §4) and the
> bootstrap seed (`SOUNIO_SOUC_ENGINE=lean_single` / `bin/souc-lean-single-x86_64`). The evidence
> gate behind most `repo-backed` rows in this table — `scripts/ci/dissertation_pbpk_suite_gate.sh`,
> which rows 62, 64, 65, 68, 69, 70, 80, 81, 86 and 87 all cite — is **structurally pinned to
> lean_single**: the gate's own source (`scripts/ci/dissertation_pbpk_suite_gate.sh:56-61`) defaults
> `SOUC_BIN` to `scripts/ci/souc-seq-leansingle.sh` because the K-AXI fusion witness and `Seq<T>`
> tests need a capability Madaros does not yet carry, and says so directly: *"`bin/souc` defaults to
> Madaros (the user-facing engine since 2026-06-14), which does not yet carry `Seq<T>`."* That means
> the gate — and therefore every "gate PASS" claim in this table's evidence columns — has not been
> run, as a gate, under the engine `CLAUDE.md` documents as this project's default.
>
> This is not a hypothetical gap. Live check (2026-08-17), row 69's canonical witness,
> `tests/run-pass/med/vancomycin_full_propagation.sio`:
> ```
> $ SOUNIO_STDLIB_PATH=stdlib bin/souc check tests/run-pass/med/vancomycin_full_propagation.sio
> error[E036]: confidence bound is not tight enough
> $ SOUNIO_STDLIB_PATH=stdlib SOUNIO_SOUC_ENGINE=lean_single bin/souc check tests/run-pass/med/vancomycin_full_propagation.sio
> elf: ... 43315 bytes   (clean compile)
> ```
> Not every cited file diverges the same way — e.g. row 62's `stdlib/darwin_pbpk/epistemic_pbpk28.sio`
> checks clean under both engines today — so this note does **not** claim every row's file currently
> fails under Madaros. What is verified and general is the structural fact: the suite gate itself
> cannot currently execute under default Madaros, so "the dissertation gate is green" is, at best, a
> lean_single-scoped claim until someone runs the suite under Madaros end-to-end and records the
> result. Cite rows accordingly: "repo-backed, verified under lean_single" is accurate; "repo-backed"
> alone, read against `CLAUDE.md`'s default-engine framing, is not.

| Claim | Status | Evidence | Allowed wording | Forbidden wording | Next gate |
|---|---|---|---|---|---|
| PBPK14 Tsit5/GUM CPU model exists for rapamycin/sirolimus with a 14-compartment state and finite-difference GUM propagation. | repo-backed | `stdlib/darwin_pbpk/epistemic_pbpk14.sio`; `stdlib/darwin_pbpk/tsit5_pbpk14.sio`; `scripts/ci/dissertation_pbpk_suite_gate.sh` | "The PBPK14 stdlib demonstrates adaptive Tsit5 simulation with numerical GUM propagation for the rapamycin case study." | "The PBPK14 model is GPU-first." | Keep `bash scripts/ci/dissertation_pbpk_suite_gate.sh` green and cite the exact source/gate pair. |
| PBPK14 GUM uses reference plus seven finite-difference perturbation simulations over epistemic parameters. | repo-backed | `stdlib/darwin_pbpk/epistemic_pbpk14.sio` (`ep14_perturb`, `ep14_fdiff_h`, `ep14_run`); `scripts/ci/dissertation_pbpk_suite_gate.sh` | "The current PBPK14 path computes first-order numerical GUM sensitivities over seven rapamycin epistemic parameters." | "The current PBPK14 path proves all parameter uncertainty exactly." | Add or keep a focused PBPK14 self-test that checks the seven perturbation path and PASS marker through the dissertation gate. |
| PBPK14 reports AUC, Cmax, Tmax, terminal compartment concentrations, mass, and uncertainty summaries. | repo-backed | `stdlib/darwin_pbpk/epistemic_pbpk14.sio` (`PBPK14Metrics`, `simulate_14`, `EpResult14`); `scripts/ci/dissertation_pbpk_suite_gate.sh` | "The PBPK14 witness tracks AUC/Cmax/Tmax and propagates GUM variance to selected outputs." | "The PBPK14 witness establishes clinically calibrated posterior risk probabilities." | Keep the metric checks in the PBPK dissertation suite and add golden output snapshots before quoting exact values. |
| Rapamycin priors and confidence scores are encoded for clearance, binding, and partition parameters. | repo-backed | `stdlib/darwin_pbpk/epistemic_pbpk14.sio` (`ep14_rapamycin_priors`); `stdlib/darwin_pbpk/drugs/rapamycin.sio`; `scripts/ci/dissertation_pbpk_suite_gate.sh` | "The rapamycin PBPK14 example attaches evidence-quality confidence scores to literature-derived priors." | "The confidence scores are Bayesian posterior probabilities" or "validated clinical probabilities." | If using clinical language, add a calibration/validation gate that estimates posterior predictive performance against held-out clinical data. |
| PBPK14 confidence aggregation weights prior confidence by AUC sensitivity contribution. | repo-backed | `stdlib/darwin_pbpk/epistemic_pbpk14.sio` (`sensitivity`, `auc_conf` in `ep14_run`) | "The implementation computes an evidence-weighted AUC confidence score from sensitivity fractions." | "The implementation outputs a patient-level probability of efficacy, toxicity, or safety." | Add a documented calibration layer before making probability or clinical-decision claims. |
| Dissertation PBPK suite covers CPU examples across rapamycin, haloperidol, tirzepatide, vancomycin, tacrolimus, tacrolimus-sirolimus DDI, cross-drug ISO budget, scenarios, ML demos, and smoke demos. | repo-backed | `scripts/ci/dissertation_pbpk_suite_gate.sh`; files named in that gate under `tests/run-pass/`, `stdlib/darwin_pbpk/validation/`, `stdlib/darwin_pbpk/pd/`, `stdlib/darwin_pbpk/scenarios/`, `stdlib/darwin_pbpk/ddi/`, `stdlib/darwin_pbpk/ml/`, and `examples/` | "The dissertation PBPK suite is a CPU evidence gate spanning multiple drug classes and PBPK/GUM witnesses." | "The dissertation PBPK suite validates GPU PBPK14." | Keep `bash scripts/ci/dissertation_pbpk_suite_gate.sh` green and archive the gate output with dissertation evidence bundles. |
| PBPK28 permeability-limited model has Node/Sounio parity for rapamycin and semaglutide, plus TMDD/PD parity and mass-conservation checks. | repo-backed | `scripts/ci/dissertation_pbpk28_parity_gate.sh`; `tests/run-pass/dissertation_pbpk28_parity_ref_rapamycin.sio`; `tests/run-pass/dissertation_pbpk28_parity_ref_semaglutide.sio`; `scripts/dissertation/run_pbpk28_node.mjs`; generated `benchmarks/pbpk/qss_residual.csv` and `benchmarks/pbpk/model_form_uc.csv` when the gate runs | "The PBPK28 dissertation gate checks permeability-limited PBPK28 parity, mass conservation, and selected TMDD/PD layers for rapamycin and semaglutide." | "PBPK28 proves PBPK14 clinical accuracy" or "PBPK28 replaces the PBPK14 evidence surface." | Keep `bash scripts/ci/dissertation_pbpk28_parity_gate.sh` green; cite PBPK28 separately from PBPK14 because the gate explicitly records model-form differences. |
| PBPK28 TMDD/PD/epistemic logic exists as Sounio stdlib (not only JS engine + parity-ref) as of 2026-05-17. | repo-backed | `stdlib/darwin_pbpk/epistemic_pbpk28.sio` (first-order GUM + Sobol/PCE, self-test); `stdlib/darwin_pbpk/epistemic_pbpk28_hessian.sio`; `stdlib/darwin_pbpk/tmdd/fkbp12_mtorc1.sio`; `stdlib/darwin_pbpk/tmdd/glp1r.sio`; `stdlib/darwin_pbpk/pd/coronary_smc_prolif.sio`; `stdlib/darwin_pbpk/pd/bergman_glucose_insulin.sio`; covered by `scripts/ci/dissertation_pbpk_suite_gate.sh` (PBPK28 epistemic/Sobol-PCE witnesses) | "The PBPK28 TMDD, PD, first-order GUM and Hessian layers are implemented as Sounio stdlib modules, consistent with the JS engine and parity references." | "The PBPK28 stdlib parameters are clinically calibrated." (Per-parameter epistemic confidence IS now carried — see the next row — but calibration is a separate, unmet claim.) | Keep the suite gate green; reconcile draft-vs-implementation parameters (`audit/gap_report.json`: `*_tmdd_params`, `*_pd_params`, `bergman_params`) before quoting exact constants. |
| **PBPK28 priors are declared as Knowledge<T>** carrying value+variance+ε+provenance (migrated 2026-05-31 from parallel f64 arrays). | repo-backed | `stdlib/darwin_pbpk/epistemic_pbpk28.sio` `EpPrior28.kn: [Knowledge[f64]; 8]`, built in `ep28_rapamycin_priors`/`ep28_semaglutide_priors` (commit `7295004`); enabled by arrays-of-Knowledge support (`d6f2d4d`). **`kn` is now the SINGLE source of truth** — the parallel f64 arrays were deleted (`77e8111`) and EVERY consumer (ep28_run, Hessian, cumulants, MC validators) reads `kn[i].value/.variance/.confidence`; verified value-preserving by the KN_EQUIV substitution-equivalence (incl. a perturbed prior) + +0 net errors across all 6 EpPrior28 files. | "Each PBPK28 prior is declared as a Knowledge<f64> carrying its value, variance, evidence-quality ε and literature provenance — the per-parameter epistemic confidence lives in the type, as the single representation (no parallel f64 array)." | "ep28_run enforces/propagates the ε at compile time" — it does NOT: even though ep28_run now READS each parameter from `kn[i].value/.variance/.confidence` (commit `2e67d5a`), the EpistemicComplete gate is construction-site-local (it fires on inline `Knowledge(.., ε=lit)` literals in a gated fn, per `dissertation_pbpk28_confidence_gate.sio`, not through struct fields / a function reading kn). | DONE: `ep28_run` consumes the Knowledge priors directly (output-preserving by the kn[i].value==mean[i] equivalence). Allowed: "the GUM kernel consumes the Knowledge<T> priors." Gate-through-kernel enforcement would need the gate to propagate confidence through struct-field reads — a separate inference-pass change. |
| **CORRECTED 2026-06-29 (HEAD `d12196618`):** the three modules formerly listed "NOT yet authored" ARE authored and compile-clean. | repo-backed (authored, compile-clean; standalone) | At HEAD `d12196618` all three exist and run under `lean_single` (exit 0): `core/tissue_composition.sio` (172 LOC), `core/peptide_partitioning.sio` (138 LOC), `tmdd/qe_approximation.sio` (134 LOC). NB: no hard `use`-import wires them into the active solver yet — standalone/reference modules; the active PBPK28 Kp path still uses empirical Kp. (Prior "re-verified absent at `8c6631a2a`" was a stale-HEAD artifact.) | "Tissue composition, peptide partitioning and the QE-approximation modules are authored and compile-clean (Rodgers-Rowland tissue inputs / Mager-Jusko rapid-binding), available as reference layers." | "They are wired into the active PBPK28 Kp solver / parity gate" (they are standalone, not yet imported by the canonical solver). | Wire into the active Kp/TMDD path (a hard `use` + a self-test/parity marker), or keep as documented reference capability. |
| **Rodgers-Rowland mechanistic Kp prediction exists** (was not even tracked). | repo-backed (authored, compile-clean; reference layer) | `stdlib/darwin_pbpk/core/rodgers_rowland.sio` (441 LOC, exit 0 under lean_single; `rr_kp`/`rr_kp_gum`/`rr_log_sensitivity`); consumes `tissue_composition.sio`. Used as a GUM reference layer, NOT in the active ODE (which uses empirical Kp). | "A mechanistic Rodgers-Rowland Kp predictor (tissue-composition based, with GUM Eq.13 sensitivity) is authored and compile-clean, used as the uncertainty reference layer." | "Rodgers-Rowland is the Kp engine of the active PBPK28 solver" (the solver uses literature/empirical Kp; R-R is a reference/GUM layer). | Wire R-R into the active Kp path behind a parity check, or cite as reference-layer capability. |
| **Value-of-Information (VoI) ranking exists** for BBB and D2. | repo-backed | `stdlib/darwin_pbpk/bbb/bbb_voi.sio` (154 LOC), `stdlib/darwin_pbpk/pd/d2_voi.sio` (394 LOC); VoI = Sᵢ·(1−confidenceᵢ); d2_voi self-test 8/8 PASS; exercised inside `dissertation_pbpk_suite_gate.sh`. | "An EVSI-style VoI ranking (Sobol share × (1−confidence)) prioritises which parameter to measure next — e.g. measure BBB permeability before more D2-binding assays." | "VoI is clinically validated" or "VoI drives a calibrated dosing decision." | Keep the suite gate green; add a documented VoI worked example to the dissertation text. |
| **PopSim separates ω² (population IIV) from u_c (epistemic GUM uncertainty)** — corrects a prior conflation. | repo-backed | `stdlib/darwin_pbpk/population/pop_epistemic.sio` nested layer (outer GUM draws of θ_pop × inner η~N(0,ω²) IIV); `tests/run-pass/darwin_pop_epistemic_smoke.sio` proves EXACT orthogonality (`POP_EPISTEMIC_SEPARATION_PASS`: varying ω moves the IIV P95/P5 ratio but not the epistemic median-CV, and vice-versa) + backward-compatible collapse (`POP_EPISTEMIC_COLLAPSE_TO_BASELINE_PASS`: u_c→0 recovers flat PopSim); `pop_sim.sio` wording corrected (IIV ≠ epistemic). | "The population layer distinguishes real between-subject variability (ω², widens P5–P95) from epistemic uncertainty about the population parameters (u_c, a confidence band on the summary), nested as outer-GUM × inner-IIV; the separation is demonstrated to be orthogonal and to collapse to flat PopSim when u_c→0." | "The nested layer is calibrated against clinical population data" (the exposure model is the analytic linear AUC=F·Dose/CL; the contribution is the uncertainty STRUCTURE, not a clinical fit). | Apply the nested layer to a drug-anchored case (e.g. venlafaxine CYP2D6-phenotype IIV × GUM-on-CL_form) with a data-backed decomposition. |
| The `Knowledge(v, ε=…, prov=…)` paren-form constructor compiles (named-arg parser fix landed 2026-05-30). | repo-backed | `self-hosted/compiler/lean_single.sio` (`compile_knowledge_paren_x86`/`_a64`, commit `18855a5`); previously rejected with E200. bin/souc canonized to fixed point `fa033ee` (commit `6c4a571`). | "The epistemic `Knowledge(value, ε=eps, prov=…)` constructor parses and lowers (positional value + named ε/prov, x86 + a64)." | "the constructor alone makes PBPK28 priors Knowledge-typed" — the constructor is a language capability; the stdlib-prior migration is tracked separately (see the single-source row above). | Done. Stdlib-prior migration + type-alias→Knowledge resolution both landed later (see following rows). |
| Knowledge<T>-**alias** types resolve in binding/return/param positions; the canonical clinical witness type-checks and runs. | repo-backed | `self-hosted/compiler/lean_single.sio` (scan_type alias guard + Pass-1c.5 FN_RET re-store, both `knowledge_hash_is`-confined, commit `9224abd`); `tests/run-pass/med/vancomycin_full_propagation.sio` compiles + runs (exit 0). Verified across 1282 tests: only this witness flipped COMPFAIL→OK, 0 regressions. | "`type CrCl = Knowledge[f64]` aliases resolve to the Knowledge type in let bindings, function returns and arguments; the vancomycin witness now type-checks, resolves its aliases, passes effect-gating and runs." | "the vancomycin witness validates a dosing computation" — it prints Knowledge struct pointers with NO assertions/PASS marker; exit 0 proves it type-checks + runs, NOT numeric/clinical correctness. Also NOT "general type aliases resolve" — only Knowledge<T> aliases (a `type Foo = SomeStruct` return keeps the Pass-1 ordering limitation). | Optional: add a `.value`-based assertion + PASS marker to make it a true witness; lift the Pass-1 ordering fix to non-Knowledge alias returns. |
| **Contribution 2 (compile-time confidence gates):** a witness DEMONSTRATES that the EpistemicComplete gate rejects an over-confident dosing function built from real rapamycin priors. | repo-backed (language capability + witness) | ε wired into the gate in `lean_single.sio` (`Knowledge` case in the inference-pass builtin branch, commit `647c482`); `tests/run-pass/dissertation_pbpk28_confidence_gate.sio` (honest `with Epistemic(400)` compiles+runs) + `tests/compile-fail/dissertation_pbpk28_overclaim.sio` (`with Epistemic(950)` on cl_hepatic ε=0.65 → EpistemicComplete violation, conf=650<950, no binary); gated by `scripts/ci/dissertation_confidence_gate_gate.sh` (PASS). | "Sounio's `with Epistemic(N)` gate rejects, at compile time, a dosing function that claims more confidence than its weakest input prior supports — demonstrated on rapamycin PBPK28 prior values." | "The PBPK28 stdlib carries per-parameter epistemic confidence" (stdlib unchanged f64). "The gate certifies combined/propagated confidence" (it is PER-INPUT: every input expr ≥ N, not the compounded result). "A computed/runtime ε satisfies the gate" — it does NOT: a non-literal ε is conservatively rejected (conf=1, commit `f5ef37b`, regression-locked by `tests/compile-fail/knowledge_nonliteral_eps_ungated.sio`), so confidence cannot be laundered through a variable; a named-constant ε is likewise rejected (const-folding is future work). | Keep `dissertation_confidence_gate_gate.sh` green (3 checks); migrate stdlib priors to Knowledge<T> before any stronger claim. |
| PBPK14 second-order Hessian GUM correction exists as a bounded dissertation witness. | repo-backed | `stdlib/darwin_pbpk/epistemic_pbpk14_hessian.sio`; `tests/stdlib/darwin_pbpk/hessian_correction_test.sio`; `tests/run-pass/dissertation_pbpk14_hessian.sio`; `benchmarks/pbpk/hessian_budget.csv`; `scripts/ci/dissertation_pbpk_hessian_gate.sh` | "The PBPK14 Hessian gate demonstrates a deterministic second-order GUM correction witness and bytewise golden drift checking for the rapamycin AUC Hessian budget." | "The Hessian witness proves global nonlinear PBPK uncertainty exactly" or "second-order GUM is clinically calibrated." | Keep `bash scripts/ci/dissertation_pbpk_hessian_gate.sh` green and regenerate the golden only with an explicit math-review/offload trail. |
| GPU K-AXI Phase Y validates GUM-through-ODE for a narrow 2-compartment PBPK witness. | repo-backed | `scripts/ci/kretikos_kaxi_phase_y_gate.sh`; `tests/golden/kaxi_ptx/f32_gum/pbpk_2comp_gum_4step.ptx`; `scripts/gpu/kaxi_pbpk_2comp_gum_sampler.c`; `scripts/gpu/kaxi_ptx_runner.c` | "Sounio has a GPU-validated K-AXI witness for 2-compartment PBPK/GUM over `pbpk_2comp_gum_4step`." | "PBPK14 currently compiles to one GPU kernel" or "all PBPK/GUM models are GPU validated." | Keep `bash scripts/ci/kretikos_kaxi_phase_y_gate.sh` green on CUDA hardware; record skip status when CUDA is absent. |
| Phase Y GPU truth claims are digest equality for C1 and V11 plus a well-formed ISO-style GUM budget. | repo-backed | `scripts/ci/kretikos_kaxi_phase_y_gate.sh`; generated `benchmarks/pbpk/gum_budget.csv` when the gate runs | "Phase Y checks exact f32 digest agreement against a CPU analytic sampler and emits an ISO-style uncertainty budget for the 2-comp witness." | "Phase Y proves PBPK14 Tsit5 GPU correctness." | Store gate logs/artifacts in the dissertation dossier and keep the claim scoped to the 2-comp Euler witness. |
| PBPK14 GPU-first Tsit5 single-kernel implementation exists. | future-work | Current repo contains `stdlib/darwin_pbpk/tsit5_pbpk14.sio` and `stdlib/darwin_pbpk/epistemic_pbpk14.sio`, but not GPU-specific PBPK14 source or a PBPK14 GPU gate. | "A PBPK14 GPU Tsit5 kernel is a future extension." | "PBPK14 Tsit5 currently compiles to one GPU kernel with 8 warps." | Create a GPU PBPK14 source file, generated PTX artifact, CPU/GPU numerical parity gate, and hardware run log. |
| `stdlib/darwin_pbpk/epistemic_pbpk14_gpu.sio` or `stdlib/darwin_pbpk/tsit5_pbpk14_gpu.sio` backs dissertation GPU claims. | unsupported/overclaim | These files are absent on `origin/main` at the time of this table. | "No current repo file by that name backs the claim." | "The PBPK14 GPU files implement the dissertation GPU model." | Add the files, review their semantics, and wire them into a named CI gate before citing them. |
| `compiler/codegen/gpu/epistemic.rs` backs PBPK/GUM GPU lowering. | unsupported/overclaim | `compiler/codegen/gpu/epistemic.rs` is absent on `origin/main`; active GPU evidence in this area is under Sounio/K-AXI scripts and golden PTX. | "Current GPU evidence is K-AXI artifact/gate based, not a Rust `compiler/codegen/gpu/epistemic.rs` path." | "The Rust GPU epistemic codegen path proves PBPK14 lowering." | Add the file only if the repo intentionally reintroduces that codegen surface, then gate it. |
| `scripts/souc_v2_gate.sh` is a PBPK/GPU evidence gate. | unsupported/overclaim | `scripts/souc_v2_gate.sh` is absent; `scripts/ci/souc_v2_gate.sh` exists but is a bootstrap/compiler fixed-point and feature gate, not a PBPK14 GPU gate. | "The existing `scripts/ci/souc_v2_gate.sh` is compiler/bootstrap evidence, not PBPK/GPU model evidence." | "souc_v2 proves PBPK14 GPU semantics." | If needed, create a dedicated PBPK14 GPU gate instead of reusing the bootstrap gate as semantic evidence. |
| GPU speedups such as `180-420x` are established for PBPK/GUM. | unsupported/benchmark-needed | No cited PBPK/GUM benchmark gate or committed result in this evidence set establishes that range. `scripts/ci/kretikos_kaxi_phase_y_gate.sh` says correctness focus, not throughput. | "Performance has not yet been established by a PBPK/GUM benchmark in this table." | "Sounio PBPK/GUM is 180-420x faster on GPU." | Add reproducible benchmark scripts, hardware metadata, CPU baseline, GPU result artifacts, confidence intervals, and an offload/publication review before quoting speedups. |
| Sounio demonstrates GUM-through-ODE in PBPK14 and GPU-validated K-AXI kernels for narrower PBPK/GUM witnesses. | repo-backed | CPU: `stdlib/darwin_pbpk/epistemic_pbpk14.sio`, `scripts/ci/dissertation_pbpk_suite_gate.sh`. GPU: `scripts/ci/kretikos_kaxi_phase_y_gate.sh`, `tests/golden/kaxi_ptx/f32_gum/pbpk_2comp_gum_4step.ptx`. | "Sounio demonstrates GUM-through-ODE in the PBPK14 stdlib and GPU-validated K-AXI kernels for narrower PBPK/GUM witnesses." | "Sounio already has a GPU-first PBPK14 Tsit5 single-kernel implementation." | Keep both gates green; include the CPU/GPU scope distinction in dissertation prose. |
| Tacrolimus 14-compartment Tsit5 PBPK validation against Jusko 1995 / Kershner 1996 / Staatz 2004. | repo-backed | `stdlib/darwin_pbpk/drugs/tacrolimus.sio`, `stdlib/darwin_pbpk/validation/tacrolimus_oral_pbpk.sio`, `scripts/ci/dissertation_pbpk_suite_gate.sh` (entry `tacrolimus_oral_pbpk`). | "The tacrolimus PBPK validation reproduces Vd_ss ≈ 1300 L, t½ ≈ 30 h, and brain/blood ratio < 0.20 within GMFE ≤ 3.0 of literature reference values." | "The tacrolimus PBPK validation is a clinically calibrated dosing engine." | Keep the gate green; cite the exact source file pair when quoting numerical findings. |
| Tirzepatide SC peptide PBPK/GUM witness covers absorption, GLP-1R/GIPR occupancy, and framework-boundary audit. | repo-backed | `stdlib/darwin_pbpk/validation/tirzepatide_sc_pbpk.sio`; `stdlib/darwin_pbpk/pd/glp1_gipr_gum.sio`; `examples/dissertation_tirzepatide_demo.sio`; `scripts/ci/dissertation_pbpk_suite_gate.sh` (entries `tirzepatide_sc_pbpk`, `glp1_gipr_gum`, `dissertation_tirzepatide_demo`). | "The tirzepatide entries extend the CPU PBPK/GUM dissertation suite to a peptide-class SC absorption witness with dual receptor occupancy uncertainty." | "The tirzepatide witness is a clinical prescribing engine" or "it validates all GLP-1/GIP coagonists." | Keep the three tirzepatide gate entries green and cite the framework-boundary output before broadening the claim. |
| Vancomycin ICU PBPK/GUM witness exists inside the CPU dissertation suite. | repo-backed | `stdlib/darwin_pbpk/validation/vancomycin_icu_pbpk.sio`; `stdlib/darwin_pbpk/pd/vancomycin_auc_gum.sio`; `examples/dissertation_vancomycin_demo.sio`; `scripts/ci/dissertation_pbpk_suite_gate.sh` (entries `vancomycin_icu_pbpk`, `vancomycin_auc_gum`, `dissertation_vancomycin_demo`). | "The dissertation PBPK suite includes a vancomycin ICU AUC/GUM witness." | "The vancomycin witness is a patient-safe dosing system" or "it is clinically deployable." | Clinical-pathway edits require the DeepSeek offload route in `.claude/AGENT_OFFLOAD_POLICY.md`; keep the suite green before citing. |
| F_oral dominates the tacrolimus C24h GUM budget under the dissertation's priors. | repo-backed | `stdlib/darwin_pbpk/pd/tacrolimus_trough_gum.sio` (14-comp simulator); `stdlib/darwin_pbpk/validation/cross_drug_iso_budget.sio` (closed-form); `scripts/ci/dissertation_pbpk_suite_gate.sh`. | "Under the dissertation's priors, F_oral contributes the largest single fraction of the tacrolimus C24h variance budget (54 % in the 14-compartment simulator, 63 % in the closed-form cross-drug synthesis)." | "F_oral dominates clinical tacrolimus failure modes in practice." | Keep both gates green; re-run when priors change; cite the priors source. |
| Dominant uncertainty source is drug-class-dependent across rapamycin, haloperidol, vancomycin, and tacrolimus. | repo-backed | `stdlib/darwin_pbpk/validation/cross_drug_iso_budget.sio` (computed inline, no hardcoded percentages); `docs/dissertation/cross_drug_iso_budget_findings.md`; `scripts/ci/dissertation_pbpk_suite_gate.sh` (entry `cross_drug_iso_budget`). | "The cross-drug ISO budget shows distinct dominant sources per drug class — CL for rapamycin, kp_brain for haloperidol, MIC for vancomycin, F_oral for tacrolimus — supporting drug-class-specific TDM strategy." | "TDM is unnecessary because all four drugs share the same dominant uncertainty source." | Keep the gate green and re-cite the run output when the CV priors are updated in the literature. |
| Tacrolimus + sirolimus DDI adds an irreducible uncertainty class bounded below by the Undre 1999 (n=12) single-study floor. | repo-backed | `stdlib/darwin_pbpk/ddi/tacrolimus_sirolimus_ddi.sio` (mechanistic P-gp inhibition); `stdlib/darwin_pbpk/validation/tacrolimus_sirolimus_ddi_clinical.sio`; `formal/lean4/SounioTacrolimusDDI.lean` (monotonicity statements). | "The tacrolimus + sirolimus DDI module models the Undre 1999 30 % F_oral boost with a 40 % CV epistemic floor and propagates it as a Fréchet-enclosed PBox; co-administration shifts the Knightian-conservative C24h band to straddle the 15 ng/mL upper toxicity limit." | "The DDI module quantifies tacrolimus toxicity risk under sirolimus co-administration to clinical-trial precision." | Re-run the clinical validation when new DDI PK studies land; cite the Undre 1999 + Lampen 1998 pair when quoting the central estimates. |
| **Language capability (no CLINICAL use):** Sounio has a native multi-measurement epistemic-fusion capability: `uncertainty_of()` reads a `Knowledge<f64>` std-dev, and `kaxi_fuse()` combines N independent `Knowledge<f64>` measurements of one quantity by inverse-variance (precision) weighting (`σ²_post = 1/Σ(1/σᵢ²)`, `μ_post = σ²_post·Σ(μᵢ/σᵢ²)`). | repo-backed (language capability) | `self-hosted/compiler/lean_single.sio` (`compile_uncertainty_of_call_x86`/`_a64`); `stdlib/epistemic/kaxi.sio` (`kaxi_fuse`); run-pass `tests/stdlib/epistemic/test_kaxi_fuse.sio` (in the Full Test Suite gate); merged `b4ead791a`; M1 math-review (xai/Grok) logged in `.claude/llm_offload_log.md`. Verified x86 + real Apple Silicon: `measure(10,u=2)` ⊕ `measure(12,u=1)` → mean 11.6, σ √0.8 ≈ 0.894, **equal to the pairwise `observe()` update** (fusion = N-measurement generalization of the normal-normal conjugate step). | "Sounio provides a native inverse-variance fusion (`kaxi_fuse`) that combines independent `Knowledge<f64>` measurements into a single posterior, order-independently and consistently with the pairwise `observe` update; this is the language primitive for ISO-style measurement combination." | "`kaxi_fuse` is clinically validated for PBPK," "it fuses patient PK observations into calibrated posteriors," or "it is wired into the PBPK uncertainty budget." (A PBPK *method* witness now exists — see next row — but there is NO clinical validation; σ>0 required; empty input returns a no-information default.) | **✓ SATISFIED 2026-05-25 (PR #199, `0db01233f`):** wired into the `rapamycin_kaxi_fuse_prior` witness, gated by `dissertation_pbpk_suite_gate.sh` (PASS). Remaining future-work: validate against real (non-synthetic) repeated measurements + a posterior-predictive calibration check before any clinical/individualization claim. |
| `kaxi_fuse` is wired into a PBPK prior-assimilation **method** witness behind the dissertation suite gate: inverse-variance assimilation of repeated rapamycin hepatic-clearance observations into the Ferron 1997 population prior (normal-normal Bayesian-forecasting step). | repo-backed | `tests/run-pass/rapamycin_kaxi_fuse_prior.sio` (asserts the posterior SD equals the closed-form `1/√Σ(1/σᵢ²)` and lies in the data envelope); `scripts/ci/dissertation_pbpk_suite_gate.sh` (entry `rapamycin_kaxi_fuse_prior`, gate PASS); M2 clinical review (deepseek) logged in `.claude/llm_offload_log.md`. | "A dissertation witness demonstrates inverse-variance assimilation of repeated clearance observations into the rapamycin population prior (the normal-normal Bayesian-forecasting step), narrowing the clearance estimate, gated by the dissertation PBPK suite gate — as a method demonstration." | "The witness individualizes real-patient rapamycin dosing," "it is a validated TDM/dose-individualization engine," "the result is suitable for individual forecasting," or "the narrowing represents clinical accuracy." (The individual observations are **illustrative/synthetic**; the prior is renal-transplant-specific; the SD narrowing is a mathematical consequence of precision-weighting, not a clinical claim.) | Replace synthetic observations with real repeated measurements and add a posterior-predictive calibration check before any clinical or dose-individualization claim. |

## Safe Dissertation Language

Say:

> Sounio demonstrates GUM-through-ODE in the PBPK14 stdlib and GPU-validated K-AXI kernels for narrower PBPK/GUM witnesses.

Do not say:

> PBPK14 Tsit5 currently compiles to one GPU kernel with 8 warps.

## Review Checklist

- Every `repo-backed` row names at least one repo path and, where available, an executable gate.
- CPU PBPK claims distinguish the broad PBPK suite, PBPK28 parity gate, and PBPK14 Hessian gate instead of treating them as interchangeable.
- GPU claims are scoped either to `pbpk_2comp_gum_4step` Phase Y evidence or marked future/unsupported.
- Confidence scores are evidence-quality scores, not clinical posterior probabilities.
- Speedup claims require a dedicated benchmark gate and committed results before use.
