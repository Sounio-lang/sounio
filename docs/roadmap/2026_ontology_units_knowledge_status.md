<!-- docs:meta
topic_id: repo.docs.roadmap.2026-ontology-units-knowledge-status
authority: repo_only
audience: users
last_validated: 2026-03-07
validated_by: A2
source_of_truth: docs/governance/topic-registry.v1.json#repo.docs.roadmap.2026-ontology-units-knowledge-status
-->

# Sounio Semantic Knowledge Spine Status

Roadmap: `docs/roadmap/2026_ontology_units_knowledge_plan.md`

Short name: Semantic Knowledge Spine.

## Current Checkpoint

Date: 2026-05-25

Current live blockers:

- `K2-CHECK-FULL-SEMANTIC-BRIDGE`: the ordinary
  `check::mod -> check::check` semantic bridge remains incomplete; standalone
  bridge classifiers are useful, but they are no longer part of the focused
  Semantic Knowledge Spine acceptance gate.
- `CURRENT-SOURCE-MAIN-DRIVER-BUILD`: the full current-source
  `self-hosted/compiler/main.sio` driver does not build in this workspace, so
  the imported-cache frontend composition proof is currently carried by the
  lean frontend driver instead of the full main driver.
- Native backend runtime guard/trap lowering for dynamic `Knowledge<T>` fields
  and native compiler `//@ ontology-bundle` live checker-kernel side-table
  loading remain future work. A pre-native
  directive expansion gate now exists and is separate from the native
  compiler-side loader; it now includes deterministic text `.ontocache` reuse
  for repeated expansion. The self-hosted compiler now also has a Sounio-side
  raw-source scanner that can extract the directive bundle path before lexing
  drops comments, build a load plan, verify that the referenced `.dontology`
  bundle exists, has bytes, carries the deterministic `DONT` v1 envelope, and
  has payload-size/file-size consistency. The load plan also summarizes the
  canonical payload shape (`ontology`, `term_count`, `terms`,
  `disjoint_count`, `disjoints`), extracts the ontology name and first term
  CURIE, materializes the first child-parent subclass edge, and materializes
  the first disjoint pair when present. It now also builds a bounded compact
  side-table prefix with primitive scalar/parallel fields for class IDs, parent
  edges, and disjoint pairs, plus an explicit truncation flag, and answers
  direct subclass, bounded subclass-chain, and symmetric disjoint queries over
  that staged prefix. It now stages kernel-shaped rows as class slots plus
  subclass/disjoint axiom rows indexed like `OntologyKernel` materialization,
  and answers equivalent kernel-row queries without mutating
  `Checker.ontology_kernel`. It also materializes a bounded
  `OntologyKernel`-like fact surface from those staged rows, including
  reflexive subclass facts, transitive subclass closure, and symmetric disjoint
  facts. It now also persists those bounded staged kernel facts into a stable
  native side-table `.ontocache` artifact generated by Sounio itself, with an
  explicit authority boundary that the artifact is not the live checker
  kernel. It can also reimport that bounded native `.ontocache` and rehydrate
  staged kernel rows so imported SNOMED and PART caches answer the same bounded
  materialized subclass/disjoint queries as the original load plans. A second
  source file can now declare `//@ ontology-side-table-cache: "..."` and import
  that bounded cache through the raw-source scanner, proving the first
  producer/consumer cross-file cache directive path. The cache directive now
  also exposes a checker-shaped semantic verdict surface: proven bounded
  subclass/disjoint queries return `0`, while rejected subsumption/disjoint
  queries return `152`. Those same bounded imported cache verdicts are now
  consumable through `check::mod` via a check-owned native side-table cache
  reader. A focused `check::check` hydrator now also builds a real `Checker`,
  populates `Checker.ontology_kernel` from the bounded imported cache slots and
  subclass/disjoint axioms, materializes that kernel, and answers the same
  verdicts through `ontology_kernel_is_subclass` and
  `ontology_kernel_is_disjoint`. Routing those live-kernel verdicts through
  `check::mod` via explicit `check::check` imports reintroduced the known
  ordinary Stage1 import side effect and remains future work. A candidate
  sourcecheck hydrator in `check::check` now parses a source file and resolves
  cache/indexes, but still segfaults at imported `collect_items` before
  `collect_ok`; that boundary is now classified by the native scan gate. It
  also has a fresh-symbol `check::mod` sourcecheck cache-verdict bridge that
  runs ordinary sourcecheck first, then accepts the bounded positive subclass
  verdict and rejects the bounded reverse subclass verdict without explicit
  `check::check` imports. A fresh same-Checker kernel classifier now proves the
  pointer-owned path reaches `hydrate_ok` after ordinary sourcecheck, but the
  same `Checker.ontology_kernel` still rejects the positive kernel verdict.
  A second same-Checker copy classifier can hydrate a local cache-derived
  checker kernel before copying, reaches `copy_ok`, and still rejects the
  positive same-Checker verdict. A third same-Checker pointer-query classifier
  also reaches `copy_ok`, then remains classified before a query verdict. A
  value-threaded precheck classifier now reaches `collect_ok` and `hydrate_ok`,
  then remains classified when reading `class_count` from the collected
  checker's hydrated ontology kernel. This narrows the next same-Checker
  problem to stable kernel-state readback/merge after `collect_items`, not
  directive parsing or cache-index resolution. A stable `check::mod`
  same-sourcecheck sidecar now hydrates bounded `.ontocache` facts after
  ordinary sourcecheck and proves SNOMED subclass plus PART subclass/disjoint
  verdicts. That sidecar now also validates bounded imported-cache
  `Knowledge<T>` proof contexts after ordinary sourcecheck, accepting SNOMED
  and PART positive contexts while rejecting reverse SNOMED and disjoint PART
  contexts. The proof-context resolver now maps generic `PREFIX_<numeric-id>`
  names, plus explicit `cache_slotN` names, to the numeric IDs in `.ontocache`
  instead of relying on a fixed ontology-specific symbol list. The path is now
  exposed through a boot4-style source-path verdict API,
  `check_items_verdict_boot4_with_ontocache_sidecar(items, source_path)`,
  returning 0/1 like the ordinary boot4 verdict surface, while still not
  mutating `Checker.ontology_kernel`. The modular frontend now detects
  `//@ ontology-side-table-cache: "..."`, calls that sidecar authority for
  imported-cache ontology proof contexts, and then runs the ordinary
  `check::knowledge_context` value-only validator so numeric and unit
  proof-context constraints remain checked through the usual Sounio checker
  path in the same `Knowledge<T>` type. A direct
  shadow-field experiment
  on the ordinary `Checker` reaches `hydrate_ok` after ordinary sourcecheck,
  but the pointer surface still reads `shadow_count=0` and returns the
  empty-shadow reject verdict. Mutating the same pointer-owned ordinary
  `Checker.ontology_kernel`, reading a hydrated value-threaded kernel after
  collection, promoting the sidecar into ordinary checker authority, or a
  stable direct shadow state surface remains future work. The
  modular bootstrap source
  bundle now includes `self-hosted/check/ontology_side_table_cache.sio` before
  `check.sio`, and `scripts/bootstrap/bootstrap_concat.sh` defaults to the raw
  checked-in `bin/souc-linux-x86_64` binary so the script no longer trips over
  the richer `artifacts/omega` CLI shape before reaching source diagnostics.
  The focused `scripts/bootstrap/run_knowledge_bootstrap_tests.sh` script now
  defaults to the canonical `bin/souc` wrapper for its `check`/`run` interface;
  it therefore reaches the real source typecheck failure instead of stopping at
  `P0021 Invalid item at module level` from the richer `artifacts/omega` CLI. It
  also now has its local hand-built AST literals synchronized with the current
  `Expr`, `ExprList`, `KnowledgeTypeInfo`, `PolicyRequirementInfo`, and `Item`
  field sets; the previous `missing field in struct literal` family is gone
  from the focused bundle. The focused bundle now also includes bootstrap-safe
  lexer/parser/ownership/trait shims, converts several recursive tuple-return
  helper paths to named result structs or integer flags, and filters unrelated
  Wave 10 hypercomplex law-profile probes out of the narrow Knowledge gate. It
  now also has pointer-shaped checker surfaces for the focused
  policy/contest/robustness/decision/deferral/transition witnesses, avoiding
  the large `(Checker, TypeEntry)` return path for direct wrapper lowering and
  the bootstrap proof-object metadata path.
  `bash scripts/bootstrap/run_knowledge_bootstrap_tests.sh` now reaches
  `check exit: 0` and `run exit: 0`; during `run`, B01-B43 pass, covering direct wrapper
  lowering, shorthand `Knowledge<T>`, empty-policy `prove_robust`, explicit
  `Contest<T>` annotation, fail-closed plain/annotation checks, named policy
  requirement satisfaction, robustness proof metadata, validation manifest
  metadata, focused metadata side-table facts, full-arity
  `Contest<T, Family, Policy>` realization, contest witness projection and
  fail-closed witness diagnostics, decision certificates, deferral
  certificates, deferral-reason witnesses, transition plan commitments,
  transition-reason witnesses, monitoring/observed-transition wrapper
  lowering, observed-transition metadata, and rollback-certificate metadata.
  The focused Knowledge bootstrap bundle currently has no remaining B-test
  runtime blocker; broader same-Checker ontology cache hydration and ordinary
  source-file checker-kernel cache hydration remain separate blockers. It still classifies malformed directive
  payloads, invalid bundle envelopes, and
  invalid payload shapes; importer FFI, full term/parent/disjoint ingestion,
  and ordinary source-file check/compile hydration remain future work.
- Dynamic `Knowledge<T>` values now have checker-visible runtime proof
  obligations for nonliteral numeric field constraints at return,
  call-boundary, and assignment sites. Backend guard/trap insertion for those
  obligations is still future work.
- A first pre-native runtime guard expansion now materializes numeric proof
  obligations as generated Sounio `assert(...)` helpers. This is executable and
  proves satisfying dynamic evidence runs while violating dynamic evidence
  fails. It enforces multiple numeric lower-bound constraints conjunctively and
  now also covers an upper-bound `<=` slice, so it is no longer lower-bound
  only. It also covers an equality `==` slice, matching the parser/checker
  proof-context surface for numeric equality constraints. It supports
  unit-suffixed numeric lower bounds by comparing a dimensionless ratio against
  `1.0`. A current-source slice covers internal validation-data unit labels
  such as `mg_dL` under the same
  no-clinical-authority boundary as the metadata/unit-label gates. The expander
  now also has deterministic pre-native text `.guardcache` reuse keyed by
  source bytes and expander bytes. The expanded source now preserves stable
  guard diagnostics for representative lower-bound, conjunctive, upper-bound,
  equality, named-unit, and internal-label constraints as source comments near
  the generated assertions, without adding `IO` effects to guard helpers. The
  expander can also emit a deterministic guard diagnostic manifest keyed by
  the same input/expander hashes, with structured `type`, `field`, `op`,
  `threshold`, `unit`, and `constraint` TSV columns for audit. The first
  generated lower-bound guard pair is also compiled through the default native
  backend: the satisfying witness exits 0 and the violating witness exits 1
  through the native `assert`/trap path. Lower-bound return, call-boundary,
  and assignment positive/reject pairs now also run through the checker
  runtime-obligation probe before and after expansion: original sources report
  nonzero runtime obligations, and expanded sources report zero obligations
  after guard insertion and proof-context lowering. The call-boundary and
  assignment pairs also compile through the default native backend after
  pre-native expansion, with their violating native binaries exiting 1 through
  the same native `assert`/trap path. The obligation-drain and native
  compile/run shape now also covers conjunctive lower bounds, upper bounds,
  equality, unit-suffixed thresholds, and internal validation-data unit labels.
  The checked `bin/souc` launcher now also has an opt-in bridge,
  `SOUNIO_KNOWLEDGE_RUNTIME_GUARDS=1`, that applies the pre-native expansion
  for compile/build/run before invoking the usual selected compiler path; the
  default launcher behavior is unchanged. Source files can also opt into the
  same launcher bridge with `//@ knowledge-runtime-guards`, which keeps the
  runtime guard intent next to the `Knowledge<T>` code while still using the
  pre-native expansion path. The self-hosted compiler side now also has a
  raw-source scanner/probe for the same directive, proving the compiler can see
  the intent before ordinary lexing drops comments. That scanner currently
  classifies pre-native expansion intent only and reports
  `native_lowering_ready=0`. A compiler-side lowering-plan probe now joins that
  directive intent with ordinary parser/checker semantic acceptance and the
  checker-visible runtime obligation count plus first-obligation payload
  extraction. Directive-bearing dynamic sources become ready for the existing
  pre-native guard expansion plan; ordinary dynamic sources still expose
  obligations without opt-in expansion intent; return/call/assignment sites
  preserve their first obligation payload; unit-suffixed constraints preserve
  their unit label; and dynamic descriptors map to staged backend-guard rows
  carrying comparison opcode, threshold kind, and trap exit code. Conjunctive
  dynamic proof contexts now stage one backend guard row per checker-visible
  obligation instead of only the first representative row. Statically
  discharged sources produce no guard plan. The plan still reports emitted
  `backend_guard_count=0` until real native lowering exists.
  Direct native lowering of checker-visible proof obligations and runtime
  diagnostic emission remain future work.

The repo already has generated ontology artifacts and witnesses under:

- `stdlib/ontology/generated/`
- `stdlib/ontology/typed/`
- `tests/run-pass/ontology_generated_*_types.sio`
- `tests/run-pass/ontology_typed_bridge_*.sio`
- `tests/compile-fail/ontology_generated_*_reject.sio`

The generated ontology lane now has a stable manifest:

- `stdlib/ontology/generated/MANIFEST.tsv`
- `scripts/ci/generated_ontology_manifest_gate.sh`

The manifest records the nine stable public bundles, generated stubs,
positive/negative witnesses, typed bridges, typed bridge witnesses, and
class/const/disjoint limits. The generation target validates only this stable
public bundle set so stray WIP bundles in the same directory do not break the
public ontology lane.

The current checkpoint includes a source-level `Knowledge<T where { ... }>`
bridge for static ontology and unit constraints:

- parser accepts proof-context constraints after the inner `Knowledge<T>` type;
- ordinary parser/AST route now retains `Knowledge` proof constraints without
  the earlier Stage1 parse segfault;
- checker source now validates `field subclass_of Class` by looking up the
  struct field type and querying the existing ontology table/kernel surfaces;
- Stage1 checker verdict now validates single-file static ontology
  proof-context constraints through the parsed AST using the dedicated
  `check::knowledge_context` checker module, not source-text matching, a
  frontend-owned precheck, or a local duplicate in `check::mod`;
- the Stage1 bridge now proves multi-constraint contexts are retained and
  validated as conjunctions, with both positive and negative witnesses;
- `Knowledge<T where { field >= value <unit> }>` now has a focused unit
  constraint gate: the ordinary `check::knowledge_context` semantic bridge
  accepts a matching derived `acceleration = m / s / s` witness and rejects an
  incompatible `velocity`-field-as-`acceleration` witness;
- `Knowledge<T where { field >= literal }>` now has a focused numeric
  constraint gate: the ordinary bridge accepts numeric literals on numeric
  fields and rejects numeric operators applied to nonnumeric fields or
  nonnumeric values;
- the same numeric gate now proves the first static threshold satisfaction
  bridge: a field refinement such as `{ v: i64 | v >= 18 }` discharges
  `Knowledge<T where { age >= 18 }>` while a weaker refinement such as
  `{ v: i64 | v >= 13 }` is rejected;
- a composite gate now proves a single `Knowledge<T where {...}>` context can
  combine ontology subclass, derived-unit dimensional, and numeric-refinement
  threshold constraints as a conjunction, with one negative witness for each
  failing constraint family;
- the modular parser path used by focused K2 probes now accepts
  `Knowledge { ... }` value literals, matching the value syntax already used by
  existing run-pass examples;
- a static value-initializer gate now proves literal
  `Knowledge { value: Struct { ... } }` return values and annotated local
  `let`/`var` bindings are checked against numeric proof-context thresholds,
  and the same literal shape is checked at function call boundaries against
  `Knowledge<Struct where {...}>` parameter types and at simple assignment
  sites into annotated local variables when constrained fields are compile-time
  literals, including unit-suffixed thresholds and literal-boolean branch
  selection plus literal-boolean branch local flow in generated constructors,
  and simple const-evaluable numeric branch conditions over literals and
  literal-backed parameters. The gate separately classifies nonliteral field
  values and dynamic branch conditions as remaining runtime-check gaps;
- a static umbrella gate now runs all focused static `Knowledge<T>` gates as
  one acceptance surface while explicitly excluding dynamic/runtime checks;
- a runtime-obligation gate now proves dynamic `Knowledge<T where {...}>`
  numeric field constraints are counted as runtime proof obligations instead of
  being indistinguishable from fully discharged static evidence; this is not yet
  backend guard/trap insertion;
- a pre-native runtime guard expansion gate now proves the first executable
  dynamic guard slice by wrapping `value: Struct { ... }` with a generated
  helper and lowering the proof-context type to `Knowledge<T>` after guard
  insertion; it now covers conjunctive numeric lower-bound constraints, not
  only a single field, plus upper-bound `<=` and equality `==` slices and
  unit-suffixed thresholds over already materialized unit-typed fields, including a
  current-source slice for internal
  validation-data unit labels such as `mg_dL`, plus deterministic `.guardcache`
  MISS/HIT reuse for the expanded source;
- a static spine umbrella gate now runs generated ontology manifest and witness
  checks, typed bridge witnesses, pre-native ontology-bundle directive
  expansion, compiler-side ontology-bundle directive raw-source scanning, unit
  dimensional-analysis gates, static `Knowledge<T>` proof-context gates,
  checker-visible runtime-obligation classification, pre-native runtime guard
  expansion, and ontology-linked unit metadata as one executable 1->2->3
  surface; it now also runs the focused imported `.ontocache` frontend
  composition gate for ontology+numeric, ontology+unit, PART subclass, PART
  disjoint-rejection, QM subclass, QM reverse-subclass rejection, and generic
  ALG/ChEBI/GO/HPO/PHYS subclass plus reverse-subclass rejection
  `Knowledge<T>` proof-contexts. It also covers LOINC with synthetic positive
  cache-slot IDs for alphanumeric CURIE tails, accepting the first bounded
  parent edge and rejecting its reverse; the K2
  `check::check` import bridge classifier is available behind
  `--with-k2-classifier` and remains a separate blocker classifier rather than
  an acceptance condition;
- LOINC/ChEBI-linked internal dimension-label metadata is now connected as
  fixture well-formedness data under
  `stdlib/data/data/ontology/unit_metadata/internal_unit_labels_no_clinical_dimension_safety.json`; the
  validator checks term existence, labels, unit atoms, and the explicit
  `checker_authority=false` boundary without making metadata a typechecker
  authority;
- validation-data internal dimension labels `mg_dL`, `mmol_L`, `U_L`, and `mm_h` are
  now registered in the current-source `lean_single` unit table, so they
  participate as internal rebuilt-current-source unit identifiers with no
  built-in conversion factors while the metadata remains non-authoritative;
  this is not a claim of clinical correctness, unit conversion safety, dosing
  safety, UCUM conformance, LOINC conformance, ChEBI conformance, or regulatory
  data-exchange readiness;
- the modular `UnitRegistry` now mirrors those internal dimension labels for
  dimensional checking only, and the modular `Knowledge<T>` unit proof-context
  gate accepts matching `mg_dL`, `mmol_L`, `U_L`, and `mm_h` constraints while
  rejecting incompatible mass-concentration vs amount-concentration labels.
  This still does not make clinical, conversion, UCUM, LOINC, ChEBI, dosing,
  or regulatory-exchange claims;
- the remaining checker work is to route the ordinary boot4 verdict path through
  that `check::check` semantic implementation, and separately repair the full
  value-style `Checker` collector bridge.

The current checkpoint also includes a pre-native ontology-bundle directive
bridge:

- `//@ ontology-bundle: "stdlib/data/data/ontology/bundles/snomed.dontology"`
  can be expanded by `scripts/ontology/expand_ontology_bundle_directives.sh`;
- the expander builds and uses the C `.dontology` importer, then emits ordinary
  ontology declarations before the source body;
- when `--cache-dir` is provided, the expander writes a deterministic text
  `.ontocache` keyed by the bundle bytes and C importer source bytes, then
  reuses it on repeated expansion;
- `scripts/ci/ontology_bundle_directive_gate.sh` proves a positive SNOMED
  subclass witness and a negative sibling-rejection witness after expansion,
  plus byte-identical repeated expansion from cache;
- this is not yet native compiler side-table loading; bounded native
  side-table `.ontocache` persistence is covered by the native scan/load-plan
  rung below.

The first native-loader rung is also present:

- `self-hosted/compiler/ontology_bundle_directive.sio` scans raw source bytes
  before ordinary lexing drops comments and builds a first load plan;
- `self-hosted/compiler/k2_ontology_bundle_directive_scan_probe.sio` proves the
  scanner extracts
  `stdlib/data/data/ontology/bundles/snomed.dontology` from the source-level
  directive witness and verifies that the referenced bundle exists with
  nonzero bytes, a consistent `DONT` v1 envelope, a canonical payload summary,
  the first payload term (`SNOMED:138875005`) with its numeric tail, and the
  first subclass edge (`SNOMED:404684003 -> SNOMED:138875005`);
- the same probe also validates a `PART` directive witness and extracts the
  first disjoint pair (`PART:0000003` disjoint `PART:0000012`);
- both witnesses also build a bounded compact side-table prefix in the load
  plan, using primitive scalar/parallel fields for class IDs, parent edges, and
  disjoint pairs with an explicit truncation flag;
- both witnesses also exercise query helpers over that staged prefix:
  direct subclass edge lookup, bounded subclass-chain lookup, and symmetric
  disjoint lookup;
- both witnesses also stage kernel-shaped rows from the prefix:
  class slots plus subclass/disjoint axiom rows indexed like `OntologyKernel`
  materialization, with equivalent kernel-row queries;
- both witnesses also materialize a bounded `OntologyKernel`-like fact surface:
  reflexive subclass facts, transitive subclass closure, and symmetric
  disjoint facts over the staged rows;
- both witnesses also write a stable bounded native side-table `.ontocache`
  artifact for those staged kernel facts, with an explicit authority boundary
  stating that the artifact is not the live checker kernel;
- `scripts/ci/ontology_bundle_directive_native_scan_gate.sh` keeps this
  separate from importer FFI and checker-kernel side-table loading, and also
  checks that a malformed directive, an invalid bundle envelope, and a valid
  envelope with an invalid payload shape are classified without producing a
  valid bundle plan, so it is evidence of compiler-side directive visibility,
  load planning, and bounded compact side-table staging, not native ontology
  kernel ingestion yet.

## Gates

### Generated Ontology

Command:

```bash
make test-generated-ontology
make test-generated-ontology-manifest
make test-generated-ontology-fresh
```

Evidence class:

- generated C FFI ingestion path;
- current compiler ontology witnesses;
- stable public manifest for bundle, stub, witness, typed bridge, and limit
  coverage;
- no Python in the PL reasoning path.

Additional ordinary witnesses:

- `tests/frontend/knowledge_static_subclass_where_ordinary_probe.sio`
- `tests/frontend/knowledge_static_transitive_where_ordinary_probe.sio`
- `tests/frontend/knowledge_static_disjoint_where_ordinary_probe.sio`

Status:

- refreshed on 2026-05-25 after adding
  `scripts/ci/generated_ontology_manifest_gate.sh`, manifest typed-bridge
  coverage columns, and `make test-generated-ontology-manifest`;
- `make generated-ontology` now validates the nine stable public bundles
  explicitly, avoiding unrelated WIP `.dontology` files in the bundle
  directory;
- command output:
  - `Generated ontology manifest gate passed.`;
  - `PASS ontology_generated_*` for 19 generated ontology witnesses when run
    through the semantic spine umbrella gate;
  - `PASS ontology_typed_bridge_*` for 10 typed bridge witnesses when run
    through the semantic spine umbrella gate.

### Semantic Knowledge Spine Umbrella

Command:

```bash
make test-semantic-knowledge-spine
```

Evidence class:

- generated ontology manifest gate;
- generated ontology positive/negative witnesses;
- typed ontology bridge witnesses;
- pre-native ontology-bundle directive expansion through the C importer;
- compiler-side ontology-bundle directive raw-source scanning before comments
  are discarded by lexing;
- bounded native side-table `.ontocache` write/read rehydration plus
  source-level cache directive import, with check::mod cache verdict
  consumption, focused check::check live-kernel hydration, the imported
  check::check collect-boundary classifier, and the fresh-symbol check::mod
  sourcecheck cache-verdict bridge plus same-Checker kernel/copy/pointer-query
  and direct shadow-field classifiers;
- Unit Types Phase 1 gate on the default compiler path;
- derived/literal/current-source unit gate, including explicit J/eV conversion;
- ontology-linked unit metadata gate for LOINC observations, optional ChEBI
  analytes, and validation-only internal label families;
- static `Knowledge<T>` umbrella gate;
- optional K2 `check::check` import bridge classifier when explicitly requested
  with `--with-k2-classifier`.

Status:

- refreshed and passed on 2026-05-25 after repairing the modular derived-unit
  parser/checker drift, ordinary `Knowledge<T where {...}>` AST retention,
  refinement type parsing, `Knowledge { value: ... }` expression parsing, and
  boot4-safe decimal float classification;
- this is the executable 1->2->3 static surface; it intentionally does not
  claim native backend runtime guard/trap lowering for dynamic `Knowledge<T>`
  values, same-Checker ontology-kernel cache hydration, or ordinary source-file
  checker-kernel cache hydration. The runtime guard `.guardcache` is a
  pre-native text expansion cache, not native runtime metadata;
- the standalone K2 `check::check` import bridge classifier remains available
  via `bash scripts/ci/semantic_knowledge_spine_gate.sh --with-k2-classifier`,
  but it is not part of the focused static Semantic Knowledge Spine acceptance
  gate because it tracks a separate modular checker import/link blocker;
- command output ends with:
  - `Semantic Knowledge Spine gate passed.`;
  - `The standalone K2 check::check import bridge classifier is intentionally optional because it tracks a separate modular-checker bridge gap.`;
  - `Native backend runtime guard/trap lowering for those obligations, same-Checker ontology-kernel or shadow-field cache hydration, and ordinary source-file checker-kernel cache hydration remain future work.`

### Knowledge Runtime Obligations

Command:

```bash
make test-knowledge-runtime-obligations
```

Evidence class:

- ordinary parser/AST route;
- `check::knowledge_context` runtime-obligation counter;
- positive static witness discharging to zero obligations;
- dynamic return, call-boundary, and assignment witnesses emitting nonzero
  runtime obligations while keeping the static verdict accepted.

Status:

- added on 2026-05-25 as the first runtime bridge rung after the static
  `Knowledge<T>` proof-context surface;
- this proves dynamic evidence is represented as explicit obligations, not that
  the native backend already emits guards or traps for them.

### Knowledge Runtime Guard Expansion

Command:

```bash
make test-knowledge-runtime-guards
```

Evidence class:

- pre-native source expansion;
- generated Sounio `assert(...)` helper for numeric comparison constraints;
- proof-context type lowering to plain `Knowledge<T>` after guard insertion;
- positive dynamic witness runs successfully;
- violating dynamic witness fails at execution time.
- conjunctive positive and negative witnesses prove multiple numeric
  lower-bound guards are enforced together.
- upper-bound positive and negative witnesses prove `<=` constraints are
  guarded dynamically, not only lower bounds.
- equality positive and negative witnesses prove `==` constraints are guarded
  dynamically, matching the parser/checker numeric equality surface.
- unit-suffixed positive and negative witnesses prove a same-unit threshold is
  materialized and checked through a dimensionless ratio.
- current-source internal-label positive and negative witnesses prove the same
  ratio guard shape for `mg_dL` without making clinical or standards claims.
- generated source diagnostics identify the guarded `Type.field op threshold`
  constraint for lower-bound, conjunctive, upper-bound, equality, named-unit,
  and internal-label slices without making the helper `IO`-effectful.
- optional guard diagnostic manifests record the same guarded constraints in a
  deterministic TSV audit artifact with input/expander hashes and structured
  `type`, `field`, `op`, `threshold`, `unit`, and `constraint` columns.
- native compile/run witnesses prove pre-native generated guard assertions
  lower through the existing backend `assert`/trap path, with the violating
  native binary exiting 1.
- checker-obligation drain witnesses prove the same positive/reject sources
  emit nonzero runtime obligations before expansion and zero obligations after
  generated guard insertion plus proof-context lowering.
- call-boundary and assignment positive/reject witnesses prove the same
  obligation-drain and generated-guard native trap shape beyond return sites.
- conjunctive lower-bound, upper-bound, equality, unit-suffixed, and
  internal-label positive/reject witnesses prove obligation-drain and
  generated-guard native trap coverage across the supported guard families.
- an opt-in launcher witness proves `SOUNIO_KNOWLEDGE_RUNTIME_GUARDS=1
  bin/souc run|compile <source>` invokes the generated guard path without a
  manual expander call, accepting the positive call-boundary witness and
  trapping the rejecting witness with exit code 1.
- source-directive witnesses prove `//@ knowledge-runtime-guards` enables the
  same launcher path for `bin/souc run|compile <source>` without an environment
  variable.
- compiler-side raw-source scanner witnesses prove the self-hosted compiler can
  detect `//@ knowledge-runtime-guards` before comments are dropped, leave
  ordinary sources inactive, classify payload-bearing malformed directives, and
  explicitly report `native_lowering_ready=0`.
- compiler-side lowering-plan witnesses prove directive intent, parser/checker
  semantic acceptance, and runtime obligation counts are joined in Sounio before
  backend lowering: directive-bearing dynamic sources are ready for pre-native
  expansion, ordinary dynamic sources expose obligations without opt-in intent,
  return/call/assignment sites preserve first-obligation payload, unit-suffixed
  constraints preserve their unit label, dynamic descriptors map to staged
  backend-guard rows, and statically discharged sources produce no guard plan.

Status:

- added on 2026-05-25 as the first executable guard rung after runtime
  obligation counting;
- extended on 2026-05-25 from one numeric lower-bound field to conjunctive
  numeric lower-bound proof contexts;
- extended on 2026-05-25 with an upper-bound `<=` dynamic guard slice;
- extended on 2026-05-25 with an equality `==` dynamic guard slice;
- extended on 2026-05-25 to unit-suffixed numeric lower bounds for materialized
  unit-typed fields;
- extended on 2026-05-25 with a current-source `mg_dL` internal-label guard
  slice, explicitly without clinical, conversion, UCUM, LOINC, ChEBI, dosing,
  or regulatory-exchange authority;
- extended on 2026-05-25 with stable pre-native guard diagnostics preserved in
  the expanded source for representative runtime guard families;
- extended on 2026-05-25 with optional guard diagnostic manifest emission and
  gate coverage for conjunctive, named-unit, and internal-label entries;
- extended on 2026-05-25 with native compile/run coverage for the generated
  lower-bound guard assertion path; this proves native trap execution after
  pre-native expansion, not direct native lowering of proof obligations;
- extended on 2026-05-25 with checker-obligation drain coverage for the
  generated lower-bound guard pair; this proves the pre-native expansion
  consumes the checker-visible obligations it handles;
- extended on 2026-05-25 with call-boundary and assignment positive/reject
  lower-bound guard witnesses, including obligation-drain and native
  compile/run trap coverage;
- extended on 2026-05-25 with obligation-drain and native compile/run trap
  coverage across conjunctive lower-bound, upper-bound, equality,
  unit-suffixed, and internal-label guard families;
- extended on 2026-05-25 with an opt-in `bin/souc` launcher bridge for
  pre-native Knowledge runtime guard expansion in compile/build/run modes;
- extended on 2026-05-25 with a source-level `//@ knowledge-runtime-guards`
  launcher directive for the same pre-native bridge;
- extended on 2026-05-25 with a compiler-side raw-source scanner for
  `//@ knowledge-runtime-guards` as a pre-native intent classifier;
- extended on 2026-05-25 with a compiler-side lowering-plan surface joining
  directive intent, semantic verdicts, runtime obligation counts, and
  first-obligation payload extraction plus staged backend-guard rows while
  keeping emitted `backend_guard_count=0`;
- extended on 2026-05-25 so conjunctive dynamic proof contexts stage one
  backend-guard row per checker-visible runtime obligation instead of only a
  single representative row;
- this is intentionally pre-native and does not claim direct native backend
  proof-obligation lowering or runtime diagnostic emission yet.
- command output now includes:
  - `PASS compiler-side Knowledge runtime guard lowering plan is ready for directive pre-native expansion`;
  - `PASS compiler-side Knowledge runtime guard lowering plan sees ordinary dynamic obligations without opt-in intent`;
  - `PASS compiler-side Knowledge runtime guard lowering plan maps return-site obligation payload to staged backend guard row`;
  - `PASS compiler-side Knowledge runtime guard lowering plan maps assignment-site obligation payload to staged backend guard row`;
  - `PASS compiler-side Knowledge runtime guard lowering plan maps unit-suffixed obligation payload to staged backend guard row`;
  - `PASS compiler-side Knowledge runtime guard lowering plan stages all checker-visible conjunctive backend guard rows`;
  - `PASS compiler-side Knowledge runtime guard lowering plan leaves statically discharged source empty`;
  - `PASS compiler-side knowledge-runtime-guards directive scanner detected valid raw-source intent`;
  - `PASS compiler-side knowledge-runtime-guards directive scanner leaves ordinary sources inactive`;
  - `PASS compiler-side knowledge-runtime-guards directive scanner classified malformed payload-bearing directive`;
  - `PASS Knowledge runtime guard expansion wrote auditable guard diagnostic manifests`;
  - `PASS Knowledge runtime guard expansion consumes checker-visible runtime obligations across dynamic sites and supported guard families`;
  - `PASS tests/frontend/knowledge_runtime_guard_positive.sio expanded with a generated Knowledge<T> runtime guard helper and diagnostic`;
  - `PASS tests/frontend/knowledge_runtime_guard_positive.sio lowered the generated Knowledge<T> runtime guard through native assert/trap path`;
  - `PASS tests/frontend/knowledge_runtime_guard_reject.sio trapped through native assert path with exit code 1`;
  - `PASS call-boundary and assignment witnesses expanded with generated Knowledge<T> runtime guards`;
  - `PASS tests/frontend/knowledge_runtime_guard_call_positive.sio accepted call-boundary dynamic value satisfying generated runtime guard`;
  - `PASS tests/frontend/knowledge_runtime_guard_call_reject.sio failed the generated call-boundary Knowledge<T> runtime guard`;
  - `PASS tests/frontend/knowledge_runtime_guard_assign_positive.sio accepted assignment dynamic value satisfying generated runtime guard`;
  - `PASS tests/frontend/knowledge_runtime_guard_assign_reject.sio failed the generated assignment Knowledge<T> runtime guard`;
  - `PASS call-boundary reject generated guard witness trapped through native assert path with exit code 1`;
  - `PASS assignment reject generated guard witness trapped through native assert path with exit code 1`;
  - `PASS call-boundary launcher opt-in run trapped reject generated guard witness with exit code 1`;
  - `PASS call-boundary launcher opt-in compile produced reject native guard witness trapping with exit code 1`;
  - `PASS source-directive directive run trapped reject generated guard witness with exit code 1`;
  - `PASS source-directive directive compile produced reject native guard witness trapping with exit code 1`;
  - `PASS tests/frontend/knowledge_runtime_guard_multi_positive.sio expanded with conjunctive Knowledge<T> runtime guards and diagnostics`;
  - `PASS conjunctive-lower-bound reject generated guard witness trapped through native assert path with exit code 1`;
  - `PASS tests/frontend/knowledge_runtime_guard_upper_positive.sio expanded with an upper-bound Knowledge<T> runtime guard and diagnostic`;
  - `PASS tests/frontend/knowledge_runtime_guard_upper_positive.sio accepted dynamic value satisfying generated upper-bound runtime guard`;
  - `PASS tests/frontend/knowledge_runtime_guard_upper_reject.sio failed the generated upper-bound Knowledge<T> runtime guard`;
  - `PASS upper-bound reject generated guard witness trapped through native assert path with exit code 1`;
  - `PASS tests/frontend/knowledge_runtime_guard_eq_positive.sio expanded with an equality Knowledge<T> runtime guard and diagnostic`;
  - `PASS tests/frontend/knowledge_runtime_guard_eq_positive.sio accepted dynamic value satisfying generated equality runtime guard`;
  - `PASS tests/frontend/knowledge_runtime_guard_eq_reject.sio failed the generated equality Knowledge<T> runtime guard`;
  - `PASS equality reject generated guard witness trapped through native assert path with exit code 1`;
  - `PASS tests/frontend/knowledge_runtime_guard_unit_positive.sio expanded with a unit-suffixed Knowledge<T> runtime guard and diagnostic`;
  - `PASS unit-suffixed reject generated guard witness trapped through native assert path with exit code 1`;
  - `PASS tests/frontend/knowledge_runtime_guard_internal_label_positive.sio expanded with an internal-label unit Knowledge<T> runtime guard and diagnostic`;
  - `PASS internal-label-unit reject generated guard witness trapped through native assert path with exit code 1`.

### Ontology Bundle Directive Expansion

Command:

```bash
make test-ontology-bundle-directive
```

Witnesses:

- `tests/frontend/ontology_bundle_directive_snomed.sio`
- `tests/frontend/ontology_bundle_directive_snomed_reject.sio`

Evidence class:

- source-level `//@ ontology-bundle: "..."` syntax;
- C `.dontology` importer expansion before checker execution;
- deterministic pre-native text `.ontocache` population and reuse;
- positive subclass acceptance after expansion;
- negative sibling subsumption rejection after expansion;
- no Python in the PL reasoning path.

Status:

- added and passed on 2026-05-25;
- this is a pre-native bridge and does not claim the compiler itself loads
  bundle side tables or persists native side-table `.ontocache`;
- command output:
  - `PASS ontology-bundle expansion populated and reused deterministic .ontocache`;
  - `PASS tests/frontend/ontology_bundle_directive_snomed.sio expanded and accepted SNOMED subclass witness`;
  - `PASS tests/frontend/ontology_bundle_directive_snomed_reject.sio expanded and rejected sibling subsumption`;
  - `Ontology bundle directive expansion gate passed.`

### Ontology Bundle Directive Native Scan

Command:

```bash
make test-ontology-bundle-directive-native-scan
```

Witnesses:

- `tests/frontend/ontology_bundle_directive_snomed.sio`
- a generated temporary `PART` directive witness inside
  `scripts/ci/ontology_bundle_directive_native_scan_gate.sh`
- `self-hosted/compiler/k2_ontology_bundle_directive_scan_probe.sio`

Evidence class:

- self-hosted compiler source reads raw source bytes;
- `//@ ontology-bundle: "..."` is visible before comments are discarded by
  lexing;
- the scanner extracts the bundle path payload into a Sounio-side load-plan
  struct;
- the load plan verifies that the referenced `.dontology` bundle exists and has
  nonzero bytes;
- the load plan validates the deterministic `DONT` v1 envelope and checks that
  the declared payload size matches the file size;
- the load plan summarizes canonical payload fields needed by the next
  ingestion stage: `ontology`, `term_count`, `terms`, `disjoint_count`, and
  `disjoints`;
- the load plan extracts the ontology name, first term CURIE, and first-term
  numeric tail for the SNOMED fixture;
- the load plan extracts the first child-parent subclass edge for the SNOMED
  fixture: `SNOMED:404684003 -> SNOMED:138875005`;
- the load plan extracts the first disjoint pair for the PART fixture:
  `PART:0000003` disjoint `PART:0000012`;
- the load plan also handles the LOINC fixture's alphanumeric CURIE tails by
  assigning synthetic positive cache-slot IDs for the bounded native cache
  artifact while preserving `first_term_numeric_id=-1` as a diagnostic for the
  raw numeric-tail parse;
- the load plan builds a bounded compact side-table prefix:
  - SNOMED: 112 classes counted, first stored class IDs
    `138875005`, `404684003`, `64572001`, `73211009`, and first stored parent
    edge IDs `404684003 -> 138875005`;
  - PART: 20 classes counted, first stored class IDs `1`, `2`, `3`, `4`, first
    stored parent edge IDs `2 -> 1`, and first stored disjoint IDs `3` and
    `12`;
  - LOINC: 20 classes counted, first stored synthetic class IDs
    `900000000`, `900000001`, `900000002`, `900000003`; its first bounded
    parent edge uses synthetic IDs `900000007 -> 900000006`;
  - both fixtures report explicit truncation because only the first four class
    and parent-edge slots are stored in this bootstrap-safe prefix;
- the load plan answers staged-prefix queries:
  - SNOMED: first direct edge lookup succeeds and
    `73211009 subclass_chain 138875005` succeeds;
  - PART: first direct edge lookup succeeds, `4 subclass_chain 2` succeeds,
    and disjoint lookup succeeds in both `3,12` and `12,3` order;
- the load plan stages kernel-shaped rows:
  - SNOMED: four class slots, three subclass axiom rows
    `1 -> 0`, `2 -> 1`, `3 -> 2`, and equivalent kernel-row chain query
    `73211009 -> 138875005`;
  - PART: five class slots, three subclass axiom rows `1 -> 0`, `2 -> 1`,
    `3 -> 2`, one disjoint axiom row `2 <-> 4`, and equivalent kernel-row
    subclass/disjoint queries;
  - LOINC: six synthetic class slots and one subclass axiom row `4 -> 5`,
    proving the first bounded alphanumeric parent edge without requiring a
    numeric CURIE tail;
- the load plan materializes bounded kernel-like facts:
  - SNOMED: 4 reflexive subclass facts and 10 total subclass facts after
    transitive closure;
  - PART: 5 reflexive subclass facts, 11 total subclass facts after transitive
    closure, and 2 symmetric disjoint facts;
  - LOINC: 6 reflexive subclass facts and 7 total subclass facts after
    transitive closure over synthetic cache-slot IDs;
- the load plan writes a stable bounded native side-table `.ontocache`
  artifact for those staged kernel facts, including the staged slots, subclass
  axiom rows, disjoint axiom rows, materialized fact counts, and an authority
  line that says the cache is not the live checker kernel;
- the native `.ontocache` can be reimported by Sounio to rehydrate staged
  kernel rows:
  - SNOMED reimport proves `73211009 -> 138875005` still succeeds over the
    cache-derived materialized facts;
  - PART reimport proves `4 -> 2` still succeeds and `3`/`12` disjointness
    still succeeds symmetrically over the cache-derived materialized facts;
  - LOINC reimport proves the bounded synthetic slot edge `4 -> 5` is preserved
    in the native `.ontocache` artifact;
- a separate source file can declare
  `//@ ontology-side-table-cache: "..."` and import the cache generated by a
  producer source file:
  - SNOMED cache directive import proves `73211009 -> 138875005` over the
    cache-derived materialized facts;
  - PART cache directive import proves `4 -> 2` plus symmetric `3`/`12`
    disjointness over the cache-derived materialized facts;
- the cache directive exposes checker-shaped verdicts:
  - SNOMED cache directive accepts `73211009 -> 138875005` with verdict `0`
    and rejects `138875005 -> 73211009` with verdict `152`;
  - PART cache directive accepts `4 -> 2` and `3` disjoint `12` with verdict
    `0`, while rejecting `2 -> 4` and `2` disjoint `4` with verdict `152`;
- the same bounded cache verdicts are consumed through `check::mod` using
  `check_ontology_side_table_cache_*_verdict_probe`, backed by
  `check::ontology_side_table_cache`, so the cache reaches the checker module
  boundary;
- `check::check` now has a focused live-kernel hydrator that reads the same
  bounded cache through `check::ontology_side_table_cache`, populates
  `Checker.ontology_kernel`, materializes it, and answers SNOMED/PART
  subclass/disjoint verdicts through the real ontology-kernel query helpers;
- routing that focused live-kernel verdict through `check::mod` is not yet an
  acceptance path because explicit `check::check` imports in `check::mod`
  reintroduced the known ordinary Stage1 import side effect;
- a candidate sourcecheck hydrator can parse the consumer source and resolve
  cache indexes, but it still hits the known imported full-checker collector
  boundary before `collect_ok`;
- a fresh-symbol `check::mod` sourcecheck cache-verdict bridge gets past the
  ordinary checker surface and proves both positive and reverse bounded
  subclass verdicts without explicit `check::check` imports;
- a fresh-symbol same-Checker kernel classifier reaches `hydrate_ok` after the
  ordinary checker surface, but still rejects the positive subclass verdict
  when querying the pointer-owned ordinary `Checker.ontology_kernel`;
- a second same-Checker copy classifier reaches `copy_ok`, but still rejects
  the positive same-Checker kernel verdict;
- a third same-Checker pointer-query classifier reaches `copy_ok`, but remains
  classified before a query verdict;
- a same-Checker precheck classifier reaches `collect_ok` and `hydrate_ok`, but
  remains classified at collected-checker `class_count` readback before it can
  prove the positive kernel verdict;
- a stable `check::mod` same-sourcecheck sidecar hydrates after ordinary
  sourcecheck and accepts SNOMED subclass plus PART subclass/disjoint verdicts,
  while still keeping the authority boundary separate from
  `Checker.ontology_kernel` mutation;
- a focused `check::mod` `Knowledge<T>` proof-context sidecar uses those
  imported `.ontocache` facts after ordinary sourcecheck, accepting bounded
  SNOMED/PART positive proof contexts and rejecting a reverse SNOMED context
  plus a disjoint PART context through generic `PREFIX_<numeric-id>` names
  via a boot4-style source-path verdict API without mutating
  `Checker.ontology_kernel`;
- the modular frontend now composes that imported-cache ontology sidecar with
  the ordinary value-only `check::knowledge_context` validator: a SNOMED
  imported-cache proof context plus `age >= 18` is accepted, the same
  ontology proof context over an invalid numeric field is rejected, a SNOMED
  imported-cache proof context plus `accel >= 1.0 <acceleration>` is accepted,
  the same ontology proof context over an invalid unit field is rejected, a
  PART imported-cache subclass proof context is accepted, a PART
  imported-cache disjoint proof context is rejected, a QM imported-cache
  subclass proof context is accepted, a QM imported-cache reverse subclass
  proof context is rejected, and ALG/ChEBI/GO/HPO/PHYS generic slot3-to-slot0
  subclass proof contexts are accepted while their reverse subclass proof
  contexts are rejected. LOINC synthetic cache-slot IDs now allow the
  alphanumeric fixture to accept `cache_slot4 subclass_of cache_slot5` while
  rejecting the reverse edge;
- a same-Checker direct shadow-field classifier reaches `hydrate_ok` after the
  ordinary checker surface, but the pointer surface still reads
  `shadow_count=0` and returns the empty-shadow reject verdict;
- malformed directive payloads are classified without producing a valid bundle
  plan;
- invalid bundle envelopes and invalid payload shapes are classified without
  producing a valid bundle plan.

Status:

- added and passed on 2026-05-25;
- this is a native-loader/load-plan side-table-staging and bounded native-cache
  write/read rehydration plus source-level cache directive import and
  checker-shaped cache verdict rung, including check::mod diagnostic
  consumption through a check-owned cache reader, a focused check::check
  live-kernel hydration probe, the imported check::check sourcecheck
  collect-boundary classifier, and a fresh-symbol check::mod sourcecheck
  cache-verdict bridge plus a check::mod same-sourcecheck sidecar and
  imported-cache Knowledge<T> sidecar now composed through the modular frontend
  with ordinary value-only numeric/unit proof-context validation, plus
  same-Checker hydrate-ok/copy/pointer-query, precheck readback, and direct
  shadow-field classifiers, not importer FFI, successful same-Checker
  ontology-kernel or shadow-field cache hydration, ordinary source-file
  checker-kernel hydration, or full term/parent/disjoint ingestion. The QM,
  ALG/ChEBI/GO/HPO/PHYS, and LOINC frontend-composition slices use generic
  cache evidence because the scan probe's named verdict fields are
  intentionally specialized to the SNOMED/PART bootstrap shapes. LOINC uses
  synthetic positive cache-slot IDs for alphanumeric CURIE tails in this
  bounded cache artifact;
- command output:
  - `PASS generated and reimported bounded SNOMED .ontocache for frontend composition`;
  - `PASS generated and reimported bounded PART .ontocache for frontend composition`;
  - `PASS generated and reimported bounded QM .ontocache for frontend composition via generic cache evidence`;
  - `PASS modular frontend accepted imported-cache ontology plus numeric Knowledge<T> proof-context`;
  - `PASS modular frontend rejected imported-cache ontology when numeric Knowledge<T> proof-context fails`;
  - `PASS modular frontend accepted imported-cache ontology plus unit Knowledge<T> proof-context`;
  - `PASS modular frontend rejected imported-cache ontology when unit Knowledge<T> proof-context fails`;
  - `PASS modular frontend accepted PART imported-cache subclass Knowledge<T> proof-context`;
  - `PASS modular frontend rejected PART imported-cache disjoint Knowledge<T> proof-context`;
  - `PASS modular frontend accepted QM imported-cache subclass Knowledge<T> proof-context`;
  - `PASS modular frontend rejected QM imported-cache reverse subclass Knowledge<T> proof-context`;
  - `PASS generated and reimported bounded ALG .ontocache for generic frontend composition`;
  - `PASS modular frontend accepted ALG generic imported-cache subclass Knowledge<T> proof-context`;
  - `PASS modular frontend rejected ALG generic imported-cache reverse subclass Knowledge<T> proof-context`;
  - `PASS generated and reimported bounded CHEBI .ontocache for generic frontend composition`;
  - `PASS modular frontend accepted CHEBI generic imported-cache subclass Knowledge<T> proof-context`;
  - `PASS modular frontend rejected CHEBI generic imported-cache reverse subclass Knowledge<T> proof-context`;
  - `PASS generated and reimported bounded GO .ontocache for generic frontend composition`;
  - `PASS modular frontend accepted GO generic imported-cache subclass Knowledge<T> proof-context`;
  - `PASS modular frontend rejected GO generic imported-cache reverse subclass Knowledge<T> proof-context`;
  - `PASS generated and reimported bounded HPO .ontocache for generic frontend composition`;
  - `PASS modular frontend accepted HPO generic imported-cache subclass Knowledge<T> proof-context`;
  - `PASS modular frontend rejected HPO generic imported-cache reverse subclass Knowledge<T> proof-context`;
  - `PASS generated and reimported bounded PHYS .ontocache for generic frontend composition`;
  - `PASS modular frontend accepted PHYS generic imported-cache subclass Knowledge<T> proof-context`;
  - `PASS modular frontend rejected PHYS generic imported-cache reverse subclass Knowledge<T> proof-context`;
  - `PASS generated and reimported bounded LOINC .ontocache for alphanumeric frontend composition`;
  - `PASS modular frontend accepted LOINC alphanumeric imported-cache subclass Knowledge<T> proof-context`;
  - `PASS modular frontend rejected LOINC alphanumeric imported-cache reverse subclass Knowledge<T> proof-context`;
  - `PASS compiler-side ontology-bundle directive load plan materialized bounded SNOMED kernel facts`;
  - `PASS compiler-side ontology-bundle directive wrote bounded SNOMED native .ontocache`;
  - `PASS compiler-side ontology-bundle directive reimported bounded SNOMED native .ontocache`;
  - `PASS compiler-side ontology side-table cache directive imported bounded SNOMED native .ontocache`;
  - `PASS check::mod consumed bounded SNOMED native .ontocache semantic verdicts`;
  - `PASS check::check hydrated live Checker.ontology_kernel from bounded SNOMED native .ontocache`;
  - `PASS modular frontend accepted imported-cache Knowledge<T> ontology plus numeric proof-context`;
  - `PASS modular frontend rejected imported-cache Knowledge<T> when numeric proof-context fails`;
  - `PASS modular frontend accepted imported-cache Knowledge<T> ontology plus unit proof-context`;
  - `PASS modular frontend rejected imported-cache Knowledge<T> when unit proof-context fails`;
  - `PASS imported sourcecheck cache hydrator remains blocked at check::check collect_items after cache index resolution`;
  - `PASS check::mod sourcecheck cache hydrator accepted positive semantic verdict after ordinary sourcecheck`;
  - `PASS check::mod sourcecheck cache hydrator rejected reverse semantic verdict after ordinary sourcecheck`;
  - `PASS check::mod same-Checker ontology-kernel cache hydration remains classified after hydrate_ok with rejected positive verdict`;
  - `PASS check::mod same-Checker ontology-kernel copy hydration remains classified after copy_ok with rejected positive verdict`;
  - `PASS check::mod same-Checker ontology-kernel pointer-query classifier remains classified after copy_ok before query verdict`;
  - `PASS check::mod same-Checker precheck ontology-kernel hydration remains classified at collected-checker class_count read`;
  - `PASS check::mod same-sourcecheck sidecar accepted bounded SNOMED cache verdict after ordinary sourcecheck`;
  - `PASS check::mod sidecar accepted SNOMED Knowledge<T> proof-context after ordinary sourcecheck`;
  - `PASS check::mod sidecar rejected reverse SNOMED Knowledge<T> proof-context after ordinary sourcecheck`;
  - `PASS check::mod same-Checker shadow cache remains classified after hydrate_ok with empty shadow state`;
  - `PASS check::mod same-Checker shadow cache rejected reverse semantic verdict through empty-shadow classifier`;
  - `PASS compiler-side ontology-bundle directive load plan materialized bounded PART kernel facts with disjoint pair`;
  - `PASS compiler-side ontology-bundle directive wrote bounded PART native .ontocache`;
  - `PASS compiler-side ontology-bundle directive reimported bounded PART native .ontocache`;
  - `PASS compiler-side ontology side-table cache directive imported bounded PART native .ontocache`;
  - `PASS compiler-side ontology-bundle directive load plan materialized bounded LOINC alphanumeric kernel facts`;
  - `PASS compiler-side ontology-bundle directive wrote bounded LOINC native .ontocache with synthetic alphanumeric slots`;
  - `PASS compiler-side ontology-bundle directive reimported bounded LOINC native .ontocache`;
  - `PASS compiler-side ontology side-table cache directive imported bounded LOINC native .ontocache`;
  - `PASS check::mod consumed bounded PART native .ontocache semantic verdicts`;
  - `PASS check::check hydrated live Checker.ontology_kernel from bounded PART native .ontocache`;
  - `PASS check::mod same-sourcecheck sidecar accepted bounded PART subclass/disjoint cache verdicts after ordinary sourcecheck`;
  - `PASS check::mod sidecar accepted PART Knowledge<T> proof-context after ordinary sourcecheck`;
  - `PASS check::mod sidecar rejected disjoint PART Knowledge<T> proof-context after ordinary sourcecheck`;
  - `PASS compiler-side ontology-bundle directive load plan classified malformed directive`;
  - `PASS compiler-side ontology-bundle directive load plan rejected invalid DONT envelope`;
  - `PASS compiler-side ontology-bundle directive load plan rejected invalid DONT payload shape`;
  - `Ontology bundle directive native scan gate passed.`

Source-alignment notes:

- `scripts/bootstrap/bootstrap_concat.sh` now includes
  `self-hosted/check/ontology_side_table_cache.sio` before `check.sio` in both
  the default file list and the core profile file list;
- `scripts/bootstrap/run_knowledge_bootstrap_tests.sh` includes the same module
  before `check.sio` in its focused Knowledge bootstrap bundle;
- `scripts/bootstrap/run_knowledge_bootstrap_tests.sh` now defaults to
  `bin/souc`, matching its `check`/`run` subcommand usage;
- `BOOTSTRAP_PROFILE=core bash scripts/bootstrap/bootstrap_concat.sh` now
  reaches the raw compiler/type-diagnostic phase by default instead of failing
  early with `unrecognized subcommand 'build/bootstrap_stage1.sio'` from the
  richer `artifacts/omega` CLI;
- `bash scripts/bootstrap/run_knowledge_bootstrap_tests.sh` now reaches the
  raw source/typecheck failure by default instead of failing early with
  `P0021 Invalid item at module level`;
- the same focused Knowledge bootstrap bundle no longer emits the local
  `missing field in struct literal` family from hand-built test AST nodes after
  synchronizing those literals with the current parser/checker struct fields;
- the broad core bootstrap bundle and the smaller Knowledge bootstrap bundle
  still fail on existing source/typecheck debt, so this is source packaging
  progress, not a claim that the full bootstrap bundle is green.

### Ontology Unit Metadata

Command:

```bash
make test-ontology-unit-metadata
```

Data:

- `stdlib/data/data/ontology/unit_metadata/internal_unit_labels_no_clinical_dimension_safety.json`

Evidence class:

- validates the metadata schema is
  `sounio.ontology.internal-dimension-labels.v1`;
- requires `status=validation-data` and `checker_authority=false`;
- requires callers to pass `--accept-no-clinical-safety` when running the
  validator against non-authoritative metadata, otherwise the validator returns
  nonzero;
- writes a structured authority-warning log containing
  `@sounio/safety:no-clinical-dimension`;
- requires the metadata itself to state that `checker_authority=false` means no
  clinical validation has been performed by this metadata file;
- requires `safety_contract.scope=fixture_well_formedness`,
  `safety_contract.enforcement=flag_gated_fixture_validation`, and
  `safety_contract.acknowledgement_flag=--accept-no-clinical-safety`, because
  local fixture label checks are not terminology-conformance certification;
- checks every LOINC CURIE and label against the local LOINC fixture slice;
- checks optional ChEBI CURIEs and labels against the local ChEBI fixture
  slice;
- checks every canonical unit only uses declared unit atoms;
- checks every non-`percent` `sounio_unit_name` has current-source
  `lean_single` checker-registration evidence;
- requires the known Sounio builtin atoms used by the metadata to remain
  declared as builtin atoms.

Status:

- added and passed on 2026-05-25;
- command output:
  - `ontology internal dimension-label metadata: OK entries=11 unit_atoms=10 chebi_links=5`;
  - `Ontology internal dimension-label metadata gate passed.`

### Clinical Unit Types Current-Source Gate

Command:

```bash
make test-unit-types-clinical-current-source
```

Positive witness:

- `tests/frontend/unit_literal_clinical_current_source.sio`

Negative witness:

- `tests/compile-fail/unit_literal_clinical_reject_mass_as_amount_concentration.sio`

Evidence class:

- rebuilt current-source `self-hosted/compiler/lean_single.sio` compiler;
- current-source unit-table registrations for `mg_dL`, `mmol_L`, `U_L`, and
  `mm_h`;
- named numeric literal unit suffix parsing for internal dimension labels;
- call-boundary rejection when mass concentration is passed where amount
  concentration is required.
- no claim of clinical correctness, built-in conversion safety, dosing safety,
  authoritative LOINC/ChEBI/UCUM conformance, or regulatory exchange readiness.

Status:

- added and passed on 2026-05-25;
- intentionally does not claim modular `UnitRegistry` or `Knowledge<T>`
  clinical-unit proof-context coverage;
- intentionally treats internal dimension labels as unit identifiers with no
  built-in conversion factors, not as validated clinical decision-support facts
  or terminology-conformant exchange codes;
- command output:
  - `PASS built current-source lean_single compiler for internal label gate`;
  - `PASS tests/frontend/unit_literal_clinical_current_source.sio accepted clinical numeric literal unit suffixes`;
  - `PASS tests/compile-fail/unit_literal_clinical_reject_mass_as_amount_concentration.sio rejected incompatible internal label dimension`.

### Knowledge Proof Context Phase 2

Command:

```bash
make test-knowledge-context-phase2
```

Positive witnesses:

- `tests/frontend/knowledge_static_diabetes_context.sio`
- `tests/frontend/knowledge_static_return_subclass_where_ordinary_probe.sio`
- `tests/frontend/knowledge_static_multi_constraint_ordinary_probe.sio`

Negative witnesses:

- `tests/frontend/knowledge_static_rejects_disjoint_diagnosis.sio`
- `tests/frontend/knowledge_static_return_disjoint_where_ordinary_probe.sio`
- `tests/frontend/knowledge_static_multi_constraint_reject_ordinary_probe.sio`

Evidence class:

- Stage1 modular frontend;
- Stage1 checker-verdict AST proof-context bridge through
  `check::knowledge_context`;
- ordinary parser/AST probe for proof-context retention;
- Sounio checker source changes present but not yet proven by the normal checker
  verdict path;
- existing ontology subclass/disjoint contract mirrored by the AST witness;
- static struct-field ontology proof context;
- multi-constraint static ontology proof context checked as a conjunction.

Status:

- passed on 2026-05-24 with ordinary parser/AST coverage and ordinary frontend
  positive/negative AST verdicts;
- refreshed on 2026-05-24 after moving the boot4-safe validator from
  `check::mod` duplication into `check::knowledge_context`;
- refreshed again on 2026-05-24 after extending the bridge to validate
  proof-context constraints on function return types;
- refreshed again on 2026-05-24 after adding two-constraint positive and
  negative witnesses to prove conjunction semantics through the ordinary
  parser/AST probe and Stage1 checker verdict bridge;
- command output:
  - `PASS tests/frontend/knowledge_static_diabetes_context.sio accepted by Stage1 checker verdict ontology proof-context bridge`;
  - `PASS tests/frontend/knowledge_static_rejects_disjoint_diagnosis.sio rejected by Stage1 checker verdict ontology proof-context bridge`;
  - `PASS tests/frontend/knowledge_static_subclass_where_ordinary_probe.sio parsed by ordinary route with one proof constraint`;
  - `PASS tests/frontend/knowledge_static_disjoint_where_ordinary_probe.sio parsed by ordinary route with one proof constraint`;
  - `PASS tests/frontend/knowledge_static_transitive_where_ordinary_probe.sio parsed by ordinary route with one proof constraint`;
  - `PASS tests/frontend/knowledge_static_return_subclass_where_ordinary_probe.sio parsed by ordinary route with one return proof constraint`;
  - `PASS tests/frontend/knowledge_static_return_disjoint_where_ordinary_probe.sio parsed by ordinary route with one return proof constraint`;
  - `PASS tests/frontend/knowledge_static_multi_constraint_ordinary_probe.sio parsed by ordinary route with two proof constraints`;
  - `PASS tests/frontend/knowledge_static_multi_constraint_reject_ordinary_probe.sio parsed by ordinary route with two proof constraints`;
  - `PASS tests/frontend/knowledge_static_subclass_where_ordinary_probe.sio accepted by ordinary Stage1 checker verdict ontology proof-context bridge`;
  - `PASS tests/frontend/knowledge_static_transitive_where_ordinary_probe.sio accepted by ordinary Stage1 checker verdict ontology proof-context bridge`;
  - `PASS tests/frontend/knowledge_static_disjoint_where_ordinary_probe.sio rejected by ordinary Stage1 checker verdict ontology proof-context bridge`;
  - `PASS tests/frontend/knowledge_static_multi_constraint_ordinary_probe.sio accepted by ordinary Stage1 checker verdict multi-constraint ontology proof-context bridge`;
  - `PASS tests/frontend/knowledge_static_multi_constraint_reject_ordinary_probe.sio rejected by ordinary Stage1 checker verdict multi-constraint ontology proof-context bridge`;
  - `PASS tests/frontend/knowledge_static_return_subclass_where_ordinary_probe.sio accepted by ordinary Stage1 checker verdict return ontology proof-context bridge`;
  - `PASS tests/frontend/knowledge_static_return_disjoint_where_ordinary_probe.sio rejected by ordinary Stage1 checker verdict return ontology proof-context bridge`.

### Unit Types Phase 1

Command:

```bash
make test-unit-types
```

Positive witnesses:

- `tests/run-pass/unit_same_add.sio`
- `tests/run-pass/unit_scalar_mul.sio`
- `tests/run-pass/unit_div_cancel.sio`
- `tests/run-pass/unit_cast_compatible.sio`

Negative witnesses:

- `tests/compile-fail/unit_mismatch_add.sio`
- `tests/compile-fail/unit_mismatch_call_arg.sio`
- `tests/compile-fail/unit_cast_incompatible.sio`

Evidence class:

- current `bin/souc` checker path;
- builtin `UnitRegistry` and `unit_id` type annotations;
- dimension-vector compatibility checks for builtin units;
- dimension cancellation for same-unit division;
- compile-fail diagnostics for arithmetic, call arguments, and casts.

Status:

- passed on 2026-05-24 after adding
  `scripts/ci/unit_types_phase1_gate.sh` and `make test-unit-types`;
- command output:
  - `PASS tests/run-pass/unit_same_add.sio preserved unit through same-unit addition`;
  - `PASS tests/run-pass/unit_scalar_mul.sio preserved unit through scalar multiplication`;
  - `PASS tests/run-pass/unit_div_cancel.sio cancelled same-unit division to a dimensionless scalar`;
  - `PASS tests/run-pass/unit_cast_compatible.sio accepted compatible unit cast`;
  - `PASS tests/compile-fail/unit_mismatch_add.sio rejected incompatible unit addition`;
  - `PASS tests/compile-fail/unit_mismatch_call_arg.sio rejected incompatible unit call argument`;
  - `PASS tests/compile-fail/unit_cast_incompatible.sio rejected incompatible unit cast`.

### Derived Unit Types Current-Source Gate

Command:

```bash
make test-unit-types-derived
```

Positive witness:

- `tests/frontend/unit_derived_velocity_decl_current_source.sio`
- `tests/frontend/unit_derived_acceleration_chain_current_source.sio`
- `tests/frontend/unit_f64_unit_expr_velocity_current_source.sio`
- `tests/frontend/unit_literal_suffix_current_source.sio`
- `tests/frontend/unit_literal_clinical_current_source.sio`
- `tests/run-pass/unit_energy_explicit_conversion.sio`

Negative witness:

- `tests/compile-fail/unit_derived_velocity_reject_length.sio`
- `tests/compile-fail/unit_derived_acceleration_reject_velocity.sio`
- `tests/compile-fail/unit_f64_unit_expr_reject_length.sio`
- `tests/compile-fail/unit_f64_unit_expr_unknown_reject.sio`
- `tests/compile-fail/unit_literal_suffix_reject_length_as_mass.sio`
- `tests/compile-fail/unit_literal_clinical_reject_mass_as_amount_concentration.sio`
- `tests/compile-fail/unit_energy_requires_explicit_conversion.sio`

Evidence class:

- modular parser AST probe for chained derived unit payload retention;
- modular `check::check` semantic probe for chained derived unit dimension
  validation using a scalar imported function and `UnitRegistry`;
- rebuilt current-source `self-hosted/compiler/lean_single.sio` compiler;
- top-level `unit velocity = m / s;` derived unit registration;
- top-level `unit acceleration = m / s / s;` chained derived unit
  registration;
- dimension-vector propagation through division;
- named-unit recovery for a non-cancelling quotient when a compatible derived
  unit is registered;
- first-rung `f64<UnitExpr>` annotations for registered unit identifiers joined
  by `*` and `/`, including rejection of wrong dimensions and unknown unit
  identifiers;
- compile-fail diagnostics when a plain length is passed where velocity is
  required and when velocity is passed where acceleration is required.
- current-source named numeric literal suffixes such as `200.0<mg>` and
  `300<mg>` lower into the same unit-tracking path as annotated unit values;
- compile-fail diagnostics when a length literal suffix is passed where a mass
  unit is required.
- current-source clinical literal unit suffixes such as `140.0<mg_dL>`,
  `4.2<mmol_L>`, `31.0<U_L>`, and `12.0<mm_h>` lower into internal
  unit-identifier dimensional call-boundary checks with no built-in conversion
  factors and without claiming clinical correctness, conversion safety, UCUM
  conformance, or regulatory exchange readiness;
- compile-fail diagnostics when a clinical mass concentration is passed where
  an amount concentration is required.
- current-source exact-unit arithmetic for addition/subtraction: compatible
  dimensions alone are not enough, so `J + eV` is rejected unless one operand is
  explicitly converted first;
- explicit compatible-dimension unit casts such as `J as eV` are accepted as
  the conversion boundary.

Status:

- passed on 2026-05-24 after adding
  `scripts/ci/unit_types_derived_gate.sh` and `make test-unit-types-derived`;
- refreshed on 2026-05-25 after repairing the modular parser token mapping and
  derived-unit item parser so the chained derived-unit AST probe reaches the
  rebuilt current-source portion again;
- the clinical current-source portion has been split into
  `make test-unit-types-clinical-current-source`, which passes and does not
  claim the modular AST/checker mirror;
- this gate intentionally uses a compiler rebuilt into `/tmp` from current
  source because the checked-in default binary predates the derived-unit
  implementation;
- modular source now mirrors the chained unit-expression representation in
  `self-hosted/parser/items.sio` and `self-hosted/check/knowledge_context.sio`;
- `self-hosted/compiler/k2_unit_derived_ast_probe.sio` is now the focused
  modular parser gate and proves the parser retains three factors, two binary
  operators, and the left-expression payload for `m / s / s`;
- `self-hosted/compiler/k2_unit_derived_checker_probe.sio` is now the focused
  modular checker gate and proves `check::knowledge_context` validates
  acceleration as `m/s/s` while keeping it incompatible with velocity `m/s`;
- refreshed on 2026-05-25 after adding current-source named numeric literal
  unit suffixes for float and integer literals;
- refreshed again on 2026-05-25 after requiring explicit conversion for
  same-dimension energy units such as J and eV in the current-source
  `lean_single` path;
- refreshed again on 2026-05-25 after registering clinical validation-data
  unit names in current-source `lean_single`; modular `UnitRegistry` coverage
  remains a separate pending mirror;
- standalone `bin/souc check self-hosted/parser/items.sio` and
  `bin/souc check self-hosted/check/check.sio` are baseline-noisy and are not
  acceptance gates for this proof;
- command output:
  - `PASS self-hosted/compiler/k2_unit_derived_ast_probe.sio retained chained derived unit AST payload`;
  - `PASS self-hosted/compiler/k2_unit_derived_checker_probe.sio validated chained derived unit dimensions through check::knowledge_context`;
  - `PASS built current-source lean_single compiler for derived unit gate`;
  - `PASS tests/frontend/unit_derived_velocity_decl_current_source.sio accepted m/s as derived velocity`;
  - `PASS tests/compile-fail/unit_derived_velocity_reject_length.sio rejected length where velocity is required`;
  - `PASS tests/frontend/unit_derived_acceleration_chain_current_source.sio accepted m/s/s as derived acceleration`;
  - `PASS tests/compile-fail/unit_derived_acceleration_reject_velocity.sio rejected velocity where acceleration is required`;
  - `PASS tests/frontend/unit_literal_suffix_current_source.sio accepted numeric literal unit suffixes`;
  - `PASS tests/compile-fail/unit_literal_suffix_reject_length_as_mass.sio rejected numeric literal unit suffix with incompatible dimension`;
  - `PASS tests/frontend/unit_literal_clinical_current_source.sio accepted clinical numeric literal unit suffixes`;
  - `PASS tests/compile-fail/unit_literal_clinical_reject_mass_as_amount_concentration.sio rejected incompatible internal label dimension`;
  - `PASS tests/run-pass/unit_energy_explicit_conversion.sio accepted explicit conversion between compatible energy units`;
  - `PASS tests/compile-fail/unit_energy_requires_explicit_conversion.sio rejected compatible-dimension energy arithmetic without explicit conversion`.

### Knowledge Unit Constraints

Command:

```bash
make test-knowledge-unit-constraints
```

Positive witness:

- `tests/frontend/knowledge_static_unit_constraint_positive.sio`
- `tests/frontend/knowledge_static_unit_internal_label_positive.sio`

Negative witness:

- `tests/frontend/knowledge_static_unit_constraint_reject.sio`
- `tests/frontend/knowledge_static_unit_internal_label_reject.sio`

Evidence class:

- ordinary `check::knowledge_context` bridge validates unit constraints only
  when a parsed `KnowledgeConstraint.unit_name` is present;
- non-unit ontology proof contexts stay on the previous ontology-only route to
  preserve the green Stage1 bridge and avoid passing `UnitRegistry` through the
  boot4-safe validator unnecessarily;
- standalone `check::check` semantic bridge collects unit declarations,
  lowers unit-typed struct fields to `unit_id`-annotated types, and checks
  dimension-vector compatibility through `UnitRegistry`;
- derived-unit compatibility is proven with `unit acceleration = m / s / s`;
- incompatible dimension rejection is proven by using a `velocity` field where
  the proof context requires `<acceleration>`;
- internal validation-data label compatibility is proven with matching
  `mg_dL`, `mmol_L`, `U_L`, and `mm_h` fields and proof contexts;
- incompatible internal label rejection is proven by using an `mg_dL` field
  where the proof context requires `<mmol_L>`;
- those internal labels are dimensional-checking labels only, not clinical,
  conversion, UCUM, LOINC, ChEBI, dosing, or regulatory-exchange authority.

Status:

- passed on 2026-05-24 after adding
  `scripts/ci/knowledge_context_unit_gate.sh` and
  `make test-knowledge-unit-constraints`;
- refreshed on 2026-05-25 after mirroring internal validation-data unit labels
  into the modular `UnitRegistry`;
- this gate proves static dimensional compatibility for the unit annotation in
  a `Knowledge<T>` proof context; it does not yet prove numeric threshold
  evaluation or runtime check generation for dynamic fields;
- command output:
  - `PASS tests/frontend/knowledge_static_unit_constraint_positive.sio accepted matching Knowledge<T> unit constraint`;
  - `PASS tests/frontend/knowledge_static_unit_constraint_reject.sio rejected incompatible Knowledge<T> unit constraint`;
  - `PASS tests/frontend/knowledge_static_unit_internal_label_positive.sio accepted internal validation-data unit labels in Knowledge<T> constraints`;
  - `PASS tests/frontend/knowledge_static_unit_internal_label_reject.sio rejected incompatible internal validation-data unit label in Knowledge<T> constraint`.

### Knowledge Numeric Constraints

Command:

```bash
make test-knowledge-numeric-constraints
```

Positive witness:

- `tests/frontend/knowledge_static_numeric_constraint_positive.sio`

Negative witnesses:

- `tests/frontend/knowledge_static_numeric_constraint_reject_bool.sio`
- `tests/frontend/knowledge_static_numeric_constraint_reject_name_value.sio`
- `tests/frontend/knowledge_static_numeric_refinement_reject_weak.sio`

Static-refinement positive witness:

- `tests/frontend/knowledge_static_numeric_refinement_positive.sio`

Evidence class:

- ordinary `check::knowledge_context` bridge enters static value validation when
  a `KnowledgeConstraint` has `>`, `>=`, `<`, `<=`, or numeric `==`;
- the standalone `check::check` semantic bridge remains a separate classifier
  lane and is not part of the focused numeric gate;
- builtin integer/float fields and unit-typed fields count as numeric;
- bool and ontology/named non-unit fields are rejected for numeric operators;
- numeric comparison operators require integer or float literal values.
- modular parser now accepts inline refinement types through the normal
  `parse_type()` entrypoint, so struct fields can carry
  `{ v: i64 | v >= 18 }` directly;
- the ordinary bridge uses structural refinement implication to prove a field
  refinement satisfies the `Knowledge<T>` numeric threshold and to reject a
  weaker field refinement.

Status:

- passed on 2026-05-24 after adding
  `scripts/ci/knowledge_context_numeric_gate.sh` and
  `make test-knowledge-numeric-constraints`;
- this gate proves static numeric shape checking for `Knowledge<T>` proof
  contexts and static threshold satisfaction through field refinement types; it
  does not yet prove that a generated or dynamic `Knowledge<T>` value satisfies
  the threshold, nor does it generate runtime checks for dynamic values;
- command output:
  - `PASS tests/frontend/knowledge_static_numeric_constraint_positive.sio accepted numeric Knowledge<T> constraint on i64 field`;
  - `PASS tests/frontend/knowledge_static_numeric_constraint_reject_bool.sio rejected numeric Knowledge<T> constraint on bool field`;
  - `PASS tests/frontend/knowledge_static_numeric_constraint_reject_name_value.sio rejected numeric Knowledge<T> constraint with nonnumeric value`;
  - `PASS tests/frontend/knowledge_static_numeric_refinement_positive.sio accepted Knowledge<T> numeric constraint implied by field refinement`;
  - `PASS tests/frontend/knowledge_static_numeric_refinement_reject_weak.sio rejected Knowledge<T> numeric constraint not implied by weak field refinement`.

### Knowledge Composite Proof Context

Command:

```bash
make test-knowledge-composite
```

Positive witness:

- `tests/frontend/knowledge_static_composite_positive.sio`

Negative witnesses:

- `tests/frontend/knowledge_static_composite_reject_ontology.sio`
- `tests/frontend/knowledge_static_composite_reject_unit.sio`
- `tests/frontend/knowledge_static_composite_reject_refinement.sio`

Evidence class:

- one `Knowledge<T where {...}>` proof context contains all three static
  constraint families:
  - ontology subclass: `dx subclass_of Diabetes`;
  - derived-unit dimensional check: `accel >= 1.0 <acceleration>`;
  - numeric-refinement threshold: `age >= 18`;
- ordinary `check::knowledge_context` bridge now validates ontology constraints
  and value constraints in separate passes to avoid the previous
  ontology-plus-UnitRegistry recursion crash;
- standalone `check::check` semantic bridge validates the same composite
  positive and negative witnesses;
- negative witnesses fail by changing exactly one constraint family at a time:
  ontology type, unit dimension, or refinement strength.

Status:

- passed on 2026-05-24 after adding
  `scripts/ci/knowledge_context_composite_gate.sh` and
  `make test-knowledge-composite`;
- this gate proves static conjunction semantics across ontology, units, and
  numeric refinements in one proof context; it does not generate runtime checks
  for dynamic fields;
- command output:
  - `PASS tests/frontend/knowledge_static_composite_positive.sio accepted composite ontology+unit+numeric-refinement proof context`;
  - `PASS tests/frontend/knowledge_static_composite_reject_ontology.sio rejected composite proof context with failed ontology constraint`;
  - `PASS tests/frontend/knowledge_static_composite_reject_unit.sio rejected composite proof context with failed unit constraint`;
  - `PASS tests/frontend/knowledge_static_composite_reject_refinement.sio rejected composite proof context with weak numeric refinement`.

### Knowledge Static Umbrella

Command:

```bash
make test-knowledge-context-static
```

Evidence class:

- runs the focused ontology proof-context gate;
- runs the focused unit proof-context gate;
- runs the focused numeric/refinement proof-context gate;
- runs the focused composite ontology+unit+numeric-refinement gate;
- runs the focused static value-initializer gate for literal numeric
  thresholds and simple generated value constructors with direct literal or
  literal-argument evidence, plus narrow const-evaluable numeric expressions
  plus simple local bindings and `=`/compound assignments in generated
  constructor bodies, plus narrow helper calls with const-evaluable arguments
  and helper-local bindings/assignments, plus literal-boolean branch selection
  and literal-boolean branch local flow in generated constructors, plus simple
  const-evaluable numeric branch conditions over literals and literal-backed
  parameters for branch local flow, direct-return branch selection, and narrow
  helper branch selection plus helper-local branch flow, plus narrow nested
  helper calls with caller parameter threading;
- intentionally excludes runtime checks for dynamic `Knowledge<T>` fields.

Status:

- passed on 2026-05-24 after adding
  `scripts/ci/knowledge_context_static_gate.sh` and
  `make test-knowledge-context-static`;
- refreshed on 2026-05-24 after extending the static value gate to simple
  zero-arg generated value constructors;
- refreshed again on 2026-05-24 after extending simple generated value
  constructors to direct parameter references supplied by literal call
  arguments;
- refreshed on 2026-05-25 after adding narrow const-eval for generated numeric
  expressions over numeric literals and literal-backed parameters;
- refreshed again on 2026-05-25 after resolving simple local `let`/`var`
  bindings in generated constructor bodies through the same narrow const-eval
  path;
- refreshed again on 2026-05-25 after resolving simple linear `=` assignments
  before the direct return in generated constructor bodies;
- refreshed again on 2026-05-25 after resolving simple linear compound
  assignments before the direct return in generated constructor bodies;
- refreshed again on 2026-05-25 after adding narrow helper-call evaluation for
  generated constructor fields;
- refreshed again on 2026-05-25 after proving helper-local bindings inside
  those narrow helper calls;
- refreshed again on 2026-05-25 after proving helper-local assignments inside
  those narrow helper calls;
- refreshed again on 2026-05-25 after adding literal-boolean branch selection
  inside generated constructors;
- refreshed again on 2026-05-25 after resolving literal-boolean branch local
  flow before the direct return in generated constructors;
- refreshed again on 2026-05-25 after resolving simple const-evaluable
  numeric branch conditions over literals and literal-backed parameters;
- refreshed again on 2026-05-25 after applying the same const-evaluable branch
  conditions to direct-return branch selection in generated constructors;
- refreshed again on 2026-05-25 after proving narrow helper calls whose direct
  return is selected by a const-evaluable branch condition;
- refreshed again on 2026-05-25 after proving narrow helper-local branch flow
  when a helper mutates a tracked local through a const-evaluable branch before
  returning it;
- refreshed again on 2026-05-25 after proving narrow nested helper calls where
  an outer helper returns an inner helper call with literal or const-evaluable
  arguments;
- refreshed again on 2026-05-25 after proving caller parameter threading
  across that narrow nested helper shape;
- command output ends with:
  - `Knowledge static proof-context umbrella gate passed.`;
  - `This covers static ontology, derived-unit dimensional, numeric-refinement, and composite proof contexts, plus literal Knowledge value initializers and simple generated value constructors with direct literal, literal-argument, or narrow const-evaluable numeric-expression evidence, including simple local bindings in generated constructor bodies and simple linear assignment/compound-assignment statements before return, plus narrow helper calls with const-evaluable arguments and helper-local bindings/assignments, and literal-boolean branch selection plus literal-boolean branch local flow in generated constructors. The gate also covers simple const-evaluable numeric branch conditions over literals and literal-backed parameters for branch local flow and direct-return branch selection, plus narrow helper branch selection and helper-local branch flow, plus narrow nested helper calls with caller parameter threading. Runtime checks for dynamic Knowledge<T> fields and dynamic branch conditions remain future work and are intentionally not claimed by this gate.`

### Knowledge Static Value Initializers

Command:

```bash
make test-knowledge-static-values
```

Positive witness:

- `tests/frontend/knowledge_static_value_positive.sio`
- `tests/frontend/knowledge_static_value_local_positive.sio`
- `tests/frontend/knowledge_static_value_call_positive.sio`
- `tests/frontend/knowledge_static_value_assign_positive.sio`
- `tests/frontend/knowledge_static_value_unit_positive.sio`
- `tests/frontend/knowledge_static_value_generator_positive.sio`
- `tests/frontend/knowledge_static_value_generator_arg_positive.sio`
- `tests/frontend/knowledge_static_value_generator_expr_positive.sio`
- `tests/frontend/knowledge_static_value_generator_local_positive.sio`
- `tests/frontend/knowledge_static_value_generator_assign_positive.sio`
- `tests/frontend/knowledge_static_value_generator_compound_positive.sio`
- `tests/frontend/knowledge_static_value_generator_helper_positive.sio`
- `tests/frontend/knowledge_static_value_generator_helper_local_positive.sio`
- `tests/frontend/knowledge_static_value_generator_helper_assign_positive.sio`
- `tests/frontend/knowledge_static_value_generator_nested_helper_positive.sio`

Negative witness:

- `tests/frontend/knowledge_static_value_reject_literal.sio`
- `tests/frontend/knowledge_static_value_local_reject_literal.sio`
- `tests/frontend/knowledge_static_value_var_reject_literal.sio`
- `tests/frontend/knowledge_static_value_call_reject_literal.sio`
- `tests/frontend/knowledge_static_value_assign_reject_literal.sio`
- `tests/frontend/knowledge_static_value_unit_reject_literal.sio`
- `tests/frontend/knowledge_static_value_generator_reject_literal.sio`
- `tests/frontend/knowledge_static_value_generator_arg_reject_literal.sio`
- `tests/frontend/knowledge_static_value_generator_expr_reject_literal.sio`
- `tests/frontend/knowledge_static_value_generator_local_reject_literal.sio`
- `tests/frontend/knowledge_static_value_generator_assign_reject_literal.sio`
- `tests/frontend/knowledge_static_value_generator_compound_reject_literal.sio`
- `tests/frontend/knowledge_static_value_generator_helper_reject_literal.sio`
- `tests/frontend/knowledge_static_value_generator_helper_local_reject_literal.sio`
- `tests/frontend/knowledge_static_value_generator_helper_assign_reject_literal.sio`
- `tests/frontend/knowledge_static_value_generator_nested_helper_reject_literal.sio`

Dynamic-gap witness:

- `tests/frontend/knowledge_static_value_dynamic_gap.sio`
- `tests/frontend/knowledge_static_value_call_dynamic_gap.sio`
- `tests/frontend/knowledge_static_value_assign_dynamic_gap.sio`
- `tests/frontend/knowledge_static_value_unit_dynamic_gap.sio`
- `tests/frontend/knowledge_static_value_generator_dynamic_gap.sio`
- `tests/frontend/knowledge_static_value_generator_arg_dynamic_gap.sio`
- `tests/frontend/knowledge_static_value_generator_expr_dynamic_gap.sio`
- `tests/frontend/knowledge_static_value_generator_local_dynamic_gap.sio`
- `tests/frontend/knowledge_static_value_generator_assign_dynamic_gap.sio`
- `tests/frontend/knowledge_static_value_generator_compound_dynamic_gap.sio`
- `tests/frontend/knowledge_static_value_generator_helper_dynamic_gap.sio`
- `tests/frontend/knowledge_static_value_generator_helper_local_dynamic_gap.sio`
- `tests/frontend/knowledge_static_value_generator_helper_assign_dynamic_gap.sio`
- `tests/frontend/knowledge_static_value_generator_nested_helper_dynamic_gap.sio`

Evidence class:

- modular parser accepts `Knowledge { ... }` as a struct-literal expression,
  so focused K2 probes can inspect value-level Knowledge construction;
- ordinary `check::knowledge_context` validates return expressions whose
  declared return type is `Knowledge<Struct where {...}>` and whose returned
  value is a direct `Knowledge { value: Struct { ... } }` literal;
- ordinary `check::knowledge_context` validates annotated local `let` and
  `var` bindings with the same direct literal shape;
- ordinary `check::knowledge_context` validates simple function call arguments
  by pairing literal arguments with the callee parameter type;
- ordinary `check::knowledge_context` validates simple assignments by resolving
  the target identifier back to an annotated local `let` or `var` type in the
  same function body;
- ordinary `check::knowledge_context` validates simple zero-arg generated
  `Knowledge<T>` values at a call site by resolving a direct function call,
  requiring the generator to return `Knowledge<SameInner>`, extracting its
  direct literal return expression, and applying the call-site proof context;
- ordinary `check::knowledge_context` also handles direct parameter references
  in that returned struct literal when the corresponding call argument is an
  integer or float literal;
- ordinary `check::knowledge_context` includes a narrow const-eval path for
  generated numeric expressions using `+`, `-`, `*`, and `/` over numeric
  literals and literal-backed parameters;
- ordinary `check::knowledge_context` resolves simple local `let`/`var`
  initializers in generated constructor bodies when those initializers are
  const-evaluable through the same narrow numeric path;
- ordinary `check::knowledge_context` resolves simple linear `=` assignments
  before the direct return, preferring the latest assignment before return and
  resolving self-references in assignment RHS expressions against the prior
  local initializer;
- ordinary `check::knowledge_context` also resolves simple linear compound
  assignments (`+=`, `-=`, `*=`, `/=`) before the direct return when both the
  current local value and right-hand side are const-evaluable;
- ordinary `check::knowledge_context` evaluates narrow numeric helper calls
  whose callee resolves to a direct-return function and whose helper call
  arguments are const-evaluable;
- ordinary `check::knowledge_context` resolves simple helper-local `let`/`var`
  bindings inside those narrow numeric helper calls;
- ordinary `check::knowledge_context` resolves simple helper-local linear
  assignments inside those narrow numeric helper calls;
- ordinary `check::knowledge_context` resolves generated constructor returns
  through literal-boolean `if true`/`if false` branches and validates the
  selected branch against the call-site proof context;
- ordinary `check::knowledge_context` resolves literal-boolean `if true`/`if false`
  local-flow blocks before return, including simple assignments to tracked
  locals, and treats dynamic branch conditions as runtime-check gaps rather
  than falling back to stale pre-branch initializers;
- ordinary `check::knowledge_context` evaluates simple boolean conditions over
  numeric comparisons, `!`, `&&`, and `||` when the operands are literals or
  literal-backed parameters, and uses that result to select local-flow
  branches before return;
- ordinary `check::knowledge_context` uses the same const-evaluable boolean
  condition path to select direct-return branches in generated constructors;
- ordinary `check::knowledge_context` also uses that branch-selection path
  inside narrow numeric helper calls used by generated constructors;
- ordinary `check::knowledge_context` resolves helper-local branch flow when a
  narrow helper mutates a tracked local through a const-evaluable branch before
  returning it;
- ordinary `check::knowledge_context` resolves narrow nested helper calls when
  an outer helper directly returns an inner helper call whose arguments are
  literal, caller-parameter-threaded, or const-evaluable in that frame;
- integer and float literal constrained fields are compared against numeric
  `>`, `>=`, `<`, `<=`, and `==` proof-context thresholds;
- unit-suffixed thresholds are validated when their numeric value is carried by
  the modular parser path;
- a literal violation is rejected at static validation time;
- a nonliteral field expression is accepted as a classified runtime-check gap,
  not as a static proof.

Status:

- passed on 2026-05-24 after adding
  `scripts/ci/knowledge_context_static_value_gate.sh` and
  `make test-knowledge-static-values`;
- refreshed on 2026-05-24 after adding local `let` and `var` binding
  witnesses;
- refreshed again on 2026-05-24 after adding simple call-boundary witnesses;
- refreshed again on 2026-05-24 after adding simple assignment witnesses;
- refreshed again on 2026-05-24 after adding unit-suffixed threshold witnesses
  with float literals;
- refreshed again on 2026-05-24 after adding simple generated-value witnesses
  for zero-arg direct literal-return functions;
- refreshed again on 2026-05-24 after adding simple generated-value witnesses
  whose constrained field is a direct parameter reference supplied by a literal
  call argument;
- refreshed on 2026-05-25 after adding simple generated-value witnesses whose
  constrained field is a const-evaluable numeric expression over literal-backed
  parameters;
- refreshed again on 2026-05-25 after adding simple generated-value witnesses
  whose constrained field flows through a local `let` binding in the generated
  constructor body;
- refreshed again on 2026-05-25 after adding simple generated-value witnesses
  whose constrained field flows through a local `var` assignment in the
  generated constructor body;
- refreshed again on 2026-05-25 after adding simple generated-value witnesses
  whose constrained field flows through a local compound assignment in the
  generated constructor body;
- refreshed again on 2026-05-25 after adding simple generated-value witnesses
  whose constrained field flows through a direct-return helper call in the
  generated constructor body;
- refreshed again on 2026-05-25 after adding simple generated-value witnesses
  whose helper call resolves a helper-local `let` binding before returning;
- refreshed again on 2026-05-25 after adding simple generated-value witnesses
  whose helper call resolves a helper-local assignment before returning;
- refreshed again on 2026-05-25 after adding simple generated-value witnesses
  whose constructor body selects a direct return through `if true`/`if false`;
- refreshed again on 2026-05-25 after adding simple generated-value witnesses
  whose constructor body mutates a tracked local through a literal-boolean
  branch before the direct return;
- refreshed again on 2026-05-25 after adding simple generated-value witnesses
  whose constructor body selects local flow through a const-evaluable numeric
  branch condition before the direct return;
- refreshed again on 2026-05-25 after adding simple generated-value witnesses
  whose constructor body selects a direct return through a const-evaluable
  numeric branch condition;
- refreshed again on 2026-05-25 after adding simple generated-value witnesses
  whose constrained field comes from a helper call with a const-evaluable
  branch-selected direct return;
- refreshed again on 2026-05-25 after adding simple generated-value witnesses
  whose constrained field comes from a helper call that mutates a tracked local
  through a const-evaluable branch before return;
- refreshed again on 2026-05-25 after adding simple generated-value witnesses
  whose constrained field comes from a nested helper call with literal or
  const-evaluable inner arguments;
- refreshed again on 2026-05-25 after making that nested helper witness carry
  a caller parameter from the outer helper into the inner helper call;
- this gate does not yet cover general interprocedural generated constructors,
  general expression evaluation, nonliteral branch-sensitive local flow,
  compound assignment beyond the linear generated-constructor path,
  helper-local loop flow, nonliteral branch conditions, helper calls with
  dynamic arguments, or runtime check generation for dynamic fields;
- command output:
  - `PASS tests/frontend/knowledge_static_value_positive.sio accepted literal Knowledge<T> initializer satisfying proof context`;
  - `PASS tests/frontend/knowledge_static_value_reject_literal.sio rejected literal Knowledge<T> initializer violating proof context`;
  - `PASS tests/frontend/knowledge_static_value_dynamic_gap.sio classified dynamic value as runtime-check gap, not static rejection`;
  - `PASS tests/frontend/knowledge_static_value_local_positive.sio accepted local literal Knowledge<T> binding satisfying proof context`;
  - `PASS tests/frontend/knowledge_static_value_local_reject_literal.sio rejected local literal Knowledge<T> binding violating proof context`;
  - `PASS tests/frontend/knowledge_static_value_var_reject_literal.sio rejected var literal Knowledge<T> binding violating proof context`;
  - `PASS tests/frontend/knowledge_static_value_call_positive.sio accepted literal Knowledge<T> call argument satisfying proof context`;
  - `PASS tests/frontend/knowledge_static_value_call_reject_literal.sio rejected literal Knowledge<T> call argument violating proof context`;
  - `PASS tests/frontend/knowledge_static_value_call_dynamic_gap.sio classified dynamic Knowledge<T> call argument as runtime-check gap`;
  - `PASS tests/frontend/knowledge_static_value_assign_positive.sio accepted literal Knowledge<T> assignment satisfying proof context`;
  - `PASS tests/frontend/knowledge_static_value_assign_reject_literal.sio rejected literal Knowledge<T> assignment violating proof context`;
  - `PASS tests/frontend/knowledge_static_value_assign_dynamic_gap.sio classified dynamic Knowledge<T> assignment as runtime-check gap`;
  - `PASS tests/frontend/knowledge_static_value_unit_positive.sio accepted unit-suffixed literal Knowledge<T> value satisfying proof context`;
  - `PASS tests/frontend/knowledge_static_value_unit_reject_literal.sio rejected unit-suffixed literal Knowledge<T> value violating proof context`;
  - `PASS tests/frontend/knowledge_static_value_unit_dynamic_gap.sio classified dynamic unit-suffixed Knowledge<T> value as runtime-check gap`;
  - `PASS tests/frontend/knowledge_static_value_generator_positive.sio accepted simple generated Knowledge<T> value satisfying call-site proof context`;
  - `PASS tests/frontend/knowledge_static_value_generator_reject_literal.sio rejected simple generated Knowledge<T> value violating call-site proof context`;
  - `PASS tests/frontend/knowledge_static_value_generator_dynamic_gap.sio classified dynamic generated Knowledge<T> value as runtime-check gap`;
  - `PASS tests/frontend/knowledge_static_value_generator_arg_positive.sio accepted simple generated Knowledge<T> value with literal argument satisfying call-site proof context`;
  - `PASS tests/frontend/knowledge_static_value_generator_arg_reject_literal.sio rejected simple generated Knowledge<T> value with literal argument violating call-site proof context`;
  - `PASS tests/frontend/knowledge_static_value_generator_arg_dynamic_gap.sio classified dynamic generated Knowledge<T> argument as runtime-check gap`;
  - `PASS tests/frontend/knowledge_static_value_generator_expr_positive.sio accepted simple generated Knowledge<T> value with const-evaluable expression satisfying call-site proof context`;
  - `PASS tests/frontend/knowledge_static_value_generator_expr_reject_literal.sio rejected simple generated Knowledge<T> value with const-evaluable expression violating call-site proof context`;
  - `PASS tests/frontend/knowledge_static_value_generator_expr_dynamic_gap.sio classified dynamic generated Knowledge<T> expression as runtime-check gap`;
  - `PASS tests/frontend/knowledge_static_value_generator_local_positive.sio accepted simple generated Knowledge<T> value through local const binding satisfying call-site proof context`;
  - `PASS tests/frontend/knowledge_static_value_generator_local_reject_literal.sio rejected simple generated Knowledge<T> value through local const binding violating call-site proof context`;
  - `PASS tests/frontend/knowledge_static_value_generator_local_dynamic_gap.sio classified dynamic generated Knowledge<T> local binding as runtime-check gap`;
  - `PASS tests/frontend/knowledge_static_value_generator_assign_positive.sio accepted simple generated Knowledge<T> value through local assignment satisfying call-site proof context`;
  - `PASS tests/frontend/knowledge_static_value_generator_assign_reject_literal.sio rejected simple generated Knowledge<T> value through local assignment violating call-site proof context`;
  - `PASS tests/frontend/knowledge_static_value_generator_assign_dynamic_gap.sio classified dynamic generated Knowledge<T> assignment as runtime-check gap`;
  - `PASS tests/frontend/knowledge_static_value_generator_compound_positive.sio accepted simple generated Knowledge<T> value through compound assignment satisfying call-site proof context`;
  - `PASS tests/frontend/knowledge_static_value_generator_compound_reject_literal.sio rejected simple generated Knowledge<T> value through compound assignment violating call-site proof context`;
  - `PASS tests/frontend/knowledge_static_value_generator_compound_dynamic_gap.sio classified dynamic generated Knowledge<T> compound assignment as runtime-check gap`;
  - `PASS tests/frontend/knowledge_static_value_generator_helper_positive.sio accepted simple generated Knowledge<T> value through helper call satisfying call-site proof context`;
  - `PASS tests/frontend/knowledge_static_value_generator_helper_reject_literal.sio rejected simple generated Knowledge<T> value through helper call violating call-site proof context`;
  - `PASS tests/frontend/knowledge_static_value_generator_helper_dynamic_gap.sio classified dynamic generated Knowledge<T> helper call as runtime-check gap`;
  - `PASS tests/frontend/knowledge_static_value_generator_helper_local_positive.sio accepted simple generated Knowledge<T> value through helper-local binding satisfying call-site proof context`;
  - `PASS tests/frontend/knowledge_static_value_generator_helper_local_reject_literal.sio rejected simple generated Knowledge<T> value through helper-local binding violating call-site proof context`;
  - `PASS tests/frontend/knowledge_static_value_generator_helper_local_dynamic_gap.sio classified dynamic generated Knowledge<T> helper-local binding as runtime-check gap`;
  - `PASS tests/frontend/knowledge_static_value_generator_helper_assign_positive.sio accepted simple generated Knowledge<T> value through helper-local assignment satisfying call-site proof context`;
  - `PASS tests/frontend/knowledge_static_value_generator_helper_assign_reject_literal.sio rejected simple generated Knowledge<T> value through helper-local assignment violating call-site proof context`;
  - `PASS tests/frontend/knowledge_static_value_generator_helper_assign_dynamic_gap.sio classified dynamic generated Knowledge<T> helper-local assignment as runtime-check gap`;
  - `PASS tests/frontend/knowledge_static_value_generator_branch_positive.sio accepted simple generated Knowledge<T> value through static branch satisfying call-site proof context`;
  - `PASS tests/frontend/knowledge_static_value_generator_branch_reject_literal.sio rejected simple generated Knowledge<T> value through static branch violating call-site proof context`;
  - `PASS tests/frontend/knowledge_static_value_generator_branch_dynamic_gap.sio classified dynamic generated Knowledge<T> branch as runtime-check gap`;
  - `PASS tests/frontend/knowledge_static_value_generator_branch_flow_positive.sio accepted simple generated Knowledge<T> value through static branch local flow satisfying call-site proof context`;
  - `PASS tests/frontend/knowledge_static_value_generator_branch_flow_reject_literal.sio rejected simple generated Knowledge<T> value through static branch local flow violating call-site proof context`;
  - `PASS tests/frontend/knowledge_static_value_generator_branch_flow_dynamic_gap.sio classified dynamic generated Knowledge<T> branch local flow as runtime-check gap`;
  - `PASS tests/frontend/knowledge_static_value_generator_branch_cond_positive.sio accepted simple generated Knowledge<T> value through const-evaluable branch condition satisfying call-site proof context`;
  - `PASS tests/frontend/knowledge_static_value_generator_branch_cond_reject_literal.sio rejected simple generated Knowledge<T> value through const-evaluable branch condition violating call-site proof context`;
  - `PASS tests/frontend/knowledge_static_value_generator_branch_cond_dynamic_gap.sio classified dynamic generated Knowledge<T> branch condition as runtime-check gap`;
  - `PASS tests/frontend/knowledge_static_value_generator_branch_return_cond_positive.sio accepted direct-return generated Knowledge<T> value through const-evaluable branch condition satisfying call-site proof context`;
  - `PASS tests/frontend/knowledge_static_value_generator_branch_return_cond_reject_literal.sio rejected direct-return generated Knowledge<T> value through const-evaluable branch condition violating call-site proof context`;
  - `PASS tests/frontend/knowledge_static_value_generator_branch_return_cond_dynamic_gap.sio classified dynamic direct-return generated Knowledge<T> branch condition as runtime-check gap`;
  - `PASS tests/frontend/knowledge_static_value_generator_helper_branch_cond_positive.sio accepted generated Knowledge<T> value through helper const-evaluable branch condition satisfying call-site proof context`;
  - `PASS tests/frontend/knowledge_static_value_generator_helper_branch_cond_reject_literal.sio rejected generated Knowledge<T> value through helper const-evaluable branch condition violating call-site proof context`;
  - `PASS tests/frontend/knowledge_static_value_generator_helper_branch_cond_dynamic_gap.sio classified dynamic helper branch condition as runtime-check gap`;
  - `PASS tests/frontend/knowledge_static_value_generator_helper_branch_flow_positive.sio accepted generated Knowledge<T> value through helper-local branch flow satisfying call-site proof context`;
  - `PASS tests/frontend/knowledge_static_value_generator_helper_branch_flow_reject_literal.sio rejected generated Knowledge<T> value through helper-local branch flow violating call-site proof context`;
  - `PASS tests/frontend/knowledge_static_value_generator_helper_branch_flow_dynamic_gap.sio classified dynamic helper-local branch flow as runtime-check gap`;
  - `PASS tests/frontend/knowledge_static_value_generator_nested_helper_positive.sio accepted generated Knowledge<T> value through nested helper call satisfying call-site proof context`;
  - `PASS tests/frontend/knowledge_static_value_generator_nested_helper_reject_literal.sio rejected generated Knowledge<T> value through nested helper call violating call-site proof context`;
  - `PASS tests/frontend/knowledge_static_value_generator_nested_helper_dynamic_gap.sio classified dynamic nested helper call as runtime-check gap`.

## Open Work

- Bootstrap snapshots have proof-context parser fields and parser logic; the
  ordinary source parser no longer segfaults on the proof-context witness after
  switching the `Knowledge` parser close-delimiter path to boot4-safe
  `parser_peek`/`parser_advance`.
- The ordinary Stage1 frontend now rejects the disjoint negative witness through
  `check::mod::check_items_verdict_boot4`, which imports
  `check::knowledge_context::knowledge_context_validate_items`. The updated
  `check::check` proof-context validator is still not proven through a
  boot4-safe `check::mod -> check::check` path, so that remains a
  build/bootstrap-path blocker rather than a parser blocker.
- `check::mod` no longer carries a local `check_mod_ast_*` duplicate of the
  current boot4-safe AST validator. That logic now lives in
  `self-hosted/check/knowledge_context.sio` as checker source.
- `check::knowledge_context` now validates proof constraints on both function
  parameter types and function return types.
- `check::knowledge_context` validates all listed proof constraints in a
  `Knowledge<T where {...}>` context recursively, so the current Stage1 gate
  covers multi-constraint conjunction success and failure.
- `check::knowledge_context` now validates direct literal Knowledge return
  values, annotated local bindings, simple call arguments, and simple
  assignments against numeric proof-context thresholds, including
  unit-suffixed thresholds whose numeric literal value is available in the
  modular parser path. The modular flat parser now carries float literal
  payloads through `PT_FLOAT_VALUE`, so `1.0 <unit>` thresholds and `0.5` values
  are checked by the same static value gate. The current implementation
  deliberately treats nonliteral constrained fields as dynamic evidence to be
  handled by a future runtime-check generator.
- `check::check` now keeps the semantic in-place validator over collected
  checker tables. The former pure AST duplicate in `check::check` has been
  retired in favor of the dedicated `check::knowledge_context` module. Current
  unreferenced value-style proof-context methods were also removed so the
  candidate semantic surface stays pointer-shaped. A scalar semantic verdict
  wrapper, `checker_knowledge_context_semantic_verdict_items`, now keeps
  allocation, minimal in-place collection, and validation inside `check::check`;
  the standalone K2 probe proves that wrapper accepts positive and
  multi-constraint witnesses while rejecting the negative witness. Importing
  that wrapper from `check::mod` is still not wired into the ordinary Stage1
  verdict path. Current evidence shows the remaining failures are bridge and
  full-collector failures, not ontology rule failures:
  - adding the in-place validator inside
    `checker_check_items_inner_store_return_had_error_mut` did not affect the
    Stage1 verdict path, and temporary sentinels in that function did not print;
    this proves the unqualified call from `check::mod` is still reaching a
    legacy/precompiled body, not the current `self-hosted/check/check.sio`
    implementation;
  - unqualified calls from `check::mod` to other new `check::check` validators
    return without executing the current body and let the disjoint witness pass;
  - a unique sentinel and a simple `Option<Box<ItemList>>` presence function in
    `check::check` both compile and execute from `check::mod`, so fresh symbol
    resolution itself is possible;
  - an earlier explicit import of the former pure AST validator rejected the
    disjoint witness, but pass-through witnesses segfaulted when the frontend
    continued into normal checker collection;
  - an explicit narrow import of the in-place validator compiles, but the
    generated Stage1 frontend segfaults after checker collection and before the
    validator can run;
  - an explicit import of the current
    `checker_check_items_inner_store_return_had_error_mut` wrapper also
    compiles, but the generated Stage1 frontend segfaults while type checking
    the disjoint witness;
  - an explicit import of the scalar
    `checker_knowledge_context_semantic_verdict_items` wrapper also compiles,
    but the generated Stage1 frontend segfaults on the positive witness before
    reaching a verdict; this remains true after the wrapper was changed to use
    minimal in-place collection and a smaller builtin heap allocation;
  - replacing the Stage1 AST bridge with an unqualified
    `checker_knowledge_context_semantic_verdict_items` call does not segfault,
    but it lets the negative witness pass, matching the existing stale-body
    resolution pattern for unqualified `check::check` calls from `check::mod`;
  - a focused `check::mod` diagnostic bridge using a fresh unqualified symbol
    (`checker_kctx_semantic_verdict_v2_items`) also completes on the positive
    witness but lets the negative witness pass with `verdict=0`; this proves
    the remaining unqualified `check::mod -> check::check` path is stale even
    for a fresh semantic wrapper name;
  - moving semantic table reads directly into `check::mod` after
    `checker_collect_items_mut` segfaults on the positive witness, confirming
    that direct `Checker` field access from `check::mod` is not a safe bridge;
  - a standalone import probe reaches an imported scalar `check::check`
    function body, but segfaults at `checker.collect_items(items)`, matching the
    known large-`Checker` value-return hazard;
  - standalone probes now distinguish the compiler builtin `heap_alloc` path
    from the imported `mem::box`/`calloc` wrapper: builtin allocation can create
    and initialize a `Checker`, while the wrapper still fails before `alloc_ok`;
  - `self-hosted/compiler/k2_check_import_accessor_probe.sio` proves standalone
    `Checker` init plus imported scalar accessor reads work when the probe uses
    builtin `heap_alloc`;
  - `self-hosted/compiler/k2_check_import_knowledge_context_semantic_probe.sio`
    proves the standalone imported semantic wrapper can parse, allocate,
    minimally collect ontology/struct/fn facts in-place, accept positive and
    multi-constraint positive witnesses, and reject the multi-constraint
    negative witness;
  - A Stage1-harness experiment made `checker_error_count_scalar` public,
    imported it explicitly from `check::mod`, and called it immediately after
    `checker_collect_items_mut`. The positive witness segfaulted before a
    verdict, so even post-collect scalar accessors are unsafe with the current
    explicit-import lowering path. The executable patch was reverted; this is
    blocker evidence, not a retained implementation.
- Current checked-in `bin/souc` parses the positive proof-context witness far
  enough to reach the existing Knowledge confidence diagnostic; it does not yet
  contain the new checker-source proof bridge.
- Unit/dimension support is no longer future-only: the Phase 1 gate proves
  registered unit declarations and builtin dimension vectors reject
  incompatible addition, call arguments, and casts while accepting compatible
  unit arithmetic/casts and same-unit division cancellation to a dimensionless
  scalar. The derived current-source gate additionally proves a one-step
  `unit velocity = m / s` declaration, a chained
  `unit acceleration = m / s / s` declaration, named quotient recovery, and
  rejection of plain length at a velocity boundary plus velocity at an
  acceleration boundary. The modular parser source now has a focused AST gate
  for chained-unit payload retention, and the modular checker source now has a
  focused `check::check` semantic gate for the same dimensional claim. The
  Knowledge unit gate now proves parsed `<unit>` proof-context constraints are
  checked for static dimensional compatibility against struct field units.
  The Knowledge numeric gate now proves comparison constraints are statically
  rejected when the field or value is nonnumeric, and proves static threshold
  satisfaction when the field type carries a strong enough refinement.
  The Knowledge composite gate now proves these static ontology, unit, and
  numeric-refinement constraints compose in one proof context. Generated/dynamic
  Knowledge value checking, checked-in binary refresh, and ontology-linked unit
  metadata remain future work.
- Runtime check generation for dynamic `Knowledge<T>` fields is still future
  work.
- `make test-knowledge-context-static` is now the static umbrella acceptance
  surface. A future dynamic/runtime umbrella remains future work.
- The next likely repair surface is the imported-call lowering/resolution path,
  not the `Knowledge<T>` parser or ontology rule. Current source pointers:
  `self-hosted/compiler/lean_single.sio` contains the imported non-pub call
  visibility check and call emission path around the `callee_is_import` /
  `callee_is_pub` logic, plus the textual `use` resolver in `resolve_imports`;
  `self-hosted/compiler/module_frontend.sio` owns the Stage1 modular preflight
  entrypoint used by the green K2 gate.
- `make test-k2-check-import-bridge` is now a focused classifier for the
  remaining import bridge blocker:
  - `self-hosted/compiler/k2_heap_alloc_builtin_probe.sio` proves the compiler
    builtin `heap_alloc` path can allocate and return a nonzero pointer in a
    small driver;
  - `self-hosted/compiler/k2_heap_alloc_small_probe.sio` proves standalone
    small-driver `mem::box::heap_alloc(8)`, which goes through the `calloc`
    wrapper, currently fails before `alloc_ok`;
  - `self-hosted/compiler/k2_heap_alloc_probe.sio` proves standalone
    small-driver `mem::box::heap_alloc(262144)` currently fails before
    `alloc_ok`;
  - `self-hosted/compiler/k2_check_import_leaf_probe.sio` proves an explicit
    `check::check` leaf import can compile and execute;
  - `self-hosted/compiler/k2_check_import_pointer_probe.sio` proves a
    `*mut Checker` value can cross an imported `check::check` function boundary
    when the callee does not dereference it;
  - `self-hosted/compiler/k2_check_import_heap_checker_probe.sio` proves
    allocating heap memory typed as `*mut Checker` succeeds when the probe uses
    the compiler builtin `heap_alloc` path instead of `mem::box`;
  - `self-hosted/compiler/k2_check_import_accessor_probe.sio` proves the
    standalone `Checker` init plus imported scalar accessor path succeeds with
    builtin `heap_alloc`;
  - `self-hosted/compiler/k2_check_import_knowledge_context_semantic_probe.sio`
    proves the imported in-place `Knowledge<T>` ontology semantic path accepts
    a positive witness and a multi-constraint positive witness, and rejects a
    multi-constraint negative witness;
  - `self-hosted/compiler/k2_check_import_collect_accessor_probe.sio` proves
    the remaining standalone imported `Checker` bridge failure starts at
    `checker_collect_items_mut`: parse, builtin heap allocation, and
    `checker_init_in_place` succeed, then the process segfaults before
    `collect_ok`.

## Active Blocker

Blocker-ID: `K2-CHECK-FULL-SEMANTIC-BRIDGE`

- severity: S2
- class: `build/bootstrap-path` plus `knowledge-proof-context`
- evidence level: Stage1 generated frontend and focused AST probe
- owner: Codex
- evidence:
  - ordinary parser/AST probe reports `knowledge_constraint_count=1` for
    positive, transitive positive, and disjoint negative witnesses;
  - ordinary parser/AST probe reports `knowledge_constraint_count=2` for
    positive and negative multi-constraint witnesses;
  - Stage1 ordinary frontend accepts the positive witness through the
    `check_items_verdict_boot4` AST bridge;
  - Stage1 ordinary frontend accepts a transitive subclass witness through the
    `check_items_verdict_boot4` AST bridge;
  - Stage1 ordinary frontend rejects the disjoint negative witness through the
    `check_items_verdict_boot4` AST bridge;
  - Stage1 ordinary frontend accepts a return-type positive witness through the
    `check_items_verdict_boot4` AST bridge;
  - Stage1 ordinary frontend rejects a return-type disjoint negative witness
    through the `check_items_verdict_boot4` AST bridge;
  - Stage1 ordinary frontend accepts a two-constraint positive witness and
    rejects a two-constraint negative witness through the
    `check_items_verdict_boot4` AST bridge;
  - the AST bridge is now owned by `check::knowledge_context` and imported by
    `check::mod`, with the previous `check_mod_ast_*` duplicate removed;
  - the previous unreferenced pure AST duplicate in `check::check` was also
    removed, leaving `check::check` focused on semantic checker-table
    validation;
  - the previous unreferenced value-style proof-context methods in
    `check::check` were removed, leaving only the in-place candidate semantic
    wrapper;
  - the scalar `checker_knowledge_context_semantic_verdict_items` wrapper keeps
    allocation, collection, and validation inside `check::check`, but importing
    it from `check::mod` still segfaults on the positive witness;
  - replacing the wrapper AST bridge with an unqualified
    `checker_validate_knowledge_context_items_mut` call compiles and returns,
    but does not execute the current `check::check` body and lets the disjoint
    witness pass;
  - moving the validator call inside
    `checker_check_items_inner_store_return_had_error_mut` did not affect the
    Stage1 verdict path; temporary sentinels in that function did not print,
    proving `check::mod` is not calling the current source body through the
    unqualified symbol;
  - an earlier pure AST duplicate inside `check::check` could reject the
    disjoint witness when explicitly imported, but positive/pass-through
    witnesses segfaulted when the frontend proceeded to normal checker
    collection;
  - importing only `checker_validate_knowledge_context_items_mut` from
    `check::check` compiles, but the rebuilt frontend segfaults after
    `checker_collect_items_mut` and before the validator runs;
  - importing the current
    `checker_check_items_inner_store_return_had_error_mut` wrapper from
    `check::check` also segfaults during Stage1 type checking;
  - importing the scalar `checker_knowledge_context_semantic_verdict_items`
    wrapper from `check::check` also segfaults on the positive witness before
    reaching a verdict, even after the wrapper uses the minimal in-place
    ontology collector and 262144-byte builtin heap allocation;
  - using the same wrapper name unqualified from `check::mod` accepts the
    positive witness but also accepts the disjoint negative witness, confirming
    that it is not executing the current semantic body;
  - `self-hosted/compiler/k2_check_mod_semantic_bridge_probe.sio` now
    classifies the same stale unqualified `check::mod` bridge behavior without
    changing the Stage1 acceptance gate;
  - explicitly importing `checker_error_count_scalar` from `check::check` and
    calling it after `checker_collect_items_mut` inside the Stage1 harness also
    segfaults on the positive witness;
  - `make test-k2-check-import-bridge` proves explicit `check::check` imports
    are not categorically broken: the leaf import executes, and an imported
    `*mut Checker` parameter call executes when the callee does not dereference
    it. It also proves the compiler builtin `heap_alloc` path supports
    standalone `Checker` allocation, init, and imported scalar accessor reads.
    It now also proves the imported in-place `Knowledge<T>` ontology semantic
    path accepts positive and multi-constraint positive witnesses while
    rejecting the multi-constraint negative witness. It also proves a
    `check::mod` diagnostic bridge using a fresh unqualified semantic symbol is
    still stale and lets the negative witness pass, so that route must remain a
    classifier rather than an acceptance path. The imported
    `mem::box`/`calloc` wrapper remains a separate small-driver allocation
    failure, and the remaining standalone full-collector bridge failure begins
    at imported `checker_collect_items_mut` after `init_ok`;
  - direct semantic reads of `Checker` tables in `check::mod` after collection
    segfault on the positive witness;
  - a standalone semantic import probe confirms the imported body can be
    entered and can produce correct ontology verdicts through the in-place
    minimal collector; the full value-style `collect_items` bridge still
    segfaults and remains a classifier, not an acceptance gate.
- acceptance gate:
  - `tests/frontend/knowledge_static_subclass_where_ordinary_probe.sio` passes
    through ordinary Stage1 frontend;
  - `tests/frontend/knowledge_static_transitive_where_ordinary_probe.sio` passes
    through ordinary Stage1 frontend;
  - `tests/frontend/knowledge_static_disjoint_where_ordinary_probe.sio` fails
    through ordinary Stage1 frontend without source preflight;
  - `tests/frontend/knowledge_static_return_subclass_where_ordinary_probe.sio`
    passes through ordinary Stage1 frontend;
  - `tests/frontend/knowledge_static_return_disjoint_where_ordinary_probe.sio`
    fails through ordinary Stage1 frontend without source preflight;
  - `tests/frontend/knowledge_static_multi_constraint_ordinary_probe.sio`
    passes through ordinary Stage1 frontend;
  - `tests/frontend/knowledge_static_multi_constraint_reject_ordinary_probe.sio`
    fails through ordinary Stage1 frontend without source preflight;
  - `make test-knowledge-context-phase2` remains green;
  - `make test-k2-check-import-bridge` remains green and includes the standalone
    imported semantic positive, multi-constraint positive, and negative
    witnesses.
  - `make test-knowledge-unit-constraints` remains green and includes ordinary
    plus standalone semantic acceptance/rejection for static `Knowledge<T>`
    unit constraints, including internal validation-data label dimensional
    checks for `mg_dL`, `mmol_L`, `U_L`, and `mm_h`.
  - `make test-knowledge-numeric-constraints` remains green and includes
    ordinary plus standalone semantic acceptance/rejection for static
    `Knowledge<T>` numeric field/value shape constraints and field-refinement
    threshold implication.
  - `make test-knowledge-composite` remains green and includes ordinary plus
    standalone semantic acceptance/rejection for one proof context combining
    ontology, unit, and numeric-refinement constraints.
  - `make test-knowledge-context-static` remains green and runs all focused
    static `Knowledge<T>` proof-context gates without claiming runtime checks.
- next action: add a safe narrow accessor/import shape for the semantic
  `check::check` validator, or repair module lowering so `check::mod` can call
  the current `check::check` body without explicit-import side effects or direct
  `Checker` field reads.

## Blocker Taxonomy

Use these labels in follow-up reports:

- ontology-kernel/checker
- unit-parser
- unit-normalization
- unit-typechecker
- knowledge-proof-context
- harness-routing
- build/bootstrap-path
- baseline noise
