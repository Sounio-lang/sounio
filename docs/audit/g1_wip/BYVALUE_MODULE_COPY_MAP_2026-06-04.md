<!-- docs:meta
topic_id: repo.docs.audit.g1-wip.byvalue-module-copy-map-2026-06-04
authority: repo_only
audience: users
last_validated: 2026-03-07
validated_by: A2
source_of_truth: docs/governance/topic-registry.v1.json#repo.docs.audit.g1-wip.byvalue-module-copy-map-2026-06-04
-->

# By-value IrModule / *box deref-copy sites — c634b38f [IrFunction;1400] crash class

Map of every large-struct by-value copy / `*box` deref-copy in the modular lowering→native-v2 backend path.
Crossing the source→native-ELF wall for CORRECT return values requires converting the on-path `yes` sites
(copy-modify-rebox → store-through-box-pointer / by-ref) so no `[IrFunction;1400]` (or [;128]/[;1024]/[;256]) inline-array copy runs.
Milestone (wall crossed, stub bodies): branch feat/native-v2-source-bridge @ b4cce3a51.

Total 135 deduped sites. Generated by Workflow wf_8c97dc4b-baf (5 mappers + audit).

## on_codegen_path = yes  (24)

- `self-hosted/compiler/main.sio:2478` **run_native_v2_compile_mode** [deref-copy] _IrModule_
  - detail: `let lowered = *module_box` where module_box: Box<IrModule>. Dereferences the heap-allocated IrModule produced by bridge_lower_single_module_box into a local stack value, then passes `&lowered` to compile_native_v2_preview_to_file. IrModule
  - fix: Pass `module_box` directly (or &*module_box) to a variant of compile_native_v2_preview_to_file that accepts &IrModule, eliminating the stack copy entirely. The `&lowered` already shows &IrModule is the right ABI — just remove the copy step:
- `self-hosted/compiler/module_frontend.sio:2865` **bridge_lower_single_module_box** [deref-copy] _IrProgramSummary_
  - detail: var summary_val = (*summary_result.summary) — dereferences Box<IrProgramSummary> into a stack local. The struct is small (3 Box pointers + bool) but is then passed as &summary_val to the body-lowering call. Risk: aliases the box's interior 
  - fix: Pass &(*summary_result.summary) directly: lower_program_bodies_from_summary_with_epistemic_boxed_ref(&prog_lower, &(*summary_result.summary), epistemic). The callee signature at lower.sio:6426 already accepts &IrProgramSummary.
- `self-hosted/ir/lower.sio:164` **lowerer_new_with_epistemic** [by-value-param] _IrEpistemicSection_
  - detail: `fn lowerer_new_with_epistemic(epistemic: IrEpistemicSection) -> Lowerer` — IrEpistemicSection is an extremely large inline struct (dozens of fixed arrays totalling many KB; defined at ir.sio:1745-1818). Passed by value through the epistemi
  - fix: Change to `fn lowerer_new_with_epistemic(epistemic: &IrEpistemicSection) -> Lowerer` and update callers to pass `&epistemic`. Inside the function replace `(*lo.module).epistemic = epistemic` with `(*lo.module).epistemic = *epistemic` (or co
- `self-hosted/ir/lower.sio:247` **lowerer_seed_module_from_summary** [deref-copy] _IrModule_
  - detail: `var seed_val = (*summary.module)` — deref-copies the Box<IrModule> (full 1400-slot IrFunction array) from the IrProgramSummary. Called at line 312 from lowerer_new_from_program_summary, so it is a second large copy in the same call frame. 
  - fix: Remove `var seed_val = (*summary.module)` and `let seed = &seed_val`. Access the Box fields directly through the reference: replace `seed.fn_count` → `(*summary.module).fn_count`, `seed.prof_counter_count` → `(*summary.module).prof_counter_
- `self-hosted/ir/lower.sio:277` **lowerer_seed_metadata_from_summary** [deref-copy] _EnumVariantTable_
  - detail: `var enum_variants = *lo.enum_variants` — deref-copies the full EnumVariantTable (`[EnumVariantEntry;1024]`) from box into local, then loops to repopulate, then reboxes at line 285.
  - fix: Mutate through the existing box pointer: `(*lo.enum_variants).count = 0; var vi: i64 = 0; while vi < ... { (*lo.enum_variants).entries[vi as usize] = ...; vi = vi + 1 } (*lo.enum_variants).count = vi` — drop the copy-rebox entirely.
- `self-hosted/ir/lower.sio:287` **lowerer_seed_metadata_from_summary** [deref-copy] _StructLayoutTable_
  - detail: `var struct_layouts = *lo.struct_layouts` — deref-copies the full StructLayoutTable (`[StructLayoutEntry;256]`) from box into local, repopulates, reboxes at line 303.
  - fix: Mutate through the pointer: `(*lo.struct_layouts).count = 0; while si < ... { (*lo.struct_layouts).entries[si as usize] = entry; si = si + 1 } (*lo.struct_layouts).count = si` — drop the copy-rebox.
- `self-hosted/ir/lower.sio:307` **lowerer_new_from_program_summary** [by-value-param] _IrEpistemicSection_
  - detail: `fn lowerer_new_from_program_summary(summary: &IrProgramSummary, epistemic: IrEpistemicSection)` — IrEpistemicSection passed by value. In addition to the Box<IrModule> deref-copy at line 309 (listed separately), the large epistemic section 
  - fix: Change `epistemic: IrEpistemicSection` to `epistemic: &IrEpistemicSection` on this function and propagate through callers. Replace `module.epistemic = epistemic` (line 310) with `module.epistemic = *epistemic`.
- `self-hosted/ir/lower.sio:309` **lowerer_new_from_program_summary** [deref-copy] _IrModule_
  - detail: `var module = *lo.module` — deref-copies the Box<IrModule> (1400 inline IrFunctions) onto the stack after lowerer_new() at line 308. Contrast with the fixed analog at lower.sio:172 (lowerer_new_with_epistemic) which uses `(*lo.module).epist
  - fix: Replace the three-statement copy-modify-rebox with direct store-through-pointer: remove `var module = *lo.module`, replace `module.epistemic = epistemic` with `(*lo.module).epistemic = epistemic`, remove `lo.module = Box::new(module)`. Matc
- `self-hosted/ir/lower.sio:1068` **lower_program_to_ir_summary_flat_owned_with_epistemic_ref** [by-value-return] _IrProgramSummaryFlatResult_
  - detail: `fn lower_program_to_ir_summary_flat_owned_with_epistemic_ref(prog: &Program, epistemic: IrEpistemicSection) -> IrProgramSummaryFlatResult` — returns IrProgramSummaryFlatResult which embeds IrProgramSummaryFlat (IrModule + EnumVariantTable 
  - fix: fn lower_program_to_ir_summary_flat_owned_with_epistemic_ref(prog: &Program, epistemic: IrEpistemicSection, out: &! IrProgramSummaryFlatResult) — write fields through out pointer; callers pre-allocate on heap (Box<IrProgramSummaryFlatResult
- `self-hosted/ir/lower.sio:1598` **flush_current_func** [deref-copy] _IrModule_
  - detail: `var module = *lo.module` — deref-copies the full IrModule to write `module.functions[lo.current_fn as usize] = *lo.current_func`, then reboxes at line 1600. Called from `lower_fn_item_ref` and `begin_function_body_lowering`. On the codegen
  - fix: `(*lo.module).functions[lo.current_fn as usize] = *lo.current_func` — store-through-pointer, no IrModule copy.
- `self-hosted/ir/lower.sio:1620` **begin_function_body_lowering** [deref-copy] _LowerLocalStack_
  - detail: `var rst_locals = *lo.locals` — deref-copies the LowerLocalStack (names/regs/depths arrays, count=4096 capacity) just to zero its count field, then reboxes at line 1622.
  - fix: `(*lo.locals).count = 0` — single field write through box pointer, no copy.
- `self-hosted/ir/lower.sio:2516` **fresh_reg** [deref-copy] _IrFunction_
  - detail: `var current_func = *lo.current_func` — deref-copies the full IrFunction (`[IrInstr;128]`) to increment `reg_count` by one, then reboxes at line 2519. Called for every virtual register allocation — fires multiple times per fn body.
  - fix: `let reg = (*lo.current_func).reg_count; (*lo.current_func).reg_count = reg + 1` — single field write through box pointer.
- `self-hosted/ir/lower.sio:2529` **fresh_label** [deref-copy] _IrFunction_
  - detail: `var current_func = *lo.current_func` — deref-copies full IrFunction to increment `label_count` by one, then reboxes at line 2532.
  - fix: `let label = (*lo.current_func).label_count; (*lo.current_func).label_count = label + 1` — single field write through box pointer.
- `self-hosted/ir/lower.sio:2548` **emit** [deref-copy] _IrFunction_
  - detail: `var current_func = *lo.current_func` — deref-copies the full IrFunction (`[IrInstr;128]`) to append one IrInstr and increment `instr_count`, then reboxes at line 2553. Called for every IR instruction emitted — the hottest copy site per fn 
  - fix: `let instr_count = (*lo.current_func).instr_count; if instr_count < IR_MAX_INSTRS { (*lo.current_func).instrs[instr_count as usize] = instr; (*lo.current_func).instr_count = instr_count + 1 }` — all stores through box pointer.
- `self-hosted/ir/lower.sio:2714` **register_enum_variants_ref** [deref-copy] _EnumVariantTable_
  - detail: `var table = *lo.enum_variants` — deref-copies the full EnumVariantTable (`[EnumVariantEntry;1024]`) to append variant entries, then reboxes at line 2732.
  - fix: Append directly through the box pointer: `if (*lo.enum_variants).count < 1024 { (*lo.enum_variants).entries[(*lo.enum_variants).count as usize] = EnumVariantEntry{...}; (*lo.enum_variants).count = (*lo.enum_variants).count + 1 }` — drop cop
- `self-hosted/ir/lower.sio:3086` **preseed_fn_signature_ref** [deref-copy] _IrFunction_
  - detail: `var func = (*lo.module).functions[fn_id as usize]` — copies a single IrFunction by value (instrs: [IrInstr; 128], param_regs: [i64; 64], plus scalars; ~4 KB). Called for every function during the preseed phase (preseed_program_items_ref → 
  - fix: Eliminate the copy: access fields through pointer directly. Replace `var func = (*lo.module).functions[fn_id as usize]` with no binding, and replace each `func.X = Y` with `(*lo.module).functions[fn_id as usize].X = Y`. Alternatively declar
- `self-hosted/ir/lower.sio:3443` **lower_fn_params_ref** [deref-copy] _IrFunction_
  - detail: `var current_func = *lo.current_func` (also `var locals = *lo.locals` at line 3444) — deref-copies full IrFunction and LowerLocalStack at fn-params lowering start; both are accumulated in the loop and reboxed at lines 3489-3490.
  - fix: Accumulate writes directly through the box pointers: `(*lo.current_func).param_regs[...] = preg; (*lo.current_func).param_count = ...; (*lo.locals).names[...] = ...; (*lo.locals).regs[...] = ...; (*lo.locals).depths[...] = ...; (*lo.locals)
- `self-hosted/ir/lower.sio:3835` **lower_fn_item_ref** [deref-copy] _IrFunction_
  - detail: let preserved = lo.module.functions[fn_id as usize] — copies one IrFunction out of the inline [IrFunction;1400] array to preserve .name/.compile_strategy/.prof_counter_id before resetting current_func; called on every function body lowering
  - fix: Replace the full-function copy with three scalar reads: current_func.name = lo.module.functions[fn_id as usize].name; current_func.compile_strategy = lo.module.functions[fn_id as usize].compile_strategy; current_func.prof_counter_id = lo.mo
- `self-hosted/ir/lower.sio:3843` **lower_fn_item_ref** [deref-copy] _LowerLocalStack_
  - detail: `var rst_locals3 = *lo.locals` — deref-copies LowerLocalStack to zero its count, then reboxes at line 3845. Called via `lower_program_bodies_ref` → `lower_fn_item_ref`; fires for every fn body on the named codegen path.
  - fix: `(*lo.locals).count = 0` — single field write through box pointer.
- `self-hosted/ir/lower.sio:6108` **ir_patch_validated_calls** [by-value-param] _IrFunction_
  - detail: `var func = module.functions[i as usize]` — copies each IrFunction (`[IrInstr;128]`) from the module array to call `ir_patch_validated_calls_in_function(..., &! func, ...)` and writes back at line 6110. Also, `ir_function_has_opcode(*func, 
  - fix: Change `ir_patch_validated_calls_in_function` to take `func: &! IrFunction` (already does, via `&! func`); also change `ir_function_has_opcode` to take `func: &IrFunction` and read through the reference — then drop the `var func` copy and p
- `self-hosted/ir/lower.sio:6144` **lower_program_to_ir_staged_with_lowerer_trace_ref** [struct-literal-copy] _IrModule_
  - detail: `module: *boxed.module` in the `IrLowerResult { ... }` struct literal — deref-copies Box<IrModule> into the IrLowerResult.module field (IrLowerResult has module: IrModule inline per ir.sio:1938). IrLowerResult is itself returned by value (S
  - fix: Return IrLowerBoxResult instead (which carries `module: Box<IrModule>`), rename lower_program_to_ir_staged_with_lowerer_trace_ref to match, and update IrLowerResult-consuming callers to deref the box themselves. lower_program_to_ir_staged_w
- `self-hosted/ir/lower.sio:6359` **lower_program_function_body_from_summary_flat_with_epistemic_ref** [by-value-return] _IrFunctionBodyResult_
  - detail: `fn lower_program_function_body_from_summary_flat_with_epistemic_ref(prog: &Program, summary: &IrProgramSummaryFlat, epistemic: IrEpistemicSection, fn_name: Name) -> IrFunctionBodyResult` — returns IrFunctionBodyResult with inline IrFunctio
  - fix: fn lower_program_function_body_from_summary_flat_with_epistemic_ref(..., out: &! IrFunctionBodyResult) — write through pointer. Callers in probe_native_streaming (main.sio:7368) pre-allocate one `var body_result: IrFunctionBodyResult`. Or c
- `self-hosted/native/codegen_x86_linux.sio:4074` **find_main_index** [deref-copy] _IrFunction_
  - detail: `name_is_main(module.functions[i as usize].name)` where `module: &IrModule`. Indexing the array through a reference likely materialises the full IrFunction element (~33 KB) before projecting the `.name` field. Called from compile_native_v2_
  - fix: Add `fn name_is_main_ref(n: &Name) -> bool` (or make the Name comparison accept a reference) and use `name_is_main_ref(&module.functions[i as usize].name)` — or rewrite find_main_index to accept an index-into-ref helper that avoids the elem
- `self-hosted/native/codegen_x86_linux.sio:7551` **compile_native_v2_preview_to_file** [deref-copy] _IrFunction_
  - detail: `let fn_body = (*module).functions[fi as usize]` — array-element copy of IrFunction from `module: &IrModule` into a local stack binding. IrFunction holds `[IrInstr;128]` plus a Name (128-byte buf) and seven i64 scalars, roughly 33 KB per el
  - fix: Drop the `fn_body` binding and pass the reference directly: `compile_ir_function_v2_from_ir_into(&! nc, &(*module).functions[fi as usize], fi)`. Requires the language to form a reference into an array element of a dereferenced pointer; if t

## on_codegen_path = maybe  (44)

- `self-hosted/compiler/module_frontend.sio:2887` **load_multimodule_lower_program_traced** [by-value-return] _IrModule_
  - detail: -> IrModule — function returns the lowered module by value; the frame holds a large IrModule local (lower_module at 2919).
  - fix: Add out: &! IrModule param; replace `let lower_module = *body_result.module; lower_module` with `(*out) = *body_result.module`; return void. Callers pre-allocate their IrModule local and pass &!.
- `self-hosted/compiler/module_frontend.sio:2901` **load_multimodule_lower_program_traced** [deref-copy] _IrProgramSummary_
  - detail: var summary_val = (*summary_result.summary) — dereferences Box<IrProgramSummary> into a by-value local. IrProgramSummary is small (3 Box pointers + bool) so not a size hazard, but the copy aliases the boxes and lands a new struct on the sta
  - fix: Pass &(*summary_result.summary) directly to lower_program_bodies_from_summary_with_epistemic_boxed_ref if the callee already takes &IrProgramSummary (it does at line 6426 in lower.sio).
- `self-hosted/compiler/module_frontend.sio:2919` **load_multimodule_lower_program_traced** [deref-copy] _IrModule_
  - detail: let lower_module = *body_result.module — dereferences Box<IrModule> into a full by-value IrModule local, then returns it (SRET). This is the exit-path large copy.
  - fix: With out: &! IrModule param: replace with `(*out) = *body_result.module` and change return type to void.
- `self-hosted/compiler/module_frontend.sio:3599` **load_multimodule_ir_traced_into** [by-value-return] _IrModule_
  - detail: var merged_imported = module_frontend_lower_imported_source_recursive(...) — receives IrModule by SRET into a stack local even in the _into variant.
  - fix: var merged_imported = ir_empty_module(); module_frontend_lower_imported_source_recursive(main_path, 0, trace, &! merged_imported) after callee signature change.
- `self-hosted/compiler/module_frontend.sio:3616` **load_multimodule_ir_traced_into** [struct-literal-copy] _IrModule_
  - detail: (*out).module = merged_imported — stores the IrModule local into the out field by value copy (one more IrModule-sized memcpy).
  - fix: If module_frontend_lower_imported_source_recursive takes &! IrModule, write directly into (*out).module: pass &! (*out).module to the callee.
- `self-hosted/compiler/module_frontend.sio:3651` **load_multimodule_ir_traced_into** [by-value-return] _IrModule_
  - detail: var single_lower_result_no_use = load_multimodule_lower_program_traced(...) — receives full IrModule by SRET into local (no-imports single-module branch).
  - fix: var single_lower_result_no_use = ir_empty_module(); load_multimodule_lower_program_traced(..., &! single_lower_result_no_use) after callee signature change.
- `self-hosted/compiler/module_frontend.sio:3672` **load_multimodule_ir_traced_into** [struct-literal-copy] _IrModule_
  - detail: (*out).module = single_lower_result_no_use — stores the full IrModule local into out.module by value.
  - fix: With &! out param threaded to load_multimodule_lower_program_traced, write directly into (*out).module.
- `self-hosted/compiler/module_frontend.sio:3693` **load_multimodule_ir_traced_into** [by-value-return] _IrModule_
  - detail: var single_lower_result = load_multimodule_lower_program_traced(...) — second single-module path (has imports but count==0), receives IrModule by SRET.
  - fix: Same as 3651: thread &! into load_multimodule_lower_program_traced.
- `self-hosted/compiler/module_frontend.sio:3714` **load_multimodule_ir_traced_into** [struct-literal-copy] _IrModule_
  - detail: (*out).module = single_lower_result — stores full IrModule into out.module by value.
  - fix: Thread &! (*out).module through load_multimodule_lower_program_traced.
- `self-hosted/compiler/module_frontend.sio:3782` **load_multimodule_ir_traced_into** [other] _IrModule_
  - detail: var merged_module = ir_empty_module() — large IrModule zero-initialised on stack as the merge accumulator (multi-module path).
  - fix: Use (*out).module as the accumulator directly, initialise it once: (*out).module = ir_empty_module(); then thread &! (*out).module into merge calls.
- `self-hosted/compiler/module_frontend.sio:3784` **load_multimodule_ir_traced_into** [deref-copy] _IrModule_
  - detail: Some(module_box) => { merged_module = *module_box } — deref-copies Box<IrModule> seed into the large local.
  - fix: (*out).module = *module_box and skip the merged_module local.
- `self-hosted/compiler/module_frontend.sio:3809` **load_multimodule_ir_traced_into** [other] _IrModule_
  - detail: var lowered_module = ir_empty_module() — per-iteration large IrModule local for each loaded module.
  - fix: Eliminate; with ir_merge_modules(&! dst, &src) pass &(*module_box) as src directly.
- `self-hosted/compiler/module_frontend.sio:3811` **load_multimodule_ir_traced_into** [deref-copy] _IrModule_
  - detail: Some(module_box) => { lowered_module = *module_box } — deref-copies Box<IrModule> per iteration.
  - fix: Pass &(*module_box) directly to ir_merge_modules after its &! / & signature change.
- `self-hosted/compiler/module_frontend.sio:3822` **load_multimodule_ir_traced_into** [by-value-param] _IrModule_
  - detail: merged_module = ir_merge_modules(merged_module, lowered_module) — passes two IrModule locals by value and receives result by value (3 full copies per iteration).
  - fix: ir_merge_modules(&! merged_module, &lowered_module) after signature change.
- `self-hosted/compiler/module_frontend.sio:3844` **load_multimodule_ir_traced_into** [struct-literal-copy] _IrModule_
  - detail: (*out).module = merged_module — final store copies the full IrModule accumulator into the output struct field.
  - fix: Use (*out).module as the accumulator throughout; this store disappears.
- `self-hosted/compiler/module_loader.sio:795` **ir_merge_modules** [by-value-param] _IrModule_
  - detail: `fn ir_merge_modules(dst: IrModule, src: IrModule) -> IrModule` — both dst and src are passed by value (each a full 1400-function inline array), and the return is also by value. Called at module_loader.sio:1249 in load_multimodule_ir_traced
  - fix: Change to `fn ir_merge_modules(dst: &! IrModule, src: &IrModule)` (in-place merge into dst, read-only src). Replace `var merged = dst` with no copy — write directly through dst pointer. Access `src.fn_count` etc. through the reference. This
- `self-hosted/compiler/module_loader.sio:814` **ir_merge_modules** [deref-copy] _IrFunction_
  - detail: var func = src.functions[fi] — same per-element IrFunction copy pattern as module_frontend.sio:881; both files define ir_merge_modules and both were partially covered at the signature level (line 795 in module_loader.sio is in the map) but 
  - fix: Work through src.functions[fi] via reference or index; only the .compile_strategy field and instruction operands need to be accessed — no need to copy the full IrFunction including its [IrInstr;128] instruction array
- `self-hosted/ir/lower.sio:125` **ir_register_knowledge_layout** [by-value-param] _StructLayoutTable_
  - detail: `fn ir_register_knowledge_layout(table: StructLayoutTable) -> StructLayoutTable` — takes and returns StructLayoutTable ([StructLayoutEntry;256], each with [StructFieldEntry;64]) by value. Body does `var t = table` then mutates and returns t
  - fix: fn ir_register_knowledge_layout(table: &! StructLayoutTable) — store the new entry directly through the pointer; caller passes `&! lo.struct_layouts` or `&! *lo.struct_layouts`. Eliminates both the incoming copy and the SRET return copy.
- `self-hosted/ir/lower.sio:923` **ir_fast_summary_register_enum_variants_owned** [by-value-param] _EnumVariantTable_
  - detail: `fn ir_fast_summary_register_enum_variants_owned(table: EnumVariantTable, enum_name: Name, variants: Option<Box<VariantDefList>>, next_disc: i64) -> EnumVariantTable` — EnumVariantTable ([EnumVariantEntry;1024]) passed and returned by value
  - fix: fn ir_fast_summary_register_enum_variants_owned(table: &! EnumVariantTable, enum_name: Name, variants: Option<Box<VariantDefList>>, next_disc: i64) — mutate table through pointer, drop return. The caller at line 1134 of lower_program_to_ir_
- `self-hosted/ir/lower.sio:947` **ir_fast_summary_register_struct_fields_owned** [by-value-param] _StructLayoutTable_
  - detail: `fn ir_fast_summary_register_struct_fields_owned(table: StructLayoutTable, type_name: Name, fields: Option<Box<FieldDefList>>, idx: i64) -> StructLayoutTable` — StructLayoutTable ([StructLayoutEntry;256], each with [StructFieldEntry;64]) pa
  - fix: fn ir_fast_summary_register_struct_fields_owned(table: &! StructLayoutTable, type_name: Name, fields: Option<Box<FieldDefList>>, idx: i64) — mutate through pointer, drop return. The caller at line 1127 passes &! struct_layouts.
- `self-hosted/ir/lower.sio:3868` **lower_fn_item_ref** [deref-copy] _IrModule_
  - detail: `var module = *lo.module` inside `if strategy == IR_STRATEGY_INSTRUMENTED` branch — deref-copies full IrModule to push a prof counter entry, then reboxes at line 3878. On the named bodies path but guarded by instrumented strategy — NOT reac
  - fix: `let ctr_idx = (*lo.module).prof_counter_count; if ctr_idx < 64 { (*lo.module).prof_counters[ctr_idx as usize] = IrProfCounterInfo{...}; (*lo.module).prof_counter_count = ctr_idx + 1; (*lo.current_func).prof_counter_id = ctr_idx }` — store-
- `self-hosted/ir/lower.sio:6137` **lower_program_to_ir_staged_with_lowerer_trace_ref** [by-value-return] _IrLowerResult_
  - detail: `fn lower_program_to_ir_staged_with_lowerer_trace_ref(lo: Lowerer, prog: &Program, patch_stats: &! IrValidatedPatchStats) -> IrLowerResult` — IrLowerResult embeds IrModule inline (ir.sio:1937). Body deboxes: `module: *boxed.module` at line 
  - fix: fn lower_program_to_ir_staged_with_lowerer_trace_ref(lo: Lowerer, prog: &Program, patch_stats: &! IrValidatedPatchStats) -> IrLowerBoxResult — already has the boxed variant lower_program_to_ir_staged_with_lowerer_box_trace_ref (6116); calle
- `self-hosted/ir/lower.sio:6151` **lower_program_to_ir_staged_with_lowerer_trace** [by-value-param] _IrLowerResult_
  - detail: `fn lower_program_to_ir_staged_with_lowerer_trace(lo: Lowerer, prog: Program, patch_stats: &! IrValidatedPatchStats) -> IrLowerResult` — by-value `prog: Program` param (not the large struct, Program is a linked list) and IrLowerResult by-va
  - fix: Switch return to IrLowerBoxResult via the existing box variant (6116). Signature: `fn lower_program_to_ir_staged_with_lowerer_trace(lo: Lowerer, prog: Program, patch_stats: &! IrValidatedPatchStats) -> IrLowerBoxResult`.
- `self-hosted/ir/lower.sio:6159` **lower_program_to_ir_trace_ref** [by-value-return] _IrLowerResult_
  - detail: `fn lower_program_to_ir_trace_ref(prog: &Program, patch_stats: &! IrValidatedPatchStats) -> IrLowerResult` — thin wrapper that calls the staged variant; the IrLowerResult with inline IrModule propagates upward through the call chain.
  - fix: fn lower_program_to_ir_trace_ref(prog: &Program, patch_stats: &! IrValidatedPatchStats) -> IrLowerBoxResult — delegate to lower_program_to_ir_staged_with_lowerer_box_trace_ref. Update callers (lower_program_to_ir_ref at 6174, lower_program_
- `self-hosted/ir/lower.sio:6167` **lower_program_to_ir_trace** [by-value-return] _IrLowerResult_
  - detail: `pub fn lower_program_to_ir_trace(prog: Program, patch_stats: &! IrValidatedPatchStats) -> IrLowerResult` — public API returning inline-IrModule IrLowerResult.
  - fix: pub fn lower_program_to_ir_trace(prog: Program, patch_stats: &! IrValidatedPatchStats) -> IrLowerBoxResult — delegates to boxed variant. All callers must be updated to access .module via Box dereference.
- `self-hosted/ir/lower.sio:6174` **lower_program_to_ir_ref** [by-value-return] _IrLowerResult_
  - detail: `fn lower_program_to_ir_ref(prog: &Program) -> IrLowerResult` — convenience wrapper; IrLowerResult inline IrModule returned. Called by lower_program_to_ir (6179).
  - fix: fn lower_program_to_ir_ref(prog: &Program) -> IrLowerBoxResult — use boxed trace variant. Or eliminate and have callers use lower_program_to_ir_staged_with_lowerer_box_trace_ref directly.
- `self-hosted/ir/lower.sio:6179` **lower_program_to_ir** [by-value-return] _IrLowerResult_
  - detail: `pub fn lower_program_to_ir(prog: Program) -> IrLowerResult` — public API; called from self-hosted/main.sio:564,595,638,700,726,780,832,977,1989 for --check/--ir-dump/--run modes. Each call materializes a full IrModule (containing [IrFuncti
  - fix: pub fn lower_program_to_ir(prog: Program) -> IrLowerBoxResult — return the boxed variant. Self-hosted/main.sio callers access `(*result.module).fn_count` etc. through the box. An IrLowerBoxResult variant already exists (struct at line 117).
- `self-hosted/ir/lower.sio:6255` **lower_program_to_ir_summary_flat_with_epistemic_ref** [by-value-return] _IrProgramSummaryFlatResult_
  - detail: `fn lower_program_to_ir_summary_flat_with_epistemic_ref(prog: &Program, epistemic: IrEpistemicSection) -> IrProgramSummaryFlatResult` — returns IrProgramSummaryFlatResult (embeds IrProgramSummaryFlat: IrModule + EnumVariantTable + StructLay
  - fix: fn lower_program_to_ir_summary_flat_with_epistemic_ref(prog: &Program, epistemic: IrEpistemicSection, out: &! IrProgramSummaryFlatResult) — write to caller-allocated output. Or add a boxed-flat variant analogous to IrProgramSummaryBoxResult
- `self-hosted/ir/lower.sio:6261` **lower_program_to_ir_summary_flat_with_epistemic_ref** [struct-literal-copy] _IrProgramSummaryFlat_
  - detail: `module: *lo2.module` in `IrProgramSummaryFlatResult { summary: IrProgramSummaryFlat { module: *lo2.module, ... } }` — deref-copies Box<IrModule> into IrProgramSummaryFlat.module which is `module: IrModule` inline (lower.sio:86). This mater
  - fix: Change IrProgramSummaryFlat.module to `module: Box<IrModule>` and update lowerer_new_from_program_summary_flat to use `&summary.module` / `(*summary.module).fn_count` etc. Or use the boxed IrProgramSummary path (IrProgramSummaryBoxResult) i
- `self-hosted/ir/lower.sio:6283` **lower_program_to_ir_probe_ref** [by-value-return] _IrLowerResult_
  - detail: `fn lower_program_to_ir_probe_ref(prog: &Program) -> IrLowerResult` — probe variant; returns IrLowerResult with inline IrModule (deboxed at line 6291: `module: *lo2.module`). Called from lower_program_to_ir_probe (6298).
  - fix: fn lower_program_to_ir_probe_ref(prog: &Program) -> IrLowerBoxResult — return lo2.module (already a Box<IrModule>) directly in an IrLowerBoxResult. Callers use `*result.module` to access the module.
- `self-hosted/ir/lower.sio:6302` **lower_program_to_ir_probe_summary_ref** [by-value-return] _IrLowerResult_
  - detail: `fn lower_program_to_ir_probe_summary_ref(prog: &Program) -> IrLowerResult` — returns IrLowerResult with inline IrModule deboxed at line 6308: `module: *lo2.module`.
  - fix: fn lower_program_to_ir_probe_summary_ref(prog: &Program) -> IrLowerBoxResult — return lo2.module directly.
- `self-hosted/ir/lower.sio:6327` **lower_program_function_body_from_summary_with_epistemic_ref** [by-value-return] _IrFunctionBodyResult_
  - detail: `fn lower_program_function_body_from_summary_with_epistemic_ref(prog: &Program, summary: &IrProgramSummary, epistemic: IrEpistemicSection, fn_name: Name) -> IrFunctionBodyResult` — IrFunctionBodyResult embeds IrFunction ([IrInstr;128] + [i6
  - fix: fn lower_program_function_body_from_summary_with_epistemic_ref(..., out: &! IrFunctionBodyResult) — write func through pointer. Or return IrFunction index + &mut module and let caller access. Minimal: `fn ... -> IrFunctionBodyResult` with t
- `self-hosted/ir/lower.sio:6344` **lower_program_function_body_from_summary_with_epistemic_ref** [deref-copy] _IrFunction_
  - detail: func = lo2.module.functions[i as usize] — inside the search loop that finds the target function by name; copies the full IrFunction to a local 'func' var that is then placed in the IrFunctionBodyResult return value; the function-start (line
  - fix: Replace the copy-and-search with a pointer/index pattern: find the index first (while loop checking .name), then do a single copy at the found index — or store an index and defer the copy until after the loop. Better: take &lo2.module.funct
- `self-hosted/ir/lower.sio:6376` **lower_program_function_body_from_summary_flat_with_epistemic_ref** [deref-copy] _IrFunction_
  - detail: func = lo2.module.functions[i as usize] — same loop pattern as 6344; copies full IrFunction to extract the matching function body; the enclosing function (line 6359) is marked on_path yes in the original map but this specific array-element 
  - fix: Same as line 6344: find the index in a name-check loop without copying; then do a single necessary copy outside the loop, or populate IrFunctionBodyResult.func field-by-field from a reference to the array element
- `self-hosted/ir/lower.sio:6391` **lower_program_function_body_from_program_with_epistemic_ref** [by-value-return] _IrFunctionBodyResult_
  - detail: `fn lower_program_function_body_from_program_with_epistemic_ref(prog: &Program, epistemic: IrEpistemicSection, fn_name: Name) -> IrFunctionBodyResult` — also calls lower_program_to_ir_summary_flat_owned_with_epistemic_ref (line 6396), mater
  - fix: fn lower_program_function_body_from_program_with_epistemic_ref(..., out: &! IrFunctionBodyResult) — write through pointer, and change the internal summary_result local to use a boxed/ref variant of flat_owned to avoid a double large-struct 
- `self-hosted/ir/lower.sio:6409` **lower_program_function_body_from_program_with_epistemic_ref** [deref-copy] _IrFunction_
  - detail: func = lo2.module.functions[i as usize] — same by-name search loop as 6344/6376; copies full IrFunction; enclosing function (line 6391) is in the original map as maybe but the specific copy line was missed
  - fix: Same pattern fix as lines 6344/6376
- `self-hosted/ir/lower.sio:6462` **lower_program_function_body_from_summary_with_epistemic** [by-value-param] _IrFunctionBodyResult_
  - detail: `pub fn lower_program_function_body_from_summary_with_epistemic(prog: Program, summary: IrProgramSummary, epistemic: IrEpistemicSection, fn_name: Name) -> IrFunctionBodyResult` — takes `summary: IrProgramSummary` by value (IrProgramSummary 
  - fix: pub fn lower_program_function_body_from_summary_with_epistemic(prog: Program, summary: IrProgramSummary, epistemic: IrEpistemicSection, fn_name: Name, out: &! IrFunctionBodyResult) — pass out pointer. Note: IrProgramSummary uses Box<IrModul
- `self-hosted/ir/lower.sio:6471` **lower_program_function_body_from_summary_ref** [by-value-return] _IrFunctionBodyResult_
  - detail: `fn lower_program_function_body_from_summary_ref(prog: &Program, summary: &IrProgramSummary, fn_name: Name) -> IrFunctionBodyResult` — thin wrapper; IrFunctionBodyResult with inline IrFunction returned.
  - fix: fn lower_program_function_body_from_summary_ref(prog: &Program, summary: &IrProgramSummary, fn_name: Name, out: &! IrFunctionBodyResult) — write through pointer, or change IrFunctionBodyResult.func to Box<IrFunction>.
- `self-hosted/ir/lower.sio:6479` **lower_program_function_body_from_summary** [by-value-return] _IrFunctionBodyResult_
  - detail: `fn lower_program_function_body_from_summary(prog: Program, summary: IrProgramSummary, fn_name: Name) -> IrFunctionBodyResult` — by-value Program (linked list, small) and IrProgramSummary (Box<IrModule>), returns IrFunctionBodyResult with i
  - fix: fn lower_program_function_body_from_summary(prog: Program, summary: IrProgramSummary, fn_name: Name, out: &! IrFunctionBodyResult) — write through pointer. IrProgramSummary is already boxed so only IrFunctionBodyResult needs the out-param f
- `self-hosted/ir/lower.sio:6487` **lower_program_to_ir_probe_callsite_ref** [by-value-return] _IrLowerResult_
  - detail: `fn lower_program_to_ir_probe_callsite_ref(prog: &Program, caller_name: Name) -> IrLowerResult` — returns IrLowerResult with inline IrModule deboxed at line 6496: `module: *lo2.module`.
  - fix: fn lower_program_to_ir_probe_callsite_ref(prog: &Program, caller_name: Name) -> IrLowerBoxResult — return lo2.module directly (already Box<IrModule>). Callers updated to access `*result.module`.
- `self-hosted/ir/lower.sio:6609` **lower_program_to_ir_with_epistemic_ref** [by-value-return] _IrLowerResult_
  - detail: `fn lower_program_to_ir_with_epistemic_ref(prog: &Program, epistemic: IrEpistemicSection) -> IrLowerResult` — thin wrapper; inline IrModule in return value.
  - fix: fn lower_program_to_ir_with_epistemic_ref(prog: &Program, epistemic: IrEpistemicSection) -> IrLowerBoxResult — use lower_program_to_ir_with_epistemic_boxed_trace_ref (already exists at line 6516) as the canonical path.
- `self-hosted/ir/lower.sio:6614` **lower_program_to_ir_with_epistemic** [by-value-return] _IrLowerResult_
  - detail: `fn lower_program_to_ir_with_epistemic(prog: Program, epistemic: IrEpistemicSection) -> IrLowerResult` — public wrapper; inline IrModule in IrLowerResult return.
  - fix: fn lower_program_to_ir_with_epistemic(prog: Program, epistemic: IrEpistemicSection) -> IrLowerBoxResult — delegate to lower_program_to_ir_with_epistemic_boxed_trace_ref. The boxed variant exists at line 6516 and is already pub.
- `self-hosted/native/machine_ir.sio:587` **native_v2_lower_function_to_machine** [by-value-param] _IrFunction_
  - detail: Signature `pub fn native_v2_lower_function_to_machine(func: IrFunction) -> MachineFunction`. Takes the full IrFunction by value. Called from native_v2_build_machine_module (machine_ir.sio:1564) on each iteration, receiving `module.functions
  - fix: Change to `pub fn native_v2_lower_function_to_machine(func: &IrFunction) -> MachineFunction` and update all internal accesses to use `(*func).field` or auto-deref `func.field`.
- `self-hosted/native/machine_ir.sio:1564` **native_v2_build_machine_module** [deref-copy] _IrFunction_
  - detail: `let lowered = native_v2_lower_function_to_machine(module.functions[i as usize])` — copies the i-th IrFunction from `module: &IrModule` by value to pass to native_v2_lower_function_to_machine's by-value param. Happens 1400 times worst-case.
  - fix: After native_v2_lower_function_to_machine is converted to take `func: &IrFunction`, write `let lowered = native_v2_lower_function_to_machine(&module.functions[i as usize])` — no copy.

## on_codegen_path = no  (67)

- `self-hosted/compiler/module_frontend.sio:862` **ir_merge_modules** [by-value-param] _IrModule_
  - detail: fn ir_merge_modules(dst: IrModule, src: IrModule) -> IrModule — both dst and src are passed by value, materialising full IrModule copies (~tens of MB each) on the callee frame.
  - fix: fn ir_merge_modules(dst: &! IrModule, src: &IrModule) — mutate dst in place through pointer, remove return; callers replace `dst = ir_merge_modules(dst, src)` with `ir_merge_modules(&! dst, &src)`.
- `self-hosted/compiler/module_frontend.sio:864` **ir_merge_modules** [by-value-param] _IrModule_
  - detail: var merged = dst — local copy of the already-by-value dst param; second full IrModule copy within the function.
  - fix: With the &! dst rewrite above this local disappears; mutations go through (*dst).fieldX = ... directly.
- `self-hosted/compiler/module_frontend.sio:881` **ir_merge_modules** [deref-copy] _IrFunction_
  - detail: var func = src.functions[fi] — copies one IrFunction out of the src IrModule's inline array into a local variable at the start of each loop iteration; the function signature (line 862) is already in the map as by-value-param but this per-el
  - fix: Work on src.functions[fi] in-place via index rather than copying to a local: mutate func.instrs[ii].fn_id directly on a &IrFunction projection, or copy only the fields that are modified (compile_strategy, fn_id adjustments in instrs) rather
- `self-hosted/compiler/module_frontend.sio:925` **ir_merge_modules** [by-value-return] _IrModule_
  - detail: merged — the local is returned by value, triggering SRET for the full IrModule.
  - fix: Eliminated by the &! dst in-place rewrite; function becomes void, no return needed.
- `self-hosted/compiler/module_frontend.sio:2513` **module_frontend_lower_source_file_summary** [by-value-return] _IrModule_
  - detail: -> IrModule — returns a fully built IrModule by value. Called by module_frontend_lower_imported_source_recursive which also returns IrModule by value.
  - fix: Change to fn ...(out: &! IrModule, ...) and write into (*out) in place; callers pass &! their local.
- `self-hosted/compiler/module_frontend.sio:2581` **module_frontend_lower_imported_source_recursive** [by-value-return] _IrModule_
  - detail: -> IrModule — the function returns the merged IrModule by value across recursive calls; each recursion level copies the accumulator.
  - fix: Change to fn ...(out: &! IrModule, ...) writing merged result in-place; callers init a local via ir_empty_module() then pass &!.
- `self-hosted/compiler/module_frontend.sio:2590` **module_frontend_lower_imported_source_recursive** [by-value-return] _IrModule_
  - detail: var merged = module_frontend_lower_source_file_summary(...) — receives a full IrModule by value from callee SRET.
  - fix: With in-place rewrite: var merged = ir_empty_module(); module_frontend_lower_source_file_summary(path, depth, trace, &! merged).
- `self-hosted/compiler/module_frontend.sio:2598` **module_frontend_lower_imported_source_recursive** [by-value-return] _IrModule_
  - detail: let dep = module_frontend_lower_imported_source_recursive(...) — recursive call returns a full IrModule by value into a local.
  - fix: var dep = ir_empty_module(); module_frontend_lower_imported_source_recursive(imports.paths[i], depth+1, trace, &! dep); then pass &dep to ir_merge_modules.
- `self-hosted/compiler/module_frontend.sio:2600` **module_frontend_lower_imported_source_recursive** [by-value-param] _IrModule_
  - detail: merged = ir_merge_modules(merged, dep) — passes both merged and dep as by-value IrModule params and receives result by value.
  - fix: ir_merge_modules(&! merged, &dep) after signature change.
- `self-hosted/compiler/module_frontend.sio:2662` **multimodule_ir_ok** [by-value-param] _IrModule_
  - detail: fn multimodule_ir_ok(m: IrModule) -> MultiModuleIrResult — takes IrModule by value as the only param, embeds it in the struct literal, returns MultiModuleIrResult (which contains an inline IrModule field) by value.
  - fix: Eliminate this helper; callers on live paths should write fields into (*out) directly: (*out).ok=true; (*out).module=m; (*out).error_msg=""; or use load_multimodule_ir_traced_into style.
- `self-hosted/compiler/module_frontend.sio:2663` **multimodule_ir_ok** [struct-literal-copy] _MultiModuleIrResult_
  - detail: MultiModuleIrResult { ok: true, module: m, error_msg: "" } — struct literal with inline IrModule field, returned by value.
  - fix: Replace callers with direct field-writes to an &! MultiModuleIrResult output parameter.
- `self-hosted/compiler/module_frontend.sio:2666` **multimodule_ir_error** [struct-literal-copy] _MultiModuleIrResult_
  - detail: MultiModuleIrResult { ok: false, module: ir_empty_module(), error_msg: msg } — constructs a full IrModule (ir_empty_module()) inline in the struct literal and returns the whole MultiModuleIrResult by value.
  - fix: On the &!out pattern: (*out).ok=false; (*out).error_msg=msg; return — the module field is left as-is (caller zeros it at declaration).
- `self-hosted/compiler/module_frontend.sio:3367` **load_multimodule_ir_traced** [by-value-return] _IrModule_
  - detail: var merged_imported = module_frontend_lower_imported_source_recursive(...) — receives a full IrModule by value from SRET callee.
  - fix: Switch entire function to the _into pattern (already done in load_multimodule_ir_traced_into); or change module_frontend_lower_imported_source_recursive to &! out.
- `self-hosted/compiler/module_frontend.sio:3380` **load_multimodule_ir_traced** [by-value-param] _IrModule_
  - detail: return multimodule_ir_ok(merged_imported) — passes the IrModule local by value to multimodule_ir_ok (another copy), then the MultiModuleIrResult is returned by value (SRET) up the chain.
  - fix: Use load_multimodule_ir_traced_into (already exists) which writes through &! instead of returning.
- `self-hosted/compiler/module_frontend.sio:3410` **load_multimodule_ir_traced** [by-value-return] _IrModule_
  - detail: var single_lower_result_no_use = load_multimodule_lower_program_traced(...) — receives a full IrModule by SRET into a local.
  - fix: See load_multimodule_ir_traced_into line 3651 for the already-written in-place equivalent.
- `self-hosted/compiler/module_frontend.sio:3425` **load_multimodule_ir_traced** [by-value-param] _IrModule_
  - detail: return multimodule_ir_ok(single_lower_result_no_use) — passes IrModule local by value into multimodule_ir_ok and returns MultiModuleIrResult by SRET.
  - fix: Use load_multimodule_ir_traced_into instead.
- `self-hosted/compiler/module_frontend.sio:3443` **load_multimodule_ir_traced** [by-value-return] _IrModule_
  - detail: var single_lower_result = load_multimodule_lower_program_traced(...) — second single-module path, same SRET IrModule copy.
  - fix: Use load_multimodule_ir_traced_into instead.
- `self-hosted/compiler/module_frontend.sio:3458` **load_multimodule_ir_traced** [by-value-param] _IrModule_
  - detail: return multimodule_ir_ok(single_lower_result) — third SRET return of MultiModuleIrResult.
  - fix: Use load_multimodule_ir_traced_into instead.
- `self-hosted/compiler/module_frontend.sio:3523` **load_multimodule_ir_traced** [other] _IrModule_
  - detail: var merged_module = ir_empty_module() — large IrModule stack local initialised to zero.
  - fix: Use load_multimodule_ir_traced_into instead which writes into caller-allocated storage.
- `self-hosted/compiler/module_frontend.sio:3525` **load_multimodule_ir_traced** [deref-copy] _IrModule_
  - detail: Some(module_box) => { merged_module = *module_box } — deref-copies Box<IrModule> into the large local.
  - fix: Use load_multimodule_ir_traced_into; or change merge_module to work via &! to avoid the local entirely.
- `self-hosted/compiler/module_frontend.sio:3546` **load_multimodule_ir_traced** [other] _IrModule_
  - detail: var lowered_module = ir_empty_module() — second large IrModule stack local per loop iteration.
  - fix: Use load_multimodule_ir_traced_into.
- `self-hosted/compiler/module_frontend.sio:3548` **load_multimodule_ir_traced** [deref-copy] _IrModule_
  - detail: Some(module_box) => { lowered_module = *module_box } — deref-copies Box<IrModule> into loop-local per module.
  - fix: Pass &(*module_box) directly to ir_merge_modules after its signature change to &! / &.
- `self-hosted/compiler/module_frontend.sio:3557` **load_multimodule_ir_traced** [by-value-param] _IrModule_
  - detail: merged_module = ir_merge_modules(merged_module, lowered_module) — passes two IrModule locals by value and receives result by value (3 copies per iteration).
  - fix: ir_merge_modules(&! merged_module, &lowered_module) after signature change.
- `self-hosted/compiler/module_frontend.sio:3575` **load_multimodule_ir_traced** [by-value-param] _IrModule_
  - detail: multimodule_ir_ok(merged_module) — final return passes IrModule by value into helper and returns MultiModuleIrResult by SRET.
  - fix: Use load_multimodule_ir_traced_into.
- `self-hosted/compiler/module_frontend.sio:4155` **module_frontend_summary_module** [by-value-return] _IrModule_
  - detail: fn module_frontend_summary_module(prog: Program) -> IrModule — returns ir_empty_module() by value; stub used only by load_multimodule_ir_probe_summary_traced (probe path, not compile path).
  - fix: Probe-path only; low priority. Could use &! out pattern for consistency.
- `self-hosted/compiler/module_frontend.sio:4235` **load_multimodule_ir_probe_summary_traced** [by-value-return] _IrModule_
  - detail: var seed_lower_result = module_frontend_summary_module(seed_prog_lower) — receives IrModule by SRET; probe path only.
  - fix: Probe-path only; low priority.
- `self-hosted/compiler/module_frontend.sio:4242` **load_multimodule_ir_probe_summary_traced** [other] _IrModule_
  - detail: var merged_module = seed_lower_result — copy-assigns IrModule local from SRET result; probe path only.
  - fix: Probe-path only; low priority.
- `self-hosted/compiler/module_frontend.sio:4259` **load_multimodule_ir_probe_summary_traced** [by-value-return] _IrModule_
  - detail: var lower_result = module_frontend_summary_module(prog_lower) — per-iteration IrModule by SRET; probe path only.
  - fix: Probe-path only; low priority.
- `self-hosted/compiler/module_frontend.sio:4268` **load_multimodule_ir_probe_summary_traced** [by-value-param] _IrModule_
  - detail: merged_module = ir_merge_modules(merged_module, lower_result) — two by-value IrModule params + by-value return; probe path only.
  - fix: Probe-path only; low priority.
- `self-hosted/compiler/module_frontend.sio:4285` **load_multimodule_ir_probe_summary_traced** [by-value-param] _IrModule_
  - detail: multimodule_ir_ok(merged_module) — passes IrModule by value and returns MultiModuleIrResult by SRET; probe path only.
  - fix: Probe-path only; low priority.
- `self-hosted/compiler/module_loader.sio:1630` **compile_singlemodule_native_streaming_result_from_program** [deref-copy] _IrFunction_
  - detail: let fn_body = (*full_module).functions[fi as usize] — mirrors the compile_native_v2_preview_to_file:7551 pattern but on the streaming compile path; fn_body is immediately passed by value to native_v2_lower_function_to_machine(fn_body) which
  - fix: Change native_v2_lower_function_to_machine signature from func: IrFunction to func: &IrFunction and pass &(*full_module).functions[fi as usize] directly; remove the fn_body stack-local copy
- `self-hosted/compiler/module_loader.sio:1935` **thin_try_read_ir_cache** [by-value-return] _IrModule_
  - detail: fn thin_try_read_ir_cache(...) -> (bool, IrModule) — all three return statements return a tuple embedding IrModule by value: (false, ir_empty_module()), (false, ir_empty_module()), (true, restored); each return materialises a ~11 MB IrModul
  - fix: Change return type to (bool, Box<IrModule>) and return Box::new(restored) / Box::new(ir_empty_module()); or use an out-parameter: fn thin_try_read_ir_cache(path, stats, out: &! IrModule) -> bool to write through the pointer
- `self-hosted/compiler/module_loader.sio:1948` **thin_try_read_ir_cache_no_stats** [by-value-return] _IrModule_
  - detail: fn thin_try_read_ir_cache_no_stats(...) -> (bool, IrModule) — same tuple-wrapping IrModule pattern as line 1935; all three returns copy a full IrModule into the tuple return slot
  - fix: Same as line 1935: change to (bool, Box<IrModule>) or use an out-pointer parameter
- `self-hosted/ir/ir.sio:3281` **ir_module_add_export** [by-value-param] _IrModule_
  - detail: `fn ir_module_add_export(module: IrModule, name: Name) -> IrModule` — takes and returns IrModule by value. No callers found in the codebase (grep confirmed zero callers). Dead code.
  - fix: Change to `fn ir_module_add_export(module: &! IrModule, name: Name)` and mutate the export_names/export_count fields in-place through the pointer. Or delete entirely since there are no callers.
- `self-hosted/ir/lower.sio:174` **lowerer_new_probe** [deref-copy] _StructLayoutTable_
  - detail: `lo.struct_layouts = Box::new(ir_register_knowledge_layout(*lo.struct_layouts))` — `*lo.struct_layouts` deref-copies the full StructLayoutTable by value into the by-value parameter of `ir_register_knowledge_layout`, then reboxes. Analogous 
  - fix: Pass `lo.struct_layouts` (the Box) into a variant of `ir_register_knowledge_layout` that takes `&! StructLayoutTable` and mutates in-place, or pass the field reference: `ir_register_knowledge_layout_mut(&! *lo.struct_layouts)` — no rebox ne
- `self-hosted/ir/lower.sio:675` **ir_fast_summary_find_or_add_fn_id** [deref-copy] _IrModule_
  - detail: `var module_value = (*module)` — deref-copies the entire IrModule (1400 IrFunctions inline) from the &! pointer parameter. Then modifies and writes back at line 691: `(*module) = module_value`. Called from ir_fast_summary_preseed_fn_signatu
  - fix: Replace `var module_value = (*module)` with direct pointer access. Access `(*module).fn_count`, `(*module).functions[i]` etc. through the &! pointer. Remove the `(*module) = module_value` writeback and instead write individual mutations dir
- `self-hosted/ir/lower.sio:706` **ir_fast_summary_preseed_fn_signature** [deref-copy] _IrModule_
  - detail: `var module_value = (*module)` at line 706 — second full IrModule deref-copy in the same call stack (ir_fast_summary_find_or_add_fn_id already did one at line 675). Writes back at line 725: `(*module) = module_value`. Dead code: no external
  - fix: Same as line 675: remove the copy-and-writeback pattern, access all fields directly through the &! IrModule pointer.
- `self-hosted/ir/lower.sio:796` **ir_fast_summary_find_or_add_fn_id_owned** [by-value-param] _IrModule_
  - detail: `fn ir_fast_summary_find_or_add_fn_id_owned(module: IrModule, name: Name) -> (IrModule, i64)` — takes IrModule by value and returns a tuple containing IrModule by value. Called only from ir_fast_summary_preseed_fn_signature_owned (line 823)
  - fix: Change to `fn ir_fast_summary_find_or_add_fn_id_mut(module: &! IrModule, name: Name) -> i64` (matches the already-existing ir_fast_summary_find_or_add_fn_id at line 674 which takes &!). Or delete the _owned family entirely since it has no l
- `self-hosted/ir/lower.sio:816` **ir_fast_summary_preseed_fn_signature_owned** [by-value-param] _IrModule_
  - detail: `fn ir_fast_summary_preseed_fn_signature_owned(module: IrModule, name: Name, params: &..., return_type: &..., effects: &...) -> IrModule` — IrModule passed and returned by value. Body: `let pair = ir_fast_summary_find_or_add_fn_id_owned(mod
  - fix: fn ir_fast_summary_preseed_fn_signature_owned(module: &! IrModule, name: Name, ...) — inline the find/add as a &! call, mutate through pointer, return void. Cascades into calling the refactored find_or_add above.
- `self-hosted/ir/lower.sio:850` **ir_fast_summary_preseed_impl_methods_owned** [by-value-param] _IrModule_
  - detail: `fn ir_fast_summary_preseed_impl_methods_owned(module: IrModule, type_name: Name, methods: Option<Box<ItemList>>, skip_name: Name) -> IrModule` — IrModule passed and returned by value through recursive calls.
  - fix: fn ir_fast_summary_preseed_impl_methods_owned(module: &! IrModule, type_name: Name, methods: Option<Box<ItemList>>, skip_name: Name) — all recursive self-calls pass &! module; no return needed.
- `self-hosted/ir/lower.sio:878` **ir_fast_summary_preseed_items_owned** [by-value-param] _IrModule_
  - detail: `fn ir_fast_summary_preseed_items_owned(module: IrModule, items: Option<Box<ItemList>>, skip_name: Name) -> IrModule` — takes and returns IrModule by value, self-recursive (line 907). No external callers found in the codebase; dead code.
  - fix: Delete or convert to &! IrModule variant (already superseded by lowerer_preseed_program_items_mut and Lowerer method preseed_program_items_ref).
- `self-hosted/ir/lower.sio:913` **ir_fast_summary_seed_empty** [by-value-return] _IrFastSummaryOwnedSeed_
  - detail: `fn ir_fast_summary_seed_empty(epistemic: IrEpistemicSection) -> IrFastSummaryOwnedSeed` — returns struct containing inline IrModule + EnumVariantTable + StructLayoutTable by value. No external callers found; only referenced internally in t
  - fix: fn ir_fast_summary_seed_empty(epistemic: IrEpistemicSection, out: &! IrFastSummaryOwnedSeed) — write fields through pointer. If the dead cluster is to be kept, also convert IrFastSummaryOwnedSeed fields to Box<> to avoid propagating huge in
- `self-hosted/ir/lower.sio:991` **ir_fast_summary_preseed_impl_methods_seed_owned** [by-value-param] _IrFastSummaryOwnedSeed_
  - detail: `fn ir_fast_summary_preseed_impl_methods_seed_owned(seed: IrFastSummaryOwnedSeed, type_name: Name, methods: Option<Box<ItemList>>, skip_name: Name) -> IrFastSummaryOwnedSeed` — seed (containing IrModule + EnumVariantTable + StructLayoutTabl
  - fix: fn ir_fast_summary_preseed_impl_methods_seed_owned(seed: &! IrFastSummaryOwnedSeed, type_name: Name, methods: Option<Box<ItemList>>, skip_name: Name) — mutate through pointer, no return. All recursive self-calls pass &! seed.
- `self-hosted/ir/lower.sio:1019` **ir_fast_summary_preseed_items_seed_owned** [by-value-param] _IrFastSummaryOwnedSeed_
  - detail: `fn ir_fast_summary_preseed_items_seed_owned(seed: IrFastSummaryOwnedSeed, ...) -> IrFastSummaryOwnedSeed` — IrFastSummaryOwnedSeed contains `module: IrModule` inline (lower.sio:104-108), so the by-value param/return copies the full 1400-fu
  - fix: Delete or replace with &! IrFastSummaryOwnedSeed variant. IrFastSummaryOwnedSeed itself should be redesigned to hold Box<IrModule> to avoid inline embedding.
- `self-hosted/ir/lower.sio:1321` **lowerer_lower_fn_item_mut** [deref-copy] _IrModule_
  - detail: `var module = *(*lo).module` — deref-copies the full IrModule to write `module.functions[current_fn] = *(*lo).current_func`, then reboxes at line 1323. Conditional on `current_func_loaded && current_fn >= 0 && current_fn < fn_count` (pre-fl
  - fix: `(*(*lo).module).functions[(*lo).current_fn as usize] = *(*lo).current_func` — store-through-pointer directly into the module array element, no IrModule copy.
- `self-hosted/ir/lower.sio:1330` **lowerer_lower_fn_item_mut** [deref-copy] _IrFunction_
  - detail: let preserved = (*(*lo).module).functions[fn_id as usize] — reads one IrFunction out of the inline [IrFunction;1400] array by value to extract .name/.compile_strategy/.prof_counter_id; the caller is the &!-style variant of begin_function_bo
  - fix: Access the needed fields through the pointer directly: current_func.name = (*(*lo).module).functions[fn_id as usize].name; current_func.compile_strategy = (*(*lo).module).functions[fn_id as usize].compile_strategy; current_func.prof_counter
- `self-hosted/ir/lower.sio:1370` **lowerer_lower_fn_item_mut** [deref-copy] _IrModule_
  - detail: `var module = *(*lo).module` inside the `IR_STRATEGY_INSTRUMENTED` branch — deref-copies full IrModule to push a prof counter entry, then reboxes at line 1380. Guard: `strategy == IR_STRATEGY_INSTRUMENTED`, which is false for plain `fn main
  - fix: `let ctr_idx = (*(*lo).module).prof_counter_count; if ctr_idx < 64 { (*(*lo).module).prof_counters[ctr_idx as usize] = IrProfCounterInfo{...}; (*(*lo).module).prof_counter_count = ctr_idx + 1 }` — all stores through the module box pointer.
- `self-hosted/ir/lower.sio:1417` **lowerer_lower_fn_item_mut** [deref-copy] _IrModule_
  - detail: `var module = *(*lo).module` — end-of-fn flush: copies full IrModule to write `module.functions[current_fn] = *(*lo).current_func`, then reboxes at line 1419. Same pattern as line 1321.
  - fix: `(*(*lo).module).functions[(*lo).current_fn as usize] = *(*lo).current_func` — store-through-pointer, no IrModule copy.
- `self-hosted/ir/lower.sio:1612` **begin_function_body_lowering** [deref-copy] _IrFunction_
  - detail: let preserved = lo.module.functions[fn_id as usize] — copies one IrFunction out of the inline array to extract three scalar fields; this method is defined but never called anywhere in the repo (dead code)
  - fix: Replace with three direct field reads: current_func.name = lo.module.functions[fn_id as usize].name; current_func.compile_strategy = lo.module.functions[fn_id as usize].compile_strategy; current_func.prof_counter_id = lo.module.functions[fn
- `self-hosted/ir/lower.sio:2464` **bind_local** [deref-copy] _LowerLocalStack_
  - detail: `var stk = *lo.locals` — deref-copies the full LowerLocalStack to append one (name, reg, depth) entry, then reboxes at line 2471. Called for each local variable binding.
  - fix: `let idx = (*lo.locals).count; if idx < 4096 { (*lo.locals).names[idx as usize] = name; (*lo.locals).regs[idx as usize] = reg; (*lo.locals).depths[idx as usize] = lo.scope_depth; (*lo.locals).count = idx + 1 }` — all stores through box poin
- `self-hosted/ir/lower.sio:2479` **lookup_local** [deref-copy] _LowerLocalStack_
  - detail: `let stk = *self.locals` — deref-copies the full LowerLocalStack by value to perform a linear scan. Called on every identifier resolution.
  - fix: `var i = (*self.locals).count - 1; while i >= 0 { if (*self.locals).depths[i as usize] <= self.scope_depth && ir_name_eq((*self.locals).names[i as usize], name) { return (*self.locals).regs[i as usize] } ... }` — read through box pointer.
- `self-hosted/ir/lower.sio:3659` **lower_fn_item_owned** [deref-copy] _LowerLocalStack_
  - detail: `var rst_locals2 = *lo.locals` — deref-copies LowerLocalStack to zero its count, then reboxes at line 3661. Inside `lower_fn_item_owned`, which is the owned-value driver (not on the named codegen path).
  - fix: `(*lo.locals).count = 0` — single field write through box pointer.
- `self-hosted/ir/lower.sio:3698` **lower_fn_item_owned** [deref-copy] _IrModule_
  - detail: `var module = *lo.module` inside `if strategy == IR_STRATEGY_INSTRUMENTED` branch — deref-copies full IrModule to push a prof counter entry, then reboxes at line 3708. Guard: instrumented strategy only (not hit by plain `fn main()->i64{42}`
  - fix: `let ctr_idx = (*lo.module).prof_counter_count; if ctr_idx < 64 { (*lo.module).prof_counters[ctr_idx as usize] = IrProfCounterInfo{...}; (*lo.module).prof_counter_count = ctr_idx + 1; (*lo.current_func).prof_counter_id = ctr_idx }` — store-
- `self-hosted/ir/lower.sio:5923` **lower_closure_expr_ref** [deref-copy] _IrModule_
  - detail: `var module = *lo.module` — deref-copies the full IrModule to write `module.functions[closure_fn_id] = *lo.current_func` (flush the closure fn), then reboxes at line 5925. On codegen path if any closure literal is compiled; not reached for 
  - fix: `(*lo.module).functions[closure_fn_id as usize] = *lo.current_func` — store-through-pointer, no IrModule copy.
- `self-hosted/ir/lower.sio:6198` **lower_program_to_ir_summary_box_probe_with_epistemic_ref** [deref-copy] _IrModule_
  - detail: `var module = *lo.module` — deref-copies the full IrModule just to set the epistemic field, then reboxes at line 6200. This is the PROBE variant of the summary function (`..._probe_...`), NOT the named `lower_program_to_ir_summary_box_with_
  - fix: `(*lo.module).epistemic = epistemic` — store-through-pointer as at the already-fixed line 172.
- `self-hosted/ir/lower.sio:6316` **lower_program_to_ir_probe_summary_box_ref** [struct-literal-copy] _IrModule_
  - detail: Box::new(summary.summary.module) — moves the inline IrModule field out of an IrProgramSummaryFlatResult into a Box; summary.summary is IrProgramSummaryFlat which embeds IrModule directly, so this line materialises the full ~11 MB IrModule o
  - fix: Avoid extracting IrModule from the flat summary by value; keep it boxed throughout (use lower_program_to_ir_probe_summary_box_ref's caller to accept a Box<IrProgramSummaryFlat> and index into its .module through the pointer)
- `self-hosted/native/codegen_x86_linux.sio:4063` **compile_module** [deref-copy] _IrFunction_
  - detail: `c = compile_ir_function(c, module.functions[fi as usize], fi)` in the legacy compile_module loop — copies the fi-th IrFunction from `module: &IrModule` by value. Not a v2 path; compile_module is only called from compile_to_elf (line 7917).
  - fix: Convert compile_ir_function to take `func: &IrFunction` and then use `&module.functions[fi as usize]` at the call site.
- `self-hosted/native/codegen_x86_linux.sio:4082` **native_func_has_control_flow** [by-value-param] _IrFunction_
  - detail: Signature `fn native_func_has_control_flow(func: IrFunction) -> bool`. By-value IrFunction param. Only called from compile_ir_function (old non-v2 path, line 4141) — not on any v2 code path.
  - fix: Change to `fn native_func_has_control_flow(func: &IrFunction) -> bool` and update the single call site at line 4141 to pass `&effective_func`.
- `self-hosted/native/codegen_x86_linux.sio:4099` **compile_ir_function** [by-value-param] _IrFunction_
  - detail: Signature `pub fn compile_ir_function(nc: NativeCompiler, func: IrFunction, fn_index: i64) -> NativeCompiler`. Both nc (NativeCompiler) and func (IrFunction ~33 KB) are by-value. Part of the legacy compile_module path (line 4057/4063); not 
  - fix: For the IrFunction copy: change to `func: &IrFunction`. For NativeCompiler: the whole function uses the value-return NativeCompiler pattern; refactoring to &! is a larger change but follows the pattern of compile_ir_function_v2_mut.
- `self-hosted/native/codegen_x86_linux.sio:4891` **compile_ir_function_v2** [by-value-param] _IrFunction_
  - detail: fn compile_ir_function_v2(nc: NativeCompiler, func: IrFunction, ...) — takes IrFunction by value; called from module_loader.sio:1644 and compile_multimodule_native_advanced; not on the compile_native_v2_preview_to_file path (that uses compi
  - fix: Change signature to func: &IrFunction (matching compile_ir_function_v2_from_ir_into); update all call sites in module_loader.sio to pass a reference
- `self-hosted/native/codegen_x86_linux.sio:5673` **compile_ir_function_v2_mut** [by-value-param] _IrFunction_
  - detail: Signature `fn compile_ir_function_v2_mut(nc: &! NativeCompiler, func: IrFunction, machine_func: MachineFunction, fn_index: i64)`. Both `func` and `machine_func` are by-value. Called from compile_module_v2_mut (line 5809) passing `module.fun
  - fix: Change signature to `fn compile_ir_function_v2_mut(nc: &! NativeCompiler, func: &IrFunction, machine_func: &MachineFunction, fn_index: i64)` and replace `func.x` accesses with `(*func).x` (or `func.x` via auto-deref). The existing `compile_
- `self-hosted/native/codegen_x86_linux.sio:5809` **compile_module_v2_mut** [deref-copy] _IrFunction_
  - detail: `compile_ir_function_v2_mut(nc, module.functions[fi as usize], built.functions[fi as usize], fi)` — passes the fi-th IrFunction from `module: &IrModule` by value (matching the by-value param of compile_ir_function_v2_mut). Not on the _to_fi
  - fix: Once compile_ir_function_v2_mut is converted to take `func: &IrFunction`, change the call site to `compile_ir_function_v2_mut(nc, &module.functions[fi as usize], &built.functions[fi as usize], fi)` — no IrFunction element is copied.
- `self-hosted/native/codegen_x86_linux.sio:5854` **spill_ir_params** [by-value-param] _IrFunction_
  - detail: Signature `fn spill_ir_params(nc: NativeCompiler, func: IrFunction) -> NativeCompiler`. By-value IrFunction param. Called only from compile_ir_function (old path, line 5159). The ref-taking sibling spill_ir_params_ref_mut (line 5873) is wha
  - fix: Eliminate or convert to call spill_ir_params_mut with `&func`: the body of spill_ir_params (line 5854) is just `var c = nc; spill_ir_params_mut(&! c, func); c` — the mut variant already takes by-value IrFunction; for full fix also convert s
- `self-hosted/native/codegen_x86_linux.sio:5860` **spill_ir_params_mut** [by-value-param] _IrFunction_
  - detail: Signature `fn spill_ir_params_mut(nc: &! NativeCompiler, func: IrFunction)`. By-value IrFunction param. Called from compile_ir_function_v2_mut (line 5708) on the compile_native_v2_preview path (not _to_file). The ref variant spill_ir_params
  - fix: Replace spill_ir_params_mut entirely with spill_ir_params_ref_mut. Update the call site in compile_ir_function_v2_mut: `spill_ir_params_ref_mut(nc, &func)` (since func is a by-value param there, `&func` borrows it). Or change compile_ir_fun
- `self-hosted/native/codegen_x86_linux.sio:7589` **compile_to_macho_arm64_preview** [by-value-param] _IrFunction_
  - detail: a64_preview_compile_function(c, module.functions[fi as usize], built.functions[fi as usize], fi) — both module.functions[fi] and built.functions[fi] are copied by value as arguments; a64_preview_compile_function's signature is (c, func: IrF
  - fix: Change a64_preview_compile_function signature to take func: &IrFunction and update the call site to pass &module.functions[fi as usize]
- `self-hosted/native/codegen_x86_linux.sio:7899` **compile_native_x86_linux_to_file** [deref-copy] _IrFunction_
  - detail: let fn_body = (*module).functions[fi as usize] — mirrors the pattern at line 7551 in compile_native_v2_preview_to_file (which is covered) but this is in the separate compile_native_x86_linux_to_file entry point; fn_body is immediately passe
  - fix: Same as the fix documented for line 7551: pass a reference directly — compile_ir_function_v2_from_ir_into(&! nc, &(*module).functions[fi as usize], fi) — eliminating the stack-local IrFunction copy
- `self-hosted/native/codegen_x86_linux.sio:8142` **compile_module_for_obj** [by-value-param] _IrFunction_
  - detail: compile_ir_function(c, module.functions[fi as usize], fi) — compile_ir_function takes NativeCompiler+IrFunction+i64 by value; module.functions[fi] is copied each loop iteration; this is the object-file compilation path, not the preview/nati
  - fix: Introduce a compile_ir_function_ref variant that takes func: &IrFunction, or pass &module.functions[fi as usize] and update compile_ir_function's signature
