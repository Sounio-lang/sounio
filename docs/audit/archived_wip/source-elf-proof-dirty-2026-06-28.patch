diff --git a/self-hosted/check/check.sio b/self-hosted/check/check.sio
index 0d84951bc..7403b6531 100644
--- a/self-hosted/check/check.sio
+++ b/self-hosted/check/check.sio
@@ -987,6 +987,9 @@ fn checker_exit_type_expr_mut(c: *mut Checker) with Mut {
     if (*c).type_depth > 0 {
         (*c).type_depth = (*c).type_depth - 1
     }
+    if (*c).complexity_budget > 0 {
+        (*c).complexity_budget = (*c).complexity_budget - 1
+    }
 }
 
 fn checker_lower_named_type_mut(c: *mut Checker, path: AstPath) -> TypeEntry with Mut, Panic, Div, Alloc {
@@ -1272,7 +1275,7 @@ fn checker_lower_fn_type_expr_mut(c: *mut Checker, te: TypeExpr) -> TypeEntry wi
         defining_module: empty_path(),
     }
     let sig_id = (*c).fn_sigs.count
-    if sig_id >= 64 {
+    if sig_id >= 4096 {
         checker_note_type_error_mut(c)
         return ty_error()
     }
@@ -2506,13 +2509,13 @@ fn checker_collect_fn_def_inplace(c: *mut Checker, opt: Option<Box<FnDef> >, vis
             // FnSigTable by value (one of the large-aggregate copies the *mut
             // conversion eliminates).
             let sig_id = (*c).fn_sigs.count
-            if sig_id < 64 {
+            if sig_id < 4096 {
                 (*c).fn_sigs.entries[sig_id as usize] = sig
                 (*c).fn_sigs.count = sig_id + 1
             }
             // In-place env bind mirrors TypeEnv::bind (avoids copying the 32KB TypeEnv).
             let benv_idx = (*c).env.count
-            if benv_idx < 128 {
+            if benv_idx < 4096 {
                 (*c).env.bindings[benv_idx as usize] = TypeBinding {
                     name: name,
                     ty: ty_fn(sig_id),
@@ -2527,6 +2530,146 @@ fn checker_collect_fn_def_inplace(c: *mut Checker, opt: Option<Box<FnDef> >, vis
     }
 }
 
+fn checker_collect_global_item_inplace(c: *mut Checker, name: Name) with Mut, Panic, Div {
+    let benv_idx = (*c).env.count
+    if benv_idx < 4096 {
+        (*c).env.bindings[benv_idx as usize] = TypeBinding {
+            name: name,
+            ty: ty_unknown(),
+            is_mutable: true,
+            scope_depth: (*c).env.scope_depth,
+        }
+        (*c).env.count = benv_idx + 1
+    }
+    checker_const_int_bind_unknown_inplace(c, name)
+}
+
+fn checker_env_has_binding_inplace(c: *mut Checker, name: Name) -> bool with Mut, Panic, Div {
+    var i = (*c).env.count - 1
+    while i >= 0 {
+        if name_eq((*c).env.bindings[i as usize].name, name) {
+            return true
+        }
+        i = i - 1
+    }
+    false
+}
+
+fn checker_bind_import_unknown_with_mutability_inplace(c: *mut Checker, name: Name, is_mutable: bool) with Mut, Panic, Div {
+    if name.len <= 0 {
+        return
+    }
+    var i = (*c).env.count - 1
+    while i >= 0 {
+        if name_eq((*c).env.bindings[i as usize].name, name) {
+            if is_mutable {
+                (*c).env.bindings[i as usize].is_mutable = true
+            }
+            return
+        }
+        i = i - 1
+    }
+    let benv_idx = (*c).env.count
+    if benv_idx < 4096 {
+        (*c).env.bindings[benv_idx as usize] = TypeBinding {
+            name: name,
+            ty: ty_unknown(),
+            is_mutable: is_mutable,
+            scope_depth: (*c).env.scope_depth,
+        }
+        (*c).env.count = benv_idx + 1
+    }
+    checker_const_int_bind_unknown_inplace(c, name)
+}
+
+fn checker_bind_import_unknown_inplace(c: *mut Checker, name: Name) with Mut, Panic, Div {
+    checker_bind_import_unknown_with_mutability_inplace(c, name, false)
+}
+
+fn checker_bind_import_mutable_unknown_inplace(c: *mut Checker, name: Name) with Mut, Panic, Div {
+    checker_bind_import_unknown_with_mutability_inplace(c, name, true)
+}
+
+fn checker_collect_import_item_list_inplace(c: *mut Checker, opt: Option<Box<ImportItemList> >) with Mut, Panic, Div {
+    var cursor = opt
+    while true {
+        match cursor {
+            Some(node) => {
+                checker_bind_import_unknown_inplace(c, (*node).head)
+                cursor = (*node).tail
+            }
+            None => break
+        }
+    }
+}
+
+fn checker_string_list_head_eq(sl: StringList, text: string) -> bool with Mut, Panic, Div {
+    let len = str_len(text)
+    if sl.head_len != len {
+        return false
+    }
+    var i: i64 = 0
+    while i < len {
+        if sl.head_buf[i as usize] != (str_char_at(text, i) as i8) {
+            return false
+        }
+        i = i + 1
+    }
+    true
+}
+
+fn checker_path_first_two_eq(path: AstPath, first: string, second: string) -> bool with Mut, Panic, Div {
+    if path.seg_count < 2 {
+        return false
+    }
+    if !checker_string_list_head_eq(path.segments, first) {
+        return false
+    }
+    match path.segments.tail {
+        Some(rest) => checker_string_list_head_eq(*rest, second)
+        None => false
+    }
+}
+
+fn checker_collect_known_module_backing_imports_inplace(c: *mut Checker, path: AstPath) with Mut, Panic, Div {
+    // The boot4/raw checker currently type-checks modules with a shared symbol
+    // table but without a full import namespace. Keep the stopgap narrow:
+    // native-v2 imports scalar bootstrap stores directly from a few modules.
+    if checker_path_first_two_eq(path, "lexer", "cursor") {
+        checker_bind_import_mutable_unknown_inplace(c, make_name("CURSOR_SOURCE"))
+        checker_bind_import_mutable_unknown_inplace(c, make_name("CURSOR_SOURCE_LEN"))
+    }
+    if checker_path_first_two_eq(path, "parser", "parser") {
+        checker_bind_import_mutable_unknown_inplace(c, make_name("PT_KIND_CODE"))
+        checker_bind_import_mutable_unknown_inplace(c, make_name("PT_START"))
+        checker_bind_import_mutable_unknown_inplace(c, make_name("PT_END"))
+        checker_bind_import_mutable_unknown_inplace(c, make_name("PT_INT_VALUE"))
+    }
+    if checker_path_first_two_eq(path, "io", "file_write") {
+        checker_bind_import_unknown_inplace(c, make_name("io_write_meg_binary"))
+        checker_bind_import_unknown_inplace(c, make_name("io_make_executable"))
+    }
+}
+
+fn checker_collect_use_item_inplace(c: *mut Checker, item: Item) with Mut, Panic, Div {
+    checker_collect_import_item_list_inplace(c, item.use_items)
+    checker_collect_known_module_backing_imports_inplace(c, item.use_path)
+}
+
+fn checker_collect_runtime_builtins_inplace(c: *mut Checker) with Mut, Panic, Div {
+    checker_bind_import_unknown_inplace(c, make_name("arg_count"))
+    checker_bind_import_unknown_inplace(c, make_name("get_arg"))
+    checker_bind_import_unknown_inplace(c, make_name("str_len"))
+    checker_bind_import_unknown_inplace(c, make_name("str_char_at"))
+    checker_bind_import_unknown_inplace(c, make_name("str_eq"))
+    checker_bind_import_unknown_inplace(c, make_name("file_size"))
+    checker_bind_import_unknown_inplace(c, make_name("read_file"))
+    checker_bind_import_unknown_inplace(c, make_name("write_file"))
+    checker_bind_import_unknown_inplace(c, make_name("print"))
+    checker_bind_import_unknown_inplace(c, make_name("print_int"))
+    checker_bind_import_unknown_inplace(c, make_name("print_char"))
+}
+
 // *mut declaration collectors — port of the by-value collect_struct_def /
 // collect_enum_def (check.sio ~11225) to mutate (*c) IN PLACE, avoiding the 8MB
 // Checker SRET copy that forced the `_ => {}` skip below. Fills (*c).structs /
@@ -2635,18 +2778,24 @@ fn checker_collect_item_inplace(c: *mut Checker, item: Item) with Mut, Panic, Di
     // checker_check_if_expr_inplace — no 8MB self SRET), and i64==enum compares are accepted
     // (C-style, matching canonical). See docs/audit/g1_wip/ENUM_PATH_TYPING_DESIGN_2026-06-01.md.
     if item.kind == ItemKind::ItemFn {
-        checker_collect_fn_def_inplace(c, item.fn_def, item.visibility, (*c).current_module)
+        match item.fn_def {
+            Some(fd) => checker_collect_fn_def_inplace(c, Some(fd), item.visibility, (*c).current_module)
+            None => checker_collect_global_item_inplace(c, item.name)
+        }
     } else if item.kind == ItemKind::ItemStruct {
         checker_collect_struct_def_inplace(c, item.name, item.struct_def, item.visibility, (*c).current_module)
     } else if item.kind == ItemKind::ItemEnum {
         checker_collect_enum_def_inplace(c, item.name, item.enum_def, item.visibility, (*c).current_module)
+    } else if item.kind == ItemKind::ItemUse {
+        checker_collect_use_item_inplace(c, item)
     } else {
-        // ItemUse / ItemSession / impl / typealias / policy / … — not yet collected by the
+        // ItemSession / impl / typealias / policy / … — not yet collected by the
         // *mut spine (no-op, same as before). *mut collectors pending.
     }
 }
 
 fn checker_collect_items_inplace(c: *mut Checker, items: Option<Box<ItemList> >) with Mut, Panic, Div, Alloc, IO {
+    checker_collect_runtime_builtins_inplace(c)
     var cursor = items
     while true {
         match cursor {
@@ -5101,11 +5250,19 @@ fn checker_check_call_arg_knowledge_boundary_inplace(c: *mut Checker, arg_ty: Ty
 fn checker_check_call_arg_unit_boundary_inplace(c: *mut Checker, arg_ty: TypeEntry, param_ty: TypeEntry, call_span: Span) with Mut, Panic, Div, Alloc, IO {
     let expected_ty = call_ref_inner_or_self(param_ty)
     let provided_ty = call_ref_inner_or_self(arg_ty)
-    if is_error_or_unknown(expected_ty) || is_error_or_unknown(provided_ty) {
+    let expected_unit = expected_ty.unit_id
+    let provided_unit = provided_ty.unit_id
+    if expected_unit < 0 || provided_unit < 0 {
+        return
+    }
+    if expected_ty.kind == TypeKind::TyError || expected_ty.kind == TypeKind::TyUnknown || expected_ty.kind == TypeKind::TyNever {
+        return
+    }
+    if provided_ty.kind == TypeKind::TyError || provided_ty.kind == TypeKind::TyUnknown || provided_ty.kind == TypeKind::TyNever {
         return
     }
-    if expected_ty.unit_id >= 0 && provided_ty.unit_id >= 0 && expected_ty.unit_id != provided_ty.unit_id {
-        let t = (*c).report_unit_call_mismatch(call_span, provided_ty.unit_id, expected_ty.unit_id)
+    if expected_unit != provided_unit {
+        let t = (*c).report_unit_call_mismatch(call_span, provided_unit, expected_unit)
         checker_store_from_value(c, t)
         return
     }
diff --git a/self-hosted/check/defs.sio b/self-hosted/check/defs.sio
index 6be31cd84..ebcd952ad 100644
--- a/self-hosted/check/defs.sio
+++ b/self-hosted/check/defs.sio
@@ -1226,13 +1226,13 @@ fn empty_fn_sig() -> FnSig {
 // ============================================================
 
 pub struct FnSigTable {
-    entries: [FnSig; 64],
+    entries: [FnSig; 4096],
     count: i64,
 }
 
 fn fn_sig_table_new() -> FnSigTable {
     FnSigTable {
-        entries: [empty_fn_sig(); 64],
+        entries: [empty_fn_sig(); 4096],
         count: 0,
     }
 }
@@ -1241,7 +1241,7 @@ impl FnSigTable {
     fn add(self, sig: FnSig) -> (FnSigTable, i64) with Mut, Panic, Div {
         var tbl = self
         let idx = tbl.count
-        if tbl.count < 64 {
+        if tbl.count < 4096 {
             tbl.entries[tbl.count] = sig
             tbl.count = tbl.count + 1
         }
diff --git a/self-hosted/check/mod.sio b/self-hosted/check/mod.sio
index 70db2e3da..2fb19f2da 100644
--- a/self-hosted/check/mod.sio
+++ b/self-hosted/check/mod.sio
@@ -59,6 +59,13 @@ var CHECK_MOD_ONTOCACHE_SIDECAR_SLOTS: [i64; 6] = [0; 6]
 var CHECK_MOD_ONTOCACHE_SIDECAR_SUBCLASS: [bool; 36] = [false; 36]
 var CHECK_MOD_ONTOCACHE_SIDECAR_DISJOINT: [bool; 36] = [false; 36]
 
+fn check_mod_alloc_checker() -> *mut Checker with Alloc {
+    // The checker grew with the wider FnSigTable and TypeEnv capacities.
+    // Keep boot4/probe heap allocations ahead of the struct size so in-place
+    // checker paths cannot scribble past a stale literal allocation.
+    heap_alloc(134217728) as *mut Checker
+}
+
 fn check_policy_requirements_to_ir(
     opt: Option<Box<PolicyRequirementInfoList>>,
     out: [IrPolicyRequirementInfo; 8],
@@ -496,7 +503,7 @@ pub fn check_items_verdict_boot4(items: Option<Box<ItemList>>) -> i64 with Mut,
 }
 
 pub fn check_items_verdict_boot4_with_module_path(items: Option<Box<ItemList>>, module_path: AstPath) -> i64 with Mut, Panic, Div, Alloc, IO {
-    let checker_ptr = heap_alloc(16777216) as *mut Checker
+    let checker_ptr = check_mod_alloc_checker()
     checker_init_in_place(checker_ptr)
     (*checker_ptr).current_module = module_path
     (*checker_ptr).crate_root = module_path
@@ -513,7 +520,7 @@ pub fn check_modules_verdict_boot4(programs: &! [Program; 256], count: i64) -> i
     if count <= 0 {
         return 0
     }
-    let checker_ptr = heap_alloc(16777216) as *mut Checker
+    let checker_ptr = check_mod_alloc_checker()
     checker_init_in_place(checker_ptr)
     (*checker_ptr).crate_root = (*programs)[0].module_path
 
@@ -1002,7 +1009,7 @@ pub fn check_ontology_side_table_cache_sourcecheck_subclass_verdict_probe(items:
         return ontology_side_table_cache_verdict_reject()
     }
     print("check_mod_sourcecheck_cache_subclass_verdict=idx_ok\n")
-    let checker_ptr = heap_alloc(16777216) as *mut Checker
+    let checker_ptr = check_mod_alloc_checker()
     checker_init_in_place(checker_ptr)
     checker_collect_items_mut(checker_ptr, items)
     print("check_mod_sourcecheck_cache_subclass_verdict=collect_ok\n")
@@ -1035,7 +1042,7 @@ pub fn check_ontology_side_table_cache_sourcecheck_subclass_verdict_v2_probe(ite
         return ontology_side_table_cache_verdict_reject()
     }
     print("check_mod_sourcecheck_cache_subclass_verdict_v2=idx_ok\n")
-    let checker_ptr = heap_alloc(16777216) as *mut Checker
+    let checker_ptr = check_mod_alloc_checker()
     checker_init_in_place(checker_ptr)
     checker_collect_items_mut(checker_ptr, items)
     print("check_mod_sourcecheck_cache_subclass_verdict_v2=collect_ok\n")
@@ -1059,7 +1066,7 @@ pub fn check_ontology_side_table_cache_same_checker_kernel_subclass_verdict_v1_p
         return ontology_side_table_cache_verdict_reject()
     }
     print("check_mod_same_checker_cache_kernel_v1=idx_ok\n")
-    let checker_ptr = heap_alloc(16777216) as *mut Checker
+    let checker_ptr = check_mod_alloc_checker()
     checker_init_in_place(checker_ptr)
     checker_collect_items_mut(checker_ptr, items)
     print("check_mod_same_checker_cache_kernel_v1=collect_ok\n")
@@ -1090,7 +1097,7 @@ pub fn check_ontology_side_table_cache_same_checker_kernel_subclass_verdict_v2_p
         return ontology_side_table_cache_verdict_reject()
     }
     print("check_mod_same_checker_cache_kernel_v2=idx_ok\n")
-    let checker_ptr = heap_alloc(16777216) as *mut Checker
+    let checker_ptr = check_mod_alloc_checker()
     checker_init_in_place(checker_ptr)
     checker_collect_items_mut(checker_ptr, items)
     print("check_mod_same_checker_cache_kernel_v2=collect_ok\n")
@@ -1124,7 +1131,7 @@ pub fn check_ontology_side_table_cache_same_checker_kernel_subclass_verdict_v3_p
         return ontology_side_table_cache_verdict_reject()
     }
     print("check_mod_same_checker_cache_kernel_v3=idx_ok\n")
-    let checker_ptr = heap_alloc(16777216) as *mut Checker
+    let checker_ptr = check_mod_alloc_checker()
     checker_init_in_place(checker_ptr)
     checker_collect_items_mut(checker_ptr, items)
     print("check_mod_same_checker_cache_kernel_v3=collect_ok\n")
@@ -1185,7 +1192,7 @@ pub fn check_ontology_side_table_cache_same_checker_shadow_subclass_verdict_v1_p
         return ontology_side_table_cache_verdict_invalid_cache()
     }
     print("check_mod_same_checker_cache_shadow_v1=cache_valid\n")
-    let checker_ptr = heap_alloc(16777216) as *mut Checker
+    let checker_ptr = check_mod_alloc_checker()
     checker_init_in_place(checker_ptr)
     checker_collect_items_mut(checker_ptr, items)
     print("check_mod_same_checker_cache_shadow_v1=collect_ok\n")
@@ -1232,7 +1239,7 @@ pub fn check_ontology_side_table_cache_same_checker_sidecar_subclass_verdict_pro
         return ontology_side_table_cache_verdict_reject()
     }
     print("check_mod_same_checker_cache_sidecar=idx_ok\n")
-    let checker_ptr = heap_alloc(16777216) as *mut Checker
+    let checker_ptr = check_mod_alloc_checker()
     checker_init_in_place(checker_ptr)
     checker_collect_items_mut(checker_ptr, items)
     print("check_mod_same_checker_cache_sidecar=collect_ok\n")
@@ -1266,7 +1273,7 @@ pub fn check_ontology_side_table_cache_same_checker_sidecar_disjoint_verdict_pro
         return ontology_side_table_cache_verdict_reject()
     }
     print("check_mod_same_checker_cache_sidecar_disjoint=idx_ok\n")
-    let checker_ptr = heap_alloc(16777216) as *mut Checker
+    let checker_ptr = check_mod_alloc_checker()
     checker_init_in_place(checker_ptr)
     checker_collect_items_mut(checker_ptr, items)
     print("check_mod_same_checker_cache_sidecar_disjoint=collect_ok\n")
@@ -1466,7 +1473,7 @@ pub fn check_knowledge_context_ontocache_sidecar_verdict_probe(items: Option<Box
         return ontology_side_table_cache_verdict_invalid_cache()
     }
     print("check_mod_knowledge_context_ontocache_sidecar=cache_valid\n")
-    let checker_ptr = heap_alloc(16777216) as *mut Checker
+    let checker_ptr = check_mod_alloc_checker()
     checker_init_in_place(checker_ptr)
     checker_collect_items_mut(checker_ptr, items)
     print("check_mod_knowledge_context_ontocache_sidecar=collect_ok\n")
@@ -1489,7 +1496,7 @@ pub fn check_knowledge_context_ontocache_sidecar_verdict_probe(items: Option<Box
 }
 
 pub fn check_items_verdict_boot4_with_ontocache_sidecar(items: Option<Box<ItemList>>, source_path: string, module_path: AstPath) -> i64 with IO, Mut, Panic, Div, Alloc {
-    let checker_ptr = heap_alloc(16777216) as *mut Checker
+    let checker_ptr = check_mod_alloc_checker()
     checker_init_in_place(checker_ptr)
     (*checker_ptr).current_module = module_path
     (*checker_ptr).crate_root = module_path
@@ -1517,7 +1524,7 @@ pub fn check_ontology_side_table_cache_sourcecheck_disjoint_verdict_probe(items:
     if left_idx < 0 || right_idx < 0 {
         return ontology_side_table_cache_verdict_reject()
     }
-    let checker_ptr = heap_alloc(16777216) as *mut Checker
+    let checker_ptr = check_mod_alloc_checker()
     checker_init_in_place(checker_ptr)
     checker_collect_items_mut(checker_ptr, items)
     let had_error = checker_check_items_inner_store_return_had_error_mut(checker_ptr, items)
diff --git a/self-hosted/parser/exprs.sio b/self-hosted/parser/exprs.sio
index 7f9fc52f1..1c3c3a341 100644
--- a/self-hosted/parser/exprs.sio
+++ b/self-hosted/parser/exprs.sio
@@ -15,6 +15,15 @@ use parser::types::*
 use parser::stmts::*
 use parser::patterns::*
 
+fn expr_kind_ends_line_stmt(kind: ExprKind) -> bool {
+    kind == ExprKind::ExprIf
+        || kind == ExprKind::ExprMatch
+        || kind == ExprKind::ExprWhile
+        || kind == ExprKind::ExprLoop
+        || kind == ExprKind::ExprBlock
+        || kind == ExprKind::ExprForIn
+}
+
 impl Parser {
     // ============================================================
     // Expression entry point (dispatches to Pratt parser)
@@ -76,6 +85,13 @@ impl Parser {
                 continue
             }
 
+            // A control/block expression followed by a newline starts a new
+            // statement. Without this, `while { ... }\n-1` becomes
+            // `(while { ... }) - 1` and type-checks as `() - i64`.
+            if p.had_newline_before() && expr_kind_ends_line_stmt((*lhs).kind) {
+                break
+            }
+
             let prec = tk_precedence(op_kind)
 
             // Stop if current operator has lower or equal precedence
diff --git a/slurm-jobs/erdos90/run_on_cluster.sh b/slurm-jobs/erdos90/run_on_cluster.sh
index f77767033..16472be8e 100755
--- a/slurm-jobs/erdos90/run_on_cluster.sh
+++ b/slurm-jobs/erdos90/run_on_cluster.sh
@@ -29,7 +29,7 @@ echo "[1/3] compiling $KERNEL -> static ELF with $SOUC"
 file "$ELF" | grep -q "statically linked" || { echo "ERROR: ELF not static"; exit 1; }
 
 echo "[2/3] shipping $(stat -c%s "$ELF") bytes to a '$PART' node via srun + running"
-base64 -w0 "$ELF" | srun --partition="$PART" --time=00:10:00 --chdir=/orangefs/training bash -c '
+base64 -w0 "$ELF" | srun --partition="$PART" --gres=gpu:0 --time=00:10:00 --chdir=/orangefs/training bash -c '
   base64 -d > /orangefs/training/e90_run && chmod +x /orangefs/training/e90_run
   echo "=== node $(hostname) ($(nproc) cores) running Sounio Erdős-[90] search ==="
   t0=$(date +%s); /orangefs/training/e90_run; echo "wall=$(( $(date +%s) - t0 ))s"
