// Annotation-only multiparam: does the parser accept Robust<i64, Stable, InDistribution>?
// No prove_robust call — just a type that must lower.
fn id(r: Robust<i64, Stable, InDistribution>) -> i64 {
    0
}
fn main() -> i64 {
    0
}
