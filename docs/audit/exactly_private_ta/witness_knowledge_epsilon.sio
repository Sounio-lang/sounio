// Multi-parameter Knowledge surface: Knowledge<f64, epsilon < 0.1>
// Parser: parse_knowledge_type (types.sio ~968) accepts comma components.
fn main() -> i32 {
    let k: Knowledge<f64, epsilon < 0.1> = measure(1.0, uncertainty: 0.05)
    0
}
