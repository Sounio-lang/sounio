// Negative control: ExactlyPrivate today accepts only one type arg (inner).
// If this typechecks, the forensic assumption is wrong.
fn main() -> i32 with ZD {
    let p: ExactlyPrivate<f64, f64> = 0.0
    0
}
