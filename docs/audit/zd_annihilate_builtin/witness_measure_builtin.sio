// Positive control: measure is a checker builtin (no stdlib import of measure).
// If call_expr_is_builtin_measure failed, this would be an unknown function.
fn main() -> i32 {
    let k: Knowledge<f64> = measure(1.0, uncertainty: 0.1)
    0
}
