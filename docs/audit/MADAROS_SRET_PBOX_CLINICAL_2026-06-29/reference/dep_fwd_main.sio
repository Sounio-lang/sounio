// Main module calling dependency return-forward fn.
use dep_fwd_lib::dep_fwd_vacuous
use epistemic::knightian::*

fn main() -> i32 with Mut, Div, Panic {
    let p = dep_fwd_vacuous()
    if pb_confidence(&p) == 0 {
        return 0
    }
    return 1
}