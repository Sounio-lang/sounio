// Witness: predict_cmin_knightian vacuous path + direct field read (no accessor).
use clinical::vancomycin_pbpk::*

fn main() -> i32 with Mut, Div, Panic {
    let p = predict_cmin_knightian(15.0, 65.0, 1000.0, 12.0, 0)
    if p.confidence == 0 {
        return 0
    }
    return 1
}