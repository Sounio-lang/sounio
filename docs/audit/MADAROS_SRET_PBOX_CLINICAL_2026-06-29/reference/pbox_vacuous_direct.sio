// Witness: multimodule pb_vacuous + pb_confidence (no clinical layer).
use epistemic::knightian::*

fn main() -> i32 with Mut, Div, Panic {
    let p = pb_vacuous()
    if pb_confidence(&p) == 0 {
        return 0
    }
    return 1
}