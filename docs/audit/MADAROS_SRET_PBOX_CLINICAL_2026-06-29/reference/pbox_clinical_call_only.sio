// Control A (vacuous contract path): call + discard — passes.
// Control B (full PK path): use weight 78.5 — crashes even without read (secondary symptom).

use clinical::vancomycin_pbpk::*

fn main() -> i32 with Mut, Div, Panic {
    // Vacuous: weight 15.0 fails vp_weight_valid → early pb_vacuous(), no field read.
    predict_cmin_knightian(15.0, 65.0, 1000.0, 12.0, 0)
    return 0
}