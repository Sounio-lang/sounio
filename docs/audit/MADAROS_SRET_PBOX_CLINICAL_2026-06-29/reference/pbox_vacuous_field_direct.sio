// Witness: multimodule pb_vacuous + direct field read (no accessor fn).
use epistemic::knightian::pb_vacuous

fn main() -> i32 with Mut, Div, Panic {
    let p = pb_vacuous()
    if p.confidence == 0 {
        return 0
    }
    return 1
}