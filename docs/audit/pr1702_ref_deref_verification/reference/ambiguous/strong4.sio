// strong4: try aliased imports — `as` syntax.
use ambiguous_public_a::{same_public_name as name_a}
use ambiguous_public_b::{same_public_name as name_b}

fn main() -> i64 {
    let a = name_a()
    let b = name_b()
    a + b
}
