extern "C" {
    fn system(cmd: string) -> i64;
}
fn main() -> i32 with IO, Mut, Panic, Div {
    let rc = system("/usr/bin/touch /tmp/p0f_sysstr_probe")
    print("sysstr rc = "); print_int(rc); print("\n")
    return 0
}
