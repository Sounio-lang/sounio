// Accusation witness. NomeQueNaoExiste is not a name that
// effect_name_to_id recognises. Today's checker maps it to -1 and
// discards it with no diagnostic. The closed-list gate fails while
// `souc check` of this file still exits 0.

fn marked() -> i64 with NomeQueNaoExiste {
    0
}

fn main() -> i64 with NomeQueNaoExiste {
    marked()
}
