// Positive control: refinement rejects a literal that fails the predicate.
// Expect E042.

type Pos = { v: i32 | v > 0 }

fn takes_pos(x: Pos) -> i32 {
    x
}

fn main() -> i32 {
    takes_pos(0)
}
