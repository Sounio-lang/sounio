// Same as D5; probe prints refine summary if the engine emits it.

fn takes_nz(d: { x: i32 | x != 0 }) -> i32 {
    d
}

fn main() -> i32 with IO {
    let y: i32 = 3
    let r = takes_nz(y)
    r
}
