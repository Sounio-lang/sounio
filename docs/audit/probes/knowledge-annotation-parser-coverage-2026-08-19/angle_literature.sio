// Coverage receipt for docs/audit/KNOWLEDGE_ANNOTATION_PARSER_COVERAGE_2026-08-19.md
// Angle form, unreachable provenance word. Literature is an Ident, not
// a keyword. Sister pin expects this to parse-fail (mirrors bracket-form
// literature.sio, which silently passes). Documented in the 2026-08-23
// addendum as the empirical discriminator for "angle form is loud,
// bracket form is silent" — the E241-era angle path refuses an unknown
// component before the Ident-epsilon sink can swallow it.
//
// Knowledge is not in the silent_type_spine_ratchet_gate.sh KINDS list,
// so the gate regex does not match this fn-parameter position.
fn f(x: Knowledge<f64, Literature>) -> Knowledge<f64, Literature> { x }
fn main() -> i32 { 0 }
