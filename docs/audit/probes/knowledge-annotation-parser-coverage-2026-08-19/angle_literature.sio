// Probe: Knowledge angle-form with `Literature` as the second component.
// Mirrors angle_source.sio; same expected behaviour. Used for documentation
// of the silent-skip / Ident-epsilon-sink gap; not part of the pin.
fn main() -> i32 with IO, Mut, Panic, Div, Alloc {
    let x: Knowledge<f64, Literature> = 0.0
    print("ok\n")
    0
}
