// Probe: Intervention<f64, Derived> (angle form, not bracket).
// Sister pin expects this to parse + check OK.
//
// Deliberately NOT in a fn-parameter position: the Contracts gate
// `silent_type_spine_ratchet_gate.sh` forbids new scaffolding of
// unlowered kinds in parameter positions. A `type X = Y` alias uses
// the construct in a non-parametric top-level position; the alias is
// unused so no construction is required.
type _AngleProbe = Intervention<f64, Derived>
fn main() -> i32 { 0 }
