// Coverage receipt for docs/audit/KNOWLEDGE_ANNOTATION_PARSER_COVERAGE_2026-08-19.md
// Angle form, unreachable provenance word. Source is an Ident, not a
// keyword. Sister pin expects this to parse-fail (mirrors bracket-form
// source.sio, which silently passes). Documented in the 2026-08-23
// addendum as the empirical discriminator for "angle form is loud,
// bracket form is silent" — the E241-era angle path refuses an unknown
// component before the Ident-epsilon sink can swallow it.
//
// Probe intentionally not in fn-parameter position? No: Knowledge is
// not in the silent_type_spine_ratchet_gate.sh KINDS list
// (Axiom|Lemma|Proof|CausalEffect|Counterfactual|Intervention|
// PotentialOutcome|DiffPrivate|DPBudget|Aleatoric|Chan|Session), so
// the gate regex `fn NAME(...: KIND[<\[])` does not match. The
// bracket-form probes in this directory use the same shape.
fn f(x: Knowledge<f64, Source>) -> Knowledge<f64, Source> { x }
fn main() -> i32 { 0 }
