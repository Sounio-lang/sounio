// Probe: Knowledge angle-form with `Source` as the second component.
// Mirrors knowledge_angle_derived.sio but with the unreachable `Source`
// word. Used for documentation of the silent-skip / Ident-epsilon-sink
// gap; not part of the pin (which uses bracket-form probes).
fn main() -> i32 with IO, Mut, Panic, Div, Alloc {
    let x: Knowledge<f64, Source> = 0.0
    print("ok\n")
    0
}
