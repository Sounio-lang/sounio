// Coverage receipt for docs/audit/KNOWLEDGE_ANNOTATION_PARSER_COVERAGE_2026-08-19.md
// Angle form, "Input" provenance word. As of PR #2229, "Input" IS a
// lexer keyword (added by 1adec5e731 "Give Input the provenance
// keyword"). The bracket-form input.sio probe is therefore a
// reachable provenance; the angle-form path is a separate question —
// the audit addendum documents angle_input as parse fail even on
// shipped ELF, and the source-built Madaros must agree to keep the
// "two ELFs, same behaviour" pin honest.
//
// Sister pin tests this on /tmp/madaros-source-built and ./bin/souc.
// If either compiler's behaviour moves, the tripwire fires.
//
// Knowledge is not in the silent_type_spine_ratchet_gate.sh KINDS list,
// so the gate regex does not match this fn-parameter position.
fn f(x: Knowledge<f64, Input>) -> Knowledge<f64, Input> { x }
fn main() -> i32 { 0 }
