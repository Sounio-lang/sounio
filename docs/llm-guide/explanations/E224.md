# E224 — Unreadable import

**Severity:** error

**Message pattern:** `error[E224]: unreadable import: PATH`

## What happened

The compiler could not read the file at `PATH` (file not found, permission denied, or empty read).

## Fix

Check that the import path is correct relative to the source file's directory:

```sounio
import "stdlib/gum.sio"   // must exist relative to source file
```

Verify the file exists and is readable.
