<!-- docs:meta
topic_id: repo.docs.llm-guide.explanations.e035
authority: repo_only
audience: users
last_validated: 2026-03-07
validated_by: A2
source_of_truth: docs/governance/topic-registry.v1.json#repo.docs.llm-guide.explanations.e035
-->

---
code: E035
title: Effect not declared in function signature
severity: error
component: type-checker/effects
---

> **⚠️ Reality check (Madaros v0.80.0, verified 2026-07-11):** this code does **not** currently fire under the default `souc check` — the "wrong" example below compiles clean under the default engine (the `lean_single` seed is stricter). Kept for the intended semantics and forward-compat; see the enforcement-reality note in [error-catalog.md](../error-catalog.md).


**E035 — Effect not declared in function signature**

A function performs an operation that requires an effect (IO, Mut, Div, Panic, Alloc, Observe, Audit, Hypothesis, Epistemic, ZD…) but the function's `with` clause does not list that effect.

Effects propagate upward: if an inner function needs `Div`, every caller must also declare `Div`.

**Minimal example:**
```sio
fn divide(a: f64, b: f64) -> f64 {   // missing `with Div`
    a / b
}
```

**Canonical fix:** Add the required effect to the `with` clause.

```sio
fn divide(a: f64, b: f64) -> f64 with Div {
    a / b
}
```

**Common effect triggers:**

| Operation | Effect required |
|-----------|----------------|
| `a / b` or `a % b` | `Div` |
| `arr[i]` | `Panic` |
| `assert(...)` | `Panic` |
| `println(...)` / `print(...)` | `IO` |
| `*x = v` or `self.field = v` | `Mut` |
| `var x = 0; x = 1` | `Mut` |
| `knowledge.value` | `Epistemic` |
| `if obs > threshold` on `Unobserved<T>` | `Observe` |
| Zero-divisor type (`ExactlyPrivate<T>` etc.) | `ZD` |
