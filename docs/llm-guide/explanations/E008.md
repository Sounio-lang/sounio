---
code: E008
title: Too many globals
severity: error
component: compiler/codegen
---

**E008 — Too many globals**

The module declares more top-level (`global`) bindings than the compiler supports.

**Canonical fix:** Move globals into a struct passed by reference, or split the module.
