<!-- docs:meta
topic_id: repo.docs.llm-guide.explanations.e211
authority: repo_only
audience: users
last_validated: 2026-03-07
validated_by: A2
source_of_truth: docs/governance/topic-registry.v1.json#repo.docs.llm-guide.explanations.e211
-->

# E211 — Study block requires at least one hypothesis

**Severity:** error

**Message pattern:** `error[E211]: study block requires at least one hypothesis at line L`

## What happened

A `study { ... }` block contains no `hypothesis` declarations. At least one hypothesis is required to use the `Study` effect.

## Example

```sounio
fn run() with Study {
    study {
        // empty — E211
    }
}
```

## Fix

Add at least one `hypothesis` inside the `study` block:

```sounio
fn run() with Study {
    study {
        hypothesis h1: x > 0
    }
}
```
