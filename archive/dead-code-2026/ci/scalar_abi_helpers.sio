module ci::scalar_abi_helpers

fn imported_const_7() -> i64 with Div {
    7
}

fn imported_identity_i64(x: i64) -> i64 with Div {
    x
}

fn imported_mark_seed_i64(x: i64) -> i64 with Div {
    x + 48
}

fn imported_add_i64(x: i64, y: i64) -> i64 with Div {
    x + y
}

fn imported_bitpack_i64(x: i64, y: i64) -> i64 with Div {
    (x * 256) + y
}

fn imported_bool_true() -> bool with Div {
    true
}

fn imported_status_code() -> i64 with Div {
    3
}
