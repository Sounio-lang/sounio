/* KL-14c probe — three symbols in one DT_NEEDED shared library. */
long kl14c_add(long a, long b) { return a + b; }
long kl14c_mul(long a, long b) { return a * b; }
long kl14c_neg(long a) { return -a; }
