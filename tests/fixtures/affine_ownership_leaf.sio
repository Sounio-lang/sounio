pub affine struct ImportedPermit {
    id: i64,
}

pub fn imported_permit(id: i64) -> ImportedPermit {
    ImportedPermit { id: id }
}

pub fn imported_spend(permit: ImportedPermit) -> i64 {
    permit.id
}
