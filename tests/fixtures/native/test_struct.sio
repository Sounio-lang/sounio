// Test: struct creation, field access, and return computed value
// Expected exit code: 42

struct Point {
    x: i64,
    y: i64,
}

fn main() -> i64 {
    let p = Point { x: 10, y: 32 }
    p.x + p.y
}
