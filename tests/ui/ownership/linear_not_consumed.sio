//@ compile-fail
//@ ignore
//@ error-pattern: not consumed
//@ description: Linear value must be consumed (not detected for function return values)

linear struct Resource {
    id: i32,
}

fn make_resource() -> Resource {
    Resource { id: 1 }
}

fn main() {
    let r = make_resource()
    // r is not consumed
}
