//@ compile-fail
//@ error-pattern: not consumed
//@ description: BLOCKED - requires linear/affine type system

linear struct Resource {
    id: i32,
}

fn make_resource() -> Resource {
    Resource { id: 1 }
}

fn main() {
    let r = make_resource()
    // r is not consumed
}
