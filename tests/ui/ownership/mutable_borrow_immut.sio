//@ compile-fail
//@ ignore
//@ error-pattern: Mut
//@ description: Cannot mutably borrow without Mut effect (mutable borrow effect not enforced)

struct Counter {
    value: i32,
}

fn main() {
    let c = Counter { value: 0 }
    let r = &!c
}
