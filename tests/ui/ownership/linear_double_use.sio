//@ compile-fail
//@ error-pattern: consumed
//@ description: Linear value used twice

linear struct Handle {
    fd: i32,
}

fn consume(h: Handle) {}

fn main() {
    let h = Handle { fd: 1 }
    consume(h)
    consume(h)
}
