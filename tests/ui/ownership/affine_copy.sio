//@ compile-fail
//@ error-pattern: affine value used more than once
//@ description: an affine value used a second time is a compile error
affine struct Unique {
    value: i32,
}
fn main() {
    let u = Unique { value: 1 }
    let a = u
    let b = u
}
