//@ compile-fail
//@ error-pattern: consumed
//@ description: Linear value must be consumed in all branches

linear struct Token {
    id: i32,
}

fn consume(t: Token) {}

fn main() {
    let t = Token { id: 1 }
    if true {
        consume(t)
    }
    // t not consumed in else branch
}
