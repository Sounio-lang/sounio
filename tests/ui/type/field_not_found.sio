//@ compile-fail
//@ ignore
//@ error-pattern: field
//@ description: BLOCKED: pinned binary does not enforce field existence; self-hosted checker has E012

struct Point {
    x: i32,
    y: i32,
}

fn main() {
    let p = Point { x: 1, y: 2 }
    let z = p.z
}
