//@ compile-fail
//@ ignore
//@ error-pattern: field
//@ description: SH_ERROR: self-hosted checker silent on field access (E012 field not found); needs implementation
struct Point {
    x: i32,
    y: i32,
}
fn main() {
    let p = Point { x: 1, y: 2 }
    let z = p.z
}
