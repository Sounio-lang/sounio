//@ compile-fail
//@ ignore
//@ error-pattern: field
//@ description: BLOCKED - compiler does not validate struct field access (E012)

struct Point {
    x: i32,
    y: i32,
}

fn main() {
    let p = Point { x: 1, y: 2 }
    let z = p.z
}
