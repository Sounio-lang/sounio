//@ compile-fail
//@ ignore
//@ error-pattern: field
//@ description: BLOCKED: pinned binary does not enforce struct field count; self-hosted checker has E046
struct Point {
    x: i32,
    y: i32,
}
fn main() {
    let p = Point { x: 1 }
}
