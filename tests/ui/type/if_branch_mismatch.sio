//@ compile-fail
//@ ignore
//@ error-pattern: type
//@ description: If branches must have matching types (branch type unification not enforced)

fn main() {
    let x = if true { 1 } else { "hello" }
}
