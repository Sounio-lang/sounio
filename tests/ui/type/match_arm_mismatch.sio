//@ compile-fail
//@ ignore
//@ error-pattern: type
//@ description: Match arms have different types (match arm type unification not enforced)

fn main() {
    let x = match true {
        true => 1,
        false => "string",
    }
}
