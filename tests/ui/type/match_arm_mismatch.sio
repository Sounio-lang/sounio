//@ compile-fail
//@ ignore
//@ error-pattern: type
//@ description: BLOCKED - compiler does not validate match arm type compatibility (E018)

fn main() {
    let x = match true {
        true => 1,
        false => "string",
    }
}
