//@ compile-fail
//@ ignore
//@ error-pattern: type
//@ description: BLOCKED: pinned binary does not enforce match arm types; self-hosted checker has E018

fn main() {
    let x = match true {
        true => 1,
        false => "string",
    }
}
