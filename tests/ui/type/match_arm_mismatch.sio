//@ compile-fail
//@ ignore
//@ error-pattern: type
//@ description: SELFHOST_PASS: passes via self-hosted JIT checker (E018 match arm type mismatch)
fn main() {
    let x = match true {
        true => 1,
        false => "string",
    }
}
