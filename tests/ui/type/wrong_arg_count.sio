//@ compile-fail
//@ ignore
//@ error-pattern: argument
//@ description: SH_TIMEOUT: self-hosted checker hangs on wrong arg count (E010); needs investigation

fn takes_two(a: i32, b: i32) -> i32 {
    a + b
}

fn main() {
    let x = takes_two(1)
}
