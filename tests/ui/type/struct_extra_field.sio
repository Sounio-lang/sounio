//@ compile-fail
//@ ignore
//@ error-pattern: field
//@ description: BLOCKED - compiler does not validate struct literal has no extra fields (E046)

struct Point {
    x: i32,
    y: i32,
}

fn main() {
    let p = Point { x: 1, y: 2, z: 3 }
}
