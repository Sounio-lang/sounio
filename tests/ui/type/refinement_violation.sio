//@ compile-fail
//@ error-pattern: refinement
//@ description: SELFHOST_PASS: passes via self-hosted JIT checker (E042 refinement violation)

type Positive = { x: i32 | x > 0 }

fn main() {
    let p: Positive = -5
}
