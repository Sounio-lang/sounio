//@ compile-fail
//@ ignore
//@ error-pattern: refinement
//@ description: BLOCKED: pinned binary cannot parse refinement type declarations; self-hosted checker has E042

type Positive = { x: i32 | x > 0 }

fn main() {
    let p: Positive = -5
}
