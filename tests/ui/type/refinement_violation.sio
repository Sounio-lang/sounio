//@ compile-fail
//@ ignore
//@ error-pattern: refinement
//@ description: Refinement type constraint violated (refinements not enforced)

type Positive = { x: i32 | x > 0 }

fn main() {
    let p: Positive = -5
}
