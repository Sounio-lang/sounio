//@ compile-fail
//@ ignore
//@ error-pattern: Panic
//@ description: BLOCKED - compiler does not validate array index type (currently passes incorrectly)

fn main() {
    let arr = [1, 2, 3]
    let x = arr["index"]
}
