//@ compile-fail
//@ ignore
//@ error-pattern: argument
//@ description: SH_TIMEOUT: self-hosted checker hangs on extra call args (E010); needs investigation

fn takes_one(x: i32) -> i32 {
    x
}

fn main() {
    let result = takes_one(1, 2, 3)
}
