//@ compile-fail
//@ ignore
//@ error-pattern: argument
//@ description: Too many arguments to function (argument count not validated)

fn takes_one(x: i32) -> i32 {
    x
}

fn main() {
    let result = takes_one(1, 2, 3)
}
