//@ compile-fail
//@ ignore
//@ error-pattern: type
//@ description: BLOCKED: pinned binary does not enforce closure arg types; self-hosted checker validates via fn sig
fn main() {
    let f = |x: i32| x + 1
    let result = f("string")
}
