//@ compile-fail
//@ ignore
//@ error-pattern: unit
//@ description: SH_TIMEOUT: self-hosted checker hangs on unit type mismatch (E041); unit registry not yet wired

fn main() {
    let distance: m = 5.0
    let mass: kg = 10.0
    let result = distance + mass
}
