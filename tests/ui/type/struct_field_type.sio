//@ compile-fail
//@ ignore
//@ error-pattern: type
//@ description: SH_ERROR: self-hosted checker silent on field type mismatch (E016 struct field type); needs implementation
struct Point {
    x: i32,
    y: i32,
}
fn main() {
    let p = Point { x: "string", y: 2 }
}
