//@ compile-fail
//@ ignore
//@ error-pattern: type
//@ description: BLOCKED: pinned binary does not enforce struct field types; self-hosted checker has E016

struct Point {
    x: i32,
    y: i32,
}

fn main() {
    let p = Point { x: "string", y: 2 }
}
