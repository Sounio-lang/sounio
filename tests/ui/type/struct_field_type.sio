//@ compile-fail
//@ ignore
//@ error-pattern: type
//@ description: Struct field type mismatch (struct field types not validated)

struct Point {
    x: i32,
    y: i32,
}

fn main() {
    let p = Point { x: "string", y: 2 }
}
