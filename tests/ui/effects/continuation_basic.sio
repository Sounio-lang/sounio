//@ run-pass
//@ description: Basic effect with continuation capture

fn main() -> i32 with IO {
    let x = 10
    let y = compute_with_effect(x)
    y
}

fn compute_with_effect(n: i32) -> i32 with IO {
    // This should trigger CPS transformation
    let doubled = n * 2
    doubled + 5
}
