//@ known-failure: full math::qd128 import lowers without crashing but native emission fails closed (qd_mul → backend rc=12). For constructors under Madaros use math::qd128_core + tests/run-pass/qd128_core_import_native_v2_smoke.sio.

use math::qd128::{qd_zero}

fn main() -> i32 with IO {
    let z = qd_zero()
    if z.x0 == 0.0 {
        println("QD128_IMPORT_NATIVE_V2 PASS")
        return 0
    }
    1
}
