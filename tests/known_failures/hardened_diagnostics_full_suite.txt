# Known full-suite blockers exposed by embedded-checker diagnostic hardening.
#
# The full suite remains strict for new regressions: any failing test not listed
# here fails CI. Remove entries as lanes repair source, routing, or compiler
# semantics. Do not add entries without a concrete failure classification.
#
# Re-opened 2026-05-17: f64 return refinement literals are still accepted by
# the checked self-hosted compiler when the return type is an inline f64
# predicate. Keep this visible while the PR #150 macOS compiler lane repairs
# host/runtime behavior; remove only when the compiler emits
# "refinement type violation" for the case again.
tests/compile-fail/refinement_f64_return_violation.sio
