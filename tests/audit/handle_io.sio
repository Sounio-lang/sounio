// handle<IO> whose handler prints a token the body does not.
// If the handler is ignored, stdout matches handle_control.sio (BODY_MARK only).
// If the handler runs, HANDLER_MARK appears.
fn main() with IO {
    handle<IO> {
        println("BODY_MARK")
    } with {
        println("HANDLER_MARK")
    }
}
