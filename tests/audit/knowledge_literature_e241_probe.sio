//@ description: Literature has no parser arm (audit 2026-08-19: it fell into the Ident epsilon-binder arm)
// E241 closed the bare-identifier sink since then (self-hosted/parser/types.sio
// ~1036: "A bare identifier (Source, a typo, …) is not an epsilon bound — it used
// to become CmpLt 0.0 silently (E241 closes that sink)"). Expected today: a
// diagnostic (E241 or a parse refusal). If this compiles with no diagnostic,
// the unwriteable-provenance hole is still open and the 2026-08-19 audit stands
// uncorrected.

fn keep(x: Knowledge[f64, Literature]) -> i64 {
    0
}

fn main() -> i64 {
    0
}
