//@ compile-fail
//@ error-pattern: Audited<T> requires ZD and Witness
//@ description: Audited<T> at a parameter with ZD but no Witness must emit E205.

fn r(x: Audited<f64>) -> f64 with ZD { 0.0 }

fn main() -> i32 with ZD {
    let _z = r(1.0 as Audited<f64>)
    0
}
