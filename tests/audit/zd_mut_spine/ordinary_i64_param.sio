//@ run-pass
//@ description: Ordinary kinds already live on both spines. A named i64
//@   parameter must keep checking after the ZD arms are added.

fn r(x: i64) -> i64 { x }

fn main() -> i32 with IO {
    let _y = r(1)
    0
}
