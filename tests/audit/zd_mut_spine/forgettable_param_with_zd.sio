//@ run-pass
//@ description: Forgettable<T> at a parameter with `with ZD` must check.

fn r(x: Forgettable<f64>) -> f64 with ZD { 0.0 }

fn main() -> i32 with IO, ZD {
    let _z = r(1.0 as Forgettable<f64>)
    0
}
