//@ compile-fail
//@ error-pattern: error[E247]
//@ description: The sedenion has 15 imaginary units; e3e99 names one that
//@   does not exist. Must refuse (E247).

fn release(x: ExactlyPrivate<f64, e3e99>) -> f64 with ZD { 0.0 }

fn main() -> i32 with IO, ZD {
    let _z = release(1.0 as ExactlyPrivate<f64, e3e99>)
    0
}
