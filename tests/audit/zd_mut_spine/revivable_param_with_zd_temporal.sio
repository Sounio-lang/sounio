//@ run-pass
//@ description: Revivable<T> at a parameter with `with ZD, Temporal` must check.

fn r(x: Revivable<f64>) -> f64 with ZD, Temporal { 0.0 }

fn main() -> i32 with IO, ZD, Temporal {
    let _z = r(1.0 as Revivable<f64>)
    0
}
