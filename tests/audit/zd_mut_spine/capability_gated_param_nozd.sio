//@ compile-fail
//@ error-pattern: CapabilityGated<T> requires ZD
//@ description: CapabilityGated<T> at a parameter must emit E203 without `with ZD`.

fn r(x: CapabilityGated<f64>) -> f64 { 0.0 }

fn main() -> i32 {
    let _z = r(1.0 as CapabilityGated<f64>)
    0
}
