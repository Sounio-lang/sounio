//@ run-pass
//@ description: Audited<T> at a parameter with `with ZD, Witness` must check.

fn r(x: Audited<f64>) -> f64 with ZD, Witness { 0.0 }

fn main() -> i32 with IO, ZD, Witness {
    let _z = r(1.0 as Audited<f64>)
    0
}
