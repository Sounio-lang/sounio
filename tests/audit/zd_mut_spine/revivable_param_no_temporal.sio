//@ compile-fail
//@ error-pattern: Revivable<T> requires ZD and Temporal
//@ description: Revivable<T> at a parameter with ZD but no Temporal must emit E206.

fn r(x: Revivable<f64>) -> f64 with ZD { 0.0 }

fn main() -> i32 with ZD {
    let _z = r(1.0 as Revivable<f64>)
    0
}
