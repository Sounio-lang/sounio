//@ run-pass
//@ description: ExactlyPrivate<T, A> accepts a well-formed ZD locus. e3e10
//@   names two distinct sedenion imaginary units with 1 <= 3 < 10 <= 15.

fn release(x: ExactlyPrivate<f64, e3e10>) -> f64 with ZD { 0.0 }

fn main() -> i32 with IO, ZD {
    let _z = release(1.0 as ExactlyPrivate<f64, e3e10>)
    0
}
