//@ run-pass
//@ description: Editable<T> at a parameter with `with ZD` must check.

fn r(x: Editable<f64>) -> f64 with ZD { 0.0 }

fn main() -> i32 with IO, ZD {
    let _z = r(1.0 as Editable<f64>)
    0
}
