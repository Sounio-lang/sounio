//@ run-pass
//@ description: The same parameter-position ExactlyPrivate with `with ZD`
//@   must still check. This file is also the always-refuse positive control:
//@   a stub that reports E201 unconditionally must fail it.

fn r(x: ExactlyPrivate<f64>) -> f64 with ZD { 0.0 }

fn main() -> i32 with IO, ZD {
    let _z = r(1.0 as ExactlyPrivate<f64>)
    0
}
