//@ compile-fail
//@ error-pattern: Editable<T> requires ZD
//@ description: Editable<T> at a parameter must emit E202 without `with ZD`.

fn r(x: Editable<f64>) -> f64 { 0.0 }

fn main() -> i32 {
    let _z = r(1.0 as Editable<f64>)
    0
}
