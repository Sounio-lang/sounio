//@ compile-fail
//@ error-pattern: Interpretable<T> requires ZD
//@ description: Interpretable<T> at a parameter must emit E207 without `with ZD`.

fn r(x: Interpretable<f64>) -> f64 { 0.0 }

fn main() -> i32 {
    let _z = r(1.0 as Interpretable<f64>)
    0
}
