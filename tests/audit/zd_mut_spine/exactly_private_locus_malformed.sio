//@ compile-fail
//@ error-pattern: ZD locus
//@ description: e3e3 names one unit twice. A privacy claim made AT it must
//@   be refused (E208), not silently accepted.

fn release(x: ExactlyPrivate<f64, e3e3>) -> f64 with ZD { 0.0 }

fn main() -> i32 with IO, ZD {
    let _z = release(1.0 as ExactlyPrivate<f64, e3e3>)
    0
}
