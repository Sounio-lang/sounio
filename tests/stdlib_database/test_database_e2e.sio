// stdlib/database E2E — ASPIRATIONAL STUB
//
// stdlib/database uses Vec<T>, String (capital), for-in loops, impl, and Result<T,E> —
// none of which exist in the running Sounio compiler (lean_single.sio).
// This file is documentation of the intended API, not a runnable test.
//
// To make this runnable, stdlib/database must be rewritten using real Sounio primitives:
//   - [string; N] fixed arrays instead of Vec<String>
//   - string (lowercase) instead of String
//   - while + index instead of for-in
//   - module-level globals instead of struct fields holding collections
//
// See stdlib/database/README.md for design intent.

fn main() -> i32 with IO {
    print("stdlib_database: aspirational stub — not yet runnable\n")
    0
}