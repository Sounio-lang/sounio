//@ run-pass
//@ expect-stdout: MEM_ARENA_OK
use mem::arena::*;

fn main() -> i32 with IO {
    var a = arena_new()
    if arena_allocated_bytes(&a) != 0 { return 1 }
    arena_drop(&!a)
    println("MEM_ARENA_OK")
    0
}
