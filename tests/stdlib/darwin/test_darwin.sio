//@ run-pass
//@ expect-stdout: DARWIN_ATLAS_OK
use darwin::atlas::*;

fn main() -> i32 with IO, Mut, Div, Panic {
    if complement_base(0 as u8) != (3 as u8) { return 1 }
    println("DARWIN_ATLAS_OK")
    0
}
