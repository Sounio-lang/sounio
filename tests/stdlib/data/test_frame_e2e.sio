//@ run-pass
//@ expect-stdout: DATA_FRAME_OK
use data::frame::*;

fn main() -> i32 with IO {
    let df = dataframe_new()
    if dataframe_ncols(df) != 0 { return 1 }
    println("DATA_FRAME_OK")
    0
}
