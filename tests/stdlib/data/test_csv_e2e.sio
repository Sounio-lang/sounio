//@ run-pass
//@ expect-stdout: DATA_CSV_LOADER_OK
use data::csv_loader::*;

fn main() -> i32 with IO, Mut, Div, Panic {
    let csv = "0,1\n"
    let net = parse_edge_list(csv)
    if net.weighted { return 1 }
    println("DATA_CSV_LOADER_OK")
    0
}
