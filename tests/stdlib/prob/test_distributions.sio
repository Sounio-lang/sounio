//@ run-pass
//@ expect-stdout: PROB_DISTRIBUTIONS_OK
use prob::distributions::*;

fn main() -> i32 with IO, Mut, Div, Panic {
    if gamma_mean(2.0, 1.0) != 2.0 { return 1 }
    println("PROB_DISTRIBUTIONS_OK")
    0
}
