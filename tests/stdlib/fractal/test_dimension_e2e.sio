//@ run-pass
//@ expect-stdout: FRACTAL_STATS_OK
use fractal::fsnn::*;
fn main() -> i32 with IO {
    var xs: [f64; 256] = [0.0; 256]
    xs[0] = 1.0; xs[1] = 2.0; xs[2] = 3.0
    if fsnn_stat_mean(xs, 3) != 2.0 { return 1 }
    println("FRACTAL_STATS_OK")
    0
}
