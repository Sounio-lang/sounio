//@ run-pass
//@ timeout: 600
// The coherent rate-uncertainty band must not scale with sqrt(dt).
use chemistry::gri30_h2::*
fn main() -> i32 with IO, Mut, Div, Panic {
    if test_g30_epistemic_step_invariance() { print("GRI30 H2 STEP INVARIANCE PASS\n"); 0 }
    else { print("FAIL g30 epistemic step invariance\n"); 1 }
}
