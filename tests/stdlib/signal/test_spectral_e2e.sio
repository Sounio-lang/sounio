//@ run-pass
//@ expect-stdout: SIGNAL_SPECTRAL_OK
use signal::spectral::*;

fn main() -> i32 with IO {
    let psd = psd_result_new()
    if psd.n_freqs != 0 { return 1 }
    println("SIGNAL_SPECTRAL_OK")
    0
}
