//@ run-pass
//@ expect-stdout: GUM_API_OK
use epistemic::gum::*;
fn main() -> i32 with IO, Mut, Div, Panic {
    let g = gum_simple(1.0, 0.1)
    if gum_value(g) != 1.0 { return 1 }
    println("GUM_API_OK")
    0
}
