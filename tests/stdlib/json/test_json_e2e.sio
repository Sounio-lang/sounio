//@ check-only
//@ contract-adjustment: json module currently exposes stub surface only; test now verifies import/module contract
// E2E contract check: stdlib/json currently provides module declaration only.

use json::*

fn main() -> i32 {
    return 0
}
