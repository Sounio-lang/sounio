//@ run-pass
//@ expect-stdout: PROFILE_CONTRACT_OK
use profile::profiler::*;

fn main() -> i32 with IO {
    let p = profiler_new()
    if profiler_slot_count(p) != 0 { return 1 }
    println("PROFILE_CONTRACT_OK")
    0
}
