// A-refine boundary: guarded arm for A but NO arm covers B at all -> not
// exhaustive -> must REJECT (all-A-guards-false would leave res uninitialized).
enum E { A{x:i64}, B }
fn main()->i64 {
    let e:E = E::A{x:3}
    match e {
        E::A{x} if x>10 => 1,
        E::A{x} => x
    }
}
