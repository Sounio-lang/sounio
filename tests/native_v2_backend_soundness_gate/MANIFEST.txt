# Backend soundness gate manifest. Shared across the native-v2 backend
# silent-miscompile fixes (bugs A/B/C). APPEND idempotently — never rewrite
# wholesale; each fix adds only its own witness lines.
#
# Format (whitespace-separated):
#   <witness_name>  <expected>  <bug>
#
# <expected> is one of:
#   <int>                   well-typed program; --check OK AND ELF runs to exit <int>.
#   REJECT                  well-typed-by-frontend but unlowerable; --native-v2-compile
#                           MUST emit NO ELF and exit nonzero (conservative reject, no
#                           miscompile). --check is NOT asserted (the frontend may say OK).
#   KNOWN_MISCOMPILE:<int>  a DEFERRED/known-bad bug that STILL silently miscompiles:
#                           --check says OK but the ELF runs to the WRONG value <int>.
#                           These do NOT count toward the green N/N. run.sh asserts the
#                           bug is still live, prints a LOUD "UNRESOLVED SILENT
#                           MISCOMPILES" line listing them, and exits nonzero if any
#                           remain. (R5: never lie by omission.)
#
# <bug> is a short tag (A-guards, B-enum-disc, C-field-hash) for grouping in output.

# ===== bug A — match-arm guards (LANDED) =====
guard_witness_taskA                                7        A-guards
g_true_guard_fires                                 100      A-guards
g_intlit_guard_false_falls_through                 2        A-guards
g_intlit_guard_true                                11       A-guards
g_cascade_multiple_guards                          2        A-guards
g_noguard_control                                  50       A-guards
g_bool_guard                                       2        A-guards
g_enum_structvariant_guard_binding_read_true       1        A-guards
g_enum_structvariant_guard_binding_read_false      2        A-guards
g_some_guard_falls_through                          2        A-guards
g_nonexh_guard_no_catchall_REJECT                  REJECT   A-guards
g_nonexh2_all_guarded_REJECT                       REJECT   A-guards
g_enum_all_guarded_or_enumarms_conservative_REJECT REJECT   A-guards

# ===== bug B — enum discriminant by (enum,variant) full path (LANDED) =====
# Two enums sharing a variant name: name-only lookup returned the first enum's
# disc; the full-path lookup keys on (enum,variant). discB_unit_collision is the
# canonical witness (Color::Mark must run to 30, not the colliding Shape::Mark).
discB_unit_collision                               30       B-enum-disc
discB_collision_red                                10       B-enum-disc
discB_collision_green                              20       B-enum-disc
discB_reversed_order_collision                     5        B-enum-disc
discB_tagged_collision                             99       B-enum-disc
# Documented parser semantics (NOT a bug, unchanged by the fix): a bare
# capitalized ident pattern is PatBinding (always-match), so the first arm wins
# -> 10. Asserted as a normal expectation so the gate does not lie by omission.
discB_bare_binding_control                         10       B-enum-disc
