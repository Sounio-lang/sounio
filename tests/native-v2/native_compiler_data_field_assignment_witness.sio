//@ run-pass
use native::elf::{StringTable}
use native::frame::{compiler_new}

fn main() -> i64 with Mut {
    var nc = compiler_new()
    var st: StringTable = nc.data
    st.len = 8
    if st.len == 8 {
        return 0
    }
    return 1
}
