struct A {
    shared: i64,
    pad: i64,
}

struct B {
    pad: i64,
    shared: i64,
}

fn make_b() -> B {
    B { pad: 11, shared: 42 }
}

fn main() -> i64 {
    let typed = make_b()
    typed.shared
}
