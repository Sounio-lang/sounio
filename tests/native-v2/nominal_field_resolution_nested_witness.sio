struct Inner {
    pad: i64,
    shared: i64,
}

struct Outer {
    inner: Inner,
}

fn make_outer() -> Outer {
    Outer { inner: Inner { pad: 11, shared: 42 } }
}

fn main() -> i64 {
    make_outer().inner.shared
}
