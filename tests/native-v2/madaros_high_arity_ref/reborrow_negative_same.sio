fn sink(out: &![i64; 2]) with Mut, Panic {
    out[0] = 42
}

fn rejected_reborrow(out: &![i64; 2]) with Mut, Panic {
    sink(&!out)
}

fn main() with Mut, Panic {
    var out = [0; 2]
    rejected_reborrow(&!out)
}
