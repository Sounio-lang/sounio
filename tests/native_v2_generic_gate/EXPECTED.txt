# native_v2_generic_gate expected exit codes (positives) / REJECT (rejects).
# Generic fns via type-erasure + call-site inference. Positives must emit ELF and exit with
# the code; rejects must FAIL --check (no ELF, no crash).
# p01-p05: id over i64 / arith / string(pointer) / two-param-consistent / concrete+generic mix.
p01_id_i64 42
p02_id_arith 42
p03_id_string_len 5
p04_two_param_consistent 40
p05_mixed_concrete_generic 42
# r01: inconsistent T (i64 vs string) -> consistency reject (proves REAL binding, not wildcard).
r01_inconsistent_tp REJECT
# r02: generic result used ill-typed (id(5):i64 -> string) -> substituted result caught downstream.
r02_illtyped_result REJECT
# r03/r04: wide-int / float instantiation -> rejected (type-erasure unsound at non-GP width).
r03_i128_width REJECT
r04_f64_width REJECT
