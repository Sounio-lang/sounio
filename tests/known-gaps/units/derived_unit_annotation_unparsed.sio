// EXPECTED: FAILS to parse on both engines: derived unit types (mol/cm3, cal/mol) are not in the grammar.
use units::*;

fn main() with IO, Mut, Div, Panic {
    let t: K = 1500.0
    let n: mol = 0.0000081
    let v: cm3 = 1.0
    let c: mol/cm3 = n / v
    let ea: cal/mol = 16812.0
    let r: cal/mol/K = 1.9872042586408316
    let x = ea / (r * t)
    println("madaros: derived units bind")
    let bad = c + t
}
