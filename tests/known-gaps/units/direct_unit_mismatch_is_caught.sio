// EXPECTED: FAILS `souc check` on both engines (control for the file beside it).
fn main() with IO, Mut, Div, Panic {
    let n: mol = 0.0000081
    let t: K = 1500.0
    let bad_direct = n + t
    println("never reached")
}
