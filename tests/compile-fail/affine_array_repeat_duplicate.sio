//@ compile-fail
//@ requires: madaros
//@ error-pattern: affine value has already been used

affine struct Permit {
    id: i64,
}

fn main() -> i32 {
    let permit = Permit { id: 173 }
    let values = [permit; 2]
    0
}
