//@ compile-fail
//@ error-pattern: observation boundary without `with Observe`

fn get_reading() -> Unobserved<f64> {
    37.5
}

// This function compares Unobserved<f64> without declaring with Observe.
// The checker should reject this.
fn bad_analyze(x: Unobserved<f64>) -> bool {
    x > 36.0
}

fn main() -> i32 {
    0
}
