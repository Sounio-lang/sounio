//@ compile-fail
//@ error-pattern: Correlated
//@ description: R6 positive control fixture (run under SOUNIO_FORCE_CORRELATED=1
//@   by scripts/ci/correlated_effect_gate.sh). Independent m1+m2 must then refuse.

fn main() -> i32 with IO, Epistemic {
    let m1 = measure(500.0, uncertainty: 2.5)
    let m2 = measure(100.0, uncertainty: 1.0)
    let s = m1 + m2
    0
}
