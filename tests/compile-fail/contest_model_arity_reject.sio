//@ compile-fail
//@ known-failure: no diagnostic is emitted today — measured 2026-08-30, this file compiles cleanly to an ELF on BOTH lean_single and Madaros (rc=0). The rejection this test asserts is not implemented; the test was dormant because it carried no //@ annotation, so the gap was never visible.
// Model aliases require exactly an input and an output type.

type BadModel = Model<i64>

fn main() -> i64 {
    0
}
