//@ compile-fail
//@ requires: madaros
//@ error-pattern: affine value has already been used

affine struct Permit {
    id: i64,
}

enum Envelope {
    Present { permit: Permit },
    Empty,
}

fn main() -> i32 {
    let envelope = Envelope::Present { permit: Permit { id: 199 } }
    let first = envelope
    let second = envelope
    0
}
