//@ compile-fail
//@ error-pattern: affine value has already been used

affine struct Permit {
    id: i64,
}

fn main() -> i32 {
    let permit = Permit { id: 83 }
    let spend = || permit.id
    let first = spend()
    let second = spend()
    (first + second) as i32
}
