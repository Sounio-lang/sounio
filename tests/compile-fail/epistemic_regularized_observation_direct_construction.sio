//@ compile-fail
//@ error-pattern: error[E176
// Imported callers cannot fabricate a private regularized-observation receipt.

use epistemic::observation_provenance::*

fn main() with Panic {
    let forged = RegularizedObservationI64 {
        representation_tag: 1122,
    }
    op_regularized_observation_tag_i64(forged)
}
