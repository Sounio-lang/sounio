//@ compile-fail
//@ requires: madaros
//@ error-pattern: undeclared
// Negative witness: a ValidWhile(expr) predicate must be resolved (and
// type-checked to bool) in its declaration scope, not silently accepted.
// `undeclared_var` is not bound anywhere in this function.

ontology ChemicalOntology {
    class Chemical
    class CHEBI_15377 subclass_of Chemical
}

fn uses_unresolved_validwhile(w: Knowledge[CHEBI_15377, ValidWhile(undeclared_var), Derived]) {
}

fn main() {
}
