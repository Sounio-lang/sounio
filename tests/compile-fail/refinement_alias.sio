//@ compile-fail
//@ error-pattern: type mismatch
//@ description: Type alias with refinement resolves to base type for compatibility checks

type Positive = { x: i64 | x > 0 };

fn main() {
    let p: Positive = "oops"   // ERROR: alias base type is i64
}
