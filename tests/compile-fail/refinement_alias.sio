//@ compile-fail
//@ ignore
//@ error-pattern: type
//@ description: Type alias with refinement (type alias refinements not fully implemented)

type Positive = { x: i32 | x > 0 }

fn main() {
    let p: Positive = 42
}
