//@ compile-fail
//@ error-pattern: E056
//@ ignore
//@ description: aleatoric uncertainty cannot be implicitly treated as epistemic

fn get_sensor_reading() -> f64 {
    // This represents aleatoric (irreducible) noise
    9.81
}

fn improve_model(x: f64) -> f64 {
    x
}

fn main() -> i64 with IO {
    42
}
