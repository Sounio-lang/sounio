//@ compile-fail
//@ error-pattern: unit mismatch in call argument
//@ description: internal dimension labels must participate in dimensional call-boundary checks

unit mg_dL;
unit mmol_L;

fn takes_potassium_mmol_l(x: mmol_L) -> f64 with Mut {
    x
}

fn main() -> i32 with IO, Mut, Panic, Div {
    let glucose = 140.0<mg_dL>
    let bad = takes_potassium_mmol_l(glucose)
    return 0
}
