//@ compile-fail
//@ error-pattern: private struct field access

use epistemic::path::*

fn main() with Panic {
    let measured = ep_measured_gum_i64(42000, 980)
    let payload = ep_payload_from_measured_gum(measured)
    let raw_value = payload.value_i64

    assert(raw_value == 42000)
}
