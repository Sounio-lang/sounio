//@ compile-fail
//@ description: V0-B still rejects f128 arithmetic — ops deferred to V0-D
//@ ladder-stage: V0-B
//@ semantic-lane: WS-G-V0B-LITERALS-CHECK
//
// After V0-B, type spellings/literals may parse, but source `+` on f128 must
// still fail check. Today (V0-A) this fails earlier with E218 on the type name.
// Gate requires: NOT `check: OK`.

fn add128(a: f128, b: f128) -> f128 {
    a + b
}

fn main() -> i32 {
    0
}
