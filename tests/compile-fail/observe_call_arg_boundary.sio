//@ compile-fail
//@ error-pattern: Unobserved<T>

// Passing Unobserved<T> to a function expecting T requires `with Observe`.

fn consume(x: f64) -> f64 {
    x
}

fn leak(u: Unobserved<f64>) -> f64 {
    consume(u)
}

fn main() -> i64 with IO {
    0
}
