//@ compile-fail
//@ error-pattern: ExactlyPrivate
//@ description: ExactlyPrivate<T> requires the ZD effect; without `with ZD`
//@   the compiler emits error code 201 because no classical (division-algebra)
//@   operation can algebraically zero a training-data contribution.

fn unsafe_remove(user_contrib: ExactlyPrivate<f64>) -> f64 with IO {
    user_contrib
}

fn main() -> i32 with IO {
    0
}
