//@ compile-fail
//@ requires: madaros
//@ error-pattern: affine value has already been used

affine struct Permit {
    id: i64,
}

fn invalid_return(permit: Permit) -> Permit {
    let returned = permit
    let invalid = permit
    invalid
}

fn main() {
    let permit = Permit { id: 17 }
    let result = invalid_return(permit)
}
