//@ compile-fail
//@ error-pattern: Knightian uncertainty (ε=⊥) cannot satisfy required confidence
// March 2020 typed refusal: Knightian input cannot satisfy a high-confidence gate.

fn authorize_lockdown(signal: Knowledge[f64, ε >= 0.95]) -> Knowledge[f64, ε >= 0.95] {
    signal
}

fn main() with IO {
    let march_unknown_r_t: Knowledge[f64, ε=⊥] = Knowledge { value: 2.4 }
    let _decision = authorize_lockdown(march_unknown_r_t)
}
