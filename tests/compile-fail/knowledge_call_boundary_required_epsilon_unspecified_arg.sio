//@ compile-fail
//@ error-pattern: E036
// Once a parameter DOES require an epsilon bound, an argument whose epsilon is
// unspecified must still be refused: the boundary fails closed on an unknown
// provided epsilon. This pins that half of the rule, independent of epsilon's
// polarity (docs/audit/EPSILON_POLARITY_FORK_2026-08-19.md).

fn prescribe(dose: Knowledge[f64, ε >= 0.82]) -> i64 {
    return 1
}

fn main() -> i32 {
    let unknown: Knowledge<f64> = Knowledge { value: 500.0 }
    let r = prescribe(unknown)
    return 0
}
