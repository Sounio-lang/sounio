//@ compile-fail
//@ error-pattern: type
//@ description: Conditioning on collider opens non-causal path — type mismatch

struct CausalEffect { value: f64 }

fn collider_error() -> i64 with IO {
    25
}

fn main() -> i64 with IO {
    let y: CausalEffect = collider_error()
    42
}
