//@ compile-fail
//@ error-pattern: error[E206]: parameter uses Revivable<T> without
//@ description: Revivable<T> requires both `with ZD` and `with Temporal`;
//@   without Temporal, the compiler emits error 206 because there is no
//@   bounded window during which the surgery can be inverted.  Without
//@   the Temporal effect the reversibility guarantee collapses to a
//@   regular surgical op (G10).

fn unsafe_revive(a: Revivable<f64>) -> f64 with IO, ZD {
    a
}

fn main() -> i32 with IO {
    0
}
