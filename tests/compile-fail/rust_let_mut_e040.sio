//@ compile-fail
//@ error-pattern: Sounio uses 'var' instead of 'let mut'
// E040: Rust `let mut` is rejected with a helpful diagnostic (use `var`).
fn main() -> i32 with IO {
    let mut x = 0
    x as i32
}
