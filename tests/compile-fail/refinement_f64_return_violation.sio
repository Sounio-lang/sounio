//@ compile-fail
//@ error-pattern: refinement type violation
//@ description: PR-10 — f64 literal 1.7 violates return type pred p >= 0.0 && p <= 1.0

fn probability() -> { p: f64 | p >= 0.0 && p <= 1.0 } {
    return 1.7  // ERROR: 1.7 > 1.0
}

fn main() {
    let _ = probability()
}
