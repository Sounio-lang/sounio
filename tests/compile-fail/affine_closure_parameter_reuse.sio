//@ compile-fail
//@ error-pattern: affine value has already been used

affine struct Permit {
    id: i64,
}

fn main() -> i32 {
    let duplicate = |permit: Permit| permit.id + permit.id
    duplicate(Permit { id: 107 }) as i32
}
