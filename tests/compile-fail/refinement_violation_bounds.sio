//@ compile-fail
//@ ignore
//@ error-pattern: refinement
//@ description: Value outside bounds violates refinement

fn takes_bounded(x: { v: i32 | v >= 0 && v <= 100 }) -> i32 {
    x
}

fn main() {
    let result = takes_bounded(150)  // ERROR: 150 violates v <= 100
}
