//@ compile-fail
//@ error-pattern: refinement type violation
//@ description: PR-6 — AND compound predicate with bounds range: 150 violates v <= 100

fn takes_bounded(x: { v: i32 | v >= 0 && v <= 100 }) -> i32 {
    x
}

fn main() {
    let result = takes_bounded(150)  // ERROR: 150 >= 0 but 150 > 100, violates v <= 100
}
