//@ compile-fail
//@ requires: madaros
//@ error-pattern: invalid Knowledge metadata constraint
// Negative witness: ValidUntil(date) requires a parsed date string, not a
// bare number -- ValidUntil(30.0) must not be silently accepted as
// timestamp 30.

ontology ChemicalOntology {
    class Chemical
    class CHEBI_15377 subclass_of Chemical
}

fn uses_number_as_deadline(w: Knowledge[CHEBI_15377, ValidUntil(30.0), Derived]) {
}

fn main() {
}
