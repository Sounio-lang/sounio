//@ compile-fail
//@ error-pattern: Type mismatch in call argument

use epistemic::path::*

fn main() with Panic {
    let imputed = ep_imputed_model_i64(42000, 980)
    let payload = ep_payload_from_imputed_model(imputed)
    ep_accept_erased_for_display(payload)
}
