//@ compile-fail
//@ requires: madaros
//@ error-pattern: return value does not match
// Soundness witness: a closure's declared return type annotation must get
// the same authoritative four-axis enforcement as a named function's return.
// A closure declared to return Knowledge<T, Valid(30), Measured> must not be
// satisfiable by a body producing only Knowledge<T, Valid(10), Derived>.

ontology ChemicalOntology {
    class Chemical
    class CHEBI_15377 subclass_of Chemical
}

fn test_closure_return_downgrade(stale: Knowledge[CHEBI_15377, Valid(10.0), Derived]) {
    let f = || -> Knowledge[CHEBI_15377, Valid(30.0), Measured] {
        stale
    }
}

fn main() {
}
