//@ compile-fail
//@ error-pattern: incompatible unit dimensions
//@ ignore
//@ description: BLOCKED - requires units of measure

// Casting between incompatible unit dimensions (mass -> length) should fail

unit mg;
unit m;

fn main() -> i32 with Mut, Panic, Div {
    let dose: mg = 500.0
    let bad: m = dose as m
    return 0
}
