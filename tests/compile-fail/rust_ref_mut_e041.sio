//@ compile-fail
//@ error-pattern: error[E041]: Sounio uses '&!' instead of '&mut'
// E041: Rust `&mut T` is rejected with a helpful diagnostic (use `&!T`).
// See rust_let_mut_e040.sio: a bare `E041` matched the compiler's own
// `source: .../rust_ref_mut_e041.sio` echo line, so the fixture passed on its
// filename. The tag plus the text cannot be matched by accident.
fn takes(a: &mut i64) with Mut { }
fn main() -> i32 with IO { 0 }
