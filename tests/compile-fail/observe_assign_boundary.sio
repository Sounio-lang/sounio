//@ compile-fail
//@ error-pattern: Unobserved<T>

// Assigning Unobserved<T> to a plain T binding requires `with Observe`.

fn collapse(x: Unobserved<f64>) -> f64 {
    let y: f64 = x
    y
}

fn main() -> i64 with IO {
    0
}
