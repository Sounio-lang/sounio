//@ compile-fail
//@ error-pattern: affine value has already been used

affine struct Permit {
    id: i64,
}

fn duplicate(value: Option<Permit>) -> (Option<Permit>, Option<Permit>) {
    (value, value)
}

fn main() -> i32 {
    0
}
