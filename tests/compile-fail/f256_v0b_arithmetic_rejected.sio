//@ compile-fail
//@ known-failure: lean_single-only gap — lean_single compiles f256 `+`/`==` to an ELF with no E218; Madaros V0-A rejects at parser with error[E218].
//@ description: f256 arithmetic/comparison must not be accepted as a finished surface (V0-A: E218; V0-B+: still no source ops until V0-D)
//@ error-pattern: error[E218]
//@ ladder-stage: V0-B
//@ semantic-lane: WS-G-V0B-LITERALS-CHECK
//
// FINDING (2026-08-17, #1767): lean_single accepts these forms; Madaros does not.

fn add256(a: f256, b: f256) -> f256 {
    a + b
}

fn equal256(a: f256, b: f256) -> bool {
    a == b
}

fn main() -> i32 {
    0
}
