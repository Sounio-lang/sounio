//@ compile-fail
//@ requires: madaros
//@ error-pattern: affine value has already been used

affine struct Permit {
    id: i64,
}

fn duplicate(value: Box<Permit>) -> (Box<Permit>, Box<Permit>) {
    (value, value)
}

fn main() -> i32 {
    0
}
