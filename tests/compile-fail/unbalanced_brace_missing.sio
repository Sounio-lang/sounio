//@ compile-fail
//@ error-pattern: unbalanced braces in module
//@ error-pattern: unclosed `{`
// #1634 companion to unbalanced_brace_extra.sio: the other direction.
//
// This one matters for WHERE it points. Reporting at the end of the token
// stream is useless -- the tail of a bundle is the appended native prelude,
// whose line numbers mean nothing to whoever wrote the file. The first attempt
// at this fix did exactly that and said `<main>:372` for a three-line file.
// It reports at the `{` that was never closed instead.

fn main() -> i64 with IO {
    print_int(7)
    0
