//@ compile-fail
//@ requires: madaros
//@ error-pattern: invalid Knowledge metadata constraint
// Negative witness: Valid(seconds) requires a numeric literal, not a date
// string -- Valid("2026-01-01") must not be silently accepted and
// reinterpreted as a 20260101-second numeric duration.

ontology ChemicalOntology {
    class Chemical
    class CHEBI_15377 subclass_of Chemical
}

fn uses_string_as_duration(w: Knowledge[CHEBI_15377, Valid("2026-01-01"), Derived]) {
}

fn main() {
}
