//@ compile-fail
//@ error-pattern: error[E216]
// compile-fail: E216 — infinite recursive type
// expected: error[E216]

struct Node {
    val: i64,
    next: Node,
}

fn main() -> i64 with IO, Panic { 0 }
