//@ compile-fail
//@ error-pattern: E067
//@ description: Causal function calling Observe without Audit triggers confounding error

fn observe_outcome() -> f64 with Observe, IO {
    42.0
}

fn confounded_ate() -> f64 with Causal, Observe, IO {
    // Missing Audit: causal inference from observational data without audit trail
    let y = observe_outcome()
    y * 2.0
}

fn main() -> i64 with IO {
    confounded_ate()
    0
}
