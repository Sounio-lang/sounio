//@ compile-fail
//@ description: V0-B keeps f128 and f256 distinct — no implicit conversion
//@ ladder-stage: V0-B
//@ semantic-lane: WS-G-V0B-LITERALS-CHECK
//
// Gate requires: NOT `check: OK`. Stops V0-B from silently growing into a
// widen-everything change.

fn widen(x: f128) -> f256 {
    x
}

fn narrow(x: f256) -> f128 {
    x
}

fn main() -> i32 {
    0
}
