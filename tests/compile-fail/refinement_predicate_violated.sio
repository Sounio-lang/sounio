//@ compile-fail
//@ error-pattern: refinement predicate violated
//@ ignore

// A refinement type constrains values to satisfy a predicate
type Positive = { x: i64 | x > 0 };

fn main() -> i32 with Mut, Panic, Div {
    let p: Positive = 0    // ERROR: 0 does not satisfy x > 0
    return 0
}
