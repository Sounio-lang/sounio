//@ compile-fail
//@ error-pattern: refinement type violation

// A refinement type constrains values to satisfy a predicate.
// This checks the failing witness directly on the binding.

fn main() -> i32 with Mut, Panic, Div {
    let p: { x: i64 | x > 0 } = 0    // ERROR: 0 does not satisfy x > 0
    return 0
}
