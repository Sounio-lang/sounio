//@ compile-fail
//@ error-pattern: unit mismatch in call argument
//@ description: A Length value must not satisfy a f64<m/s> velocity parameter.

fn takes_velocity(v: f64<m/s>) -> f64 {
    return v
}

fn main() -> i32 with IO, Mut, Panic, Div {
    let distance: m = 10.0
    let bad = takes_velocity(distance)
    return 0
}
