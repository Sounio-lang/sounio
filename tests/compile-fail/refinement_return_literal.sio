//@ compile-fail
//@ error-pattern: refinement type violation
//@ description: PR-5 — return of literal violating the declared refinement return type

type Pos = { v: i32 | v > 0 }

fn make_pos() -> Pos {
    return -5  // ERROR: -5 violates v > 0
}

fn main() {
    let _ = make_pos()
}
