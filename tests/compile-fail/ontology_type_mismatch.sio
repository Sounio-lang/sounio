//@ compile-fail
//@ error-pattern: cannot prove ontology subsumption

ontology Bio {
    class Drug
    class Food
}

fn treat(d: Drug) -> i32 { 0 }

fn main() -> i32 {
    let f: Food = make_food()
    treat(f)
    0
}
