//@ compile-fail
//@ error-pattern: E219
// R2 narrow-integer recusa: width `i7` is not in the spec set {8, 16, 32, 64, 128};
// the parser must reject the type annotation before checker, IR, SOIR, ABI,
// or native lowering.
fn main() -> i7 with IO {
    0
}
