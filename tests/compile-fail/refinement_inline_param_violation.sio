// refinement_inline_param_violation
//@ compile-fail
//@ error-pattern: refinement type violation

fn positive_only(x: { n: i32 | n > 0 }) -> i32 {
    x
}

fn main() {
    let result = positive_only(0 - 5)
}
