//@ compile-fail
//@ error-pattern: cannot cast epistemic type
//@ description: casting Knowledge<T> to T via `as` is blocked — epistemic wrapping cannot be silently stripped

fn strip_uncertainty(k: Knowledge<f64>) -> f64 {
    k as f64
}

fn main() -> i64 with IO {
    0
}
