//@ compile-fail
//@ ignore BLOCKED: pinned binary does not reject Rust syntax in all cases
//@ error-pattern: &!
//@ description: Sounio uses &! not &mut for mutable references

fn increment(x: &mut i32) {  // ERROR: use &! instead of &mut
    x = x + 1
}

fn main() {
    var n = 0
    increment(&mut n)
}
