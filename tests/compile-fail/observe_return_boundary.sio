//@ compile-fail
//@ error-pattern: Unobserved<T>

// Returning Unobserved<T> as a non-Unobserved type requires `with Observe`.

fn peek(x: Unobserved<f64>) -> f64 {
    x
}

fn main() -> i64 with IO {
    0
}
