//@ compile-fail
//@ error-pattern: refinement type violation
//@ description: Negative value violates positive refinement

fn takes_positive(x: { n: i32 | n > 0 }) -> i32 {
    x
}

fn main() {
    let result = takes_positive(-5)  // ERROR: -5 violates n > 0
}
