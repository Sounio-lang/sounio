//@ compile-fail
//@ description: V0-B still rejects f64↔f128 casts — explicit conversion is later
//@ ladder-stage: V0-B
//@ semantic-lane: WS-G-V0B-LITERALS-CHECK
//
// Gate requires: NOT `check: OK`.

fn cast_up(x: f64) -> f128 {
    x as f128
}

fn cast_down(x: f128) -> f64 {
    x as f64
}

fn main() -> i32 {
    0
}
