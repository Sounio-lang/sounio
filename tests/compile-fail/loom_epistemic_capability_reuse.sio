//@ compile-fail
//@ error-pattern: E039

use coordination::loom_epistemic_machine::{
    LinearWriteCapability,
    release_linear_write_capability,
}

fn reuse(capability: LinearWriteCapability) -> i64 {
    let first = release_linear_write_capability(capability)
    let second = release_linear_write_capability(capability)
    first + second
}

fn main() -> i64 { 0 }
