//@ compile-fail
// NOTE: kept as compile-fail (not typecheck-fail): the source-built
// Madaros raw-AST parser cannot parse this test's imported module
// (model_contest / endogenous_observability) -> closure incomplete, no
// diagnostic emitted. Guarantee verified on prebuilt souc. Re-pin once the
// parser handles these modules. See audit 2026-07-25.
use epistemic::proof_carrying_endogenous_observability::*

fn require_recoverability(r: GlobalRecoverabilityReceipt) -> i64 { 0 }

fn main() {
    let equivalence = ObservedTraceEquivalenceReceipt {
        equivalence_id: 1, left_hypothesis_id: 311, right_hypothesis_id: 312,
        left_mode: 1, right_mode: 2, left_hidden_target_value: 2,
        right_hidden_target_value: 8, original_custody_traces_equal: true,
        legacy_tokens_equal: true, retry_predictions_differ: true,
        empirically_identified_from_original_trace: false,
        global_recoverability_established: false,
        missingness_taxonomy_established: false,
    }
    let _illegal = require_recoverability(equivalence)
}
