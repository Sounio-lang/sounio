//@ compile-fail
//@ error-pattern: ontology classes are disjoint

ontology Bio {
    class Drug
    class Rapamycin subclass_of Drug
    class Food
    disjoint Drug, Food
}

fn accept(f: Food) -> i32 { 0 }

fn reject(r: Rapamycin) -> i32 {
    accept(r)
}
