//@ compile-fail
//@ error-pattern: refinement type violation
//@ description: PR-4 — fn-call with immutable var binding propagating literal violates param refinement

type Pos = { v: i32 | v > 0 }

fn takes_pos(x: Pos) -> i32 {
    x
}

fn main() {
    let bad = -7
    let r = takes_pos(bad)  // ERROR: -7 propagated through let, violates v > 0
}
