//@ compile-fail
//@ error-pattern: error[E202]: parameter uses Editable<T> without
//@ description: Editable<T> requires the ZD effect. Without `with ZD` the
//@   compiler emits error code 202 because locality-bounded edits rely on the
//@   4D ZD kernel; rank-1 and mask-based updates leak into orthogonal subspaces.

fn unsafe_edit(fact: Editable<f64>) -> f64 with IO {
    fact
}

fn main() -> i32 with IO {
    0
}
