//@ compile-fail
//@ error-pattern: ontology classes are disjoint

ontology Medicine {
    class Drug
    class Food
    disjoint Drug, Food
}

fn treat(d: Drug) -> i32 { 0 }

fn main() -> i32 {
    let f: Food = make_food()
    treat(f)
    0
}
