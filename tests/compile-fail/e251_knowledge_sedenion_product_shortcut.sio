//@ compile-fail
//@ requires: madaros
//@ error-pattern: product variance shortcut is unsound beyond the octonions
//
// Third axis of anti-garbling (formal/lean4/EpistemicEffectsNSA.lean §J–§K). The product
// of two Knowledge<Hyper<Sedenion, f64>> values propagates variance by the GUM shortcut
// Var(x·y) = |y|²·Var x + |x|²·Var y, which silently imposes |xy| = |x||y|. That identity
// is Hurwitz: true on R, C, H, O (`norm_mult_of_polarBasis`), FALSE on the sedenions
// (`not_polarBasis4`). Kernel-checked witness: d = e1+e10 on one source, y = e4+e15 exact —
// disjoint sources, one product, no re-association — the shortcut reports 4, the true
// first-order variance is 8 (`sed_shortcut_understates`). Precision fabricated with a clean
// bill from E230. The rule refuses the product (E251); addition is untouched.
//
// Measured 2026-09-01 on the committed Madaros (md5 ff69dae4): `check: OK`. This fixture
// exists because that was wrong.

fn g(a: Knowledge<Hyper<Sedenion, f64>>, b: Knowledge<Hyper<Sedenion, f64>>) -> Knowledge<Hyper<Sedenion, f64>> with Epistemic, NonAssoc, Div {
    a * b
}

fn main() -> i64 with IO { 0 }
