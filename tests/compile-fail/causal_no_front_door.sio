//@ compile-fail
//@ error-pattern: type
//@ description: No front-door or backdoor adjustment possible — direct confounding

struct CausalEffect { value: f64 }

fn get_value() -> i64 with IO {
    100
}

fn main() -> i64 with IO {
    let effect: CausalEffect = get_value()
    42
}
