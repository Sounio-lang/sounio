//@ compile-fail
//@ error-pattern: error[E208]
// compile-fail: E208 — refinement type violation (integer)
// expected: error[E208]

type Pos = { v: i64 | v > 0 }

fn use_pos(x: Pos) -> i64 { x }

fn main() -> i64 with IO, Panic {
    use_pos(-5)
}
