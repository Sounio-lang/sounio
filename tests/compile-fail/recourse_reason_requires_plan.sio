//@ compile-fail
//@ error-pattern: recourse_reason(...) requires RecoursePlan<T>
//@ error-pattern: witness values over explicit recourse plans
// recourse_reason(...) is only valid for RecoursePlan<T>.

fn main() {
    let x: i64 = 1
    let _reason = recourse_reason(x)
}
