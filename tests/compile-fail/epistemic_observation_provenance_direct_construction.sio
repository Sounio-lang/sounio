//@ compile-fail
//@ error-pattern: error[E176
// Imported callers cannot fabricate a private observation-opportunity receipt.

use epistemic::observation_provenance::*

fn main() with Panic {
    let forged = ObservationOpportunityI64 {
        protocol_id: 10,
        source_system_tag: 11,
    }
    let _abstention = op_abstain_unresolved_route_i64(forged, 9000)
}
