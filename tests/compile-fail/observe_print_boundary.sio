//@ compile-fail
//@ error-pattern: Unobserved<T>

// Printing Unobserved<T> is IO that collapses uncertainty — requires `with Observe`.

fn spy(x: Unobserved<i64>) with IO {
    print_int(x)
}

fn main() -> i64 with IO {
    0
}
