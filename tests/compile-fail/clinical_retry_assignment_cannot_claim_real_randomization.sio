//@ typecheck-fail
//@ requires: madaros
//@ error-pattern: error[E009
// Madaros raw-AST parser cannot parse this test's imported module
// (model_contest / endogenous_observability) -> closure incomplete, no
// diagnostic emitted. Guarantee verified on prebuilt souc. Re-pin once the
// parser handles these modules. See audit 2026-07-25.
use epistemic::proof_carrying_endogenous_observability::*

fn require_real(r: RealPersonRandomizationReceipt) -> i64 { 0 }

fn main() {
    let assignment = SyntheticExogenousRetryAssignmentReceipt {
        assignment_id: 1, protocol_id: 8300, retry_tick: 3,
        retry_window_ticks: 0, exogenously_assigned_within_fixture: true,
        real_person_randomization_claimed: false, consent_authority: false,
        clinical_authority: false,
    }
    let _illegal = require_real(assignment)
}
