//@ compile-fail
//@ error-pattern: private struct literal

use epistemic::path::*

fn main() with Panic {
    let imputed = ep_imputed_model_i64(42000, 980)
    let payload = ep_payload_from_imputed_model(imputed)

    let forged = MeasuredGUMI64 {
        payload: payload,
    }

    assert(ep_accept_measured_gum(forged))
}
