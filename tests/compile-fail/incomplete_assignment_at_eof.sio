//@ compile-fail
//@ error-pattern: syntax appears incomplete after assignment

// The other half of the pre-scan contract: a genuinely unfinished assignment must still be
// rejected. Making the scan comment-aware must not make it blind.

fn main() with IO, Mut, Panic, Div {
    let x =
}
