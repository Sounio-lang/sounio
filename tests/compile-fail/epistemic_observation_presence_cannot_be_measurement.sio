//@ compile-fail
//@ error-pattern: argument type does not match parameter
// Channel presence cannot be passed to the recorded-measurement accessor.

use epistemic::observation_provenance::*

fn main() with Panic {
    let presence = op_encounter_presence_i64(20, 21)
    op_recorded_measurement_tag_i64(presence)
}
