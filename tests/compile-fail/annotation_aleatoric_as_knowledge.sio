//@ compile-fail
//@ description: Aleatoric<f64> and Knowledge<f64> are distinct — type mismatch

fn get_noise() -> f64 with IO {
    0.1
}

fn check_knowledge(k: Knowledge<f64>) -> f64 with IO {
    0.0
}

fn main() -> i64 with IO {
    let noise: Aleatoric<f64> = get_noise()
    let result = check_knowledge(noise)
    42
}
