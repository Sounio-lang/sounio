//@ compile-fail
//@ error-pattern: unit mismatch
//@ description: energy units with compatible dimensions still require explicit conversion before arithmetic

fn main() -> i32 with IO, Mut, Panic, Div {
    let joules: J = 1.0
    let electron_volts: eV = 2.0
    let bad = joules + electron_volts
    return 0
}
