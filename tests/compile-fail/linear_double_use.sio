//@ compile-fail
//@ error-pattern: moved
//@ description: Linear value cannot be used twice

linear struct Token {
    id: i32,
}

fn consume(t: Token) {
    // consumes the token
}

fn main() {
    let tok = Token { id: 1 }
    consume(tok)   // first use - moves tok
    consume(tok)   // ERROR: tok already consumed/moved
}
