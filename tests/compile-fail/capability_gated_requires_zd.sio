//@ compile-fail
//@ error-pattern: CapabilityGated
//@ description: CapabilityGated<T> requires the ZD effect. Without `with ZD`
//@   the compiler emits error code 203 because capability containment requires
//@   exact ZD annihilation — activation patching and masking leave residual.

fn unsafe_remove_capability(cap: CapabilityGated<f64>) -> f64 with IO {
    cap
}

fn main() -> i32 with IO {
    0
}
