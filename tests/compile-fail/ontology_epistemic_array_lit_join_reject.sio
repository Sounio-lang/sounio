//@ compile-fail
//@ requires: madaros
//@ error-pattern: array elements must all have the same type
// Soundness witness: array-literal element unification is a join site, same
// as if/else and match-arm joins. A list literal mixing a strongly-valid
// element with a weakly-valid element (identical ontology and epsilon) must
// not be silently accepted and typed by whichever element happened to come
// first.

ontology ChemicalOntology {
    class Chemical
    class CHEBI_15377 subclass_of Chemical
}

fn test_array_lit_join_metadata_mismatch(
    strong: Knowledge[CHEBI_15377, Valid(60.0), Measured],
    weak: Knowledge[CHEBI_15377, Valid(10.0), Derived]
) {
    let arr = [strong, weak]
}

fn main() {
}
