//@ compile-fail
//@ error-pattern: unit mismatch in call argument

unit acceleration = m / s / s;

fn takes_acceleration(a: acceleration) -> f64 with Mut {
    a
}

fn main() -> i32 with IO, Mut, Panic, Div {
    let distance: m = 18.0
    let time: s = 3.0
    let speed = distance / time
    let bad = takes_acceleration(speed)
    return 0
}
