//@ compile-fail
//@ error-pattern: not consumed
//@ description: Linear value must be consumed before going out of scope

linear struct Handle {
    fd: i32,
}

fn close(h: Handle) {
    // consumes the handle
}

fn main() {
    let h = Handle { fd: 42 }
    // ERROR: h is not consumed before scope exit
}
