//@ compile-fail
//@ error-pattern: refinement type violation
//@ description: PR-2 — named-type alias propagates refinement predicate

type Pos = { n: i32 | n > 0 }

fn main() {
    let a: Pos = -5  // ERROR: -5 violates n > 0 via Pos alias
}
