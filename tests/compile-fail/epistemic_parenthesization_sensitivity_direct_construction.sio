//@ compile-fail
//@ error-pattern: error[E176
// Imported callers cannot fabricate a parenthesization-sensitivity receipt.

use epistemic::parenthesization_receipts::*

fn main() with Panic {
    let forged = ParenthesizationSensitivityI64 {
        discriminating_feature_tag: 7002,
    }
    pr_parenthesization_sensitivity_feature_tag_i64(forged)
}
