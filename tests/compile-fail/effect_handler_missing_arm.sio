//@ compile-fail
//@ ignore
//@ description: SELFHOST_PASS
//@ error-pattern: effect

effect Counter {
    fn increment() -> i64
    fn reset() -> i64
}

fn use_counter() with Counter {
    // calls both ops — but handler is missing 'reset'
}

fn main() {
    use_counter()  // ERROR: Counter effect not declared
}
