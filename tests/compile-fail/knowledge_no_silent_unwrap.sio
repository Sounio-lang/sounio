//@ compile-fail
//@ error-pattern: Knowledge
// Knowledge<T> must not implicitly coerce to T.

fn main() {
    // measure(...) is the checked constructor path for a Knowledge<f64> value;
    // assigning it to f64 must remain an explicit type error.
    let k: Knowledge<f64> = measure(1.0, uncertainty: 0.1)
    let x: f64 = k
}
