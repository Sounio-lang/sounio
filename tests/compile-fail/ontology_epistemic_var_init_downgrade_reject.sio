//@ compile-fail
//@ requires: madaros
//@ error-pattern: this binding expects a different type
// Negative witness: a `var` initializer, not just `let`, must go through the
// same authoritative four-axis subsumption check. A `var` declared with a
// 30s validity window must not be initializable from a value only valid for
// 10s, even though the inner ontology type and epsilon are identical.
//
// The diagnostic here is the generic E001 ("this binding expects a
// different type"), not the axis-specific E263 call boundaries get -- see
// ontology_epistemic_assign_downgrade_reject.sio for why. The four-axis
// REJECTION is what this fixture proves.

ontology ChemicalOntology {
    class Chemical
    class CHEBI_15377 subclass_of Chemical
}

fn test_var_init_validity_downgrade(stale: Knowledge[CHEBI_15377, Valid(10.0), Derived]) {
    var w: Knowledge[CHEBI_15377, Valid(30.0), Derived] = stale
}

fn main() {
}
