//@ compile-fail
//@ error-pattern: arity mismatch
//@ description: acknowledge() requires exactly 2 arguments (value + reason string)

fn main() with IO {
    let k = measure(42.0, uncertainty: 2.5)
    let v: f64 = acknowledge(k)
}
