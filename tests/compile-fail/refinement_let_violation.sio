//@ compile-fail
//@ error-pattern: refinement type violation
//@ description: Let-binding value violates refinement type constraint

fn main() {
    let pos: { x: i32 | x > 0 } = -5  // ERROR: -5 violates x > 0
}
