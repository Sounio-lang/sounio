//@ compile-fail
//@ requires: madaros
//@ error-pattern: invalid Knowledge metadata constraint
// Negative witness: a ValidWhile(expr) predicate must type-check to bool.
// ValidWhile(42) is an integer literal, not a condition, and must be
// rejected rather than silently persisted as an enforceable predicate.

ontology ChemicalOntology {
    class Chemical
    class CHEBI_15377 subclass_of Chemical
}

fn uses_nonbool_validwhile(w: Knowledge[CHEBI_15377, ValidWhile(42), Derived]) {
}

fn main() {
}
