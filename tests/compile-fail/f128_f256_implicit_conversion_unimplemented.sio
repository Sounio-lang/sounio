//@ compile-fail
//@ description: f128 and f256 are distinct and never implicitly converted
//@ error-pattern: f128/f256 is reserved
// The pattern names the diagnostic, not the number. E218 is claimed by two
// different diagnostics -- check.sio's f128/f256 refusal and lean_single's
// codegen "tail type mismatch" -- so a fixture keyed on the bare code passes
// under lean_single for an unrelated reason. Keyed on the text it cannot.

fn widen(x: f128) -> f256 { x }
fn main() -> i32 { 0 }
