//@ compile-fail
//@ error-pattern: Array repeat count must be a compile-time constant
//@ description: Card A parser boundary: named const array dimensions parse, checker constant-eval still rejects them.

const PARSER_CARD_A_N: i64 = 4

fn main() -> i64 with Mut, Panic, Div {
    var xs: [i64; PARSER_CARD_A_N] = [0; PARSER_CARD_A_N]
    xs[0]
}
