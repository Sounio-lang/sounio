//@ compile-fail
//@ error-pattern: ontology classes are disjoint

ontology Bio {
    class Drug
    class Food
    disjoint Drug, Food
}

fn accept(d: Drug) -> i32 { 0 }

fn reject(f: Food) -> i32 {
    accept(f)
}
