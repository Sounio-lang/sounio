//@ compile-fail
// A GO root coerced via the bridge must NOT satisfy a descendant-typed parameter.
use ontology::typed::go::*
fn needs_signal_transduction(x: GO_0007166) -> i32 { 1 }
fn main() -> i32 with IO {
    let iri = go_biological_process()                 // GO_0008150 (root)
    let root = iri_as_go_biological_process(&iri)     // nominal GO_0008150
    let r = needs_signal_transduction(root)           // root is NOT <: GO_0007166 -> E152
    print_int(r)
    0
}
