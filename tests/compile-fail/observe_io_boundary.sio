//@ compile-fail
//@ error-pattern: cannot print Unobserved<T>
fn leak(x: Unobserved<f64>) with IO {
    // Should require 'with Observe' — passing Unobserved<T> to IO function
    println(x)
}
fn main() -> i32 with IO { 0 }
