//@ compile-fail
//@ error-pattern: cannot take exclusive borrow

// Cannot take two exclusive borrows of the same variable
fn main() -> i32 with Mut {
    var x: i64 = 42
    let a = &!x    // exclusive borrow
    let b = &!x    // ERROR: another exclusive borrow while first is active
    return 0
}
