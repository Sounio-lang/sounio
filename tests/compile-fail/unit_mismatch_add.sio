//@ compile-fail
//@ ignore BLOCKED: pinned binary resolver does not register unit decls as types
//@ error-pattern: unit mismatch

// Cannot add values with different units
fn main() -> i32 with IO, Mut, Panic, Div {
    let mass: mg = 500.0
    let length: m = 10.0
    let bad = mass + length    // ERROR: mg + m is dimensionally inconsistent
    return 0
}
