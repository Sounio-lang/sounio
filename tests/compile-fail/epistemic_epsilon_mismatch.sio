//@ compile-fail
//@ error-pattern: E170

// Callee requires `with Epistemic` for .value (E170). Epsilon subsumption at call
// boundaries with measure()+annotation is not yet enforced — see
// tests/ui/type/epistemic_call_boundary_invalid_provided_epsilon.sio for the
// intended P0003 rejection with explicit Knowledge literals (blocked on
// Knowledge() constructor in the modular driver).

fn needs_precise(k: Knowledge<f64, epsilon < 0.05>) -> f64 {
    k.value
}

fn main() -> i32 with IO, Epistemic {
    let loose: Knowledge<f64, epsilon < 0.2> = measure(500.0, uncertainty: 100.0)
    let result = needs_precise(loose)
    return 0
}