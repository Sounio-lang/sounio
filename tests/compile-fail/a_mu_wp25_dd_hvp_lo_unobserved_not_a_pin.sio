//@ compile-fail
//@ error-pattern: Unobserved<T>
// Stand-in for "pretend WP25 published a data-driven LO". Unobserved<f64>
// still cannot become a pin without Observe. This file is self-contained
// because Madaros does not wrap literals into Unobserved in stdlib (E008).
// 0.0 is not a WP25 estimate.

fn fake_wp25_dd_hvp_lo() -> Unobserved<f64> {
    0.0
}

fn as_pin(x: Unobserved<f64>) -> f64 {
    x
}

fn main() -> i64 {
    0
}
