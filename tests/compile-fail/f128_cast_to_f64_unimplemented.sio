//@ compile-fail
//@ known-failure: lean_single-only gap; source-fresh Madaros E218 is owned by scripts/ci/madaros_f128_f256_format_identity_gate.sh until fixed-point rebootstrap
//@ description: f128 cannot be cast to f64 before explicit wide conversion exists
//@ error-pattern: error[E218]

fn cast_down(x: f128) -> f64 { x as f64 }
fn main() -> i32 { 0 }
