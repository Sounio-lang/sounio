//@ compile-fail
//@ error-pattern: effect not declared in function signature
//@ description: SELFHOST_PASS — function using a session channel must declare the Session effect

session Greeter {
    send i64,
    recv i64,
    close
}

// This function uses a channel but declares no Session effect — should be rejected.
fn use_channel(ch: chan Greeter) -> i64 {
    0
}

fn main() -> i64 {
    0
}
