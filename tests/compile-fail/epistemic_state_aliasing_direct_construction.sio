//@ compile-fail
//@ error-pattern: error[E176
// Imported callers cannot fabricate an observed-proxy receipt.

use epistemic::state_aliasing_receipts::*

fn main() with Panic {
    let forged = ObservedProxyI64 {
        history_tag: 1001,
        source_system_tag: 11,
        observed_proxy_tag: 9001,
    }
    sar_observed_proxy_tag_i64(forged)
}
