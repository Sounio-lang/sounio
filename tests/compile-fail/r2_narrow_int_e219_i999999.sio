//@ compile-fail
//@ error-pattern: E219
// R2 narrow-integer recusa: width `i999999` is not in the spec set
// (it is far larger than the largest spec width, i128).
fn main() -> i999999 with IO {
    0
}
