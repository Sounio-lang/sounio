//@ compile-fail
//@ requires: madaros
//@ error-pattern: Temporal validity window mismatch
// Soundness witness: `as` between two epistemic wrappers must not be a bare
// escape hatch. Casting a value only valid for 10s to an annotation claiming
// validity for 30s must be rejected, the same as it would be at a let
// binding, return, or call argument -- a cast must not let a value claim
// stronger epistemic guarantees than it actually earned.

ontology ChemicalOntology {
    class Chemical
    class CHEBI_15377 subclass_of Chemical
}

fn test_cast_validity_escalation(stale: Knowledge[CHEBI_15377, Valid(10.0), Derived]) {
    let claimed = stale as Knowledge[CHEBI_15377, Valid(30.0), Derived]
}

fn main() {
}
