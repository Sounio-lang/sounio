//@ compile-fail
//@ error-pattern: arity mismatch
//@ description: require_confidence() requires exactly 2 arguments

fn main() with IO {
    let k = measure(42.0, uncertainty: 2.5, confidence: 0.98)
    let v: f64 = require_confidence(k)
}
