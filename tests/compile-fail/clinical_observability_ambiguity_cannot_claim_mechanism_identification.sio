//@ typecheck-fail
//@ requires: madaros
//@ error-pattern: error[E009
// Madaros raw-AST parser cannot parse this test's imported module
// (model_contest / endogenous_observability) -> closure incomplete, no
// diagnostic emitted. Guarantee verified on prebuilt souc. Re-pin once the
// parser handles these modules. See audit 2026-07-25.
use epistemic::proof_carrying_endogenous_observability::*

fn require_identification(r: DeclaredResponseMechanismIdentificationReceipt) -> i64 { 0 }

fn main() {
    let ambiguity = EndogenousObservabilityAmbiguityReceipt {
        ambiguity_id: 1, family_id: 320, survivor_mask: 6, survivor_count: 2,
        target_independent_hypothesis_survives: true,
        target_dependent_hypothesis_survives: true,
        custody_process_partially_identified: true,
        response_mechanism_identified: false, latent_target_observed: false,
        biological_mechanism_established: false, clinical_authority: false,
    }
    let _illegal = require_identification(ambiguity)
}
