//@ compile-fail
//@ error-pattern: refinement type violation
//@ description: PR-4 — fn-call with literal arg violating param refinement predicate

type Pos = { v: i32 | v > 0 }

fn takes_pos(x: Pos) -> i32 {
    x
}

fn main() {
    let r = takes_pos(-5)  // ERROR: -5 violates v > 0
}
