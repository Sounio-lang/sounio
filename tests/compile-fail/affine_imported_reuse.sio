//@ compile-fail
//@ requires: madaros
//@ error-pattern: affine value has already been used

use fixtures::affine_ownership_leaf::{imported_permit, imported_spend}

fn main() -> i32 {
    let permit = imported_permit(73)
    let first = imported_spend(permit)
    let second = imported_spend(permit)
    (first + second) as i32
}
