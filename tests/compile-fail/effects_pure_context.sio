//@ compile-fail
//@ description: SELFHOST_PASS
//@ error-pattern: effect not declared in function signature
//@ error-pattern: not declared
//@ description: Effectful operation in pure function context

fn read_input() -> string with IO {
    // reads from stdin
    ""
}

fn pure_compute(x: i32) -> i32 {
    // This function has no effect annotation, so it should be pure
    let s = read_input()  // ERROR: IO effect not declared
    x + 1
}

fn main() with IO {
    let result = pure_compute(42)
}
