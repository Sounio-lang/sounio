// Rustless Regression Test 08: String Operations
//
// Tests: string literals, string comparison, string operations
//
// Note: String support is limited in bootstrap phase.
// This test focuses on what's currently available.

fn string_length_test() -> i64 {
    // Currently testing that strings compile correctly
    // Full string operations will be tested once stdlib is available
    let msg = "hello"
    42  // Placeholder return
}

fn main() -> i64 with IO {
    // Test that string literals compile and don't crash
    let s1 = "test"
    let s2 = "rustless"
    let s3 = "cutover"

    // Test string_length_test function
    let r1 = string_length_test()
    if r1 != 42 { return 1 }

    // For now, just verify strings don't cause crashes
    // More comprehensive string tests will come with stdlib

    0  // All tests passed
}
