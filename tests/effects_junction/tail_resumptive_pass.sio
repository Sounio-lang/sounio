//@ run-pass
//@ expect-stdout: TR 5
// Fast-path fixture: a tail-resumptive handler. The clause value IS the perform
// value (single tail resume). This is the ONLY resume strategy expressible today.
// The full certified demonstrator is examples/effect_uncertainty_gum_vs_mc.sio.
fn main() -> i32 with IO {
    let r = handle<Epistemic> {
        Epistemic.add(2, 3)
    } with {
        let add = |a: i64, b: i64| { a + b }
    }
    print("TR ")
    println(r)
    0
}
