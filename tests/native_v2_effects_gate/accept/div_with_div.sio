// ACCEPT: integer division requires `with Div`, which main declares.
// Cardinal twin of reject/div_no_div.sio — identical except the `with Div` clause.
fn main() -> i64 with Div {
    let a = 10
    let b = 2
    a / b
}
