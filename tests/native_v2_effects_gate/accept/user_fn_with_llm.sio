// ACCEPT (firewall OFF): a user-space function may declare `with LLM` freely — the
// stochastic-oracle effect is a first-class citizen outside the compiler's codegen path.
// Cardinal twin of reject/codegen_fn_with_llm.sio: identical source, checked with
// SOUNIO_CODEGEN_FIREWALL unset (=0), so the build-mode firewall does not fire.
fn main() -> i64 with LLM {
    0
}
