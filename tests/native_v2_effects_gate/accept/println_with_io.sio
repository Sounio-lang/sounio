// ACCEPT: body calls the IO output builtin `println`, and main declares `with IO`.
// Cardinal twin of reject/println_no_io.sio — identical except the `with IO` clause.
fn main() -> i64 with IO {
    println("hello")
    0
}
