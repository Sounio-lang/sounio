// REJECT (E035, firewall ON): checked with SOUNIO_CODEGEN_FIREWALL=1, the build-mode
// codegen firewall forbids the stochastic `LLM` effect on the deterministic codegen
// path. Cardinal twin of accept/user_fn_with_llm.sio — identical source; only the
// SOUNIO_CODEGEN_FIREWALL flag differs. The firewall fires on direct declaration of
// LLM(22)/Prob(7) in checker_check_fn_item_inplace.
fn main() -> i64 with LLM {
    0
}
