// REJECT (E035): body calls the IO output builtin `println` but main is pure (no `with IO`).
// Cardinal twin of accept/println_with_io.sio — identical except the missing effect clause.
// POST-HOOK enforcement: the direct-use require-effect hook at the call site
// (call_expr_is_output_builtin -> checker_require_effect_inplace id 0).
fn main() -> i64 {
    println("hello")
    0
}
