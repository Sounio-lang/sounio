// REJECT (E035): integer division `a / b` requires `with Div`, but main is pure.
// Cardinal twin of accept/div_with_div.sio — identical except the missing `with Div`.
// POST-HOOK enforcement: the require-effect hook in checker_check_binary_op_types_inplace
// (OpDiv/OpRem -> checker_require_effect_inplace id 4).
fn main() -> i64 {
    let a = 10
    let b = 2
    a / b
}
