// Wide mod by a single-limb divisor: (15 * 2^64) % 7 = 2.
// (3*2^32)*(5*2^32) = 15*2^64 -> limbs lo=0, hi=15; % 7 -> 2 (high limb decides).
// A low-limb-only (broken) impl would compute 0 % 7 = 0.
fn main() -> i64 {
    ((((12884901888 as i128) * (21474836480 as i128)) % (7 as i128)) as i64)
}
