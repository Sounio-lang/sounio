// i128 VARIABLE used in wide arithmetic (not the inline-cast form): let x = 2^32, x*x = 2^64,
// (x*x)>>64 == 1. Proves x is tracked as wide (a narrow i64 x would give x*x=2^64 mod 2^64=0).
fn main() -> i64 {
    let x: i128 = (4294967296 as i128)
    (((x * x) >> 64) as i64)
}
