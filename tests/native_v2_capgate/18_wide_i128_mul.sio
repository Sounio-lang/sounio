// i128 multiply producing a >64-bit result: 2^32 * 2^32 = 2^64.
// >>64 (limb-aligned) extracts the high limb; `as i64` returns it = 1.
// A 64-bit (i64) compile would truncate the product to 0.
fn main() -> i64 {
    ((((4294967296 as i128) * (4294967296 as i128)) >> 64) as i64)
}
