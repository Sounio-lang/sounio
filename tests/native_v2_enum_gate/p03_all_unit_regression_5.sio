// Pure all-unit enum match (no payload variants): protects the bare-int path the
// self-hosted compiler relies on for its own IrOpcode/ExprKind matches.
enum E { A, B }
fn main() -> i64 {
    let e = E::B;
    match e {
        E::A => 1,
        E::B => 5,
    }
}
