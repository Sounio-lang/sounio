// == on a tagged enum vs a variant path. The boxed-pointer representation would
// miscompile this to a pointer-identity compare. Now checker-REJECTED (no ELF).
enum E { A, C { x: i64, y: i64 } }
fn main() -> i64 {
    let e = E::A;
    if e == E::A { 1 } else { 0 }
}
