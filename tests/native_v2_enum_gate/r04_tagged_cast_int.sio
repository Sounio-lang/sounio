// `as i64` on a tagged enum. The boxed-pointer value would yield the pointer, not
// the discriminant (silent miscompile). Now checker-REJECTED (no ELF). R1 closure.
enum E { A, C { x: i64, y: i64 } }
fn main() -> i64 {
    let e = E::A;
    e as i64
}
