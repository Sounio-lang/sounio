// Plain struct keeps the untouched 64-slot layout (full-path keying prevents a
// plain struct from being misread as a tagged variant).
struct Point { x: i64, y: i64 }
fn main() -> i64 {
    let p = Point { x: 20, y: 22 };
    p.x + p.y
}
