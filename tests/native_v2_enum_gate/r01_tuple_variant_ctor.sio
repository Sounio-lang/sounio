// Tuple-variant construction that type-checks (payload unused). At base this
// miscompiled to SIGSEGV (exit 139, ir_call to a nonexistent fn). Now REJECTED
// in lower.sio (no ELF). R1 closure.
enum Event { A, B(i64) }
fn main() -> i64 {
    let e = Event::B(9);
    match e {
        Event::A => 0,
        Event::B(n) => 7,
    }
}
