// Tagged enum returned from a fn then matched: proves the handle survives the
// return ABI.
enum E { A, C { x: i64, y: i64 } }
fn mk() -> E { E::C { x: 7, y: 35 } }
fn main() -> i64 {
    match mk() {
        E::A => 0,
        E::C { x, y } => x + y,
    }
}
