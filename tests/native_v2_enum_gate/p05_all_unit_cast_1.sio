// All-unit enum `as i64` must still yield the discriminant (E::B = 1). Only
// payload-carrying enums reject the cast (boxed pointer would miscompile).
enum E { A, B }
fn main() -> i64 {
    let e = E::B;
    e as i64
}
