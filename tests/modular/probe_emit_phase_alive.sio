// Modular phase liveness probe — uses emit/ phase as a library.
// Prints a single line via the modular pipeline.

use emit::mod::*

fn main() with IO, Mut, Panic, Div, Alloc {
    emit_set_silent(true)
    emit_set_silent(false)
    println("modular emit phase: ALIVE")
}
