//@ run-pass
//@ expect-stdout: declared measure precedence: passed

fn measure(value: i64, delta: i64) -> i64 {
    value + delta
}

fn main() -> i64 with IO, Panic {
    let result = measure(40, 2)
    assert(result == 42)
    println("declared measure precedence: passed")
    0
}
