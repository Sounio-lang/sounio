// Unimportable witness: epistemic::knowledge (Knowledge<T> / Epistemic) segfaults
// the native compiler on import (self-contained, cause TBD). Expected: compile FAILS.
use epistemic::knowledge::*
fn main() -> i32 with IO, Mut, Div, Panic {
    print_int(ep_decay(1000, 0.5))
    print("\n")
    return 0
}
