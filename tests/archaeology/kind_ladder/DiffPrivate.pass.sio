//@ run-pass
//@ requires: madaros
//@ description: Kind-ladder pass fixture for TyDiffPrivate — as-cast inhabits the keyword TypeKind and identity preserves it.

fn id(x: DiffPrivate<f64>) -> DiffPrivate<f64> {
    x
}

fn main() -> i64 {
    let a = 1.0 as DiffPrivate<f64>
    let b = id(a)
    0
}
