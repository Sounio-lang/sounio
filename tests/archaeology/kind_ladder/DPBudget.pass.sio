//@ run-pass
//@ requires: madaros
//@ description: Kind-ladder pass fixture for TyDPBudget — as-cast inhabits the keyword TypeKind and identity preserves it.

fn id(x: DPBudget<f64>) -> DPBudget<f64> {
    x
}

fn main() -> i64 {
    let b0 = 1000.0 as DPBudget<f64>
    let b1 = id(b0)
    0
}
