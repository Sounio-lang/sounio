# compile_fail_parity_baseline.txt — arm64 mirror checks still missing.
#
# Each line is a tests/compile-fail case that the x86 target REFUSES and the
# aarch64-linux target still ACCEPTS. Read by
# scripts/ci/a64_compile_fail_parity_gate.sh.
#
# Measured 2026-09-04 against lean_single with the TYPECHECK_FAILED gate in
# compile_all_arm64 and the hard arity check at the arm64 general-call site.
# Before the gate, the hard arity check and the literal-tracking mirror, this
# list held 265 entries.
#
# These are NOT tolerated behaviour. Each one is a check the x86 pass has and
# the arm64 mirror does not. Removing a line belongs in the commit that adds
# the missing check, together with the fixture that proves it fires on both
# targets. Never add a line to make the gate pass.
# The eight gtt_* entries below are a REGRESSION introduced by the hessian_of
# mirror (ba6edc523f), not a pre-existing gap. Before it they were refused
# because hessian_of was an unknown identifier; now the dispatch exists and its
# GTT refusal cannot fire.
#
# CORRECTION to what 98de6da2f6's message said. That message claimed
# MEAS_KNOW_IDX "has no a64 site at all". It does -- in compile_postfix_tail_a64's
# `.value` block, which maintains MEAS_KNOW_IDX, CHANNEL_INPUT_VAR_SLOT and
# EXPR_SSHADOW. I read that from a grep truncated by `head -14`, which showed only
# the x86 hits. What is actually missing at that site is one line,
# `EXPR_CH_SET = 1 << (MEAS_KNOW_IDX - 1)`.
#
# Adding that line alone does NOT fix this, and the measurement is the point:
# it closes all eight entries below and BREAKS about ten run-pass programs --
# drift_gate_hessian_of, epistemic_hessian_of, epistemic_hessian_8inputs,
# epistemic_hessian_two_arg, epistemic_hessian_transcendentals,
# epistemic_second_order_variance_sanity, gtt_loop_topology, gtt_nary_topology,
# gtt_interprocedural_topology, gtt_body_precision_param_usage. The topology then
# exists but does not propagate, so legitimate queries look out-of-topology.
# LOOP_SNAP_CH_SET, the loop-carried channel merge, has three x86 sites and zero
# on a64. The seed is not the fix; the whole beta-9 chain is.
#
gtt_body_precision_unused_param_refused
gtt_hessian_out_of_topology
gtt_indirect_call_out_of_topology
gtt_let_bound_out_of_topology
gtt_loop_wrong_channel
gtt_match_bound_var_out_of_topology
gtt_nary_out_of_topology
gtt_reassignment_wrong_channel
