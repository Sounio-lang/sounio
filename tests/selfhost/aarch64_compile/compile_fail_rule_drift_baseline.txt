# compile_fail_rule_drift_baseline.txt
#
# Cases where BOTH targets refuse, but report different primary diagnostics.
# The exit-code probe this gate replaced could not observe this at all: two
# nonzero exit codes were parity regardless of what the compilers said.
#
# Pinned 2026-09-06 at 6 cases over the 392-file lean_single-relevant corpus,
# down from 63 before #2438 gave tc_undefined_var the E200 code. That one
# change retired 57 of them: they were never 57 problems, they were one shared
# helper emitting a finding without a code while the x86 inline site emitted it
# with one. Triage in #2423.
#
# What is left reads better than what it replaced. Four cases moved from
# `X -> none` to `X -> E200`, which is a stronger statement than "aarch64 said
# nothing": it says the aarch64 path falls through to "unknown identifier"
# instead of applying the check the fixture pins.
#
#   parser_rust_let_mut       E040 -> E200
#   parser_rust_macro         E043 -> E200
#   rust_let_mut_e040         E040 -> E200
#   robust_not_validated      E221 -> E200
#   tuple_pattern_arity_e213  E200 -> none    one uncoded path remains
#   causal_non_identifiable   E067 -> E035    a real missing check (#2429)
#
# KNOWN COARSENESS: the comparison is on the primary diagnostic code only. An
# aarch64 refusal that is semantically identical but printed without a code
# lands here as drift.
#
# Do not add lines to make the gate pass; a new pair is a regression.
#
# Format: <case>\t<x86 primary diagnostic>\t<aarch64 primary diagnostic>
causal_non_identifiable	E067	E035
parser_rust_let_mut	E040	E200
parser_rust_macro	E043	E200
robust_not_validated	E221	E200
rust_let_mut_e040	E040	E200
tuple_pattern_arity_e213	E200	none
