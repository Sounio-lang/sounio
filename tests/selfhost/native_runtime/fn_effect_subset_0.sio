fn double(x: i64) -> i64 {
    x * 2
}

fn choose(which: i64) -> fn(i64) -> i64 with Mut, Panic, Div {
    if which == 0 { double } else { double }
}

fn main() -> i64 with Mut, Panic, Div {
    let f = choose(0)
    let y = f(21)
    0
}
