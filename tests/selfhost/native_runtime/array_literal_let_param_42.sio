fn take(xs: [i64; 4]) -> i64 with Panic, Div {
    xs[0 as usize] + xs[1 as usize] + xs[2 as usize] + xs[3 as usize]
}

fn main() -> i64 with Panic, Div {
    let xs: [i64; 4] = [10, 10, 10, 12]
    take(xs)
}
