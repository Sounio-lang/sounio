struct Pair {
    a: i64,
    b: i64,
}

fn main() -> i32 with Mut, Panic, Div {
    var buf: [i64; 2] = [0; 2]
    let raw = (&!buf) as *mut Pair
    (*raw).a = 10
    (*raw).b = 32
    return (buf[0] + buf[1]) as i32
}
