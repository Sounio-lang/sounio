//@ run-pass

fn make() -> [f64; 3] with Mut, Panic, Div {
    var xs: [f64; 3] = [0.0, 0.0, 0.0]
    xs[1] = 42.5
    return xs
}

fn main() -> i64 with Mut, Panic, Div {
    let ys = make()
    if ys[1 as usize] > 42.0 {
        return 42
    }
    return 0
}
