fn base() -> i64 {
    let seed: i64 = 17 + 20
    let lift = seed + 1
    return lift
}

fn answer() -> i64 {
    let start = base()
    let bonus = 4
    let total = start + bonus
    return total
}
