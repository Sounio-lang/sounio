// Narrow imported-HOF ABI witness: named fn refs plus fn-typed
// params/returns across an imported module boundary. No captured closures.

fn double(x: i64) -> i64 {
    x * 2
}

fn apply(f: fn(i64) -> i64, x: i64) -> i64 {
    f(x)
}

fn pass_through(f: fn(i64) -> i64) -> fn(i64) -> i64 {
    f
}

fn select_unary(which: i64) -> fn(i64) -> i64 with Mut, Panic, Div {
    if which == 0 { double } else { double }
}

fn entry(f: fn(i64) -> i64, x: i64) -> i64 {
    apply(f, x)
}
