//@ run-pass

struct Map<K, V> {
    key: K,
    val: V,
}

struct Wrapper<T> {
    inner: T,
    tag: i64,
}

fn main() -> i64 {
    let m = Map { key: 1, val: 99 }
    let w = Wrapper { inner: m.val, tag: 1 }
    w.inner + w.tag
}
