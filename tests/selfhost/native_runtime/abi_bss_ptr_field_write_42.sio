struct Pair {
    a: i64,
    b: i64,
}

var GBUF: [i64; 2] = [0; 2]

fn main() -> i32 with Mut, Panic, Div {
    let raw = (&!GBUF) as *mut Pair
    (*raw).a = 10
    (*raw).b = 32
    return (GBUF[0] + GBUF[1]) as i32
}
