use import_body_lowering::mod::{entry, make_triple, pass_through, select_unary}

fn main() -> i64 with Mut, Panic, Div {
    let triple = make_triple()
    let selected = select_unary(0)
    let kept = pass_through(selected)
    entry(kept, 11) + triple.a + triple.c
}
