//@ run-pass
//@ description: Axiom<T> type annotation parses and type-checks

fn assume_continuity(x: f64) -> f64 with IO {
    x
}

fn main() -> i64 with IO {
    let a: Axiom<f64> = assume_continuity(1.0)
    42
}
