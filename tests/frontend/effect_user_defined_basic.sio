//@ run-pass

// User-defined algebraic effect declarations parse correctly.
// Sprint 16/E1: effect keyword with semicoloned op defs.

effect Storage {
    fn get(key: i64) -> i64;
    fn put(key: i64, val: i64) -> i64;
}

// A second user-defined effect
effect Logger {
    fn log(level: i64) -> i64;
}

fn main() -> i64 {
    0
}
