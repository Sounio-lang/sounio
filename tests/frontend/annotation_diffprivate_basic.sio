//@ run-pass
//@ description: DiffPrivate<T> type annotation parses and type-checks

fn dp_mechanism(x: f64) -> f64 with IO {
    x + 0.1
}

fn main() -> i64 with IO {
    let result: DiffPrivate<f64> = dp_mechanism(42.0)
    42
}
