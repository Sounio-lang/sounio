//@ run-pass
//@ description: basic session type declarations parse correctly

// Sprint 19/Track S: session protocol declarations with send/recv/close steps.

session PingPong {
    send i64,
    recv i64,
    close
}

session RequestReply {
    send i64,
    recv bool,
    close
}

fn main() -> i64 {
    0
}
