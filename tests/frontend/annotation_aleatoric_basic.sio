//@ run-pass
//@ description: Aleatoric<T> type annotation parses and type-checks

fn sensor_noise(reading: f64) -> f64 with IO {
    reading + 0.01
}

fn main() -> i64 with IO {
    let noise: Aleatoric<f64> = sensor_noise(25.3)
    42
}
