//@ run-pass
//@ description: Sprint 52 — conservative loop LICM frontend hoist fixture

fn main() -> i64 {
    var acc: i64 = 0
    var i: i64 = 0
    while i < 3 {
        let bias = 1 + 2
        let widened = bias + 4
        acc = acc + widened
        i = i + 1
    }
    acc
}
