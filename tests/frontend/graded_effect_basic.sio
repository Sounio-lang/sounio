//@ run-pass
//@ description: graded epistemic effect — basic grade subsumption

fn main() -> i64 with IO, Div {
    // Epistemic grade: model parameter uncertainty, reducible
    let param_estimate: f64 = 0.75
    let grade_tight: f64 = 0.05
    let grade_loose: f64 = 0.10

    // Subsumption: tight grade satisfies loose requirement
    let satisfies = grade_tight <= grade_loose
    42
}
