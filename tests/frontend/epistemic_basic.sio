//@ run-pass
//@ description: basic epistemic uncertainty — reducible by more data

fn main() -> i64 with IO {
    // Epistemic uncertainty: model parameter estimation
    let param_estimate: f64 = 0.75
    let param_epsilon: f64 = 0.10
    42
}
