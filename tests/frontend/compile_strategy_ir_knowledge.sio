//@ run-pass
//@ description: Sprint 38 — Knowledge<T> return triggers IR_STRATEGY_PRECISION_PRESERVING

fn measure_gravity() -> Knowledge<f64> with IO {
    measure(9.81, uncertainty: 0.05)
}

fn main() -> i64 with IO {
    let g = measure_gravity()
    let _sink = g
    0
}
