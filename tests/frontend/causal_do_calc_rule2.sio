//@ run-pass
//@ description: Do-calculus Rule 2 — do(Z) replaceable by observation

struct CausalEffect { value: f64 }

fn action_observation(z: f64) -> f64 with IO {
    z + 0.7
}

fn main() -> i64 with IO {
    let effect = CausalEffect { value: action_observation(2.0) }
    42
}
