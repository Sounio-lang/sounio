//@ run-pass

// Napkin graph: identifiable only via full ID algorithm
// Structure: W -> X -> Y, W -> Y, W <-> Z, Z -> X
// P(Y | do(X)) is identifiable but requires the ID algorithm
// (not backdoor or front-door alone).

fn main() -> i64 {
    0
}
