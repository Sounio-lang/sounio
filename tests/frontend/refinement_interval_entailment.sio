// refinement_interval_entailment
//@ run-pass

type BigPositive = { n: i32 | n > 5 }
type Positive = { n: i32 | n > 0 }

fn needs_positive(x: Positive) -> i32 {
    x
}

fn main() -> i32 {
    let big: BigPositive = 10
    needs_positive(big)
}
