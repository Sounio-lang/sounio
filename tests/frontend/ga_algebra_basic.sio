//@ run-pass
//@ description: basic Geometric Algebra multivector creation and access

fn main() -> i64 with IO {
    // Cl(3,0,0) — 3D vector algebra, 2^3 = 8 components
    let sig_p: i64 = 3
    let sig_q: i64 = 0
    let sig_r: i64 = 0
    let dim: i64 = sig_p + sig_q + sig_r
    // 8 basis blades: 1, e1, e2, e3, e12, e13, e23, e123
    42
}
