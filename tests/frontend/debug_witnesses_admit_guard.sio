//@ run-pass
//@ description: Admit action with valid witness passes type-checking

fn get_value(x: f64) -> f64 with IO {
    x + 1.0
}

fn main() -> i64 with IO {
    let val = get_value(3.14)
    42
}
