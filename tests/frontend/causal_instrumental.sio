//@ run-pass
//@ description: Instrumental variable Z->X->Y with X<->Y confounded

struct CausalEffect { value: f64 }

fn instrumental_effect(z: f64, x: f64) -> f64 with IO {
    x + z + 0.1
}

fn main() -> i64 with IO {
    let y = CausalEffect { value: instrumental_effect(1.0, 2.0) }
    42
}
