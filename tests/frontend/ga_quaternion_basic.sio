//@ run-pass
//@ description: quaternion as Cl(0,2) — GA unification of hypercomplex types

fn main() -> i64 with IO {
    // Cl(0,2): 4 basis blades {1, e1, e2, e12}
    // e1^2 = -1, e2^2 = -1, e12^2 = -1
    // This is isomorphic to quaternion algebra H
    let w: f64 = 1.0
    let x: f64 = 0.0
    let y: f64 = 0.0
    let z: f64 = 0.0
    // norm_sq = w^2 + x^2 + y^2 + z^2 = 1.0
    42
}
