//@ run-pass
//@ description: PotentialOutcome<T> type annotation parses and type-checks

fn potential_outcome(group_effect: f64) -> f64 with IO {
    group_effect + 1.0
}

fn main() -> i64 with IO {
    let y1: PotentialOutcome<f64> = potential_outcome(2.5)
    42
}
