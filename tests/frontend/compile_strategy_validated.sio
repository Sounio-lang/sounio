//@ run-pass
//@ description: Validated<T> return type triggers StrategyAggressive
//@ note: compile strategy computed during HLIR lowering, not visible at check level

fn main() -> i64 with IO {
    // Validated<T> functions get Aggressive strategy:
    // - full inlining, SIMD vectorization
    // - no uncertainty guards needed
    let calibrated: f64 = 9.80665
    42
}
