//@ run-pass
//@ description: CausalEffect<T> type annotation parses and type-checks

fn estimate_ate(treatment: f64, outcome: f64) -> f64 with IO {
    outcome + (0.0 - treatment)
}

fn main() -> i64 with IO {
    let ate: CausalEffect<f64> = estimate_ate(1.0, 3.5)
    42
}
