//@ run-pass
//@ description: Lemma<T> type annotation parses and type-checks

fn derive_bound(x: f64) -> f64 with IO {
    x + 1.0
}

fn main() -> i64 with IO {
    let l: Lemma<f64> = derive_bound(5.0)
    42
}
