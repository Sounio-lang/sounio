//@ run-pass
//@ description: Knowledge<T> return type triggers StrategyPrecisionPreserving
//@ note: compile strategy computed during HLIR lowering, not visible at check level

fn main() -> i64 with IO {
    // Knowledge<T> functions get PrecisionPreserving strategy:
    // - no float reassociation
    // - interval tracking preserved
    let measurement: f64 = 9.81
    let uncertainty: f64 = 0.05
    42
}
