use lexer::{lex}
use parser::{parse, ExprKind}

fn main() -> i32 with Mut, Panic, Div {
    let toks = lex("x + 1;")
    let ast = parse(toks.items, toks.len)
    if ast.kind == ExprKind::Block { 0 } else { 1 }
}
