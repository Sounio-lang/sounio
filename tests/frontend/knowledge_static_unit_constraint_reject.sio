unit velocity = m / s;
unit acceleration = m / s / s;

struct MotionReadingUnitReject {
    speed: velocity,
}

fn reject_motion_unit_constraint(
    reading: Knowledge<MotionReadingUnitReject where { speed >= 1.0 <acceleration> }>
) -> i32 {
    return 1
}
