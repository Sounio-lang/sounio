//@ run-pass
//@ description: Sprint 70 — two-function loop staged lowering stability fixture

fn bump(x: i64) -> i64 {
    x + 1
}

fn main() -> i64 {
    var acc: i64 = 0
    var i: i64 = 0
    while i < 3 {
        acc = acc + i
        i = i + 1
    }
    acc
}
