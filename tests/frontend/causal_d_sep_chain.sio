//@ run-pass
//@ description: D-separation in chain X->Z->Y — conditioning on Z blocks path

struct CausalEffect { value: f64 }

fn chain_effect(x: f64, z: f64) -> f64 with IO {
    z + 0.5
}

fn main() -> i64 with IO {
    let y = CausalEffect { value: chain_effect(1.0, 2.0) }
    42
}
