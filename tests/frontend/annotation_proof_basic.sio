//@ run-pass
//@ description: Proof<T> type annotation parses and type-checks

fn prove_convergence(bound: f64) -> f64 with IO {
    bound
}

fn main() -> i64 with IO {
    let p: Proof<f64> = prove_convergence(0.001)
    42
}
