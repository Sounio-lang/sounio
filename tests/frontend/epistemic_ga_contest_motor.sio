//@ run-pass
//@ description: Contest<Motor> return type triggers StrategyInstrumented with dual-path

fn main() -> i64 {
    // Contest<Motor> functions get Instrumented strategy:
    // - dual-path codegen (fast + interval-instrumented)
    // - runtime dispatch on validation flag
    // - zero-cost when validated (branch predictor)
    let x: f64 = 1.0
    0
}
