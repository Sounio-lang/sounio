//@ run-pass
//@ description: D-separation in fork X<-Z->Y — conditioning on Z blocks path

struct CausalEffect { value: f64 }

fn fork_effect(z: f64) -> f64 with IO {
    z + 0.3
}

fn main() -> i64 with IO {
    let y = CausalEffect { value: fork_effect(1.5) }
    42
}
