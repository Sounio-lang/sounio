//@ run-pass
//@ description: DPBudget<T> type annotation parses and type-checks

fn init_budget(total: f64) -> f64 with IO {
    total
}

fn main() -> i64 with IO {
    let budget: DPBudget<f64> = init_budget(1000.0)
    42
}
