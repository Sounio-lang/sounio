//@ run-pass
//@ description: Debug witness infrastructure parses and type-checks

fn compute_metric(x: f64) -> f64 with IO {
    x + 0.5
}

fn main() -> i64 with IO {
    let metric = compute_metric(0.8)
    42
}
