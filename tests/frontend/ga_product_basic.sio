//@ run-pass
//@ description: geometric product — foundation of Clifford algebra computation

fn main() -> i64 with IO {
    // In Cl(3,0,0): e1 * e2 = e12 (bivector)
    // e1 * e1 = +1 (positive metric)
    // e12 * e12 = -1 (bivector squares to -1 in Euclidean space)
    let metric_positive: i64 = 1
    let bivector_sq: i64 = 0 - 1
    42
}
