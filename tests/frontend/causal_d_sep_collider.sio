//@ run-pass
//@ description: D-separation at collider X->Z<-Y — unconditionally d-separated

struct CausalEffect { value: f64 }

fn collider_outcome(x: f64) -> f64 with IO {
    x + 0.2
}

fn main() -> i64 with IO {
    let y = CausalEffect { value: collider_outcome(1.0) }
    42
}
