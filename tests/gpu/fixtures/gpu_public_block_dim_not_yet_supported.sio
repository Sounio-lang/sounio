// Public checked-artifact negative fixture:
// the checked GPU profile does not currently resolve gpu.block_dim.*.

kernel fn block_dim_probe(n: i64) with GPU {
    let bdim = gpu.block_dim.x
    if bdim < n {
    }
}

fn main() -> i32 with IO {
    0
}
