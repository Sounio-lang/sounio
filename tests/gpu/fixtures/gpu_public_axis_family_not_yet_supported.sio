//@ check-fail
// the checked GPU profile does not currently resolve non-x axis spellings of
// gpu.thread_id.*, gpu.block_id.*, or gpu.block_dim.*.

kernel fn axis_family_probe(n: i64) with GPU {
    let tidy = gpu.thread_id.y
    let bidz = gpu.block_id.z
    let bdimy = gpu.block_dim.y
}
