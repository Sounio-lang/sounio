// Public checked-artifact negative fixture:
// the checked GPU profile does not currently resolve gpu.block_id.*.

kernel fn block_probe(n: i64) with GPU {
    let bid = gpu.block_id.x
    if bid < n {
    }
}

fn main() -> i32 with IO {
    0
}
