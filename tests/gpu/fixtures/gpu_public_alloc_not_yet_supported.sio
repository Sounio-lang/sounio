// Public checked-artifact negative fixture:
// the checked GPU profile does not currently resolve gpu.alloc<T>(...).

fn main() with GPU, Alloc, IO {
    let n = 4
    let a = gpu.alloc<f32>(n)
    perform GPU.sync()
}
