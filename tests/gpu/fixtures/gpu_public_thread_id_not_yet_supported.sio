// Public checked-artifact negative fixture:
// the checked GPU profile does not currently resolve gpu.thread_id.*.

kernel fn thread_probe(n: i64) with GPU {
    let tid = gpu.thread_id.x
    if tid < n {
    }
}

fn main() -> i32 with IO {
    0
}
