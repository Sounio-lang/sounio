// No f32 material contract exists yet. This source may be rejected or lowered
// generically, but it must never select GpuXorConvolutionF64.
kernel fn harbor_f32(
    left: &Hyper<Sedenion, f32>,
    right: &Hyper<Sedenion, f32>,
    out: &! Hyper<Sedenion, f32>
) with GPU, NonAssoc {
    *out = (*left) * (*right)
}

fn main() -> i32 with IO { 0 }
