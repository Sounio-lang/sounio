// Negative control: unsupported compound Metal shape must be refused.
kernel fn step(pa: &Hyper<Sedenion, f64>, ph: &Hyper<Sedenion, f64>,
    pout: &! Hyper<Sedenion, f64>) with GPU, NonAssoc {
    let a = *pa
    let h = *ph
    *pout = h * a
}
fn main() -> i32 with IO { 0 }
