//@ run-pass
//@ requires: madaros
//@ expect-stdout: STR_SLICE_RANGE_OK

use epistemic_core::*

fn main() with IO, Panic {
    let middle = str_slice("0123456789", 2, 5)
    assert(str_eq(middle, "234"))
    assert(str_len(middle) == 3)
    println("STR_SLICE_RANGE_OK")
}
