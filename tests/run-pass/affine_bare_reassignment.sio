//@ run-pass
//@ requires: madaros

affine struct Permit {
    id: i64,
}

fn spend(permit: Permit) -> i64 {
    permit.id
}

fn main() -> i32 with Mut, Panic {
    var permit = Permit { id: 113 }
    permit = Permit { id: 127 }
    assert(spend(permit) == 127)
    0
}
