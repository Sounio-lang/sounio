//@ run-pass

// Kernel function with explicit GPU effect annotation.
// GPU effect is allowed (and auto-injected if missing).

kernel fn scale(factor: f64, n: i64) with GPU, Div {
}

kernel fn identity() with GPU {
}

fn main() -> i32 with IO {
    0
}
