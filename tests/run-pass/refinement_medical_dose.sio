//@ run-pass
//@ description: Medical domain - safe dose refinement

fn administer_dose(dose: { d: f64 | d > 0.0 && d <= 1000.0 }) -> f64 {
    dose
}

fn main() {
    let safe_dose = administer_dose(500.0)
}
