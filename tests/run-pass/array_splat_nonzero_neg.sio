//@ run-pass
//@ description: [V;N] splat with negative V (regression test for the [V;N] zero-fill miscompile)
fn main() -> i64 {
    var a: [i64; 8] = [-1; 8];
    var s: i64 = 0; var i: i64 = 0;
    while i < 8 { s = s + a[i]; i = i + 1; }
    if s == 0 - 8 { return 0; }
    return 1;
}
