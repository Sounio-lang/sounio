//@ run-pass
// Test: nested generic struct

struct Wrapper<T> {
    inner: T,
    tag: i64,
}

fn main() with IO {
    let w = Wrapper { inner: 99, tag: 1 }
    println(w.inner)
    println(w.tag)
}
