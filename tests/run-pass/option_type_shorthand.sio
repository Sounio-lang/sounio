//@ run-pass
//@ description: Option type shorthand ?T is accepted in type positions

struct ShorthandHolder {
    value: ?i64,
}

fn accepts_shorthand(v: ?i64) -> i64 with IO {
    0
}

fn main() -> i64 {
    0
}
