//@ run-pass
// gpu_launch_multidim_surface.sio - explicit multidimensional launch tuples
// accepted by the checked GPU artifact and deterministic sim-runtime lane.
//
// This fixture is evidence for tuple-shaped launch-surface acceptance only.
// It does not claim physical multidimensional GPU execution on hardware.

kernel fn tile_kernel(nx: i64, ny: i64, nz: i64) with GPU {
}

fn main() with GPU, IO {
    let grid = (3, 2, 2)
    let block = (8, 4, 2)

    perform GPU.launch(tile_kernel, grid, block)(96, 8, 4)
    perform GPU.sync()
    print("gpu_launch_multidim_surface ok\n")
}
