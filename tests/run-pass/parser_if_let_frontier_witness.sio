//@ run-pass
//@ description: Parser frontier witness for if-let patterns with else blocks.

enum Maybe {
    None,
    Some(i64),
    Point { x: i64 }
}

fn main() -> i64 {
    let tuple_value = Maybe::Some(7)
    if let Maybe::Some(x) = tuple_value {
        let seen = x
    } else {
        let fallback = 0
    }

    let struct_value = Maybe::Point { x: 11 }
    if let Maybe::Point { x: px } = struct_value {
        let seen2 = px
    } else {
        let fallback2 = 0
    }

    0
}
