//@ run-pass
// Madaros Root 2: imported &self method resolves to its defining module.

use epistemic::knowledge::{Epistemic, ep_measured}

fn main() -> i32 with IO, Mut, Div, Panic {
    let e = ep_measured(10.0, 0.5)
    if e.val() != 10.0 { return 20 }
    print("ROOT2_INSTANCE_IMPORT_OK\n")
    return 0
}
