//@ run-pass
//@ description: MEASURED fabrication — Vec::new on nonexistent type compiles and evals to 0 (W44 never fires)
// Pre-honesty: Madaros check OK, run prints 0. Post-fix should refuse (E137 or hard error).
// This witness documents current lie; gate may mark known-failure until refuse lands.
fn main() with IO {
    let v = Vec::new()
    print_int(v as i64)
    print_char(10)
    println("PASS vec_new_nonexistent_type_eval_zero_DOCUMENTS_FABRICATION")
}
