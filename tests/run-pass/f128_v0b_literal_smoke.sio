//@ run-pass
//@ description: V0-B — f128 type spellings and decimal/scientific literals accepted through check
//@ ladder-stage: V0-B
//@ semantic-lane: WS-G-V0B-LITERALS-CHECK
//
// Target after V0-B lands: `souc check` prints `check: OK` and does NOT emit
// error[E218]. Today (V0-A) the parser rejects f128 with E218 before check.
// No arithmetic, no casts, no print of the wide value — identity only.
// Gate: scripts/ci/madaros_f128_f256_ladder_gate.sh --stage v0b

fn identity_f128(x: f128) -> f128 {
    x
}

fn main() -> i32 {
    let a: f128 = 1.0
    let b: f128 = 2.5
    let c: f128 = 1.23e-4
    let d = identity_f128(a)
    let _keep = d
    let _keep_b = b
    let _keep_c = c
    0
}
