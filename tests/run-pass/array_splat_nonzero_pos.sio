//@ run-pass
//@ description: [V;N] splat with positive V (regression test for the [V;N] zero-fill miscompile)
fn main() -> i64 {
    var b: [i64; 8] = [5; 8];
    var s: i64 = 0; var i: i64 = 0;
    while i < 8 { s = s + b[i]; i = i + 1; }
    if s == 40 { return 0; }
    return 1;
}
