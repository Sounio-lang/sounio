//@ run-pass
//@ description: compound OR guard narrows the disjunction into the then-branch

fn takes_extreme(x: { v: i32 | v < 0 || v > 100 }) -> i32 {
    x
}

fn main() -> i32 with IO {
    let z: i32 = 150
    if z < 0 || z > 100 {
        let _ = takes_extreme(z)
    }
    0
}
