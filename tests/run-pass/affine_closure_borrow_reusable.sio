//@ run-pass
//@ requires: madaros

affine struct Permit {
    id: i64,
}

fn inspect(permit: &Permit) -> i64 {
    permit.id
}

fn main() -> i32 with Panic {
    let permit = Permit { id: 97 }
    let read = || inspect(&permit)
    assert(read() == 97)
    assert(read() == 97)
    0
}
