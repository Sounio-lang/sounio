//@ run-pass
//@ requires: madaros
//@ expect-stdout: affine once OK

affine struct Permit {
    id: i64,
}

fn spend(permit: Permit) -> i64 {
    permit.id
}

fn main() -> i32 with IO, Panic {
    let permit = Permit { id: 42 }
    assert(spend(permit) == 42)
    print("affine once OK\n")
    0
}
