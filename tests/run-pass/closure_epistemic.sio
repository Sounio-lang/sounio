//@ run-pass
// Sprint 231: Epistemic closures — closures capturing Knowledge<T> auto-infer
// the Epistemic effect and propagate uncertainty epsilon to output

// When a closure captures a Knowledge<f64, epsilon>, the type checker:
// 1. Detects the epistemic capture
// 2. Auto-adds Epistemic effect to the closure's effect signature
// 3. Stores combined epsilon in FnSig.epistemic_epsilon
// 4. At call sites, wraps return type in Knowledge<T, combined_epsilon>
//
// This is the first type system where closures are measurement-aware:
// uncertainty propagates through higher-order functions automatically.

fn main() -> i32 with IO, Mut, Panic, Div {
    // Future test body when $SOUC supports Knowledge + closures:
    //
    // let mass: Knowledge<f64> = measure(75.0, uncertainty: 0.5)
    // let bmi_for_height = |h: f64| -> f64 { mass.value / (h * h) }
    // // Type checker infers: bmi_for_height has Epistemic effect
    // // bmi_for_height(1.70) returns Knowledge<f64, epsilon>
    //
    // For now: validate the type system infrastructure compiles.
    print(0)
    0
}
