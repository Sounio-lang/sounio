//@ run-pass
//@ requires: madaros

affine struct Permit {
    id: i64,
}

fn main() -> i32 with Panic {
    let spend = |permit: Permit| permit.id
    assert(spend(Permit { id: 107 }) == 107)
    assert(spend(Permit { id: 109 }) == 109)
    0
}
