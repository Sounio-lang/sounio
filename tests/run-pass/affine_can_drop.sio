//@ run-pass
//@ ignore
//@ description: Affine value can be dropped without explicit consumption (affine checking not implemented)

affine struct Optional {
    value: i32,
}

fn main() {
    let opt = Optional { value: 10 }
    // OK: affine values can be implicitly dropped
}
