//@ run-pass
//@ expect-stdout: unit energy explicit conversion: PASS

fn takes_ev(energy: eV) -> f64 with Mut {
    energy
}

fn main() -> i32 with IO, Mut, Panic, Div {
    let joules: J = 1.0
    let electron_volts: eV = joules as eV
    let checked = takes_ev(electron_volts)
    print("unit energy explicit conversion: PASS\n")
    return 0
}
