//@ run-pass
//@ description: Parser regression for wildcard and destructuring let patterns plus public fields.

struct ParserCardAPair {
    pub x: i64,
    pub y: i64,
}

fn main() -> i64 {
    let _ = 7
    let ParserCardAPair { x, y } = ParserCardAPair { x: 2, y: 3 }
    let (a, b) = (x, y)
    let _sum = a + b
    0
}
