//@ run-pass
// Test: basic module import resolution in the lean single compiler.
// Validates that `use` declarations resolve relative paths, read imported
// files, and make imported functions available to the main module.

use import_basic_a::{imported_add}

fn main() -> i64 with IO {
    let result = imported_add(10, 32)
    print_int(result)
    print("\n")
    0
}
