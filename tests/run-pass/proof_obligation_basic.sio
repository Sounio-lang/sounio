//@ run-pass
// Test: proof obligation — known constant satisfies refinement

type PosI64 = { x: i64 | x > 0 }

fn accept_positive(x: PosI64) -> i64 {
    x
}

fn main() -> i32 with IO {
    let v: i64 = 42
    // Compiler should prove: 42 > 0 via const_int_lookup
    let r = accept_positive(v)
    print_int(r)
    println("")
    0
}
