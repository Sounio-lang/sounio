//@ run-pass
// Leaf module for 3-level import chain test.

fn chain_c_value() -> i64 {
    42
}

fn main() -> i64 with Panic {
    let v = chain_c_value()
    if v == 42 { return 0 } else { return 1 }
}
