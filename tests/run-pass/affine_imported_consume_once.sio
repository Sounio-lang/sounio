//@ run-pass
//@ requires: madaros
//@ expect-stdout: imported affine once OK

use fixtures::affine_ownership_leaf::{imported_permit, imported_spend}

fn main() -> i32 with IO, Panic {
    let permit = imported_permit(73)
    assert(imported_spend(permit) == 73)
    print("imported affine once OK\n")
    0
}
