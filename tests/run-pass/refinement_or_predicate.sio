//@ run-pass
//@ description: Refinement with OR predicate (values outside middle range)

fn takes_extreme(x: { v: i32 | v < 0 || v > 100 }) -> i32 {
    x
}

fn main() {
    let a = takes_extreme(-10)
    let b = takes_extreme(150)
}
