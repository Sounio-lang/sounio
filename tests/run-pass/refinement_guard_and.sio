//@ run-pass
//@ description: compound AND guard narrows both conjuncts into the then-branch

fn takes_bounded(x: { n: i32 | n > 0 && n < 100 }) -> i32 {
    x
}

fn main() -> i32 with IO {
    let y: i32 = 42
    if y > 0 && y < 100 {
        let _ = takes_bounded(y)
    }
    0
}
