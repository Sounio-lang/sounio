//@ run-pass
//@ expect-stdout: PASS

fn apply(f: fn(i64) -> i64, x: i64) -> i64 { f(x) }

fn main() -> i64 with IO, Mut {
    let result = apply(|x| x * 2, 21)
    if result == 42 { print("PASS\n") }
    else { print("FAIL\n") }
    return 0
}
