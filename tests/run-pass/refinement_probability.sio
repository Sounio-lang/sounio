//@ run-pass
//@ description: Probability refinement type (bounded [0,1])

fn takes_probability(p: { prob: f64 | prob >= 0.0 && prob <= 1.0 }) -> f64 {
    p
}

fn main() {
    let p = takes_probability(0.5)
}
