//@ run-pass
//@ description: Inline refinement types in function parameters

fn takes_positive(x: { n: i32 | n > 0 }) -> i32 {
    x
}

fn takes_bounded(x: { v: i32 | v > 0 && v < 100 }) -> i32 {
    x
}

fn main() {
    let a = takes_positive(42)
    let b = takes_bounded(50)
}
