//@ run-pass

// Verify that algebra declarations without explicit properties default correctly.
// Default: mul commutative + associative.

algebra Complex over f64 {
    basis: re, im
}

fn main() -> i64 with IO {
    let z = Complex { re: 3.0, im: 4.0 }
    0
}
