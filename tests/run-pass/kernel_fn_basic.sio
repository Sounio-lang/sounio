//@ run-pass

// Basic kernel function parsing and type-checking.
// Verifies that `kernel fn` is accepted by the parser and checker.

kernel fn vec_add(a: i64, b: i64, n: i64) with GPU {
}

fn main() -> i32 with IO {
    0
}
