//@ run-pass
//@ description: Refinement types in return position

fn make_positive(x: i32) -> { n: i32 | n > 0 } {
    if x > 0 {
        x
    } else {
        1
    }
}

fn main() {
    let result = make_positive(42)
}
