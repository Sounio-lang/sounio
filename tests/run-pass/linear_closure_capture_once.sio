//@ run-pass
//@ requires: madaros

linear struct Receipt {
    id: i64,
}

fn main() -> i32 with Panic {
    let receipt = Receipt { id: 89 }
    let close = || receipt.id
    assert(close() == 89)
    0
}
