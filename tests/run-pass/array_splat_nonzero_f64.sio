//@ run-pass
//@ description: [V;N] float splat (regression test for the [V;N] zero-fill miscompile)
fn main() -> i64 {
    var c: [f64; 8] = [1.0; 8];
    var s: f64 = 0.0; var i: i64 = 0;
    while i < 8 { s = s + c[i]; i = i + 1; }
    if s > 7.5 { if s < 8.5 { return 0; } }
    return 1;
}
