//@ run-pass
//@ expect-stdout: aarch64 closure capture ok
fn main() -> i32 with IO, Panic {
    let bias = 10
    let add_bias = |x: i64| -> i64 x + bias

    let zero = || -> i64 bias

    assert(add_bias(4) == 14)
    assert(zero() == 10)

    print("aarch64 closure capture ok\n")
    0
}
