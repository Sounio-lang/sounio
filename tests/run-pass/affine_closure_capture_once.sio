//@ run-pass
//@ requires: madaros

affine struct Permit {
    id: i64,
}

fn main() -> i32 with Panic {
    let permit = Permit { id: 83 }
    let spend = || permit.id
    assert(spend() == 83)
    0
}
