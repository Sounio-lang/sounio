//@ run-pass

// Study function called from Audit context — valid.

study {
    hypothesis {
        name: "treatment_response"
        direction: "positive"
    }

    fn run_analysis() -> i64 with IO {
        42
    }
}

fn execute() -> i64 with IO, Audit, Hypothesis {
    run_analysis()
}

fn main() -> i64 with IO {
    0
}
