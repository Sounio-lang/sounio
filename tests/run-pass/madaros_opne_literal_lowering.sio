//@ run-pass
// Regression: literal binary inequality must lower both as an assertion
// condition and as a tail expression without a raw Madaros crash.
fn local_literal_comparison_tail() -> bool with Panic {
    assert(1 != 0)
    1 != 0
}

fn main() with IO, Panic {}
