//@ run-pass
// Regression: a literal binary inequality must lower without the raw Madaros
// expression-lowering crash that previously occurred before native emission.
fn local_literal_comparison_tail() -> bool {
    1 != 0
}

fn main() with IO, Panic {}
