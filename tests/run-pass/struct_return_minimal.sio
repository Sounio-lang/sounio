//@ run-pass

struct Pair {
    a: f64,
    b: f64,
}

fn make_pair() -> Pair {
    Pair { a: 1.0, b: 2.0 }
}

fn main() -> i32 with Mut, Panic, Div {
    let p = make_pair()
    if p.a != 1.0 || p.b != 2.0 { return 1 }
    println("struct_return_minimal: PASS")
    return 0
}
