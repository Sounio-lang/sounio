//@ run-pass
fn main() -> i32 with IO, Panic {
    let s = "hello"
    let empty = ""
    let long = "sounio language"
    assert(s.len() == 5)
    assert(empty.len() == 0)
    assert(long.len() == 15)
    print("string_len: PASS\n")
    0
}
