//@ run-pass
//@ requires: madaros

affine struct Permit {
    id: i64,
}

fn identity<T>(value: T) -> T {
    value
}

fn spend(permit: Permit) -> i64 {
    permit.id
}

fn main() -> i32 with Panic {
    let permit = identity::<Permit>(Permit { id: 59 })
    assert(spend(permit) == 59)
    0
}
