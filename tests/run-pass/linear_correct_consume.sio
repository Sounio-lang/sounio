//@ run-pass
//@ description: Linear value correctly consumed by function call

linear struct FileHandle {
    fd: i32,
}

fn close(h: FileHandle) -> i32 {
    h.fd
}

fn main() {
    let handle = FileHandle { fd: 42 }
    let result = close(handle)
}
