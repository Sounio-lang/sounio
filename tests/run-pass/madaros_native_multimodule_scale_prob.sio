// Issue #901 acceptance probe — large multi-module native scale under default Madaros.
//
// Historical fail class (2026-07-14): `use prob::distributions::*` type-checked but
// native-compiled failed with imported_simple_ir_emit_failed / thin-link rc=12 on a
// ~210-fn merged graph (special::gamma + igamma + erf).
//
// Measured Wave15C 2026-07-22 on origin/main post into-acc (#1402) + specialized-list
// DCE (#1397): default Madaros full-IR path compiles (~70–73 merged fns after into-acc)
// and runs uniform_mean(0,10)=5.000000.
//
// Gate: scripts/madaros_native_multimodule_scale_901_gate.sh
// Audit: docs/audit/MADAROS_WAVE15C_ISSUE901_SCALE_CLOSEOUT_2026-07-22.md
// Original dispatch: docs/audit/MADAROS_NATIVE_MULTIMODULE_SCALE_2026-07-14.md
use prob::distributions::*

fn main() -> i32 with IO, Mut, Div, Panic {
    print("m=")
    println(uniform_mean(0.0, 10.0))
    return 0
}
