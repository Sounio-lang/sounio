module d3_openslice_len::main

use d3_openslice_len::lib::slice_len

fn main() with IO, Panic {
    let values = [1.0, 2.0, 3.0, 4.0]
    let n = slice_len(&values)
    assert(n == 4)
    println(n)
}
