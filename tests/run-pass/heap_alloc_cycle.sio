//@ run-pass
//@ description: 1000x alloc/free cycle — verifies no leak or crash

fn main() with IO, Alloc {
    var i: i64 = 0
    while i < 1000 {
        let p = heap_alloc(4096)
        write_i64(p, 0, i)
        heap_free(p)
        i = i + 1
    }
    print("heap_alloc_cycle: PASS\n")
}
