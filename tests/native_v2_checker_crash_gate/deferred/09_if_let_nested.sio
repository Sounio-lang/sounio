fn get_some(v: i64) -> Option<i64> {
    Some(v)
}

fn main() -> i64 {
    let outer = get_some(10)
    if let Some(a) = outer {
        let inner = get_some(32)
        if let Some(b) = inner {
            a + b
        } else {
            a
        }
    } else {
        0
    }
}
