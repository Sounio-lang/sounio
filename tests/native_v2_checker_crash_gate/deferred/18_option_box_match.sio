struct Node {
    value: i64,
}

fn main() -> i64 {
    let n = Box::new(Node { value: 42 })
    let opt: Option<Box<Node>> = Some(n)
    match opt {
        Some(v) => (*v).value,
        None => 0,
    }
}
