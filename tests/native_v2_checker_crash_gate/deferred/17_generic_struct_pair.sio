struct Pair<T> {
    first: T,
    second: T,
}

fn make_pair(a: i64, b: i64) -> Pair<i64> {
    Pair::<i64> { first: a, second: b }
}

fn main() -> i64 {
    let p = make_pair(10, 32)
    p.first + p.second
}
