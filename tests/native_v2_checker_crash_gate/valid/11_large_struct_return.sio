struct Wide {
    a: i64,
    b: i64,
    c: i64,
}

fn make_wide(a: i64, b: i64, c: i64) -> Wide {
    Wide { a: a, b: b, c: c }
}

fn main() -> i64 {
    let w = make_wide(10, 20, 12)
    w.a + w.b + w.c
}
