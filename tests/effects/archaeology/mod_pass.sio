// Held: founder hold_mod_claude1. Still dropped (-1), same as nosuch_drop.
// Not a live extra. Do not read absence from the enum as forgetfulness.
fn marked() -> i64 with Mod { 7 }
fn forwarding() -> i64 with Mod { marked() }

fn main() -> i32 with Mod {
    if forwarding() == 7 { 0 } else { 1 }
}
