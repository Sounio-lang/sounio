# Parity manifest for the Sounio compiler.
#
# Each entry: <mode> <path> [expected-marker]
#
# mode:
#   run    - program must compile, execute, exit rc=0, and contain <marker>
#            in stdout (usually "PASS", "READY", or a specific string).
#   fail   - program must FAIL to compile; stderr must contain <marker>
#            (usually an error code like "E201" or "E204").
#
# This manifest is consumed by tests/parity/run_parity.sh in three modes:
#   --capture       write current compiler output to golden/<name>.{stdout,stderr,rc}
#   --check         run current compiler, diff against golden/
#   --parity B=...  run both the default compiler and $B, diff their outputs
#
# The manifest is intentionally a small but representative set that exercises
# every Wave 9 type gate and the core examples whose numbers appear in the
# papers.  Adding entries here is the canonical way to lock in a semantic
# invariant against silent divergence between lean_single.sio and the
# future modular build path (see docs/compiler/KNOWN_LIMITATIONS.md).

# === Wave 9 compile-fail gates (E201-E206) ===
fail  tests/compile-fail/exactly_private_requires_zd.sio    E201
fail  tests/compile-fail/editable_requires_zd.sio           E202
fail  tests/compile-fail/capability_gated_requires_zd.sio   E203
fail  tests/compile-fail/composable_requires_zd.sio         E204
fail  tests/compile-fail/audited_requires_witness.sio       E205
fail  tests/compile-fail/revivable_requires_temporal.sio    E206

# === Wave 9 run-pass examples (numbers cited in papers) ===
run  examples/zd_machine_unlearning.sio          PASS
run  examples/zd_model_editing_locality.sio      PASS
run  examples/zd_capability_removal.sio          PASS
run  examples/zd_forgettable_training.sio        PASS
run  examples/zd_ratio_sweep.sio                 SWEEP DONE
run  examples/meta_self_editing.sio              PASS
