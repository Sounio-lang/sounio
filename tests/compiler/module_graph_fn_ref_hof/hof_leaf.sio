fn double(x: i64) -> i64 {
    x * 2
}

pub fn select_unary(which: i64) -> fn(i64) -> i64 with Mut, Panic, Div {
    if which == 0 { double } else { double }
}

pub fn apply(f: fn(i64) -> i64, x: i64) -> i64 {
    f(x)
}
