// Free fns literally named exp/cos — must keep user body under Madaros.
pub fn exp(x: f64) -> f64 {
    x + 1.0
}

pub fn cos(x: f64) -> f64 {
    x + 2.0
}

pub fn my_exp(x: f64) -> f64 {
    x + 1.0
}
