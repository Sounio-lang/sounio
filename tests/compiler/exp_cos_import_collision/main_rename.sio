use exp_cos_import_collision::lib::{my_exp}

fn main() -> i32 with IO {
    let v = my_exp(1.0)
    let scaled = (v * 1000.0) as i64
    print_int(scaled)
    print("\n")
    if scaled == 2000 {
        println("RENAME_OK")
        return 0
    }
    println("RENAME_FAIL")
    return 1
}
