extern "C" {
    fn exp(x: f64) -> f64;
}

fn exp(x: f64, shift: f64) -> f64 {
    x + shift
}

fn main() -> i32 with IO {
    let value = exp(2.0, 40.0)
    if value < 41.9 || value > 42.1 { return 1 }
    print("ISSUE1021_EXTERN_LOCAL_COLLISION_OK\n")
    return 0
}
