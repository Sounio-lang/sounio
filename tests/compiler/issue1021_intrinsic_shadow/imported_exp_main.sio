use imported_exp_leaf::{exp}

fn main() -> i32 with IO {
    let bare = exp(2.0)
    let qualified = imported_exp_leaf::exp(2.0)
    if bare < 41.9 || bare > 42.1 { return 1 }
    if qualified < 41.9 || qualified > 42.1 { return 2 }
    print("ISSUE1021_IMPORTED_EXP_OK\n")
    return 0
}
