pub fn imported_seq_len_unwrap() -> i64 with Alloc, Mut, Panic, Epistemic {
    var s: Seq<i64> = seq_new()
    s.push(10)
    s.push(20)
    s.push(30)
    let method_len = s.len().unwrap("method length")
    let free_len = seq_len(s).unwrap("free length")
    method_len * 10 + free_len
}
