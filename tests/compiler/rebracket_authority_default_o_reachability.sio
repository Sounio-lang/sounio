pub fn authority_apply(x: i64) -> i64 {
    (x & 255) & 15
}

fn main() -> i64 {
    let observed = authority_apply(171)
    // authority_apply is range-limited by & 15, so zero is exact equality.
    observed ^ 11
}
