//@ compile-fail
//@ error-pattern: unresolved or ambiguous function provenance

// Both imported methods lower to Authority_Collision_witness. The compiler
// must refuse the ambiguous defining-module identity instead of selecting one.
use left::{Authority_Collision}
use right::{Authority}

fn main() -> i64 {
    Authority_Collision::witness()
}
