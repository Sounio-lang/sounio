use rebracket_authority_imported_leaf::{authority_apply_imported}

fn main() -> i64 {
    let observed = authority_apply_imported(171)
    // authority_apply_imported is range-limited by & 15, so zero is exact equality.
    observed ^ 11
}
