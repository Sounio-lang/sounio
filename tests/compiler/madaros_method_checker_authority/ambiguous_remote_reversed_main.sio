use ambiguous_remote_target::{make_authority}
use ambiguous_remote_distractor::{unrelated_marker}

fn main() -> i64 {
    if unrelated_marker() != 7 { return 1 }
    let value = make_authority()
    value.witness()
}
