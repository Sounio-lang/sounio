use private_distractor_target::{make_visibility_authority}
use private_distractor_unrelated::{private_distractor_marker}

fn main() -> i64 {
    if private_distractor_marker() != 7 { return 1 }
    let value = make_visibility_authority()
    value.witness()
}
