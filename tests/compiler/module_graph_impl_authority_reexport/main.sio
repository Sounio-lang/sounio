use facade::{Routed}
use distractor::{unrelated_marker}

fn main() -> i64 with IO {
    if unrelated_marker() != 7 { return 1 }
    if Routed::witness() != 42 { return 2 }
    println("MODULEGRAPH_IMPL_AUTHORITY_REEXPORT_OK")
    0
}
