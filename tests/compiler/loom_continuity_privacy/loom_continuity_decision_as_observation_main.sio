//@ compile-fail
//@ error-pattern: E009

use coordination::loom_continuity::{
    VerifiedPreSpawnDecision,
    pre_spawn_roles_are_nominally_distinct,
}

fn collapse_roles(
    decision: VerifiedPreSpawnDecision,
) {
    let collapsed = pre_spawn_roles_are_nominally_distinct(
        decision,
        decision,
    )
}

fn main() -> i64 {
    0
}
