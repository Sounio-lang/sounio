//@ compile-fail
//@ error-pattern: E009

use coordination::loom_continuity::{
    VerifiedPreSpawnDecisionFacts,
    pre_spawn_measurement_roles_are_nominally_distinct,
}

fn collapse_measurement_role(
    decision: VerifiedPreSpawnDecisionFacts,
) {
    let collapsed = pre_spawn_measurement_roles_are_nominally_distinct(
        decision,
        decision,
    )
}

fn main() -> i64 {
    0
}
