//@ compile-fail
//@ error-pattern: E009

use coordination::loom_continuity::{
    VerifiedFullDigestDecisionFacts,
    VerifiedFullDigestMeasuredFacts,
    VerifiedJournalAuthority,
    journal_quorum_roles_are_nominally_distinct,
}

fn collapse_single_authority_into_quorum(
    decision: VerifiedFullDigestDecisionFacts,
    measurement: VerifiedFullDigestMeasuredFacts,
    journal: VerifiedJournalAuthority,
) {
    let collapsed = journal_quorum_roles_are_nominally_distinct(
        decision,
        measurement,
        journal,
    )
}

fn main() -> i64 {
    0
}
