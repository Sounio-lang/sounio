//@ compile-fail
//@ error-pattern: E009

use coordination::loom_continuity::{
    VerifiedFullDigestDecisionFacts,
    VerifiedJournalAuthority,
    observation_authority_roles_are_nominally_distinct,
}

fn collapse_full_measurement_role(
    decision: VerifiedFullDigestDecisionFacts,
    journal: VerifiedJournalAuthority,
) {
    let collapsed = observation_authority_roles_are_nominally_distinct(
        decision,
        decision,
        journal,
    )
}

fn main() -> i64 {
    0
}
