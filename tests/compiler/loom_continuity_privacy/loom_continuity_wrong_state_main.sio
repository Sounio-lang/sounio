//@ compile-fail
//@ error-pattern: E009

use coordination::loom_continuity::{
    ObservedContinuity,
    promote_pod_resurrection,
}

fn launder(observed: ObservedContinuity) -> (i64, i64, i64, i64, i64, i64) {
    promote_pod_resurrection(observed)
}

fn main() -> i64 {
    0
}
