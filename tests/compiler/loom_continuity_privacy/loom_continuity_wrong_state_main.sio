//@ compile-fail
//@ error-pattern: E009

use coordination::loom_continuity::{observe_continuity, promote_pod_resurrection}

fn main() -> i64 {
    match observe_continuity(
        1, 2, 3, 4, 5, 6, 7, 8, 9,
        3, 1, 1, true, true, true,
    ) {
        Some(observed) => promote_pod_resurrection(observed),
        None => 0,
    }
}
