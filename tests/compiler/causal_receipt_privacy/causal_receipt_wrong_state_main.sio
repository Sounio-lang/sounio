//@ compile-fail
//@ error-pattern: E009

use coordination::causal_receipt::{preregister_experiment, promote_supported}

fn main() -> i64 {
    match preregister_experiment(1, 2, 3) {
        Some(preregistered) => promote_supported(preregistered),
        None => 0,
    }
}
