use public_facade_leaf::{public_value, PublicStruct, PublicEnum}

fn main() -> i64 with IO {
    let record = PublicStruct { value: public_value() }
    let state = PublicEnum::Ready
    if record.value != 42 { return 31 }
    println("PASS public_facade_module_authority")
    0
}
