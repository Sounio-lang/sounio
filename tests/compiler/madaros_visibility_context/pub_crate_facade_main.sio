use pub_crate_facade_leaf::{crate_value, CrateStruct, CrateEnum}

fn main() -> i64 with IO {
    let record = CrateStruct { value: crate_value() }
    let state = CrateEnum::Ready
    if record.value != 42 { return 31 }
    println("PASS pub_crate_facade_module_authority")
    0
}
