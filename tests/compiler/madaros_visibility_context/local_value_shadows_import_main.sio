use local_value_shadows_import_leaf::{selected}

fn main() -> i64 with IO {
    let selected = || 42
    let answer: i64 = selected()
    if answer != 42 {
        return 72
    }
    println("PASS local_value_shadows_import")
    0
}
