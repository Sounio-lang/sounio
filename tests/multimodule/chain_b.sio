//@ run-pass
// Chain import middle: wraps leaf computation

fn leaf_value() -> i64 {
    42
}

fn middle_compute() -> i64 {
    leaf_value() + 1
}

fn main() -> i32 with IO {
    let v = middle_compute()
    0
}
