//@ run-pass
// WP-A3 W3 (no-misfire guard): imported single-letter struct F used by main.
// No generics here, so the specializer must leave everything unchanged. Exit 7.
use w3_mod::{F, make_f}

fn main() -> i64 with Mut, Panic {
    let f = make_f(7)
    return f.x
}
