//@ run-pass
// WP-A3 W2: cross-module generic-struct-return via turbofish. Expected exit code 9.
use w2_mod::{wrap, W}

fn main() -> i64 with Mut, Panic {
    let w = wrap::<i64>(9)
    return w.val
}
