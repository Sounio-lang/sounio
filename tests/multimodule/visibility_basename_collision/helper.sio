module bb::helper

use aa::helper::{secret}

// Calls a PRIVATE fn in a different module that happens to share this file's basename.
// Under basename-only module identity the two are one module and this is legal.
pub fn entry() -> i64 { secret() }
