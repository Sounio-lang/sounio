// Dependency half of the -O chained-call accumulator regression fixture.
// The caller assigns this function's result back into the SAME variable
// several times in a row; the accumulator must survive each call.
pub fn occ_add(a: i64, b: i64) -> i64 {
    return a + b
}
