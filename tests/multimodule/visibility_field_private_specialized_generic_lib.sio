//@ run-pass
//
// Copilot review (PR #2515), comment 4109848820: sibling of
// visibility_fn_private_specialized_generic_lib.sio /
// visibility_struct_private_specialized_generic_lib.sio, but for a
// PRIVATE FIELD instead of a private function/struct itself. See the
// _main.sio fixture's own comment for the bug this pair reproduces.
pub struct PrivateFieldBig<F> {
    x: F,
}

pub fn identity<T>(x: T) -> T {
    x
}

pub fn make_private_field_big(v: i64) -> PrivateFieldBig<i64> {
    PrivateFieldBig { x: v }
}
