// Exported (pub(crate)); loaded FIRST, so it is met before the private one and
// must be tracked, not silently ignored. The private `helper` in pfi_rm_a is
// then renamed away, which leaves this one unique: no collision remains.
pub(crate) fn helper() -> i64 { 20 }
pub fn rb_get() -> i64 { helper() }
