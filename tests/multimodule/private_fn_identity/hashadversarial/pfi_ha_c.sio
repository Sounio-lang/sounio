// `helper__m1` (what module 1's colliding `helper` would be renamed to) shares
// ast_name_hash with the distinct, real, unrelated `helper__lR` -- confirmed:
// both hash to 8246400112883890369 under djb2. A hash-only availability check
// (no exact follow-up) marks the generated name unsafe anyway.
pub fn helper__lR() -> i64 { 999 }
