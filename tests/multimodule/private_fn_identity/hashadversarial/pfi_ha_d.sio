// `helper__m2` (module 2's candidate rename) shares ast_name_hash with the
// distinct, real, unrelated `helper__lS` the same way (confirmed: both hash
// to 8246400112883890370). With BOTH candidate names blocked by a hash-only
// check, neither module's `helper` could be renamed -- a genuine false
// refusal of a program with no real generated-name collision at all.
pub fn helper__lS() -> i64 { 888 }
