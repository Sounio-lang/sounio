// `pub(crate)` is exported exactly like `pub`: importers name it, so it cannot be
// renamed. Two of them with one name is an unresolvable collision -> refuse.
pub(crate) fn helper() -> i64 { 1 }
pub fn ra_get() -> i64 { helper() }
