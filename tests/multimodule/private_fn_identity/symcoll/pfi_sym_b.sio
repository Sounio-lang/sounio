struct Counter { n: i64 }

impl Counter {
    fn bump(self) -> i64 with Mut, Panic, Div, Alloc { self.n + 500 }
}

pub fn b_call(n: i64) -> i64 with Mut, Panic, Div, Alloc {
    let c = Counter { n: n }
    c.bump()
}
