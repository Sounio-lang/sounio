use madaros_imported_call_arity_13_dep::{imported_ordered_sum_7, imported_ordered_sum_8, imported_ordered_sum_9, imported_ordered_sum_13}

fn main() with IO, Panic {
    assert(imported_ordered_sum_7() == 140)
    assert(imported_ordered_sum_8() == 204)
    assert(imported_ordered_sum_9() == 285)
    assert(imported_ordered_sum_13() == 819)
    println("MADAROS_IMPORTED_CALL_ARITY_13_OK")
}
