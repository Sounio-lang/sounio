//@ run-pass
//
// Copilot review (PR #2515): sibling of visibility_fn_private_lib.sio, but
// paired with a _main.sio that also instantiates a generic -- forcing
// check_items_verdict_boot4_with_module_map (self-hosted/check/mod.sio),
// the specialized multi-module checking path, instead of the ordinary
// check_modules_verdict_boot4_with_visibility. See the _main.sio fixture's
// own comment for why that specific path matters here.
pub fn identity<T>(x: T) -> T {
    x
}

fn private_fn() -> i64 {
    13
}
