//@ run-pass
// Main module: imports math_lib for multi-module compilation testing
// Standalone-valid: defines its own add for souc check compatibility

fn add(a: i64, b: i64) -> i64 {
    a + b
}

fn main() -> i32 with IO {
    let result = add(3, 4)
    0
}
