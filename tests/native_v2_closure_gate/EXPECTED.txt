# No-capture closure gate for the modular native-v2 backend (--native-v2-compile).
# Positive (p*) witnesses: name -> expected process exit code (compile must emit ELF).
p01_nocapture_inc_42 42
p02_nocapture_add2_42 42
p03_nocapture_square_49 49
p04_nocapture_noarg_99 99
# p05: a closure body that CALLS a genuine top-level fn (not a capture) must still resolve.
p05_calls_toplevel_42 42
# Reject (r*) witnesses: capturing closures MUST NOT emit an ELF (no silent miscompile) AND
# MUST NOT crash the compiler (clean reject, rc<128 -- guards the lookup_fn_by_name SIGSEGV).
r01_capture_rejected REJECT
# r02: a captured var that SHADOWS a top-level fn name must reject, never emit that fn's
# address (the checker permits the shadow; without the enclosing-locals capture check this
# was a silent fn-address miscompile).
r02_capture_shadows_fn REJECT
