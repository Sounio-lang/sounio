# amortized — fixtures intentionally absent (Garden)

No `pass.sio` / `refuse.sio` pair lives here.

**PROTOCOLO v3:** missing fixtures ⇒ gate derives **Garden**. Do not invent a
`pass.sio` that only exercises a TyNamed surface label.

Historical prose census (v1/v2) recorded internal checker seeds
(`ty_*` constructors, epistemic bridges) as **Hypothesis**. That archaeology
is preserved in `docs/audit/TYPEKIND_ARCHAEOLOGY_FH_2026-08-19.md` as dig notes.
It does **not** write the ladder position — only a real pass/refuse pair would.
