// TypeKind Proof — refuse fixture. Proof and Lemma are distinct tags (E008).
// This is not a refusal of the proposition: Proof<bool> -> Proof<i64> still
// checks OK. If that inner mismatch starts being refused, add it here and
// this file becomes the weaker tag pair.

fn coerce(p: Proof<bool>) -> Lemma<bool> {
    p
}

fn main() -> i64 {
    0
}
