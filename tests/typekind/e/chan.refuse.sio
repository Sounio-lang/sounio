// TypeKind Chan — refuse fixture. A chan value is not i64 (E008 found chan).
// This does not test protocol mismatch: `chan i64` parse-fails before check.

fn bad(c: chan) -> i64 {
    c
}

fn main() -> i64 {
    0
}
