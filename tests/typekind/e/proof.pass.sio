// TypeKind Proof — pass fixture. Identity keeps TyProof in a module that
// compiles and runs. There is no surface that inhabits Proof<P> from a value
// (annotation_proof_basic.sio is E001). Do not treat this as a proof of P.

fn id(p: Proof<bool>) -> Proof<bool> {
    p
}

fn main() -> i64 {
    let f = id
    0
}
