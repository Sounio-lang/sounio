// TypeKind Chan — pass fixture. Bare `chan` is the keyword path (TypeChan).
// `chan T` does not parse on this Madaros, so the protocol argument is not
// constructed here.

fn id(c: chan) -> chan {
    c
}

fn main() -> i64 {
    let f = id
    0
}
