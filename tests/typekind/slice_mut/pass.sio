//@ run-pass
//@ description: typekind archaeology — TySliceMut construct via &!
fn take(s: &![i64]) -> i64 {
    s[0]
}
fn main() -> i32 {
    var a: [i64; 2] = [7, 8]
    let v = take(&!a)
    if v == 7 { 0 } else { 1 }
}
