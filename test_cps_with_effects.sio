// Test file that actually performs effects

effect Reader {
    fn read() -> i32
}

fn compute(x: i32) -> i32 with Reader {
    let val = perform Reader::read()
    x + val
}

fn main() -> i32 {
    handle<Reader> {
        compute(10)
    } with {
        fn read() { resume 42 }
    }
}
