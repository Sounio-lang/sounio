use kernel_ir_wmma_lean::*
use lower_to_ptx_wmma_lean::*
use ptx::*

fn main() -> i32 with IO, Mut, Panic, Div, Alloc {
    var k = gpu_kernel_new()
    k.kernel_name = ki_ir_name_from_str("x", 1)
    let buf = gpu_lower_to_ptx(k)
    ptx_buf_print(buf)
    0
}
