// Control for LIMITATION P4: imports a leaf with 10,200 assignment
// statements (51 functions x 200, generated by p4_gen_fixture.py).
// Expected and observed: builds, runs, prints 400.
use p4_thinlink_control_leaf::{f0, f1}
fn main() -> i32 with IO {
    println(f0() + f1())
    return 0
}
