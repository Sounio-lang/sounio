// Demo for stdlib/prob/mcmc.sio
//
// This file demonstrates usage of the prob::mcmc module.
// Module source: stdlib/prob/mcmc.sio

    return current
}

fn main() -> i32 {
    println("=== MCMC Metropolis-Hastings Test ===")
    println("")

    // Start far from target (5.0)
    var x = 0.0

    println("Starting value = ")
    println(x)

    // Take 10 MH steps
    var i = 0
    let max_iter = 10

    if i < max_iter { x = mh_step(x); i = i + 1 }
    if i < max_iter { x = mh_step(x); i = i + 1 }
    if i < max_iter { x = mh_step(x); i = i + 1 }
    if i < max_iter { x = mh_step(x); i = i + 1 }
    if i < max_iter { x = mh_step(x); i = i + 1 }
    if i < max_iter { x = mh_step(x); i = i + 1 }
    if i < max_iter { x = mh_step(x); i = i + 1 }
    if i < max_iter { x = mh_step(x); i = i + 1 }
    if i < max_iter { x = mh_step(x); i = i + 1 }
    if i < max_iter { x = mh_step(x); i = i + 1 }

    println("After 10 steps = ")
    println(x)
    println("")

    // Check that x moved toward 5.0
    if x > 0.5 && x < 10.0 {
        println("TEST PASSED - MCMC moves toward target")
        return 0
    }

    println("TEST FAILED")
    return 1
}
