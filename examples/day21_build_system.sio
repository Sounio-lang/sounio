/*
Aspirational example preserved below.
To restore, remove this stub and uncomment the original content.

// Example demonstrating Day 21 build system concepts
// This file showcases build system integration

// Module with dependencies
module math;

// Export a function
pub fn factorial(n: int) -> int {
    if n <= 1 {
        return 1;
    }
    return n * factorial(n - 1);
}

// Another function for the build graph
pub fn fibonacci(n: int) -> int {
    if n <= 1 {
        return n;
    }
    return fibonacci(n - 1) + fibonacci(n - 2);
}

// Main entry point
fn main() -> int {
    let f5 = factorial(5);
    let fib10 = fibonacci(10);
    return 0;
}

*/

struct Sample {
    value: i64
}

fn scale(item: Sample, factor: i64) -> Sample {
    return Sample { value: item.value * factor }
}

fn checksum(values: [i64]) -> i64 {
    var sum: i64 = 0
    for v in values {
        sum = sum + v
    }
    return sum
}

fn main() with IO {
    let seed = Sample { value: 3 }
    let scaled = scale(seed, 7)
    let data: [i64] = [scaled.value, 2, 5, 8]
    let total = checksum(data)
    println("example: day21_build_system")
    println(total)
}

