// Float arithmetic smoke test.
// Tests: f64 literals, add/sub/mul/div via exit code.
//
// Computes 3.0 * 2.0 = 6.0, then checks via integer comparison.
// Expected: exit code 0.

fn main() -> i64 with IO {
    let a = 3.0 + 2.0
    let b = 10.0 - a
    if b == a { 0 } else { 1 }
}
