// Minimal mutable variable test — isolate assignment bug.
// Expected: prints "10", exits 0.

fn print_int(n: i64) -> i64 with IO { 0 }
fn print_char(c: i64) -> i64 with IO { 0 }

fn main() -> i64 with IO, Mut {
    var x: i64 = 5
    x = x + 5
    print_int(x)
    print_char(10)
    0
}
