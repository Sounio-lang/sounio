/*
Aspirational example preserved below.
To restore, remove this stub and uncomment the original content.

// Example demonstrating Day 20 tooling features
// This file showcases formatting, linting, and analysis capabilities

fn calculate_factorial(n: int) -> int {
    if n <= 1 {
        return 1;
    }
    return n * calculate_factorial(n - 1);
}

fn process_data(values: [int]) -> int with Alloc {
    var sum = 0;
    for value in values {
        sum = sum + value;
    }
    return sum;
}

fn main() -> int {
    let result = calculate_factorial(5);
    let data = [1, 2, 3, 4, 5];
    let total = process_data(data);
    
    return 0;
}

*/

struct Sample {
    value: i64
}

fn scale(item: Sample, factor: i64) -> Sample {
    return Sample { value: item.value * factor }
}

fn checksum(values: [i64]) -> i64 {
    var sum: i64 = 0
    for v in values {
        sum = sum + v
    }
    return sum
}

fn main() with IO {
    let seed = Sample { value: 3 }
    let scaled = scale(seed, 7)
    let data: [i64] = [scaled.value, 2, 5, 8]
    let total = checksum(data)
    println("example: day20_tooling")
    println(total)
}

