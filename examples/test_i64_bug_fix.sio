// Test that 0_i64 is now correctly typed as i64
// Previously this was misclassified as a unit literal with unit "i64"

fn main() -> i32 {
    let x = 0_i64
    let y: i64 = 100

    // This should work - both are i64
    let z = x + y

    42
}
