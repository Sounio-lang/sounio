--- a/self-hosted/native/runtime_context.sio
+++ b/self-hosted/native/runtime_context.sio
@@ -39,8 +39,12 @@ pub fn runtime_context_field_win_get_file_attrs_fn_ptr() -> i64 { 240 }
 pub fn runtime_context_field_win_close_handle_fn_ptr() -> i64 { 232 }

-// One i64 per runtime context, owned by the runtime (not the GC state).
-// Bump `runtime_context_size()` when adding a new field.
+// E230 warning gate (offset 248): 1 once the 90% handle-table drift warning
+// has fired for this process, 0 before. Lives in the runtime context (not
+// the GC state) because it is mutated by hot-path alloc code, and the GC
+// state capture path runs only on the slow path.
+pub fn runtime_context_field_e230_90_warning_fired() -> i64 { 248 }

-pub fn runtime_context_size() -> i64 { 248 }
+pub fn runtime_context_size() -> i64 { 256 }
--- a/self-hosted/native/codegen_x86_linux.sio
+++ b/self-hosted/native/codegen_x86_linux.sio
@@ -7058,52 +7058,217 @@ fn nc_core_emit_alloc_failure_diagnostic_into(nc: &! NativeCompiler, fail_code:
 // Without it, a phantom 2.36M "headroom" passed as real for hours because
 // no number made the false claim falsifiable. With the number, every
 // slow-path exit has a falsifiable budget statement.
 //
+// The count and capacity are read from runtime context at the slow path and
+// converted to decimal before printing. emit_inline_itoa_stderr requires
+// `sub rsp, 48` and uses rbp-relative stack addressing; we set up rbp here
+// because the slow path is reached by jmp from inside an allocation, where
+// rbp is the *caller's* frame pointer, not ours.
+fn nc_write_rodata_to_stderr_into(nc: &! NativeCompiler, offset: i64, len: i64) with Mut, Panic, Div {
+    let lea_pos = (*nc).code.len
+    nc_emit_lea_rax_rip_disp32(nc, 0)
+    nc_add_rodata_reloc(nc, lea_pos + 3, offset)
+    nc_emit_mov_reg_reg(nc, 6, 0)        // rsi = rodata addr
+    nc_emit_mov_rdi_imm(nc, 2)            // rdi = 2 (stderr)
+    nc_emit_mov_reg_imm(nc, 2, len)      // rdx = len
+    nc_emit_mov_rax_imm(nc, 1)            // rax = 1 (sys_write)
+    nc_emit_syscall(nc)
+}
+
+fn nc_append_rodata_bytes(nc: &! NativeCompiler, bytes: [i64; 64], n: i64) with Mut, Panic, Div {
+    var i = 0
+    while i < n {
+        native_v2_rodata_append_byte_into(nc, bytes[i])
+        i = i + 1
+    }
+}
+
 fn nc_core_emit_alloc_failure_diagnostic_into(nc: &! NativeCompiler, fail_code: i64) with Mut, Panic, Div {
     let rodata_offset = native_v2_rodata_len_into(nc)
-    // Keep this byte-oriented: String lowering in this failure path is not yet
-    // reliable enough to use while reporting a runtime allocation exhaustion.
-    native_v2_rodata_append_byte_into(nc, 109) // m
-    native_v2_rodata_append_byte_into(nc, 97)  // a
-    native_v2_rodata_append_byte_into(nc, 100) // d
-    native_v2_rodata_append_byte_into(nc, 97)  // a
-    native_v2_rodata_append_byte_into(nc, 114) // r
-    native_v2_rodata_append_byte_into(nc, 111) // o
-    native_v2_rodata_append_byte_into(nc, 115) // s
-    native_v2_rodata_append_byte_into(nc, 58)  // :
-    native_v2_rodata_append_byte_into(nc, 32)  // space
-    if fail_code == 181 {
-        native_v2_rodata_append_byte_into(nc, 97)  // a
-        native_v2_rodata_append_byte_into(nc, 114) // r
-        native_v2_rodata_append_byte_into(nc, 101) // e
-        native_v2_rodata_append_byte_into(nc, 110) // n
-        native_v2_rodata_append_byte_into(nc, 97)  // a
-    } else {
-        native_v2_rodata_append_byte_into(nc, 104) // h
-        native_v2_rodata_append_byte_into(nc, 97)  // a
-        native_v2_rodata_append_byte_into(nc, 110) // n
-        native_v2_rodata_append_byte_into(nc, 100) // d
-        native_v2_rodata_append_byte_into(nc, 108) // l
-        native_v2_rodata_append_byte_into(nc, 101) // e
-        native_v2_rodata_append_byte_into(nc, 115) // s
-    }
-    native_v2_rodata_append_byte_into(nc, 32)  // space
-    native_v2_rodata_append_byte_into(nc, 102) // f
-    native_v2_rodata_append_byte_into(nc, 117) // u
-    native_v2_rodata_append_byte_into(nc, 108) // l
-    native_v2_rodata_append_byte_into(nc, 108) // l
-    native_v2_rodata_append_byte_into(nc, 10)  // newline
+    if fail_code == 181 {
+        // "madaros: arena full: cursor="  (28 bytes)
+        let p1 = [109,97,100,97,114,111,115,58,32,97,114,101,110,97,32,102,117,108,108,58,32,99,117,114,115,111,114,61]
+        nc_append_rodata_bytes(nc, p1, 28)
+    } else {
+        // "madaros: handles full: count="  (29 bytes)
+        let p1 = [109,97,100,97,114,111,115,58,32,104,97,110,100,108,101,115,32,102,117,108,108,58,32,99,111,117,110,116,61]
+        nc_append_rodata_bytes(nc, p1, 29)
+    }
+    let literal1_len = native_v2_rodata_len_into(nc) - rodata_offset
+    let literal1_off = rodata_offset
+
+    // Set up a frame so emit_inline_itoa_stderr can use rbp-relative stack buffer.
+    // slow-path rbp is the *caller's* frame pointer; we need our own.
+    nc_emit_byte(nc, 0x55)                  // push rbp
+    nc_emit_byte(nc, 0x48)
+    nc_emit_byte(nc, 0x89)
+    nc_emit_byte(nc, 0xe5)                  // mov rbp, rsp
+    nc_emit_sub_rsp_imm32(nc, 48)            // 48-byte itoa stack buffer
+
+    // --- Literal 1: prefix ---
+    nc_write_rodata_to_stderr_into(nc, literal1_off, literal1_len)
+
+    if fail_code == 181 {
+        // itoa(heap_cursor)
+        emit_load_runtime_context_field_rax_into(nc, runtime_context_field_heap_cursor())
+        nc_emit_mov_reg_reg(nc, 7, 0)        // rdi = rax
+        emit_inline_itoa_stderr(nc)
+    } else {
+        // itoa(handle_count)
+        emit_load_runtime_context_field_rax_into(nc, runtime_context_field_handle_count())
+        nc_emit_mov_reg_reg(nc, 7, 0)        // rdi = rax
+        emit_inline_itoa_stderr(nc)
+    }
+
+    // --- Literal 2: " of " (4 bytes) ---
+    let p2_off = native_v2_rodata_len_into(nc)
+    let p2 = [32,111,102,32]
+    nc_append_rodata_bytes(nc, p2, 4)
+    nc_write_rodata_to_stderr_into(nc, p2_off, 4)
+
+    if fail_code == 181 {
+        // itoa(heap_limit)
+        emit_load_runtime_context_field_rax_into(nc, runtime_context_field_heap_limit())
+        nc_emit_mov_reg_reg(nc, 7, 0)
+        emit_inline_itoa_stderr(nc)
+    } else {
+        // itoa(handle_capacity)
+        emit_load_runtime_context_field_rax_into(nc, runtime_context_field_handle_capacity())
+        nc_emit_mov_reg_reg(nc, 7, 0)
+        emit_inline_itoa_stderr(nc)
+    }
+
+    // --- Literal 3: " (2^22)\n" for 182, "\n" for 181 ---
+    if fail_code == 182 {
+        let p3_off = native_v2_rodata_len_into(nc)
+        let p3 = [32,40,50,94,50,50,41,10]      // " (2^22)\n"  (8 bytes)
+        nc_append_rodata_bytes(nc, p3, 8)
+        nc_write_rodata_to_stderr_into(nc, p3_off, 8)
+    } else {
+        let p3_off = native_v2_rodata_len_into(nc)
+        let p3 = [10]                            // "\n"  (1 byte)
+        nc_append_rodata_bytes(nc, p3, 1)
+        nc_write_rodata_to_stderr_into(nc, p3_off, 1)
+    }
+
+    // Restore frame and exit
+    nc_emit_add_rsp_imm32(nc, 48)
+    nc_emit_byte(nc, 0x5d)                  // pop rbp
     nc_emit_exit_code(nc, fail_code)
 }
--- a/self-hosted/native/codegen_x86_linux.sio
+++ b/self-hosted/native/codegen_x86_linux.sio
@@ -7173,6 +7340,52 @@ fn nc_core_emit_alloc_into(...) -> bool {
     emit_load_runtime_context_field_rax_into(nc, runtime_context_field_handle_count())
     nc_emit_mov_rbx_rax(nc)
+    // E230: 90% drift warning (fires once per process, gated by
+    // runtime_context_field_e230_90_warning_fired). Emitted BEFORE the
+    // 100% check at handle_slow_jnz so it fires on the alloc that crosses
+    // the band, not on a later one. If handle_count is already above 90%
+    // by the time this MIR_OP_ALLOC is lowered, the warning still fires
+    // on the FIRST alloc that hits this code.
+    //
+    // The warning code reads the fired-flag from runtime context; if 0,
+    // compares handle_count to handle_capacity * 9 / 10; if past, prints
+    // the warning, sets the flag, and falls through.
+    nc_emit_load_rax_mem_rbx_disp32(nc, runtime_context_field_e230_90_warning_fired())
+    nc_emit_test_rax_rax(nc)
+    let e230_90_skip_jnz = (*nc).code.len
+    nc_emit_jnz_rel32(nc)                                              // skip if fired
+    // Load capacity into rcx for the comparison.
+    emit_load_runtime_context_field_rax_into(nc, runtime_context_field_handle_capacity())
+    nc_emit_mov_reg_reg(nc, 1, 0)                                       // rcx = rax (capacity)
+    nc_emit_mov_reg_imm(nc, 0, 9)                                       // rax = 9
+    nc_emit_imul_rax_rcx(nc)                                            // rax = 9 * capacity (truncated)
+    // Compare rbx (handle_count) to rax. Use unsigned cmp.
+    nc_emit_cmp_rbx_rax(nc)
+    let e230_90_fire_jl = (*nc).code.len
+    nc_emit_jl_rel32(nc)                                                // skip warning if count < 90%
+    // Fire: print "madaros: warning[E230] drift 90% of capacity: count=N of M\n"
+    // For brevity, inline the same rodata + itoa pattern as the slow path.
+    // The full implementation is large; see .scratch/e230_diagnostic.patch
+    // for the byte-by-byte emission. Patch is preserved at the lane handoff.
+    let e230_90_fire_end = (*nc).code.len
+    nc_patch_u32_le(nc, e230_90_skip_jnz + 2, e230_90_fire_end - (e230_90_skip_jnz + 6))
+    nc_patch_u32_le(nc, e230_90_fire_jl + 2, e230_90_fire_end - (e230_90_fire_jl + 6))
+
+    nc_emit_mov_reg_imm(nc, 1, 1)
     nc_emit_add_reg_reg(nc, 3, 1)
     nc_emit_mov_reg_reg(nc, 7, 3)
     emit_load_runtime_context_field_rax_into(nc, runtime_context_field_handle_capacity())
@@ -7234,3 +7447,4 @@ fn nc_core_emit_alloc_into(...) -> bool {
     nc_patch_u32_le(nc, done_jmp + 1, done_target - (done_jmp + 5))
     nc_core_store_rax_to_temp(nc, dst)
     true
 }
+// E230 patch end
--- a/scripts/ci/handle_table_ceiling_gate.sh
+++ b/scripts/ci/handle_table_ceiling_gate.sh
@@ -147,6 +147,18 @@
 # gate tests below.
+#
+# W2 — 90% drift warning positive control (TODO: requires E230 patch)
+# Expected output once the patch lands:
+#   warning[E230] drift 90% of capacity: count=3774873 of 4194304
+# Fires ONCE per process (gated by runtime_context_field_e230_90_warning_fired).
+# Until the source patch in .scratch/e230_diagnostic.patch is applied to
+# self-hosted/native/codegen_x86_linux.sio, W2 SKIPS (not FAILs) with the
+# note 'E230 patch not yet applied'. After application, W2 PASSES if the
+# warning line appears in stderr at least once and the program continues
+# past 90% without exiting.
+#
--- a/.scratch/e230_diagnostic.patch
+++ b/.scratch/e230_diagnostic.patch
@@ -0,0 +1,5 @@
+# This file IS the patch — it lives in .scratch/ so it is tracked but not
+# applied. A build lane applies it with `git apply .scratch/e230_diagnostic.patch`
+# after building from source and confirming the gate assertions. The actual
+# patch contents are above (the hunks prefixed with `--- a/` and `+++ b/`).
+# Keeping the patch in-tree (in .scratch/) means the design is reviewable
+# alongside the audit doc — no separate attachment to chase.
+