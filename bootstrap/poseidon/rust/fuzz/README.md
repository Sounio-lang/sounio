# Poseidon Fuzz Targets

This directory contains local/manual `cargo fuzz` targets for:

- `load_bytes`
- `run`

Refresh the seed corpus from the canonical Poseidon SOIR fixtures with:

```bash
./refresh_corpus.sh
```
