# Event-normal source-face review

Review the scoped mathematical claim for the frozen witness subdomain.

The unrestricted degree-2 Taylor model uses normalized primary variables
`xi`, `eta`, `rho0`, `rho1`, `rho2`, and `rho3`, plus four dynamic carrier
coordinates. A source exit face is formed before carrier installation by exact
polynomial substitution `xi=-1` or `xi=+1`: powers of `xi` are evaluated
exactly, like monomials are accumulated, and the interval remainder is retained.

The face transport uses the unchanged event-normal reconditioner, strict
whole-slab transversality, interval Newton projection, exact `w=0` section
anchor, and complete branch accounting. It requires `xi` weight exactly zero
and positive represented-dependence weights for `eta` and `rho0` through
`rho3`. The two faces are transported independently rather than inferred from
the unrestricted support hull.

Assess whether successful LEFT and RIGHT runs rigorously enclose the images of
the exact `xi` faces. This claim alone must not establish exit inequalities,
degree, a covering relation, recurrence, chaos, or an open-problem solution.

