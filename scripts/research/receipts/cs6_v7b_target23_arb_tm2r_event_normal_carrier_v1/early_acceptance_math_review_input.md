# Focused review: early event projection

Review only the logical promotion below. Return PASS or a concrete missing
mathematical obligation.

The inherited diagnostic integrates a hash-bound leaf from an exact `w=0`
section state. After a strictly negative departure, it finds a later event tube
containing `w=0`. For each candidate, `project_upward_cover` does all of the
following on every leaf:

1. requires the TM2 event derivative `x*y-w-ZS` to have strictly positive lower
   endpoint;
2. requires the cheap Newton correction to lie strictly inside `[-1/256,1/256]`;
3. builds a two-sided signed Picard slab and requires its derivative lower
   endpoint to remain strictly positive on the whole slab;
4. requires the parametric interval-Newton event-time image to lie strictly
   inside that slab;
5. evaluates the TM2 flow at that event-time model, sets `w=0`, and applies a
   section-anchored rational carrier on `(x,y,ell)`;
6. requires exact projected `w=[0,0]` and a strictly positive lower endpoint for
   the projected normal `x*y-ZS`;
7. subdivides until every leaf succeeds, otherwise refuses the entire cover.

Job 9878 returned one projected leaf satisfying this full path, with all six
original variable weights (`xi,eta,rho0..rho3`) strictly positive and two exact
section-anchor input/output checks. The inherited diagnostic nevertheless set
`accepted=false` only because this success occurred before it had reproduced a
historical refusal generated by the old lineage-preserving carrier.

The new wrapper promotes that exact `early_projection` to accepted only if:

- the inherited record already says the projection was accepted;
- the carrier list is nonempty and equals `projected_leaves`;
- every serialized derivative and projected-normal lower endpoint is positive;
- every one of the first six variable-weight upper endpoints is positive;
- the aggregate and per-carrier six-variable flags are true;
- the receipt preserves `production_boundary_reproduced=false` and adds
  `historical_frozen_refusal_control_superseded=true`.

The verifier independently recomputes these interval signs, weights, counts,
and historical-control fields. It also requires at least two section anchors
and equality of exact input/output anchor checks with the anchor count.

Question: Is it mathematically sound to classify this as a certified local
upward interval-Newton projection of the whole retained leaf, despite not
reproducing the old method's refusal first? This does not claim a second return,
covering relation, recurrence, chaos, or solution of an open problem.
