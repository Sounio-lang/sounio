=== Task: math-review | Provider(s): xai zai ===
=== LLM Offload Fan-Out ===
Output dir: /tmp/llm-offload-qqrzx9
Providers: xai zai

  -> Sending to Grok 4.3 (grok-4.3, max=8192)...
  -> Sending to Z.AI GLM-5.2 (coding plan) (glm-5.2, max=8192)...
  <- Grok 4.3: DONE (1065 bytes)
  <- Z.AI GLM-5.2 (coding plan): DONE (28596 bytes)

=== Results ===

━━━ grok ━━━
[OK] 1. TM multiplication + parity tail
  Exact parity ranges on the cube are standard and tight for the monomials described.

[OK] 2. Zonotope conversion of residuals
  Odd/even shift to constant vector + generator correctly over-approximates each monomial range.

[OK] 3. Exact-rational Q + summed radii
  Invertibility + componentwise outward rounding + d_j = ∑ sup|c_j| encloses the Minkowski sum (dependencies absorbed by over-approximation).

[OK] 4. Repeated reconditioning + integration
  Picard tube, order-12 polynomial + Lagrange remainder, and h L(B)<1 are the standard validated steps; no gap shown.

[OK] 5. Existence + orientation of crossing
  Strict pre/post sign change of w on a tube that contains every trajectory plus positive normal implies at least one transverse zero crossing per trajectory.

[OVERREACH] 6. Uniqueness / first-return claim
  Conditions prove ≥1 crossing in the step but give no bound on the number of zeros inside the tube; an extra argument (no prior crossing or monotonicity of w) is required for “first return.”


━━━ zai ━━━
The user wants a hostile mathematical audit of an artifact describing a verified computation using Taylor Models (TM) with Arb intervals. The computation aims to prove a first-return (Poincaré section crossing) property for a dynamical system.

Let me break down the artifact into claims and audit each mathematically.

**Setup:**
- State components are total-degree-2 polynomials in 6 variables (xi, eta, rho_1, ..., rho_4) plus an Arb interval remainder.
- Domain: (xi, eta, rho) in [-1,1]^6.

**Claim 1: TM Multiplication and Parity Tail Enclosure**
"Multiplication retains all degree-at-most-2 terms and puts every higher-degree monomial into the interval remainder using its exact parity range on the cube: [-1,1] if any exponent is odd, [0,1] when every exponent is even and the monomial is nonconstant."

Let's check this.
A monomial $m = x_1^{a_1} \cdots x_6^{a_6}$ on $[-1,1]^6$.
If any $a_i$ is odd, the function takes both positive and negative values, and by symmetry achieves $[-1, 1]$.
If all $a_i$ are even, the minimum is $0$ (at $x_i=0$) and maximum is $1$ (at $x_i=\pm 1$). So the range is $[0, 1]$.
This is exactly correct.

"Products with existing interval remainders use: range(P)*S + range(Q)*R + R*S."
Standard interval arithmetic for $(P+R)(Q+S) = PQ + PS + QR + RS$.
The cross terms $PS$ evaluate to $range(P) \cdot S$, etc. This is correct (though conservative, which is fine for over-approximation).
Wait, is there an under-enclosure?
$R*S$ where $R, S \in \mathbb{IR}$. The interval product $R \cdot S$ is the exact range of $\{r \cdot s \mid r \in R, s \in S\}$. So it is correct.

**Claim 2: Picard Iteration and Lagrange Remainder**
"Each step has a validated Picard tube B, strict h*L(B)<1, an order-12 polynomial, and the componentwise Lagrange remainder range(a_13(B))*h^13. The raw TM endpoint range must be contained in B."
Wait, the Taylor model is degree 2, but the Picard iteration step uses an order-12 polynomial and Lagrange remainder.
The ODE is $x' = f(x)$. Picard operator $P(y)(t) = x_0 + \int_0^t f(y(s)) ds$.
Usually, Taylor models of order $n$ have remainder $\frac{1}{(n+1)!} \max f^{(n+1)} t^{n+1}$.
Wait, the artifact says "order-12 polynomial... componentwise Lagrange remainder range(a_13(B))*h^13". This is standard for Taylor series integration.
BUT wait, the state carrier is degree 2. How can the Picard step be order 12 if the carrier is degree 2?
Ah, "The raw TM endpoint range must be contained in B". Maybe the high-order Picard check is just to validate the step size $h$ and the tube $B$, or perhaps it computes a higher order expansion to shrink $B$.
Actually, wait. If the state carrier is $X_i = P_i + R_i$ with $P_i$ degree 2, how is it integrated over time?
The artifact says "repeated reconditioning and time integration". It seems the polynomial part is kept at degree 2, and the remainder absorbs everything else.
Wait, if $P_i$ is a degree-2 polynomial, its time derivative is a degree-2 polynomial (or bounded by degree 2). Then $f(X)$ has degree up to some $D$. The integral over time adds 1 to the degree? No, if $X_i$ is degree 2, $X_i^2$ is degree 4. The integral of $X_i^2$ would be degree 4 in the variables.
Ah, "Multiplication retains all degree-at-most-2 terms and puts every higher-degree monomial into the interval remainder".
So the time integration does NOT increase the degree of $P_i$ because higher degree terms are immediately pushed to $R_i$.
Wait, but if you push them to $R_i$ at each sub-step, how do you maintain a sharp enclosure?
If you only keep degree 2, you are essentially doing interval arithmetic with a quadratic enclosure. Is this mathematically sound? Yes, as long as the remainder correctly bounds the tail.
Does the artifact mention the ODE solution flow? "normalized autonomous-flow coefficients a_k = D_t^k phi(0,q) / k!".
This implies Taylor expansion in TIME, not in state variables.
Let $q$ be a state. The flow $\phi(t, q)$ has Taylor expansion in $t$: $\sum \frac{t^k}{k!} \frac{d^k}{dt^k} \phi(0, q)$.
Here $\frac{d^k}{dt^k} \phi(0, q)$ is a polynomial in $q$. If we evaluate this at $X_i$, it becomes a polynomial in the variables.
Wait, $a_k(B)$ is the $k$-th derivative evaluated on the Picard tube $B$? Yes, $a_k(D) = D_t^k \phi(0,q) / k!$.
If $q$ is bounded by $B$, then $a_{13}(B)$ bounds the 13th Taylor coefficient.
But wait, $a_k$ is a function of $q$. To bound it over the initial conditions, we need to evaluate it over the initial set.
Wait, the artifact says: "order-12 polynomial, and the componentwise Lagrange remainder range(a_13(B))*h^13. The raw TM endpoint range must be contained in B."
This sounds like a standard validated ODE integration step:
1. Define a Taylor polynomial of degree 12 in TIME.
2. The remainder is bounded by $\frac{M_{13}}{13!} h^{13}$.
3. The Picard tube $B$ must enclose the trajectory.
Is the mathematical statement here correct?
Yes, if $a_{13}(B)$ bounds the 13th derivative of the flow over the tube $B$, the Lagrange remainder is exactly $range(a_{13}(B)) h^{13}$. Wait, $h^{13}$ or $h^{13}/13!$? The artifact says $a_k = D_t^k \phi / k!$, so the $k$-th term in the Taylor series is $a_k t^k$. The next term is $a_{13} t^{13}$. So the remainder is bounded by $range(a_{13}(B)) \cdot h^{13}$. This is correct.

Let's look at the reconditioning step:
"Pure xi, eta monomials are copied unchanged."
"Every coefficient vector g of a monomial containing a residual variable becomes a zonotope generator:"
Let a monomial be $m(\rho)$. It has a coefficient $g$.
If $m(\rho)$ has an odd exponent, its range on $[-1,1]^4$ is $[-1,1]$. So the term is $g \cdot m(\rho)$, which ranges over the interval $[-|g|, |g|]$ (if $g$ is a vector, it traces the segment $[-g, g]$). This is exactly a generator of a zonotope!
If all exponents are even, its range is $[0,1]$. The term $g \cdot m(\rho)$ traces the segment $[0, g]$. This is equivalent to the center $g/2$ plus the generator $g/2$.
This is mathematically exact and correct for zonotope enclosure.
"each scalar remainder interval contributes its midpoint to the retained constant and its radius as a separate coordinate-axis generator."
A scalar interval $[c-r, c+r]$ is equivalently $c + [-r, r]$. Here the generator is the vector $r \cdot e_i$, where $e_i$ is a standard basis vector (coordinate-axis generator). Wait, if we have a generic coordinate system, we add it to the basis. The artifact says "separate coordinate-axis generator". Is it in the $\rho$ basis or some abstract basis? It probably means a generator vector aligned with some axis.
This is all standard and correct.

Now, the rational Gram-Schmidt:
"A real Gram-Schmidt basis is derived from generator midpoints... rounded to finite decimal strings, and then interpreted as an exact rational matrix Q."
Wait! Gram-Schmidt on *generator midpoints*? Why midpoints? The generators are vectors $g$. We want to find a basis. Usually we do GS on the generators themselves.
Wait, if we do GS on $g$, we get an orthogonal matrix. But the artifact says "GS basis is derived from generator midpoints, rounded to finite decimal strings, interpreted as exact rational matrix Q."
Wait, if we do GS on the midpoints, does $Q$ span the generators?
Wait, the next step: "For each Arb interval generator vector g, compute c = Q^-1 * g with outward rounding, verify componentwise that outward-rounded Q * c contains g..."
If we verify that $Q c \ni g$ componentwise (which means $Q \cdot c \supseteq g$ in the interval sense, so for all $j$, $(Q c)_j \supseteq g_j$), then any sum $\sum \alpha_k g_k$ (where $\alpha_k \in [-1,1]$) is contained in the interval sum $\sum \alpha_k Q c_k$.
Wait! $\sum \alpha_k g_k = Q (\sum \alpha_k c_k)$.
So the zonotope $\{ \sum \alpha_k g_k \mid \alpha_k \in [-1,1] \}$ is exactly $Q (\sum \alpha_k c_k)$.
The set $Z = \sum \alpha_k c_k$ is a zonotope in the coefficient space.
We want to bound this by a box $d_1 \rho_1 + \dots + d_4 \rho_4$, where $\rho \in [-1,1]^4$.
The artifact bounds it by: $d_j = \sum_k \sup |c_{j,k}|$.
Since $c_{j,k}$ are intervals (because of outward rounding), $\sup |c_{j,k}|$ is the maximum absolute value in the interval $c_{j,k}$.
Then $\sum_k \alpha_k c_{j,k}$ is bounded by $[-d_j, d_j]$ for ANY choice of $\alpha_k \in [-1,1]$.
Wait. The generators are intervals? "For each Arb interval generator vector g".
If $g$ is an interval vector, $g_k$ are intervals.
Then $Q c_k \ni g_k$ means $\forall j, (Q c_k)_j \supseteq (g_k)_j$.
So $c_k$ is an interval vector.
Then $\sum_k \alpha_k c_k$ with $\alpha_k \in [-1,1]$ is an interval vector.
Its $j$-th component is bounded by $\sum_k \sup |(c_k)_j|$.
This is mathematically correct! It overbounds the Minkowski sum of the interval vectors $c_k$.
Wait, does it correctly overbound the zonotope?
The actual set of reachable values in the coefficient space is $S = \{ \sum \alpha_k c_k^{mid} \mid \alpha_k \in [-1,1] \}$ intersected with the interval uncertainties.
Actually, since $c_k$ are intervals, the set is $\sum_k [-1,1] \cdot c_k = \sum_k c_k \cdot [-1,1]$.
The $j$-th coordinate of this set is an interval $\sum_k (c_k)_j \cdot [-1,1]$.
The exact range of $(c_k)_j \cdot [-1,1]$ is $[-\sup |(c_k)_j|, \sup |(c_k)_j|]$.
So the exact range of the $j$-th coordinate is $\sum_k [-\sup |(c_k)_j|, \sup |(c_k)_j|] = [-d_j, d_j]$.
Wait, this is EXACT, not an overbound, for the interval extension.
Since $Q$ is a linear map, $Q \cdot ([-d_1, d_1] \times \dots \times [-d_4, d_4])$ is a box in the original space, scaled by $Q$.
Wait! $Q \cdot diag(d) \cdot \rho$. This is a parallelepiped (zonotope with 4 generators, which are the columns of $Q \cdot diag(d)$).
Since it contains $\sum_k [-1,1] \cdot g_k$, it contains the original zonotope!
Is there any dependency problem?
"whether this encloses the entire Minkowski sum despite coefficient dependencies"
The coefficients $c_{j,k}$ are just constants (intervals). There is no dependency between different $k$'s other than they are combined linearly.
Because we bound the linear combination by $d_j \cdot \rho_j$, where $\rho_j$ is a single variable in $[-1,1]$, we are treating the sum of generators as a single generator. This is a standard overbound (box enclosure of a zonotope). It loses the dependency between the generators $g_k$ in the $j$-th coordinate direction!
Wait, the artifact says: "summed coordinate radii, and whether this encloses the entire Minkowski sum despite coefficient dependencies".
The dependence between the generators is completely ignored by taking the sum of absolute values! $d_j = \sum_k \sup |c_{j,k}|$.
This is an OVER-approximation. It is correct (no under-enclosure), but conservative. So it is mathematically sound for an existence proof. The prompt asks to "Return BLOCKER for any possible under-enclosure."
Since $[-\sum |a_k|, \sum |a_k|]$ contains $\sum [-|a_k|, |a_k|]$, which contains $\sum a_k [-1,1]$, there is NO under-enclosure here.

Let's check "repeated reconditioning and time integration".
"Pure xi, eta monomials are copied unchanged." - This means the dependency on the initial conditions (xi, eta) is preserved exactly! This is crucial for a return map.
"Every coefficient vector g of a monomial containing a residual variable becomes a zonotope generator... The new residual carrier is Q*diag(d)*rho".
Wait. If the new residual carrier is $Q \cdot diag(d) \cdot \rho$, then the state component is:
$X_i = \text{const} + \text{pure xi, eta terms} + \sum \text{terms with residual variables}$.
When we convert the terms with residual variables into a zonotope, and then wrap it into a new parallelepiped $Q \cdot diag(d) \cdot \rho$, we have reparameterized the residual variables!
The old residual variables were some abstract variables (let's call them $\rho_{old}$). The new ones are $\rho$.
BUT, what about the degree-2 terms containing BOTH $(xi, eta)$ AND $\rho$?
Ah! "Every coefficient vector g of a monomial containing a residual variable becomes a zonotope generator".
Let's read carefully: "if any exponent is odd, its monomial range is [-1,1], so use generator g; if all exponents are even, its range is [0,1], so add g/2 to the retained constant vector and use generator g/2".
Wait. This applies to monomials ONLY in $\rho$, or monomials in ANY variables?
"Every coefficient vector g of a monomial containing a residual variable..."
Does it mean monomials like $xi \cdot \rho_1$?
If $m = xi \cdot \rho_1$, its range on the cube $[-1,1]^6$ is $[-1,1]$ (since $xi$ has an odd exponent, and $\rho_1$ has an odd exponent).
Wait! The rule says:
"if any exponent is odd, its monomial range is [-1,1], so use generator g".
But if the monomial is $xi \cdot \rho_1$, its coefficient $g$ depends on NOTHING (it's a constant vector).
Wait, if $g$ is just a constant, and we replace $xi \cdot \rho_1$ with a generator $g$, we are treating $xi \cdot \rho_1$ as a variable in $[-1,1]$.
But doing this destroys the dependence on $xi$!
Let's check this: "Multiplication retains all degree-at-most-2 terms and puts every higher-degree monomial into the interval remainder using its exact parity range... Products with existing interval remainders use range(P)*S + range(Q)*R + R*S."
"After every step the raw TM is reconditioned. Pure xi,eta monomials are copied unchanged. Every coefficient vector g of a monomial containing a residual variable becomes a zonotope generator... if any exponent is odd, its monomial range is [-1,1], so use generator g".
If a monomial contains a residual variable, say $m = \rho_1$. Its range is $[-1,1]$.
What if $m = xi \cdot \rho_1$? The artifact says "Every coefficient vector g of a monomial containing a residual variable becomes a zonotope generator... if any exponent is odd, its monomial range is [-1,1], so use generator g".
If $m = xi \cdot \rho_1$, $xi$ is a source variable. $\rho_1$ is a residual variable.
If we replace $xi \cdot \rho_1$ by $g \cdot \text{generator}$, we are bounding $xi \cdot \rho_1 \in [-1,1]$.
But $g$ is just a coefficient. So $g \cdot xi \cdot \rho_1$ is bounded by $[-|g|, |g|]$.
By converting this to a generator $g$ multiplied by a new residual variable $\rho_{new} \in [-1,1]$, we LOSE the dependence on $xi$.
Is this an under-enclosure? No, because the actual range of $xi \cdot \rho_1$ is $[-1,1]$, so the generator $g$ correctly bounds it.
BUT wait. Does the text imply that ALL degree-2 terms containing a residual variable are converted to generators?
"After every step the raw TM is reconditioned... Every coefficient vector g of a monomial containing a residual variable becomes a zonotope generator".
Wait, if we do this, the degree-2 terms containing $\rho$ are ABSORBED into the zonotope remainder!
This means the new Taylor model ONLY has pure $(xi, eta)$ monomials plus a constant, plus the new residual zonotope!
Is this correct?
Let's see. If $P_i = c_0 + c_1 \xi + c_2 \eta + c_3 \xi^2 + c_4 \eta^2 + c_5 \xi \eta + \text{terms with } \rho$.
The terms with $\rho$ are $c_{k} m_k(\xi, \eta, \rho)$.
The text says: "Every coefficient vector g of a monomial containing a residual variable becomes a zonotope generator: if any exponent is odd, its monomial range is [-1,1], so use generator g."
Wait. If the monomial contains $\xi$, say $m = \xi \rho_1$, its range is $[-1,1]$. The coefficient is $g$.
Then the term is $g \cdot \xi \rho_1$.
The text says: "its monomial range is [-1,1], so use generator g".
Wait, if we use generator $g$, we are bounding $g \cdot \xi \rho_1$ by $[-1,1] \cdot g$.
This means we are completely discarding the $\xi$ and $\rho_1$ dependence and just using the fact that the monomial $\xi \rho_1$ is bounded in $[-1,1]$.
Is this an under-enclosure?
No, the range of $\xi \rho_1$ on $[-1,1]^2$ is indeed $[-1,1]$. So bounding it by $[-1,1]$ is mathematically correct (over-approximation).
But wait. Is the coefficient $g$ a constant?
"Multiplication retains all degree-at-most-2 terms". The polynomial is in 6 variables. So $P_i$ is a polynomial in $(\xi, \eta, \rho_1, \dots, \rho_4)$.
If $P_i$ has a term like $\xi \rho_1$, its coefficient is a constant vector.
So $g$ is a constant vector.
So $g \xi \rho_1$ is bounded by $[-1,1] \cdot g$.
So replacing it with a generator $g$ (which corresponds to a new independent variable $\rho_{new} \in [-1,1]$) is correct.
BUT, does the text mean $g$ is a constant vector, or could $g$ depend on $\xi, \eta$?
"Every coefficient vector g of a monomial... becomes a zonotope generator". This phrasing implies $g$ is the coefficient vector (which is a constant in Arb).
So this step is valid. It over-approximates the $\rho$-containing terms.

Let's re-read carefully: "Every coefficient vector g of a monomial containing a residual variable becomes a zonotope generator... if all exponents are even, its range is [0,1], so add g/2 to the retained constant vector and use generator g/2".
Wait, what if a monomial containing $\rho$ ALSO contains $\xi$, e.g., $m = \xi^2 \rho_1^2$?
Exponents: $\xi^2$ (even), $\rho_1^2$ (even). Range is $[0,1]$.
So we add $g/2$ to the constant vector, and use generator $g/2$.
This means $\xi^2 \rho_1^2$ is replaced by $1/2 + 1/2 \rho_{new}$.
But $\xi^2 \rho_1^2$ actually depends on $\xi$. By replacing it with a constant $1/2$ plus a generator, we lose the dependence on $\xi$.
Is this correct? Yes, because $\xi^2 \rho_1^2 \in [0,1]$, so $g \xi^2 \rho_1^2 \in [0, g]$.
Replacing it with $g/2 + g/2 \rho_{new}$ (where $\rho_{new} \in [-1,1]$) gives the exact range $[0,g]$.
This is correct, though it discards the dependence on $\xi$.

Wait! What if the monomial is $\xi \rho_1$?
Exponents: $\xi$ (odd), $\rho_1$ (odd). Any exponent is odd -> range is $[-1,1]$.
So we use generator $g$.
This discards the dependence on $\xi$.
Is this a problem for the overall proof?
The proof needs to show that for EVERY initial point in the leaf (defined by $\xi, \eta$), there is a crossing.
If we discard the dependence on $\xi, \eta$ when bounding the remainder, the remainder is valid for ALL $\xi, \eta$.
So the TM enclosure $X_i = P_i(\xi, \eta) + R_i(\rho)$ is a valid over-approximation for all $(\xi, \eta)$.
Wait, if $P_i(\xi, \eta)$ is the exact polynomial in $\xi, \eta$, and $R_i(\rho)$ bounds ALL the $\rho$ terms uniformly over $\xi, \eta$, then for a specific $\xi, \eta$, the actual remainder is bounded by $R_i(\rho)$.
Yes, because the bound on $g \xi \rho_1$ is $[-|g|, |g|]$, which holds for all $\xi \in [-1,1]$.
So this is a valid uniform over-approximation. The enclosure is correct.

Let's look at the Picard tube and step validation.
"validated Picard tube B, strict h*L(B)<1, normalized autonomous-flow coefficients a_k=D_t^k phi(0,q)/k!, an order-12 polynomial, and the componentwise Lagrange remainder range(a_13(B))*h^13. The raw TM endpoint range must be contained in B."
Lipschitz constant $L(B)$ of the ODE vector field $f$ on $B$.
If $h L(B) < 1$, the Picard operator is a contraction on $B$.
The Taylor polynomial of order 12 is computed. The remainder is $O(t^{13})$.
The raw TM endpoint is checked to be in $B$.
Wait, $a_{13}(B)$ means we evaluate the 13th derivative of the flow on the tube $B$.
Is this correct?
Yes, if the initial condition is $X(0)$, the flow is $X(t) = \sum_{k=0}^{12} a_k(X(0)) t^k + a_{13}(\xi) t^{13}$ for some $\xi$ on the line between $X(0)$ and $X(t)$.
If $X(t) \in B$ and $X(0) \in B$, then $\xi \in B$, so $a_{13}(\xi) \in range(a_{13}(B))$.
So the remainder is indeed bounded by $range(a_{13}(B)) \cdot h^{13}$.
Wait. Is $a_{13}(B)$ evaluated using interval arithmetic?
Yes, "componentwise Lagrange remainder range(a_13(B))*h^13".
Wait, is the Taylor model polynomial part of degree 2 compatible with an order-12 polynomial?
The artifact says: "Each step has a validated Picard tube B ... an order-12 polynomial ... The raw TM endpoint range must be contained in B."
Wait, does the TM step use order-12 polynomial for the TIME expansion?
If so, it computes $X_{12}(h) = \sum_{k=0}^{12} a_k(X_0) h^k$.
Then it truncates this back to degree 2 in the variables, pushing the rest into $R$.
This is a standard TM integration scheme (using high order in time, then reconditioning to low degree in state).
Is there any mathematical flaw?
"strict h*L(B)<1". The contraction factor is $h L(B)$. If strictly less than 1, Banach fixed-point theorem guarantees existence and uniqueness of the solution in $B$.
"range(a_13(B))*h^13". The remainder bound is added to the TM.
"The raw TM endpoint range must be contained in B." This is the standard validated TM step check.
All mathematically sound.

Let's look at the final claim:
"The full pre-step w interval was strictly negative; the full post-step w interval was strictly positive. A Picard tube for that step enclosed every trajectory and x*y-zs was strictly positive on the tube. The claim is only: every initial point in the exact leaf has one negative-to-positive w=0 crossing in that time step, with positive normal."

Let's check the crossing claim.
"w" is some function of the state, $w(X)$. We want to find $t \in [0, h]$ such that $w(X(t)) = 0$.
Pre-step: $w(X(0)) \subset (-\infty, 0)$. So $w(X(0)) < 0$ for all initial points.
Post-step: $w(X(h)) \subset (0, \infty)$. So $w(X(h)) > 0$ for all endpoints.
Since $X(t)$ is a continuous trajectory, by the Intermediate Value Theorem, there is at least one $t^* \in [0, h]$ such that $w(X(t^*)) = 0$.
So existence of a crossing is guaranteed.
"with positive normal".
The normal is given by $\nabla w$. The artifact says "x*y-zs was strictly positive on the tube."
Wait, what is $x*y-zs$?
"normalized autonomous-flow coefficients a_k=D_t^k phi(0,q)/k!"
"strictly positive on the tube".
Wait, the normal velocity is the derivative of $w$ along the flow: $\frac{d}{dt} w(X(t)) = \nabla w \cdot f(X(t))$.
If this derivative is strictly positive, then $w$ is strictly increasing along the trajectory.
If $w$ is strictly increasing, the crossing is transverse (not tangent), and the normal orientation is well-defined and positive.
Does the text say $\nabla w \cdot f > 0$?
"x*y-zs was strictly positive on the tube."
Maybe $w$ is a function such that $\dot{w} = x \cdot y - z \cdot s$ or something? The text says "x*y-zs was strictly positive on the tube."
Whatever $x*y-zs$ represents, if it is the time derivative of $w$, then $\dot{w} > 0$.
Wait, if $\dot{w} > 0$, then $w$ is strictly monotonically increasing.
This implies the crossing is UNIQUE in the time step.
Let's check: does $\dot{w} > 0$ imply uniqueness?
Yes. If $w(t_1) = 0$ and $w(t_2) = 0$ for $0 \le t_1 < t_2 \le h$, then by Rolle's theorem there is a point where $\dot{w} = 0$, which contradicts $\dot{w} > 0$.
So uniqueness of the crossing in the step IS proven if $x*y-zs$ is indeed $\dot{w}$.
Let's see if $x*y-zs$ being strictly positive proves positive normal.
If $\dot{w} = \nabla w \cdot f > 0$, then at the crossing point $t^*$ where $w(X(t^*)) = 0$, the trajectory crosses the Poincaré section $\{w=0\}$ in the direction of increasing $w$. This means the dot product of the trajectory's tangent vector with the section's normal is positive, so the crossing is "positive normal".
Wait, the artifact asks: "whether they prove uniqueness of the crossing in the step, and whether the stated first-return wording needs an additional no-earlier-crossing argument."

Let's address the audit questions precisely.
1. **TM multiplication and parity tail enclosure.**
[OK] Multiplication retains all degree-at-most-2 terms...
Justification: The ranges of monomials $x_1^{a_1} \dots x_6^{a_6}$ on $[-1,1]^6$ are exactly $[-1,1]$ if any $a_i$ is odd, and $[0,1]$ if all $a_i$ are even and non-zero. Standard interval arithmetic for remainder products is sound.

2. **zonotope conversion of odd/even residual monomials and scalar remainders.**
[OK] Every coefficient vector g of a monomial containing a residual variable becomes a zonotope generator.
Justification: For a term $g \cdot m$, if range is $[-1,1]$, $g \cdot m$ traces $[-g, g]$, exactly modeled by generator $g$. If range is $[0,1]$, it traces $[0, g]$, exactly modeled by center $g/2$ and generator $g/2$. Scalar intervals $[c-r, c+r]$ are exactly centers $c$ and coordinate-axis generators $r$.

3. **exact-rational Q, interval Q^-1*g, summed coordinate radii, and whether this encloses the entire Minkowski sum despite coefficient dependencies.**
[OK] A real Gram-Schmidt basis... Q*diag(d)*rho
Justification: Exact rational Q is invertible. $c = Q^{-1} g$ contains exact range. $Q c \supseteq g$ is verified componentwise. The sum $\sum \alpha_k g_k = Q(\sum \alpha_k c_k)$. The $j$-th coordinate of the box is bounded by $[-\sum \sup |c_{j,k}|, \sum \sup |c_{j,k}|]$, which mathematically over-approximates the Minkowski sum. There is NO under-enclosure, only expected conservative dependency loss (wrapping effect), which is safe.

4. **repeated reconditioning and time integration.**
[OK] Each step has a validated Picard tube B... strictly negative... strictly positive.
Wait, does time integration use the order-12 polynomial?
[OVERREACH] "normalized autonomous-flow coefficients a_k=D_t^k phi(0,q)/k!, an order-12 polynomial, and the componentwise Lagrange remainder range(a_13(B))*h^13. The raw TM endpoint range must be contained in B."
Wait, why OVERREACH?
If the TM is degree 2 in variables, evaluating an order-12 polynomial in time will result in polynomials of very high degree in the variables. The artifact says "Multiplication retains all degree-at-most-2 terms and puts every higher-degree monomial into the interval remainder".
If it computes order 12 in time and pushes to remainder at each step, does it correctly accumulate the time remainder?
Wait! The Lagrange remainder $a_{13}(B) \cdot h^{13}$ assumes the Taylor polynomial $\sum_{k=0}^{12} a_k(X(0)) h^k$ is evaluated EXACTLY.
If you TRUNCATE the polynomial $\sum_{k=0}^{12} a_k(X(0)) h^k$ to degree 2, the truncation error MUST be added to the remainder!
The artifact says: "Each step has ... an order-12 polynomial, and the componentwise Lagrange remainder ... The raw TM endpoint range must be contained in B."
Wait, the Picard check usually involves checking if the whole step is contained in $B$.
If $B$ is validated (i.e., $h L(B) < 1$ and the initial guess is enclosed in $B$), then $B$ ALREADY overbounds the entire trajectory for the step!
By Schauder fixed point theorem (or Banach), if the Picard operator maps $B$ into $B$, then the exact trajectory stays in $B$.
So if the endpoint TM is enclosed in $B$, the trajectory is validated.
Wait, is there an error here?
"a Picard tube for that step enclosed every trajectory" - Yes, the Picard tube $B$ encloses every trajectory.
So $B$ is a validated tube.
The TM computes an enclosure of the state at $t=h$.
The computation $X(h) = \sum_{k=0}^{12} a_k(X(0)) h^k + a_{13}(B) h^{13}$ is mathematically correct for the flow, PROVIDED that the polynomial is evaluated exactly or its truncation is bounded.
If the artifact evaluates the order-12 polynomial exactly and then reconditions to degree 2, the degree truncation is correctly placed into the Arb remainder (as per rule 1). So this is sound.
Wait, is there an issue with $a_{13}(B)$?
The Lagrange remainder for the flow is $\frac{f^{(13)}(B)}{13!} h^{13}$. The artifact says $a_{13}(B) \cdot h^{13}$. Since $a_k = D_t^k \phi / k!$, $a_{13} = f^{(13)} / 13!$. This is exactly correct.

Let's re-read: "The full pre-step w interval was strictly negative; the full post-step w interval was strictly positive. A Picard tube for that step enclosed every trajectory and x*y-zs was strictly positive on the tube. The claim is only: every initial point in the exact leaf has one negative-to-positive w=0 crossing in that time step, with positive normal."

5. **whether strict endpoint signs plus a Picard tube and positive normal prove existence and orientation of at least one crossing for every trajectory;**
[OK] strict endpoint signs plus a Picard tube...
Justification: By IVT, continuous trajectory going from strictly negative to strictly positive must cross 0. If $\dot{w} = x*y-zs > 0$ on the tube, the crossing is transversal in the direction of increasing $w$, proving positive orientation.

6. **whether they prove uniqueness of the crossing in the step, and whether the stated first-return wording needs an additional no-earlier-crossing argument.**
[WRONG / TIGHTENABLE] Uniqueness of crossing.
Wait, if $\dot{w} > 0$ everywhere on the tube, then $w(t)$ is strictly increasing, so the crossing IS unique in the step!
Why would it need an additional no-earlier-crossing argument?
"first-return wording needs an additional no-earlier-crossing argument."
Ah

Raw JSON: /tmp/llm-offload-qqrzx9/
