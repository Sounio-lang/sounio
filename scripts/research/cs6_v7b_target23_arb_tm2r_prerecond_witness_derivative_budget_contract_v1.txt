CS6 v7b target23 pre-QR witness derivative budget contract v1

Purpose
- Diagnose the exact source of width in D = u*v - w - z_s at the frozen
  production and terminal pre-QR witness endpoints.
- Decide whether one more symbolic spatial split is a credible next step or
  whether the carrier representation must be reconditioned first.

Frozen inputs
- The witness-event receipt SHA-256 is hard-bound in the worker and verifier.
- The original symbolic variables remain xi, eta, rho0, rho1, rho2, rho3.
- The witness domain is copied exactly from the frozen receipt.

Arithmetic
- All diagnostic arithmetic uses fractions.Fraction.
- TM2R multiplication retains total degree at most two.
- Every discarded degree-greater-than-two product is added to the interval
  remainder.
- The derivative remainder is decomposed into the five additive sources:
  truncated polynomial tail, P_u*R_v, P_v*R_u, R_u*R_v, and -R_w.
- Reported widths and fractions are exact rational numbers.

One-level falsifier
- For each original symbolic variable, substitute old = -1/2 + new/2 and
  old = +1/2 + new/2.
- Preserve the inherited interval remainder exactly; do not reintegrate.
- Rank a variable by the worse child radius.
- A split succeeds only if both child derivative lower bounds are positive.

Acceptance
- Every width, attribution, remainder decomposition, split domain, contraction
  factor, and classification passes the independent verifier.
- Every negative mutation is rejected.

Nonclaims
- This diagnostic performs no new flow transport and no interval Newton step.
- It certifies no section crossing, covering relation, recurrent graph, chaos,
  or solution of an open problem.
