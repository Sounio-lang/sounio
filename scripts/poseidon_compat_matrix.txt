# Poseidon compatibility matrix (locked baseline)
# Format:
#   case_id|mode|command|compare
#
# mode:
#   run_selfhost -> baseline runs default self-host path; candidate adds deprecated --use-sounio-compiler compatibility flag
#   direct       -> baseline and candidate run identical command
#
# compare tokens:
#   exit,stdout,stderr

run_minimal|run_selfhost|run examples/minimal.sio|exit,stdout
run_hello|run_selfhost|run examples/hello.sio|exit,stdout
check_minimal|direct|check examples/minimal.sio|exit,stdout,stderr
emit_hir_minimal|direct|compile --emit hir examples/minimal.sio|exit,stdout,stderr
