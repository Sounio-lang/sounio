diff --git a/self-hosted/compiler/lean_single.sio b/self-hosted/compiler/lean_single.sio
index 4b76bf32a3..67b7c07aad 100644
--- a/self-hosted/compiler/lean_single.sio
+++ b/self-hosted/compiler/lean_single.sio
@@ -16042,7 +16042,18 @@ fn compile_postfix_tail() with IO, Mut, Panic, Div {
                 let elem_hash = arr_hash_elem_hash(inner_hash)
                 LAST_ALEN = arr_hash_alen(inner_hash)
                 if esiz == 8 {
-                    em(0x48); em(0x8d); em(0x04); em(0xd1)  // lea rax, [rcx + rdx*8]
+                    // #1678: array-of-AGGREGATE elements are stored pointer-per-slot
+                    // (see the comments on the store path). The value path already
+                    // LOADS the element pointer here; this borrow path handed back the
+                    // address of the pointer CELL instead, so the callee read the
+                    // pointer bits as the first field. One opcode: 0x8b, not 0x8d.
+                    // This is the unfinished half of #740, which fixed the TYPE of
+                    // &!module.functions[i] and left the address arithmetic wrong.
+                    if (elem_ty == 6 || elem_ty == 7 || elem_ty == 8) && elem_hash != 0 {
+                        em(0x48); em(0x8b); em(0x04); em(0xd1)  // mov rax, [rcx + rdx*8] (element ptr)
+                    } else {
+                        em(0x48); em(0x8d); em(0x04); em(0xd1)  // lea rax, [rcx + rdx*8]
+                    }
                 } else {
                     em(0x48); em(0x8d); em(0x04); em(0x11)  // lea rax, [rcx + rdx] (esiz=1)
                 }
