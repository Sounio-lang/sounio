# R6 Correlated slot-identity ratchet (founder 2026-08-20).
# Count of versioned Knowledge-binding same-ident arithmetic sites that would
# require `with Correlated` under the conservative detector (ExprIdent name
# equality only; Knowledge-typed operands). Corpus migration is NOT done.
# The total may only SHRINK.
#
# Measured 2026-08-20: after excluding f64 helpers mis-tagged by name reuse,
# live Knowledge-binding slot-identity debt outside the new R6 tests is 0.
total=0
