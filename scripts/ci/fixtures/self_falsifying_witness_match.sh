#!/usr/bin/env bash
# Probe gate: emits BOTH a verdict token and a witness fingerprint, and the
# claim declares both correctly. Exits 0.
echo "PROBE_VERDICT TOKEN_ALPHA"
echo "PROBE_WITNESS abc123def456"
exit 0
