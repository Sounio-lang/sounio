//@ ignore
// Backward compatibility: no provenance field -> unchanged behaviour.
claim sfc_provenance_optional {
    hypothesis = "claims that declare no provenance are unaffected",
    falsifier  = "adding the provenance mechanism changed unrelated claims",
    evidence   = Evidence::GateGreen,
    harness    = "scripts/ci/fixtures/self_falsifying_witness_match.sh",
    gate       = "scripts/ci/fixtures/self_falsifying_witness_match.sh",
    verdict_token = "TOKEN_ALPHA",
    verdict    = Verdict::Alive,
}
fn main() -> i64 with IO { print("SFC_PROVENANCE_COMPAT_OK\n") 0 }
