#!/usr/bin/env bash
# THE POINT OF THE RUNG. The proposition still holds -- the verdict token is
# exactly what the claim declares -- but the evidence establishing it has been
# replaced. Exit code 0, token correct, witness different. Nothing below the
# witness binding can tell this from a passing build.
echo "PROBE_VERDICT TOKEN_ALPHA"
echo "PROBE_WITNESS 999999999999"
exit 0
