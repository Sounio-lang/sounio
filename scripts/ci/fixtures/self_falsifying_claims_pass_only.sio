// Fixture source for the self-falsifying compiler gate (clause F4): every
// claim's gate passes, so --verify-claims must report VERIFY_CLAIMS_OK and
// let the compile proceed.

claim sfc_only_pass {
    hypothesis = "a source whose claim gates all pass compiles under --verify-claims",
    falsifier  = "any gate exits non-zero",
    evidence   = Evidence::GateGreen,
    harness    = "scripts/ci/fixtures/self_falsifying_claim_gate_pass.sh",
    gate       = "scripts/ci/fixtures/self_falsifying_claim_gate_pass.sh",
    verdict    = Verdict::Alive,
}

claim sfc_metadata_only {
    note = "no gate field: metadata placeholder, must be skipped",
    verdict = Verdict::Alive,
}

fn main() -> i64 with IO {
    print("SELF_FALSIFYING_PASS_ONLY_OK\n")
    0
}
