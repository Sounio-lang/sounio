// Fixture source for the self-falsifying compiler gate (clause F3): no
// claims at all, so --verify-claims must be a no-op (VERIFY_CLAIMS_NOOP).

fn main() -> i64 with IO {
    print("SELF_FALSIFYING_NO_CLAIMS_OK\n")
    0
}
