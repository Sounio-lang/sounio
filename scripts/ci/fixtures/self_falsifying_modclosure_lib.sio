//@ ignore
// Module-closure probe, imported leaf (rung R1 of the self-falsifying
// compilation line: docs/research/self_falsifying_compilation_line_r1_2026-07-26.md).
//
// This module carries a claim whose gate ALWAYS FAILS. If claim verification
// followed the module closure, importing this module would falsify the build.
// The mechanism spec (self_falsifying_compiler_spec_2026-07-25.md §6) states
// that only the main source file's claims are executed; this fixture turns that
// stated limitation into an executable observation.

claim mcl_library_claim_that_is_false {
    hypothesis = "a claim in an IMPORTED module is verified when its importer is compiled",
    falsifier  = "the importer compiles cleanly even though this gate always exits non-zero",
    evidence   = Evidence::Implemented,
    harness    = "scripts/ci/fixtures/self_falsifying_claim_gate_fail.sh",
    gate       = "scripts/ci/fixtures/self_falsifying_claim_gate_fail.sh",
    verdict    = Verdict::Alive,
}

pub fn modclosure_lib_value() -> i64 {
    42
}
