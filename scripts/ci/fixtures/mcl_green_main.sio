//@ ignore
// Importer of a module whose claim is TRUE. Compiled with --verify-claims the
// module closure carries two claims, both green: VERIFY_CLAIMS_OK pass=2.

use mcl_green_lib::{mcl_green_lib_value}

claim mcl_green_main_claim {
    hypothesis = "the importing file's own claims are verified",
    falsifier  = "this gate does not run, or runs and fails",
    evidence   = Evidence::GateGreen,
    harness    = "scripts/ci/fixtures/self_falsifying_claim_gate_pass.sh",
    gate       = "scripts/ci/fixtures/self_falsifying_claim_gate_pass.sh",
    verdict    = Verdict::Alive,
}

fn main() -> i64 with IO {
    if mcl_green_lib_value() == 42 {
        print("MCL_GREEN_MAIN_OK\n")
    }
    0
}
