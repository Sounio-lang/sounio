// Fixture source for the self-falsifying compiler gate (optional clause F6,
// SFC_TEST_TIMEOUT=1): the gate sleeps past the executor timeout and must be
// reported CLAIM_TIMEOUT, aborting the compile.

claim sfc_gate_timeout {
    hypothesis = "a hung gate is killed and counted as a falsification",
    falsifier  = "the executor waits forever or lets the compile proceed",
    evidence   = Evidence::Implemented,
    harness    = "scripts/ci/fixtures/self_falsifying_claim_gate_sleep.sh",
    gate       = "scripts/ci/fixtures/self_falsifying_claim_gate_sleep.sh",
    verdict    = Verdict::Alive,
}

fn main() -> i64 with IO {
    print("SELF_FALSIFYING_TIMEOUT_UNREACHABLE\n")
    0
}
