//@ ignore
// Token-binding probe: the gate exits 0 and emits no verdict token.
// A claim that declares a token but whose check states none cannot be
// verified. Expect CLAIM_TOKEN_ABSENT, VERIFY_CLAIMS_FALSIFIED, NO ELF.

claim sfc_token_absent {
    hypothesis = "the gate emits TOKEN_ALPHA",
    falsifier  = "the gate emits no machine-readable verdict",
    evidence   = Evidence::GateGreen,
    harness    = "scripts/ci/fixtures/self_falsifying_token_absent.sh",
    gate       = "scripts/ci/fixtures/self_falsifying_token_absent.sh",
    verdict_token = "TOKEN_ALPHA",
    verdict    = Verdict::Alive,
}

fn main() -> i64 with IO {
    print("SELF_FALSIFYING_TOKEN_ABSENT_REACHED_CODEGEN\n")
    0
}
