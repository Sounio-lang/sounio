//@ ignore
// Depth-2 probe: the refuted claim is two import hops away.

claim mcl_chain_leaf_claim_false {
    hypothesis = "closure probe",
    falsifier  = "the gate does not run",
    evidence   = Evidence::Implemented,
    harness    = "scripts/ci/fixtures/self_falsifying_claim_gate_fail.sh",
    gate       = "scripts/ci/fixtures/self_falsifying_claim_gate_fail.sh",
    verdict    = Verdict::Alive,
}

pub fn mcl_chain_leaf_value() -> i64 {
    7
}
