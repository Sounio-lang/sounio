#!/usr/bin/env bash
# Token present and correct; no witness emitted at all. A claim that declares a
# witness must fail closed here, not pass because the proposition holds.
echo "PROBE_VERDICT TOKEN_ALPHA"
exit 0
