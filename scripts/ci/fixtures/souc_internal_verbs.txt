# Verbs the binary dispatches that `souc --help` deliberately does not advertise.
#
# One per line, followed by the reason. Read in review: a line here is a claim
# that a user never needs this verb, and the gate takes your word for it. The
# alternative -- letting an undeclared verb pass silently -- is what left
# `ontology` out of the usage block long enough to produce two wrong
# documentation edits and a wrong premise handed to seven agents.
#
# This file is the ONLY way a dispatched verb may stay unadvertised.
# scripts/ci/souc_help_verb_parity_gate.sh
lean-single    engine selector, not a user verb: forces the legacy lean_single path
lean_single    underscore spelling of the same selector
leansingle     third spelling accepted for compatibility
emit-lean-obligations   internal proof-obligation dump consumed by formal/ tooling
advisory       argument value of --science-boundary, matched by the same case
strict         argument value of --science-boundary
off            argument value of --science-boundary
auto           argument value of --backend / precision selection
gpu            argument value of --backend
f32            argument value of a precision selector
f64            argument value of a precision selector
