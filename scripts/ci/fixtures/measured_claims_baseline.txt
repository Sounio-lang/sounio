# Rows that ALREADY disagree on main the day this gate landed.
#
# One id per line. A baselined row is reported and does not fail the gate. A row
# NOT in this file must agree, or the gate fails. So the debt is visible, frozen,
# and cannot grow — the same shape as gate_workflow_reference.frozen.
#
# Removing a line is how a claim gets fixed. Adding one is how a claim gets
# excused, and should be argued for in the pull request that adds it.
#
# gate_ratchet_list_pair was here, and came out: #2217 regenerated the
# .frozen/.frozen.list pair and the gate reported FIXED at 468 on its own.
#
# The file is now EMPTY of active entries. Both debts this gate shipped with
# were paid within a day, and in both cases the gate is what noticed -- not a
# person remembering to re-check. That is the whole point of the ratchet: the
# empty state is reachable, and getting there is the normal outcome rather
# than an aspiration.
# witness_census_total was here, and came out: #2207 regenerated the census and
# the gate reported FIXED on its own. That is the loop working — the debt was
# named, paid, and the gate said so without anyone remembering to check.
