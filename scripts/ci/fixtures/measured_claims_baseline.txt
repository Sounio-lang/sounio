# Rows that ALREADY disagree on main the day this gate landed.
#
# One id per line. A baselined row is reported and does not fail the gate. A row
# NOT in this file must agree, or the gate fails. So the debt is visible, frozen,
# and cannot grow — the same shape as gate_workflow_reference.frozen.
#
# Removing a line is how a claim gets fixed. Adding one is how a claim gets
# excused, and should be argued for in the pull request that adds it.
#
# gate_ratchet_list_pair — main carries 472 in .frozen and 474 lines in
# .frozen.list. Someone lowered the count without regenerating the list, so two
# entries stayed in the list after a workflow began naming them. Fixed by the
# regeneration in #2206; this line comes out when that lands.
gate_ratchet_list_pair
# witness_census_total — the committed census says 1837; tests/run-pass holds
# 1842 .sio files. The artifact is generated, and was not regenerated as the
# corpus grew. Fixed by the regeneration in #2207; this line comes out then.
witness_census_total
