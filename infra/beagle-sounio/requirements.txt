# beagle-sounio runtime Python deps
# Pinned to minor version to balance reproducibility against security patches.
# Keep this small — heavy deps (torch, jax) belong in a GPU-specific image.

numpy~=2.2
scipy~=1.15
pandas~=2.2
matplotlib~=3.10

# HTTP fetch (ABIDE preprocessor uses urllib by default; requests is for
# future scripts that need richer error handling).
requests~=2.32

# S3 client — for pushing campaign artifacts to archive buckets or pulling
# from private mirrors. Public ABIDE S3 URLs use plain HTTPS, so boto3 is
# optional at runtime.
boto3~=1.35
