# Dependências e saídas de build
node_modules/
web/dist/
swift/.build/
swift/dist/
.vercel/
web/tsconfig.tsbuildinfo

# Índice de evidência: gerado por scripts/index-evidence.mjs a partir de
# artifacts/**/*.json, e regenerado por `npm run build` e `npm run check`.
# Não é commitado porque commitá-lo permitiria que divergisse dos artefatos
# que ele indexa — que é exactamente o tipo de deriva que este site recusa.
web/src/evidence/index.generated.ts
